---
title: "Generalization, memorization, and overfitting for diffusion models trained in the lazy high-dimensional regime"
title_zh: 惰性高维机制下训练的扩散模型的泛化、记忆与过拟合
authors: "Hugo Latourelle-Vigeant, Sinho Chewi, Aram-Alexandre Pooladian, John Sous, Theodor Misiakiewicz"
date: 2026-08-25
pdf: "https://arxiv.org/pdf/2608.23938v1"
tags: ["query:dmg"]
score: 8.0
evidence: 通过去噪得分匹配研究扩散模型的理论泛化
tldr: 扩散模型精确回归有限数据会重现训练样本，泛化依赖隐式正则化。本文在惰性高维机制下，于内积核向量值RKHS中研究去噪分数匹配，推导梯度流训练的风险轨迹。结果显示三阶段：谱估计器负责泛化，纯噪声分数插值训练目标，经验贝叶斯估计器记忆数据；沿反向时间SDE这些估计器组合影响样本分布。研究揭示了核线性化和自诱导正则化等监督学习机制，也发现生成建模特有的现象。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.23938v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1561, \"height\": 636, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.23938v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1686, \"height\": 698, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.23938v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1679, \"height\": 690, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.23938v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1593, \"height\": 174, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.23938v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1712, \"height\": 2432, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.23938v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1716, \"height\": 1859, \"label\": \"Table\"}]"
motivation: 扩散模型在有限数据上精确回归会重现训练样本，其泛化机制缺乏理论理解，需在惰性高维机制下刻画。
method: 在向量值再生核希尔伯特空间中用内积核对去噪分数匹配建模，推导n≍d比例高维下梯度流训练的风险轨迹。
result: 梯度流风险轨迹呈现三阶段：谱估计器泛化、纯噪声分数插值训练目标、经验贝叶斯估计器记忆数据；反向SDE中这些估计器组合决定样本分布。
conclusion: 揭示了核线性化与自诱导正则化等监督学习机制，并发现生成建模特有的新现象。
---

## 摘要
现代基于分数的生成模型在高维任务（如图像、音频和视频合成）中取得了显著的实证成功。这些模型将分布学习简化为一系列回归问题，如果这些问题在有限数据上精确求解，最终将重现训练样本。因此，它们的泛化能力必然源于训练过程中的隐式或显式正则化。在这项工作中，我们为监督惰性训练机制下过参数化神经网络的良性过拟合和算法正则化理论，发展了一个生成式对应物。我们研究了具有内积核的向量值再生核希尔伯特空间中的去噪分数匹配。在比例高维机制n≍d下，我们推导了梯度流训练下的精确风险轨迹。这些轨迹表现出由定性不同的估计器所控制的三个阶段：一个可泛化的谱估计器，一个具有局部峰值、对训练目标进行插值的纯噪声分数，以及一个记忆数据的经验贝叶斯估计器。随后，我们分析了这些估计器如何沿逆时随机微分方程组合，并刻画了所得样本的分布。该分析揭示了监督学习中熟悉的机制，包括核线性化和来自核非线性部分的自诱导正则化，但也揭示了一种特定于生成建模的独特现象学。

## Abstract
Modern score-based generative models have achieved remarkable empirical success in high-dimensional tasks such as image, audio, and video synthesis. These models reduce distribution learning to a sequence of regression problems that, if solved exactly on finite data, would ultimately reproduce the training samples. Their ability to generalize must therefore arise from the implicit or explicit regularization during training. In this work, we develop a generative counterpart to the theory of benign overfitting and algorithmic regularization for overparameterized neural networks in the supervised lazy-training regime. We study denoising score matching in a vector-valued reproducing kernel Hilbert space with an inner-product kernel. In the proportional high-dimensional regime $n\asymp d$, we derive exact risk trajectories under gradient flow training. These trajectories exhibit three phases governed by qualitatively distinct estimators: a spectral estimator that generalizes, a pure-noise score with localized peaks that interpolate the training objective, and an empirical Bayes estimator that memorizes the data. We then analyze how these estimators combine along the reverse-time SDE and characterize the distribution of the resulting samples. The analysis reveals familiar mechanisms from supervised learning, including kernel linearization and self-induced regularization from the nonlinear part of the kernel, but also reveals a distinct phenomenology specific to generative modeling.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

本文围绕扩散模型的一个核心悖论展开：**扩散模型的训练目标——去噪分数匹配——在有限数据上精确求解时，会收敛到经验贝叶斯去噪器，从而完全记忆训练样本，而非泛化到真实数据分布。** 然而，实际训练的扩散模型却普遍展现出良好的泛化能力，能够生成真实且新颖的样本。这种理论与实践的矛盾促使作者提出关键问题：是什么机制阻止了学习到的分数退化为记忆性的经验贝叶斯解？模型类与训练算法中哪些成分充当了隐式正则化器？最终生成的分布又是什么样的？论文在**惰性高维训练机制**（lazy training regime）下，为生成式模型建立了与监督学习中良性过拟合和算法正则化理论相对应的理论基础，系统性地回答了上述问题。

## 2. 论文提出的方法论

### 2.1 核心建模框架

- 研究对象：向量值再生核希尔伯特空间（vector-valued RKHS）中的去噪分数匹配，核函数为内积核 `K(x, x') = h(⟨x, x'⟩/d)`，该模型对应于惰性机制下训练的宽神经网络。
- 设置：分别独立分析不同噪声水平上的回归问题；在比例高维机制（`n ≍ d`）下进行渐近分析。
- 假设：数据分布满足对数 Sobolev 不等式（LSI），仅需约束前两阶矩；核函数满足正则性与泰勒展开条件。

### 2.2 关键技术步骤

- **核算子的线性化（Proposition 1）**：证明经验核算子 `K̂` 与有效线性算子 `K̂_eff` 在算子范数下接近，误差阶为 `d^{-3/2}`。有效算子由一个线性核算子加上每样本修正项 `δ_n = (h(ρ²τ_Σ) − ρ²τ_Σ)/n`（自诱导正则化）构成。这不同于监督学习中引入岭惩罚，而是对每个高斯管进行平均。
- **不可约误差的消失（Proposition 2）**：证明经验退化问题的不可约训练误差指数级小（`e^{-d^c}`），即经验去噪回归本质上是无噪声的——这与有噪监督回归形成鲜明对比。
- **梯度流求解**：从零初始化对经验风险进行梯度流，解为 `f̂_t = S*(∫₀ᵗ exp(−(t−s)K̂) f̂*_v ds)`，等价于对经验核算子的谱滤波器 `λ → 1 − e^(−tλ)`。

### 2.3 三类估计器的特征刻画

论文识别了梯度流轨迹中的三个训练阶段：

| 阶段 | 时间尺度 | 估计器行为 | 训练损失 | 测试风险 |
|------|---------|-----------|---------|---------|
| Phase I（泛化） | `t = Θ(d)` | 谱收缩估计器：`f̂ ≈ ψ(Σ̂,t)u` | 缓慢下降 | 先降后升，在 `t⋆ ≍ d` 处取最优 |
| Phase II（过拟合） | `t = d^{1+Θ(1)}` | 纯噪声分数：`f̂ ≈ −u/σ` + 不可见局部峰 | 以 `d/t` 速率趋零 | 稳定在 `ρ²/σ² τ_Σ` |
| Phase III（记忆） | `t = d^{ω(1)}` | 经验贝叶斯去噪器：`f̂ ≈ (ρx_{0,i⋆} − u)/σ` | 完全为零 | 上升至 `2ρ²/σ² τ_Σ` |

### 2.4 逆过程分析

- **路径线性化（Theorem 4）**：证明在训练时间 `t_t ≲ d^{1+c} (c < 1/6)` 条件下，唯一样本轨迹具有**离域性**（与每个训练样本的归一化重叠为 `O(d^{-1/2})`），轨迹不访问记忆样本的局部区域，因此非线性分数与线性替代路径测度之间的 KL 散度渐近为零。
- **高斯生成分布（Theorem 5）**：生成分布近似高斯，其协方差由经验协方差的 Marchenko–Pastur 型映射与 ODE 流的复合给出，即 `Σ̄_t = ∫Λ̄_t(λ)Ω(dλ)`。

## 3. 实验设计

论文以理论分析为主，辅以两项数值验证：

- **图 2**：使用高斯数据（`π₀ = N(0, I_d)`，`n = 240`），核函数取 `h(x) = x + x⁴/12`，验证 Phase I 中训练/测试误差动力学与确定性等价的一致性，并考察 `d/n` 比率和 `δ_n` 对过拟合起始的影响。
- **图 3**：使用 `d = 720, n = 800`，目标为高斯分布（两特征空间，条件数 51），核函数 `h(x) = x + 0.005x⁵`。比较三种早期停止策略（逐点 DSM 最优、常数和指数时间表），评估学习到的协方差、累积分数 MSE 和 KL 散度之间的差异。

未使用真实数据集，未与其他方法进行实证对比，属于纯理论型论文的实验验证风格。

## 4. 资源与算力

论文**未提及任何算力配置**（GPU 型号、数量、训练时长等），也未说明数值实验的计算资源。数值实验仅用于验证理论预测的示意图，计算负担极小。整体而言，本文是理论性论文，算力并非其主要考量。

## 5. 实验数量与充分性

- **实验规模有限**：仅有两组简单的数值演示（图 2 和图 3），验证了部分理论预测。
- **对标良好**：数值结果与理论确定性等价线高度吻合，验证了推导的正确性。
- **充分性评估**：对于一篇以理论证明为核心贡献的论文，这些演示足以说明理论结果的合理性，但**缺乏**大规模实验来检验理论在真实数据上的实用意义。

## 6. 论文的主要结论与发现

1. **过拟合不等于记忆**：在惰性高维机制下，产生过拟合（零训练损失）与产生记忆（再现训练样本）之间存在超多项式的时间间隔；在中间阶段，模型可以插值目标函数但仍然生成全新样本。
2. **过拟合与泛化的关系**：训练损失消失与分数级泛化损失几乎同时发生——模型通过遗忘数据协方差来实现插值，因此得分层面的过拟合并非良性。
3. **生成层面的良性过拟合**：非线性分数贡献只存在于训练样本周围的薄高斯管中，扩散采样的离域化轨迹不会访问这些区域，因此生成分布仍然有效且接近真实分布。
4. **高斯归纳偏置**：惰性扩散模型的生成分布是渐近高斯的，仅依赖于数据的前两阶矩，这是对高维核方法中“多项式壁垒”的生成式类比——在 `n ≍ d` 下，惰性模型至多生成高斯分布。
5. **三阶段训练的发现**：与经验观察（学习全局统计先于局部细节）一致，并揭示了核非线性强度（`δ_n`）和数据协方差对泛化窗口长度的控制作用。

## 7. 优点

- **理论深度与完整性**：建立了算子层面的核线性化，适用于高斯混合，超越了监督学习中有限维核矩阵的线性化；推导了训练和测试误差的精确确定性等价，涵盖整个梯度流轨迹。
- **将生成建模特有的现象纳入理论框架**：区分了是否过拟合（O）、是否记忆（M）、以及是否泛化（G₀）三个层级，明确指出 O ⇒ M 并不成立，澄清了文献中常被混淆的概念。
- **逆过程直接分析**：现有工作多聚焦于得分匹配目标本身，而本文直接分析反向 SDE 驱动下的生成分布几何，这是一项关键创新；路径线性化证明（Theorem 4）展示了非线性分数在采样过程中的不可见性。
- **对非高斯数据的适应性**：仅要求数据满足 LSI 条件而非高斯假设，理论上可处理多峰分布。
- **严密的理论工具链**：整合对数 Sobolev 不等式（浓度）、随机矩阵确定性等价、Rosenthal 不等式、厄米特张量等技术工具，形成了完整的证明体系；对岭回归版本（Appendix A）也做了并行分析，证明了梯度流与岭回归间的等价性（`κ = d/t`）。

## 8. 不足与局限

- **机制受限**：分析仅限于 `n ≍ d` 比例高维机制，在该规模下惰性模型只能学习线性分数并生成高斯输出，未扩展至 `n ≍ d^ℓ` 的多项式样本体量。
- **独立噪声水平假设**：各噪声水平使用独立的 RKHS 模型，而实际扩散模型通常用单个时间条件网络建模，这会耦合不同噪声水平的回归问题，可能导致理论预测与实际训练存在偏差。
- **逆向过程线性化的时间限制**：路径线性化定理只覆盖训练时间 `t_t ≲ d^{1+c} (c < 1/6)`，未覆盖完全过拟合阶段的更长时间，虽论文预期可通过改进证明扩展。
- **从分数风险到采样质量的鸿沟**：图 3 展示了 DSM 最优计划与生成质量最小计划之间存在轻微差异，说明分数匹配性能与生成分布质量的关系尚未完全澄涩。
- **实验验证较弱**：未在真实数据集或大规模模型上验证理论；数值演示仅涉及高斯目标。对于生成建模中常见的流形结构数据，论文明确将其列为未解决的未来方向。

（完）
