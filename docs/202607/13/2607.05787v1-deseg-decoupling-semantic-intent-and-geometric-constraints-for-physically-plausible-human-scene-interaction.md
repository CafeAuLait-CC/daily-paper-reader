---
title: "DeSeG: Decoupling Semantic Intent and Geometric Constraints for Physically Plausible Human-Scene Interaction"
title_zh: "DeSeG: 解耦语义意图与几何约束以实现物理合理的人-场景交互"
authors: "Jiakun Li, Zhe Li, Wenqiang Wu, Zheng Chang, Mingqi Gao, Jinyu Yang, Feng Zheng"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05787v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 合成人类-场景交互，包含运动生成
tldr: "生成物理合理的人-场景交互面临语义-几何纠缠问题，模型易在严格几何约束下覆盖语义意图并产生穿透。本文提出DeSeG分层框架，通过残差语义规划器将文本指令与目标体素解耦为独立语义空间，再以物理正则化扩散执行器将可微排斥势场融入扩散目标，实现碰撞感知的运动生成。在Lingo数据集上，DeSeG将场景穿透降低47%，语义对齐提升29%，达到SOTA。该工作为复杂交互场景的物理合理生成提供了新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05787v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 863, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2607.05787v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1472, \"height\": 949}, {\"url\": \"assets/figures/arxiv/2607.05787v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1723, \"height\": 1057}, {\"url\": \"assets/figures/arxiv/2607.05787v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 863, \"height\": 426}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05787v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1664, \"height\": 677}, {\"url\": \"assets/tables/arxiv/2607.05787v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 810, \"height\": 271}, {\"url\": \"assets/tables/arxiv/2607.05787v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 772, \"height\": 532}, {\"url\": \"assets/tables/arxiv/2607.05787v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 846, \"height\": 299}, {\"url\": \"assets/tables/arxiv/2607.05787v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 733, \"height\": 643}]"
motivation: 现有HSI生成模型因语义-几何纠缠，在严格几何约束下会覆盖语义意图并产生身体-场景穿透。
method: 提出DeSeG框架，包含残差语义规划器解耦语义意图与几何约束，以及物理正则化扩散执行器引入可微排斥势场。
result: "场景穿透减少47%，语义对齐提升29%，在Lingo数据集上达到SOTA。"
conclusion: DeSeG有效缓解了语义-几何纠缠，显著提升了HSI的物理合理性与语义一致性。
---

## 摘要
合成物理合理的人-场景交互（HSI）仍然是计算机视觉和人类化身发展中的一个关键挑战。尽管最近的生成模型能够实现多样化的运动合成，但它们存在一种称为语义-几何纠缠的归纳偏差。由于空间约束通常与训练数据中的特定动作高度相关，单一模型将学会捷径偏差，在面对严格的几何线索时激进地覆盖语义意图。此外，这种纠缠加剧了物理幻觉，例如身体-场景穿透。为了解决这些限制，我们提出了DeSeG，一个明确将语义意图与几何约束解耦的分层框架。首先，我们引入了一个残差语义规划器，将文本指令和规范化的目标体素编码到紧凑的潜在空间中，实现独立于空间轨迹的细粒度语义控制。其次，我们提出了一个物理正则化扩散执行器，将可微的排斥势场直接纳入扩散目标中，强制实现碰撞感知的运动生成。在Lingo数据集上的大量实验表明，DeSeG实现了最先进的性能，与最先进的基线相比，平均场景穿透减少了47%，语义对齐提高了29%。

## Abstract
Synthesizing physically plausible human-scene interactions (HSI) remains a critical challenge in computer vision and the development of human avatars. Although recent generative models enable diverse motion synthesis, they suffer from an inductive bias referred to as semantic-geometric entanglement. Because spatial constraints often strongly correlate with specific actions in training data, monolithic models will learn the shortcut bias, aggressively overriding the semantic intent when faced with strict geometric cues. Furthermore, this entanglement exacerbates physical hallucinations, such as body-scene penetrations. To address these limitations, we propose DeSeG, a hierarchical framework that explicitly decouples semantic intent from geometric constraints. First, we introduce a Residual Semantic Planner that encodes textual instructions and canonicalized goal voxels into a compact latent space, enabling fine-grained semantic control independent of spatial trajectories. Second, we propose a physics regularized diffusion executor that incorporates differentiable repulsive potential fields directly into the diffusion objective, enforcing collision-aware motion generation. Extensive experiments on the Lingo dataset demonstrate that DeSeG achieves state-of-the-art performance, reducing mean scene penetration by 47% and improving semantic alignment by 29% over the SOTA baselines.

---

## 论文详细总结（自动生成）

# 论文总结：DeSeG: Decoupling Semantic Intent and Geometric Constraints for Physically Plausible Human-Scene Interaction

## 1. 核心问题与整体含义
- **研究动机**：合成物理合理的人-场景交互（HSI）是计算机视觉和虚拟人类发展的关键挑战。现有生成模型（如基于扩散的模型）虽然能生成多样化运动，但存在两个严重问题：
  - **语义-几何纠缠**：模型学会“捷径偏置”——几何线索（如目标位置）与特定动作高度相关，导致当几何线索与文本指令冲突时，模型会忽视语义意图而执行默认动作。
  - **物理幻觉**：生成的运动常出现身体-场景穿透（身体穿透家具等），缺乏物理约束。
- **整体含义**：本文旨在通过显式解耦语义意图和几何约束，实现既语义一致又物理合理的长序列人-场景交互生成。

## 2. 方法论
- **核心思想**：提出DeSeG分层框架，将生成过程分解为两个阶段：
  1. **残差语义规划器（Residual Semantic Planner）**：将文本指令和**规范化后的目标体素**编码到紧凑的潜在空间 \(z\) 中，提取纯粹的语义affordance，独立于空间轨迹。
  2. **物理正则化扩散执行器（Physics-Regularized Diffusion Executor）**：将可微排斥势场直接融入扩散训练损失，使模型内在地学会碰撞避免，无需测试时优化。
- **关键技术细节**：
  - **规范化操作（Canonicalization）**：将目标体素转换为以智能体朝向为中心的规范坐标，去除绝对方向信息，防止几何信号泄漏到语义潜在空间。
  - **残差潜在空间**：使用条件变分自编码器（CVAE），训练时运动编码器仅预测相对于先验的残差均值和方差，迫使规划器专注于抽象意图。
  - **物理正则化损失**：利用Tweedie公式从预测噪声估计干净运动 \(\hat{M}_0\)，对每个关节位置查询预计算的SDF（有符号距离场），定义排斥势能 \(U_{rep}(o) = \frac{1}{2}\eta \cdot \text{ReLU}^2(d_{safe} - D(o))\)，并对全身关节加权（躯干惩罚高，末端接触点惩罚低）。
  - **两阶段训练**：先训练规划器，冻结后训练执行器。
  - **A*路径引导**：在执行器中引入A*规划的无碰撞路径作为宏观方向提示。
- **公式概要**：
  - 联合分布分解：\(p(M|T,S,g) = \int p_{\phi}(M|z, S_{local}, g) \cdot p_{\phi}(z|T, \Psi(S,g)) dz\)
  - 先验分布：\(p_{\phi}(z_t | T, V_{can}) = \mathcal{N}(\mu_{\phi}, \sigma_{\phi}^2)\)
  - 物理损失：\(\mathcal{L}_{phy}(\hat{M}_0) = \sum_{t=1}^T \sum_{j=1}^J w_j \cdot U_{rep}(o_{j,k})\)
  - 总损失：\(\mathcal{L}_{overall} = \mathcal{L}_{ddpm} + \lambda \cdot \mathcal{L}_{phy}(\hat{M}_0)\)

## 3. 实验设计
- **数据集**：
  - **Lingo**：主要数据集，16小时、120个室内场景的HSI运动捕捉，带有文本标注。4:1场景分割。
  - **TRUMANS**：用于跨数据集泛化测试。
- **评估基准**：对比方法包括**Lingo**、**TRUMANS**、**TeSMo**（均重训在Lingo数据集上）。
- **评估指标**：
  - 语义/质量：FID、Diversity、Multi-modality、R-Precision
  - 物理合理性：平均穿透深度（Pene mean）、最大穿透深度（Pene max）
  - 额外：**NC-Bench**（负约束基准）中的语义-几何一致性（SGC），由10名标注者进行二元判断（Fleiss κ=0.818）。
- **消融实验**：去除规划器（w/o Hierarchical Latent）、去除交叉注意力（w/o Cross-Attention）、去除物理正则化（w/o Physical Regularization）、去除A*路径（w/o A*）。
- **跨数据集泛化**：在TRUMANS上重训并评估。

## 4. 资源与算力
- 文中提及**所有模型在单块NVIDIA RTX A6000 GPU上训练**，使用官方公开配置，并未详细给出训练时长。提到规划器训练50K次迭代、执行器100K次迭代，批量大小128。因此算力要求中等（单卡），但未提供精确小时数。

## 5. 实验数量与充分性
- **实验充分性**：论文进行了多组实验：
  - 主表（Tab.1）：在Lingo上对比三个基线并给出标准差（3次随机种子的均值）。
  - 消融实验：3组消融（Tab.1中部），以及A*消融（Tab.4）。
  - 跨数据集泛化实验（Tab.1底部）。
  - NC-Bench测试（Tab.2）。
  - 定性分析（Fig.3, Fig.4）。
  - 统计显著性：主要指标差距超过2σ。
- **客观性与公平性**：所有基线按官方配置重训，使用相同数据划分。NC-Bench采用人类评估以避免指标偏差。实验设计较为全面，消融验证了各模块贡献。总体充分且客观。

## 6. 主要结论与发现
- **核心结果**：DeSeG在Lingo数据集上实现**FID=2.882**（优于Lingo的3.093），**平均穿透减少47%**（从0.392降至0.207），**R-Precision提升29%**（从0.437增至0.562）。在TRUMANS跨数据集测试中，FID从0.493降至0.338，穿透减少约50%。
- **NC-Bench**：DeSeG的SGC达到**72.3%**，远超Lingo（58.2%）和TRUMANS（38.6%），表明解耦有效避免了几何优先的捷径。
- **A*消融**：A*路径主要改善导航质量，但语义对齐（R-Precision）主要来源于分层解耦，而非路径引导。
- **结论**：显式解耦语义与几何并内化物理先验，能同时提升物理合理性、语义一致性和跨数据集泛化能力。

## 7. 优点
- **创新性解耦机制**：通过残差CVAE和规范化操作在潜在空间分离语义与轨迹，从根本上解决捷径学习问题。
- **训练时物理正则化**：将可微排斥势场直接嵌入扩散损失，避免测试时优化（比TeSMo快约20%），且保持生成多样性。
- **负约束基准（NC-Bench）**：专门设计并评估冲突场景，揭示传统指标（FID）掩盖的纠缠问题。
- **全面实验**：对比多个SOTA，跨数据集验证，统计显著性报告，消融充分。
- **高效推理**：内化物理约束，推理无需后处理优化，线性时间扩展。

## 8. 不足与局限
- **密集/抽象场景**：在极其杂乱的物体环境中或几何抽象目标（无清晰affordance）下表现不佳，体素表示可能限制。需要更细粒度的场景编码。
- **紧耦合任务限制**：对于需要严格语义-几何耦合的任务（如“面对电视观看”），通过紧凑潜在代码解耦可能丢弃细粒度对齐信息。作者建议交互类型可调节解耦强度。
- **数据集覆盖**：主要在Lingo和TRUMANS上评估，场景以室内为主，未涉及室外或复杂动态环境。通用性需更多验证。
- **物理损失超参数敏感**：安全距离、权重系数等需人工设定，可能影响稳定性和泛化。
- **计算资源未详细报告**：训练具体耗时未给出，不利于复现成本评估。

（完）
