---
title: "ARDY: Autoregressive Diffusion with Hybrid Representation for Interactive Human Motion Generation"
title_zh: "ARDY: 基于混合表示的自回归扩散用于交互式人体运动生成"
authors: "Kaifeng Zhao, Mathis Petrovich, Haotian Zhang, Tingwu Wang, Siyu Tang, Davis Rempe"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08741v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 自回归扩散用于交互式3D人体运动生成
tldr: 交互式应用需要实时生成可控的3D人体运动，现有离线方法推理慢，在线方法可控性差。为此，提出ARDY，一种流式生成框架，通过混合表示（显式根特征+隐式身体嵌入）平衡轨迹控制与生成效率，采用两阶段自回归Transformer去噪器支持长程运动学约束和在线文本提示。在HumanML3D和Bones Rigplay数据集上，ARDY实现了高保真运动质量和约束遵循，验证了架构设计的有效性。该方法支持动态文本控制、关键帧姿态约束、路径跟随及交互式运动控制，填补了在线可控运动生成的空白。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08741v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1852, \"height\": 678}, {\"url\": \"assets/figures/arxiv/2607.08741v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 839, \"height\": 470}, {\"url\": \"assets/figures/arxiv/2607.08741v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1788, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2607.08741v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 893, \"height\": 564}, {\"url\": \"assets/figures/arxiv/2607.08741v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 892, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2607.08741v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1794, \"height\": 1016}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08741v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1863, \"height\": 327}, {\"url\": \"assets/tables/arxiv/2607.08741v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1850, \"height\": 278}, {\"url\": \"assets/tables/arxiv/2607.08741v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1852, \"height\": 952}, {\"url\": \"assets/tables/arxiv/2607.08741v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 896, \"height\": 289}, {\"url\": \"assets/tables/arxiv/2607.08741v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 897, \"height\": 323}, {\"url\": \"assets/tables/arxiv/2607.08741v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 833, \"height\": 178}]"
motivation: 现有离线运动生成方法推理速度慢，而在线方法难以同时实现实时合成、高可控性和复杂文本语义理解。
method: 提出ARDY，使用混合表示（显式根特征与隐式身体嵌入）和两阶段自回归Transformer去噪器，支持可变历史上下文和长程运动学约束。
result: 在HumanML3D和Bones Rigplay数据集上，ARDY取得高运动质量和约束遵循，优于现有方法。
conclusion: ARDY实现了交互式应用中实时、高保真、可控的人体运动生成，并展示了多种交互控制能力。
---

## 摘要
在交互式应用中实时生成逼真的3D人体运动对于动画、仿真和人形机器人至关重要。虽然最近的离线运动生成方法通过文本和运动学约束提供了精确控制，但它们缺乏交互式设置所需的推理速度。相反，现有的在线方法能够实现实时合成，但往往牺牲了可控性，或者由于有限的上下文窗口而难以处理复杂的文本语义和长期目标。在这项工作中，我们提出了ARDY，一个流式生成框架，通过实现可通过在线文本提示和灵活运动学约束控制的高保真运动生成来弥合这一差距。ARDY采用了一种混合表示，将显式的根特征与潜在身体嵌入相结合，在精确轨迹控制与高效生成学习之间取得平衡。我们提出了一种两阶段自回归变换器去噪器，该去噪器具有可变的上下文历史，并支持对灵活的长期运动学约束进行条件化。通过在大规模运动捕捉数据集上进行训练，并直接以文本标签和从真实姿态采样的运动学约束为条件，ARDY自然地学习了支持在线提示和灵活长期目标的可控生成。在HumanML3D基准测试和大规模高保真Bones Rigplay数据集上的广泛评估证明了ARDY的高运动质量和约束遵守程度，验证了我们关键架构决策的有效性。最后，我们通过一个交互式演示展示了该方法的实际多功能性，该演示具有动态文本控制、多样化关键帧姿态约束、路径跟随以及通过鼠标和键盘进行的交互式运动控制。补充视频结果、代码和模型发布可在https://research.nvidia.com/labs/sil/projects/ardy/ 获取。

## Abstract
Generating realistic 3D human motions in real-time within interactive applications is key for animation, simulation, and humanoid robotics. While recent offline motion generation approaches offer precise control via text and kinematic constraints, they lack the inference speed required for interactive settings. Conversely, existing online methods enable real-time synthesis but often sacrifice controllability or struggle with complex text semantics and long-horizon goals due to limited context windows. In this work, we introduce ARDY, a streaming generation framework that bridges this gap by enabling high-fidelity motion generation controllable via online text prompts and flexible kinematic constraints. ARDY employs a hybrid representation that combines explicit root features with a latent body embedding, balancing precise trajectory control with efficient generative learning. We propose a two-stage autoregressive transformer denoiser that features variable history context and supports conditioning on flexible, long-horizon kinematic constraints. By training on a large-scale motion capture dataset and being directly conditioned on text labels and kinematic constraints sampled from ground truth poses, ARDY natively learns controllable generation that supports online prompting and flexible long-horizon goals. Extensive evaluations on the HumanML3D benchmark and the large-scale, high-fidelity Bones Rigplay dataset demonstrate ARDY's high motion quality and constraint adherence, validating the efficacy of our key architectural decisions. Finally, we demonstrate the method's practical versatility through an interactive demo featuring dynamic text control, diverse keyframe pose constraints, path following, and interactive locomotion control via mouse and keyboard. Supplementary video results, code, and model releases can be found at https://research.nvidia.com/labs/sil/projects/ardy/.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：在动画、仿真和人形机器人等交互式应用中，需要实时生成逼真且可控的3D人体运动。然而，现有方法存在两难困境：
  - **离线方法**（如MaskControl、Kimodo）支持复杂文本和运动学约束，但推理速度慢（需多步扩散或全局生成），不适用于实时交互。
  - **在线方法**（如A-MDM、CAMDM、MotionStreamer）能实时生成，但可控性差：要么不支持文本，要么只能处理有限约束，或者上下文窗口短导致无法理解全局语义和长期目标。
- **整体含义**：本文提出ARDY，一个**流式生成框架**，同时实现**高保真运动生成**、**在线文本提示**和**灵活的运动学约束**（包括根轨迹、全身关键帧、末端执行器位置/旋转等），且能处理**长程目标**（超出当前生成窗口）。填补了在线可控运动生成的空白。

## 2. 方法论：核心思想、关键技术细节与算法流程

### 2.1 核心思想
- **混合运动表示**：将运动分解为**显式根特征**（全局位置、朝向）和**潜身体嵌入**（由学习到的tokenizer压缩），平衡精确轨迹控制与高效生成学习。
- **两阶段自回归Transformer去噪器**：先在根Transformer中预测干净的显式根，再在身体Transformer中条件于干净根预测潜身体嵌入，两者在去噪循环中交替影响。
- **直接条件化训练**：在训练时从真实运动中采样文本和运动学约束，无需测试时优化或额外的RL策略。

### 2.2 关键技术细节

#### 混合运动表示
- 显式表示每帧：`m = (m_root, m_body)`，其中`m_root = (p, cosψ, sinψ) in R^5`，`m_body`包含全局关节旋转、位置、速度、足接触等。
- 混合表示：将`m_body`替换为tokenizer输出的潜向量`x_body in R^L`，与分片后的显式根拼接成混合token `x = [m_root_patched; x_body]`。

#### 身体运动Tokenizer
- 采用**因果Transformer**的**条件自编码器**，输入分片（patch size=4帧）后的身体运动，编码为潜向量。解码时使用局部根表示（角速度、线速度、高度）以减轻足滑。
- 默认使用**有限标量量化（FSQ）**，增强训练稳定性。

#### 两阶段自回归扩散模型
- **定义**：生成下一窗口`C`个混合token，条件于历史`H`个token、文本`s`和未来约束`g`（可超出当前窗口`F`帧）。
- **约束注入**：将约束表示为带掩码的显式运动帧，通过**覆盖根**和**拼接身体约束+掩码**的方式输入。长程约束作为附加token输入。
- **两阶段去噪器架构**：
  1. **根Transformer**：从带噪混合token预测干净根（显式）。
  2. **身体Transformer**：条件于预测的干净根，预测干净潜身体。
  3. 两个阶段在每步去噪中交替，保证根与身体相互影响。
- **损失函数**：混合损失(L1)、解码身体损失、目标损失（约束误差）、一致性损失（位置与FK一致）。

#### 测试时操作
- 自回归滑动窗口，使用**延迟感知重规划**：当检测到新输入或缓冲将耗尽时，使用下一个B帧作为重规划缓冲，同时进行异步生成，隐藏推理延迟。

### 2.3 算法流程（文字说明）
1. **训练Tokenizer**：用自编码器（因果Transformer+FSQ）将身体运动压缩为潜向量，重建时加入足滑损失。
2. **训练两阶段去噪器**：用DDPM框架，从训练序列中随机采样窗口，生成历史、约束（在窗口内/外）。随机丢弃文本/约束以支持无分类器引导。使用10步扩散。
3. **推理**：初始无历史时生成第一个窗口；之后用之前生成的运动作为历史；根据用户实时输入（文本、约束）触发重规划。通过重规划缓冲隐藏推理延迟。

## 3. 实验设计：数据集、Benchmark与对比方法

### 3.1 数据集
- **Bones Rigplay**（主要实验）：约700小时专业运动捕捉数据，315k训练/35k测试，保留10%动作类别未见类别测试泛化。动作多样化（走路、活动、战斗等）。数据以20fps子采样，最长10秒。
- **HumanML3D**（基准对比）：约30小时运动数据，排除HumanAct12子集（无关节旋转且存在伪影），保留原始SMPL旋转以便实时动画。

### 3.2 Benchmark与任务
- **文本+约束条件生成**：约束包括根轨迹、根路点、全身关键帧、末端执行器位置/旋转。
- **指标**：FID（分布相似性）、Top-3 R-Precision（文本-运动对齐，用TMR评估器）、足滑比率、约束误差（位置/旋转）。
- **实验设置**：在Bones Rigplay上做消融和超参数分析；在HumanML3D上对比离线（MaskControl）和在线（DiP）方法，还进行了out-of-horizon约束测试以及感知研究（240对比较）。

### 3.3 对比方法
- **离线**：MaskControl（SOTA，支持文本+关节约束，需测试时优化）。
- **在线**：DiP（CAMDM扩展，支持文本+3D目标位置，但上下文短）。
- **消融基线**：显式表示、全局根解码器、一阶段架构。

## 4. 资源与算力

- **Tokenizer训练**：1块NVIDIA A100-SXM4-80GB GPU，400万步，batch size=128，学习率2e-5，余弦调度+10k步热身。
- **Denoiser训练**：4块NVIDIA A100-SXM4-80GB GPU，100万步，batch size=512，学习率2e-5，不采用dropout。
- **交互式Demo**：RTX 4090 GPU，4步模型延迟33ms，10步模型63ms。
- 文中未提及总训练时间，但可估算约数天。

## 5. 实验数量与充分性

### 5.1 实验数量
- **消融实验**（表2）：3组（混合vs显式、全局vs局部根、两阶段vs一阶段），均覆盖文本+多种约束指标。
- **超参数分析**（表3）：生成窗口（4/8/12/20/40帧）、扩散步数（1/2/3/4/10/100）、patch size（1/4/8）、潜容量（16-32/64-32/64-128/64-256）、tokenizer类型（AE/VAE/FSQ）。总共5组，每组含不同配置。
- **HumanML3D基准对比**（表4、5）：离线优化/无优化、在线in-horizon/out-of-horizon。
- **感知研究**（表6）：240对比较，三个准则。
- 总计超过20项定量实验，并包含定性demo和视频。

### 5.2 充分性与公平性
- **充分**：覆盖架构、超参数、不同数据规模、不同约束类型、不同方法，消融合理。
- **公平**：
  - 在Bones Rigplay上使用同一评估器（TMR训练于该数据），避免数据偏差。
  - 在HumanML3D上使用原始评估器，不计入自身预处理优势（如保留SMPL旋转反而可能使指标轻微下降）。
  - 对比MaskControl时区分了有无优化，对比DiP时确保了相同目标设计。
  - 感知研究为盲比，用户投票。
- 略有不足：HumanML3D对比中未与更多近期方法（如PRIMAL、DartControl）比较，但文中表1显示它们缺乏部分能力（如不支持文本或长程约束），因此未直接对比是合理的。

## 6. 主要结论与发现

- **混合表示显著优于显式表示**：在运动质量（FID降低、R-Precision提高）和约束控制误差（尤其在关键帧、路点上大幅降低）上均有优势。
- **两阶段架构优于一阶段**：约束误差（如全身关键帧误差从0.079降至0.023）和运动质量均改善。
- **全局-局部根转换减少足滑**：足滑从0.303降至0.264 m/s。
- **长程规划能力突出**：在out-of-horizon目标上，ARDY（误差2.92cm）远超DiP（17.64cm），且无需额外RL策略。
- **4步扩散即可达到高质量**：10步模型略优，但4步（33ms）已满足实时交互。
- 在HumanML3D上，无需优化的ARDY即可达到与MaskControl+优化相当的约束误差（4.15cm vs 0.45cm），且运动更流畅（足滑更低）。
- 感知研究中，ARDY在运动质量、语义对齐、目标准确度上均被显著偏好（分别65.8%、67.5%、64.6%）。

## 7. 优点

1. **创新性**：首次将混合表示（显式根+潜身体）与两阶段自回归扩散结合，原生支持可变历史和长程约束，无需额外控制模块。
2. **实用性**：
   - 提供完整交互式演示（文本、关键帧、路径、键盘控制），延迟低至33ms。
   - 使用重规划缓冲机制平滑处理推理延迟。
3. **验证充分性**：
   - 在700小时专业数据集上做大规模消融，结论可信。
   - 在公开基准上超越现有方法，并进行感知研究，证明实际效果。
4. **设计可扩展**：Tokenizer支持多种变体（VAE/FSQ），可灵活调整潜容量；扩散步数可权衡速度与精度。

## 8. 不足与局限

1. **历史上下文管理效率低**：显式使用所有过去帧作为条件，对于超长时间任务（数分钟）可能内存和计算开销大，未探索结构化记忆或摘要机制。
2. **计算成本**：尽管4步达到33ms，但相对于单步模型（如A-MDM）仍较慢；多步扩散推理仍是瓶颈，可结合蒸馏或一致性模型进一步加速。
3. **物理不真实**：纯运动学模型，可能出现足滑、穿透或抖动。缺少物理模拟约束，无法直接应用于需要物理反馈的机器人控制或碰撞响应场景。
4. **数据集依赖**：
   - HumanML3D对比中，由于自身预处理保留了联合旋转，评估器基于原版数据可能不利，但作者已指出这点。
   - Bones Rigplay为私有数据，结果无法完全独立复现，但公开了HumanML3D上的代码和模型。
5. **约束类型局限**：目前支持根轨迹、全身/关节关键帧、末端执行器等，但未涵盖物体交互（如拿物品）、多角色交互等更复杂场景。
6. **未见动作类别泛化**：虽然Bones Rigplay测试集包含未见动作，但HumanML3D上未专门验证；在未知类别上鲁棒性需进一步评估。

（完）
