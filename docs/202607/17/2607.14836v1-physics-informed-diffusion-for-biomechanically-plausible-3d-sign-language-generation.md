---
title: Physics-Informed Diffusion for Biomechanically Plausible 3D Sign Language Generation
title_zh: 用于生物力学合理的三维手语生成的物理信息扩散
authors: "Emanuele Colonna, Moises Diaz, Gennaro Vessio, Miguel Angel Ferrer, Giovanna Castellano"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14836v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 用于生物力学合理3D骨骼运动生成的扩散模型
tldr: 手语生成要求语义保真与生物力学合理性，现有方法将骨骼视为非结构化向量导致骨骼长度漂移和关节角违规。PIDiffSign将解剖约束融入扩散模型架构和训练目标，使用可微几何模块确保骨骼长度和关节角一致，结合对比损失与无分类器引导。在PHOENIX14T和CSL-Daily上，姿态精度、关节角正确性、分布真实性和回译质量均显著优于基线。表明物理信息扩散可同时提升动作真实性和语义保真。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14836v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1170, \"height\": 436, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14836v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1072, \"height\": 495, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14836v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1423, \"height\": 417, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14836v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1249, \"height\": 362, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14836v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1268, \"height\": 545, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14836v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 483, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14836v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1263, \"height\": 382, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14836v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 921, \"height\": 295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14836v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1021, \"height\": 294, \"label\": \"Table\"}]"
motivation: 现有手语生成模型因忽略骨骼长度和关节角约束，导致生成动作不自然且语义模糊，亟需引入解剖约束。
method: 提出PIDiffSign，将解剖约束嵌入扩散模型，使用可微几何模块和多种损失（几何、对比、无分类器引导）优化生成。
result: 在PHOENIX14T和CSL-Daily上，姿态精度、关节角正确性、分布真实性和回译质量均显著优于标准扩散基线。
conclusion: 物理信息扩散能有效增强手语生成的生物力学真实性和语义保真度，为可控运动生成提供新范式。
---

## 摘要
手语生成是指从口语输入生成连续的三维骨骼运动，它必须同时满足两个约束：语义保真度，使得聋人观众能够识别预期的词汇序列；以及生物力学合理性，使得生成的骨骼尊重解剖学限制。现有方法通过基于坐标的目标优化语义重建，将骨骼视为非结构化向量，从而允许骨长漂移、关节角违规和暂时手指锁定。我们提出了PIDiffSign，一种用于词汇到姿态翻译的物理信息扩散模型，它将解剖学约束融入模型架构和训练目标中。该模型采用Transformer编码器-解码器结构，其中解码器通过自适应零初始化的层归一化以扩散时间步为条件，并与词汇表示进行交叉注意力。一个可微分的几何模块在整个生成过程中强制执行骨长一致性和生物上有效的关节角度。训练结合了拟人化、运动学、角度和手指关节约束，以及对比性词汇-姿态对齐损失和无分类器引导，以实现语义条件采样。在PHOENIX14T和CSL-Daily基准上的实验表明，在姿态准确性、关节角正确性、分布真实性和反向翻译质量方面，均比强扩散基线有持续改进。这些结果表明，物理信息扩散提高了手语生成的运动真实性和语义保真度。

## Abstract
Sign language production, which generates continuous 3D skeletal motion from spoken language input, must simultaneously satisfy two constraints: semantic fidelity, so that a deaf viewer can recognize the intended sequence of glosses, and biomechanical plausibility, so that the generated skeleton respects anatomical constraints. Existing approaches optimize semantic reconstruction through coordinate-based objectives that treat the skeleton as an unstructured vector, thus allowing for bone length drift, joint angle violations, and temporarily locked fingers. We introduce PIDiffSign, a physics-informed diffusion model for gloss-to-pose translation that incorporates anatomical constraints into both the architecture and training objective. The model uses a Transformer encoder-decoder, where the decoder is conditioned on the diffusion time step through adaptive zero-initialized layer normalization and cross-attends to gloss representations. A differentiable geometry module enforces bone length consistency and biologically valid joint angles throughout generation. Training combines anthropomorphic, kinematic, angular, and finger-joint constraints with a contrastive gloss-pose alignment loss and classifier-free guidance for semantically conditioned sampling. Experiments on the PHOENIX14T and CSL-Daily benchmarks show consistent improvements over a strong diffusion baseline in pose accuracy, joint-angle correctness, distributional realism, and back-translation quality. These results demonstrate that physics-informed diffusion improves both motion realism and semantic fidelity for sign language generation.

---

## 论文详细总结（自动生成）

# 基于物理信息扩散的生物力学合理三维手语生成 —— 论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：手语生成（Sign Language Production, SLP）需要将口语/词汇序列映射为连续的3D骨骼运动，同时满足两个正交约束：
  - **语义保真度**：生成的骨骼序列能被聋人观众识别为正确的词汇序列；
  - **生物力学合理性**：生成的骨骼必须尊重人体解剖约束（恒定骨长、关节角在生物范围内、手指主动运动、轨迹平滑）。
- **现有方法的不足**：
  - 大部分SLP系统将骨骼视为非结构化向量，仅通过坐标级别的MSE损失优化语义重建，**骨长漂移、关节角超限、手指冻结**等失效模式普遍存在，严重影响手语可理解性。
  - 即使近年扩散模型（如Sign-IDD）改善了帧级真实感，仍缺乏对骨骼几何结构的显式建模，解剖约束仅通过数据隐式学习，不足以防止不可行姿态。
- **核心动机**：将手语生成重新定义为**受约束的生成问题**，即在解剖可行的人体运动流形上进行生成；借鉴物理信息神经网络（PINNs）思想，将生物力学先验直接嵌入优化过程，而不是仅靠数据隐式恢复。

## 2. 方法论：核心思想、关键技术细节

### 2.1 整体框架（PIDiffSign）

- 采用**编码器-解码器扩散模型**，其中解码器通过**自适应零初始化层归一化（AdaLN-Zero）** 以扩散时间步为条件，并通过交叉注意力关注词汇编码。
- 输出经过**可微几何精化模块（Geometric Refiner）**，强制执行骨长一致性和关节角约束，最后输出解剖学合理的姿态序列。
- 训练结合**综合损失**（物理信息损失 + 对比对齐损失 + 标准扩散损失），推理使用**无分类器引导（CFG）** 和DDIM采样。

### 2.2 关键技术细节

- **输入表示**：借鉴Sign-IDD的**象似性解缠**，将每根骨骼（父-子关节对）编码为7维向量（包含子关节位置、子-父偏移、方向单位向量），将骨长和方向显式输入网络。
- **解码器条件化**：AdaLN-Zero模块通过零初始化线性投影，将时间步嵌入调制到层归一化的尺度和平移上，使得初始阶段（t=0）保持恒等映射。
- **几何精化模块（Rgeo）**：
  - **骨长校正**：对每根骨骼学习参考长度，沿骨骼方向将子关节移至参考长度，通过可学习门控 `glen` 控制校正强度。
  - **关节角裁剪**：对每个解剖三角（肩、肘、腕、手指链等共43个角），利用Rodrigues公式绕轴旋转，将当前角夹持到生物范围 `[θ_min, θ_max]`，通过门控 `gang` 混合。
  - 整个模块可微，作为训练中的结构归纳偏置，推理时作为硬约束投影器。
- **损失函数**：`L_total = L_diff + λ_a L_anthro + λ_ω L_ang + λ_k L_kin + λ_f L_finger + λ_fm L_fm + λ_align L_align`
  - `L_anthro`：骨长一致性的平方误差。
  - `L_ang`：关节角三项（范围惩罚 + 跟踪Huber + 旋转轴方向余弦）。
  - `L_kin`：二阶时间差分（离散加速度），抑制抖动。
  - `L_finger`：腕相对手指坐标的MSE，隔离手形误差。
  - `L_fm`：手指一阶时间差分，防止手指冻结。
  - `L_align`：基于InfoNCE的交叉模态对比损失，对齐词汇编码器与姿态解码器的平均池化表示，投影到512维单位超球面。
- **训练与采样**：使用DDPM前向过程、余弦噪声调度、x0预测（直接输出干净姿态，方便物理信息损失计算）。推理用DDIM（50步）和CFG（默认ω=2.0）。CFG训练时以概率`p_drop`用填充PAD代替词汇，实现无条件路径。

## 3. 实验设计

### 3.1 数据集与基准

| 数据集 | 语言 | 规模（训练/开发/测试） | 姿态获取 | 回译模型 |
|--------|------|------------------------|----------|----------|
| PHOENIX14T | 德国手语（天气） | 7096/519/642 | OpenPose + 逆运动学 | SignJoey |
| CSL-Daily | 中文手语 | 18401/1077/1176 | SMPL-X正运动学 | JoeyNMT |

### 3.2 指标

- **姿态准确性**：DTW（归一化FastDTW）、MPJPE（mm或归一化单位）、MPJAE（度）
- **分布真实感**：FID（基于逐帧姿态特征）
- **回译质量**：BLEU-4、WER（将生成姿态输入SLT模型，输出文本与真值比较）

### 3.3 对比方法

- **内部基线**：DDPM baseline（相同架构，仅使用masked MSE损失，无物理信息和对比对齐）。
- **已发表SOTA**（仅PHOENIX14T比较）：Progressive Transformers、PT+GAN、GEN-OBT、D3DP-sign、G2P-DDM、GCDM、Sign-IDD等（注：因评价协议不同，用双线隔开，声明不完全可比）。

## 4. 资源与算力

- **硬件**：单张NVIDIA A100 GPU。
- **训练细节**：batch size 64，Adam优化器（lr=1e-4，weight decay 1e-4），1000步线性热身+ReduceLROnPlateau调度，梯度裁剪1.0，最大训练步数根据早停（每250步评估）确定。
- **训练时长**：论文未明确给出总训练时间（如小时数），但提及早期轮次（epoch≈3-5）出现模式坍缩，收敛在epoch≈250-400。具体算力消耗未量化。

## 5. 实验数量与充分性

- **主实验**：在两个数据集上对比PIDiffSign与无物理基线，报告6个指标。
- **消融实验**：逐一去除每个损失组件（`L_align`, `L_ang`, `L_anthro`, `L_kin`, `L_finger`, `L_fm`），观察DTW、MPJPE、MPJAE、FID、BLEU-4的变化，在两个数据集上均进行。
- **CFG尺度消融**：ω从1.0到3.0，观察指标变化。
- **超参数敏感性**：对5个损失权重进行±50%扰动，报告DTW相对变化百分比（小于2.3%）。
- **定性分析**：展示了PHOENIX14T和CSL-Daily的骨骼叠加可视化。

**评价**：实验设计较为充分，覆盖了不同语言、消融所有组件、关键超参数、对比基线。但存在局限性：
- 与已发表SOTA未在统一协议下严格比较（因缺乏公开检查点和SLT模型）。
- 仅在两个数据集上验证，数据集规模中等、领域有限（天气、日常会话），且均为单签名人语料。
- 未进行跨签名人泛化实验或用户研究（真实聋人评估）。

## 6. 主要结论与发现

1. **物理信息扩散显著提升运动真实性和语义保真**：在PHOENIX14T上，PIDiffSign在所有6个指标上优于无物理基线；在CSL-Daily上，除MPJPE略高外，其他5个指标均提升。
2. **对比对齐头（`L_align`）对回译质量贡献最大**：去除后BLEU-4下降最显著（PHOENIX14T: 2.083→1.113），同时FID恶化。
3. **解剖约束驱动分布真实感**：去除`L_kin`导致FID最大退化；去除`L_ang`导致MPJAE上升最多；全模型获得最低FID。
4. **几何约束与回译存在折中**：单独去除某些几何项，BLEU-4有时略升，但以FID和MPJAE下降为代价。
5. **跨语言泛化性**：在中文手语上也取得一致提升，证明物理信息归纳偏置可迁移。
6. **CFG尺度折中**：ω增大提升FID但轻微损害逐帧精度，几何精化模块可以吸收过度引导的分布偏移。

## 7. 优点

- **创新性**：首次将物理信息神经网络范式（PINNs）扩展到离散结构化序列生成（手语骨骼运动），以可微约束替换传统物理模拟器。
- **架构设计**：几何精化模块可微，既作为训练归纳偏置，又作为推理硬约束；门控机制允许自适应调节校正强度。
- **损失设计**：针对手语特有的失效模式（骨长漂移、关节角超限、手指冻结）分别设计损失，并加入对比对齐提升语义区分度。
- **实验严谨**：包含两组不同语言数据集，充分消融所有组件，超参数敏感性分析，CFG尺度分析，定性可视化完整。
- **结果显著**：在不依赖输入级几何解缠的情况下，达到与SOTA相当甚至更优的关节角准确性。

## 8. 不足与局限

- **数据集局限**：仅使用PHOENIX14T（天气领域）和CSL-Daily（日常会话），词汇量有限、单签名人，无法评估跨签名人变化和复杂场景。
- **非手动特征缺失**：未建模面部表情、口型、视线等非手动韵素，而这些对语义至关重要。
- **评估偏差**：回译评估依赖在同一数据集上训练的SLT模型，可能产生领域内膨胀效应，缺乏真实聋人用户研究。
- **计算开销**：CFG需要两次前向推理（有条件和无条件），推理速度降低；几何精化模块虽轻量但仍增加计算。
- **未与SOTA在统一协议下比较**：因不易复现现有系统评估管线，只能定性对比数值，结论的竞争力有待直接比较验证。
- **超参数手动调节**：损失权重通过粗网格搜索得到，在CSL-Daily上MPJPE略劣表明可能校准不足，存在跨数据集敏感度。

（完）
