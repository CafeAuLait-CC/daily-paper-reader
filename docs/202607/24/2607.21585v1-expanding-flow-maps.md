---
title: Expanding Flow Maps
title_zh: 扩展流映射
authors: "Sophia Tang, Pranam Chatterjee"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21585v1"
tags: ["query:3d-motion-generation"]
score: 8.0
evidence: 提出扩展流映射，一类新的基于流的生成模型
tldr: 现有流生成模型受限于固定维度或固定序列长度，无法处理输出大小可变的生成任务。本文提出扩展生成流（EFlows），通过在维度递增的空间上定义扩展插值，将流映射分解为条件扩展算子和传输算子，并蒸馏为少步扩展流映射（EFMs）模型。EFMs联合实现状态扩展与去噪，恢复固定画布流为特例。该方法进一步推广到离散单纯形，支持可变大小图生成和可变长度序列生成。实验表明，EFlows/EFMs将输出大小作为可学习自由度，为变尺寸生成提供了统一且高效的理论框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21585v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1790, \"height\": 537, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21585v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 706, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21585v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1675, \"height\": 1639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21585v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1677, \"height\": 1633, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21585v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1788, \"height\": 606, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21585v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 978, \"height\": 425, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21585v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 744, \"height\": 329, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21585v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 977, \"height\": 348, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21585v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1208, \"height\": 298, \"label\": \"Table\"}]"
motivation: 现有流模型输出维度固定，无法处理输出大小可变的生成任务，如图和序列。
method: 定义维度递增的扩展插值，将流映射分解为条件扩展算子和传输算子，并蒸馏为少步EFMs。
result: 在图像、图、序列等连续/离散模态上实现可变尺寸生成，保持高质量。
conclusion: 将输出大小作为可学习自由度，为变尺寸生成提供统一理论框架。
---

## 摘要
基于流的生成模型在连续和离散状态空间中的快速可控生成方面取得了显著进展，然而现有的参数化方法受限于固定维度或固定序列长度。本文中，我们引入了扩展生成流（EFlows），它通过沿着一个扩展插值器定义维度递增的分布之间的流，该插值器通过添加条件噪声来增长状态。基于这一构造，我们提出了扩展流映射（EFMs），这是一类新的流映射，能够将扩展插值器蒸馏为高效的少步生成模型。每个EFM将任意两个时间步之间的映射分解为两个可学习操作：一个扩展操作，它用新坐标或令牌（以当前状态为条件）扩充状态空间；一个传输映射，它将扩展后的状态沿着插值器向前推动。组合这些操作产生一个单一的映射，它同时扩展和去噪状态，将现有的固定画布流和流映射作为特殊情况，其中扩展操作是恒等映射。我们进一步将框架扩展到离散单纯形，从而实现了可变大小图生成和可变长度序列生成。在连续和离散两种模态中，我们将EFlows和EFMs确立为一个原则性框架，适用于输出大小本身是学习得到的、可控的自由度的场景。

## Abstract
Flow-based generative models have enabled remarkable progress in fast and controllable generation across continuous and discrete state spaces, yet existing parameterizations are constrained to fixed dimensions or fixed sequence lengths. Here, we introduce Expanding Generative Flows (EFlows), which define flows between distributions of increasing dimensionality along an expanding interpolant that grows the state by augmenting it with conditional noise. Building on this construction, we propose Expanding Flow Maps (EFMs), a new class of flow maps that distill the expanding interpolant into efficient few-step generative models. Each EFM factors the map between any two timesteps into two learnable operations: an expand operator, which augments the state space with new coordinates or tokens conditioned on the current state, and a transport map, which pushes the expanded state forward along the interpolant. Composing these operators yields a single map that jointly expands and denoises the state, recovering existing fixed-canvas flows and flow maps as the special case in which the expand operator is the identity. We further extend the framework to the discrete simplex, enabling variable-size graph generation and variable-length sequence generation. Across both continuous and discrete modalities, we establish EFlows and EFMs as a principled framework for settings in which output size is itself a learned, controllable degree of freedom.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Expanding Flow Maps》的详细结构化中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：现有基于流的生成模型（如扩散模型、流匹配模型），其输出空间（维度、序列长度）在生成开始时就被固定。这意味着它们无法处理输出大小本身也是需要被模型学习并确定的生成任务，例如可变分辨率图像、变长音频/视频、可变大小分子图以及不同长度的自然语言句子。这种“固定画布”的限制是当前流模型的一个主要瓶颈。
*   **研究动机**：本文旨在解决“如何在推理阶段实现具有自适应维度或自适应长度的少步（few-step）生成”这一核心问题。目标是构建一个统一的生成框架，使得输出大小成为模型可以学习并控制的自由度。

### 2. 论文提出的方法论：核心思想、关键技术细节

*   **核心思想**：提出**扩展生成流（EFlows）**和**扩展流映射（EFMs）**。EFlows 定义了一种在维度随时间去线性增长的“扩展插值器”（expanding interpolant）上的生成过程。EFMs 则基于 EFlows 进行蒸馏，将多步的扩展生成过程压缩成高效的少步（甚至单步）映射。
*   **关键技术细节**：
    *   **扩展插值器（Expanding Interpolant）**：允许生成路径的维度从 `d(s)` 随时间 `t` 非递减增长到 `d(t)`，这个维度大小的增加是通过引入条件噪声分量来实现的。
    *   **扩展算子（Expand Operator, `E_{s,t}`）**：一个可学习的操作，用于将当前状态 `x_s` 的维度扩充到目标时间 `t` 的维度。它通过在当前状态的特定位置（如末尾、间隙中）插入从条件分布 `p_{ϵ|s,t}` 中采样的噪声 `ϵ` 来实现。不同的插入策略适用于不同的任务：
        *   **连接（Concatenation）**：适用于自回归生成或点云追加。
        *   **位置插入（Positional Insertion）**：适用于变长序列生成或填充。
        *   **子节点扩展（Child Expansion）**：适用于粗到细的生成，如从分子骨架到氢原子的生成。
    *   **传输映射（Transport Map, `X_{s,t}`）**：一个在扩展后的高维空间 `R^{d(t)}` 上运行的标准流映射，用于将扩展后的状态沿着插值器向前推进（即去噪）。对于每个新增的维度，都维护一个“局部时间坐标” `t_i`，表示该维度被插入后的去噪进度。
    *   **扩展流映射（EFM, `Φ_{s,t}`）**：核心模型，将扩展和传输过程联合为一个单一、可端的映射：`Φ_{s,t} = X_{s,t} ∘ E_{s,t}`。这使得模型能在一步操作内同时增加维度并去噪。
    *   **分段确定性马尔可夫过程（PDMP）**：从理论层面统一了EFlow的扩展（跳跃）和传输（连续）过程，为模型提供了一个坚实的数学基础。
    *   **离散空间扩展**：将EFlows和EFMs扩展到离散单纯形空间，用于变长离散序列和可变大小图生成。其关键组件是：
        *   **插入头（Insertion Head）**：一个可学习网络，用于预测在给定上下文中，每一步需要插入多少个新令牌或新节点。
        *   **离散扩展算子**：根据插入头的预测，从二项分布或泊松分布中采样，决定在哪些“空隙”中插入新令牌。新增的令牌以噪声形式加入。
        *   **离散传输映射**：基于扩展后的状态，通过一个平均去噪器（Mean Denoiser）将状态往干净数据的方向推动。

### 3. 实验设计：数据集、基准与对比方法

*   **分子构象生成（连续空间）**：
    *   **数据集**：GEOM-QM9 和 GEOM-Drugs。
    *   **基准**：与多步扩散模型（GeoDiff, SubgDiff, MSGEN）和传统深度生成模型（GraphDG, ConfGF等）对比。
    *   **度量**：覆盖率（COV）和平均最小RMSD（AMR）在召回率和精确率两个维度。
*   **离散分子图生成**：
    *   **数据集**：QM9 小分子数据集。
    *   **基准**：与多步离散流模型（DeFoG）和少步分类流映射（CFM）对比。
    *   **度量**：有效性（Validity）、独特性（Uniqueness）和弗雷歇 ChemNet 距离（FCD）。
*   **变长语言建模**：
    *   **数据集**：One Billion Word Benchmark (LM1B)。
    *   **基准（多步）**：与固定长度流语言模型（FLM）对比。
    *   **基准（少步）**：与多种蒸馏扩散模型（Duo, MDLM的变体）和少步流映射模型（CFM, FMLM）对比。
    *   **度量**：生成困惑度（Gen. PPL）和样本熵（Entropy）。

### 4. 资源与算力

*   **分子构象生成**：在单张 NVIDIA A100 GPU上训练，需要 300 个 epoch。
*   **分子图生成**：在单张 NVIDIA A100 GPU上训练，最多 2×10^6 训练步。
*   **语言建模**：在 4 张 NVIDIA B200 GPU上进行训练，共 2×10^5 步。
*   **总结**：文中明确提到了不同实验使用了不同数量和型号的GPU（A100，B200），算力信息详实。

### 5. 实验数量与充分性

*   **实验数量**：论文在**三个**截然不同的任务（连续空间分子构象、离散空间分子图、变长文本）上进行了评估。在每个任务内部，又在**多个采样步数**（从1步到上千步）下进行了测试，并与**多种**当前最优方法进行了比较。例如，在分子构象任务上，EFM 在 1、2、4 步等多个设置下进行了测试。
*   **充分性与公平性**：
    *   **充分性**：实验覆盖了连续和离散两大模态，并在多步（教师模型）和少步（蒸馏学生模型）两个场景下进行验证，设计相当全面。论文还进行了化学性质预测等额外评估，进一步验证了生成的物理合理性。
    *   **客观性与公平性**：
        1.  **对比全面**：与当前最相关、最先进的多种基线模型都进行了比较。
        2.  **实验设置透明**：在附录中详细描述了数据拆分、模型架构、超参数等细节。
        3.  **结果分析客观**：能指出自身在单步语言生成上的模式坍缩不足，并对实验结果进行了解释。

### 6. 论文的主要结论与发现

*   EFlows 和 EFMs 成功构建了一个统一的生成框架，能够处理连续/离散模态中输出维度/长度可变的生成任务。
*   EFMs 通过蒸馏，实现了少步（低至1-4步）生成，同时在图像/分子构象、分子图和文本生成任务上达到了与多步基线模型竞争甚至超越的性能。
*   “扩展”和“传输”的解耦设计，使得模型将输出大小作为学习到的自由度，克服了固定画布模型的限制。
*   该方法在分子科学（构象、图生成）和语言建模领域都展现了强大的适用性和灵活性，为少步变尺寸生成提供了新的有效范式。

### 7. 优点

*   **理论贡献**：提出了一个新颖且坚实的变维度生成框架（EFlows, EFMs），并为其提供了丰富的理论支撑（如PDMP表述、一致性恒等式）。
*   **通用性**：框架天生支持连续和离散两种模态，并在三种差异很大的任务上均得到验证，展现了极强的通用性。
*   **高效率**：通过蒸馏得到的EFMs模型，能够实现高效的少步甚至单步生成，显著降低了推理成本。
*   **实验设计严谨**：实验设置全面，对比了多步和少步两个维度的多种基线方法，结果分析客观公正，证明了方法的有效性和优越性。

### 8. 不足与局限

*   **计算复杂度**：模型需要处理并预测每个维度的“局部时间坐标”，在扩展到非常大维度或长度时，理论上的复杂度会增加，论文承认这一点未在实验中充分探索。
*   **单步生成性能不佳**：在语言建模实验中发现，1步采样时会出现模式坍缩，生成质量下降。这表明在极少数步数下，将插入和去噪过程完美压缩到单次调用仍存在挑战。
*   **结构依赖性**：扩展算子的具体形式（如连接、位置插入）会对任务结构有一定依赖，虽然论文给出了几种形式，但对于任意结构的新任务可能需要重新设计。
*   **实验范围**：验证的分子和文本长度有限（分子最大181原子，文本最大128 tokens），在更大尺度任务上的表现有待进一步验证。

（完）
