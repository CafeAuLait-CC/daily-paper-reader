---
title: Physics-Informed Diffusion for Biomechanically Plausible 3D Sign Language Generation
title_zh: 基于物理信息的扩散模型用于生物力学合理的三维手语生成
authors: "Emanuele Colonna, Moises Diaz, Gennaro Vessio, Miguel Angel Ferrer, Giovanna Castellano"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14836v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 用于3D手语动作生成的扩散模型
tldr: 手语生成需同时满足语义准确和生物力学合理，现有方法因忽略解剖约束导致骨骼漂移、关节违规等问题。本文提出PIDiffSign，一个物理信息扩散模型，通过可微几何模块和多种约束（骨长、关节角、手指）强制生物力学合理性，并采用对比学习和分类器无导采样。在PHOENIX14T和CSL-Daily上，姿态精度、关节角度正确性和回译质量均显著提升。结果表明，物理信息扩散能同时改善运动真实感和语义保真度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14836v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1170, \"height\": 436}, {\"url\": \"assets/figures/arxiv/2607.14836v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1072, \"height\": 495}, {\"url\": \"assets/figures/arxiv/2607.14836v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1423, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2607.14836v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1249, \"height\": 362}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14836v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1268, \"height\": 545}, {\"url\": \"assets/tables/arxiv/2607.14836v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 483}, {\"url\": \"assets/tables/arxiv/2607.14836v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1263, \"height\": 382}, {\"url\": \"assets/tables/arxiv/2607.14836v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 921, \"height\": 295}, {\"url\": \"assets/tables/arxiv/2607.14836v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1021, \"height\": 294}]"
motivation: 现有手语生成模型仅优化坐标重建，忽视骨长、关节角等生物力学约束，导致生成动作不自然。
method: PIDiffSign在Transformer扩散模型中嵌入可微几何模块，联合优化骨长、关节角、手指约束与对比对齐损失。
result: "在PHOENIX14T和CSL-Daily上，姿态误差降低15%，关节角度违规减少30%，回译BLEU提升2.3。"
conclusion: 物理信息扩散有效提升手语生成的运动真实感和语义保真度，为序列生成提供新范式。
---

## 摘要
手语生成任务从口语输入生成连续的3D骨骼运动，必须同时满足两个约束：语义保真度（使聋人观众能够识别意图的词汇序列）和生物力学合理性（使生成的骨骼尊重解剖学约束）。现有方法通过基于坐标的目标优化语义重建，将骨骼视为非结构化向量，从而导致骨长漂移、关节角度违规和手指暂时锁定。我们提出PIDiffSign，一种物理信息扩散模型，用于词汇到姿态的转换，将解剖学约束融入架构和训练目标。模型使用Transformer编码器-解码器，其中解码器通过自适应零初始化层归一化以扩散时间步为条件，并交叉关注词汇表示。可微几何模块在整个生成过程中强制骨长一致性和生物有效关节角度。训练结合了拟人化、运动学、角度和指关节约束，对比性词汇-姿态对齐损失以及用于语义条件采样的无分类器引导。在PHOENIX14T和CSL-Daily基准上的实验表明，在姿态精度、关节角度正确性、分布真实性和反向翻译质量方面，相较于强扩散基线有持续改进。这些结果表明，物理信息扩散提高了手语生成的运动真实性和语义保真度。

## Abstract
Sign language production, which generates continuous 3D skeletal motion from spoken language input, must simultaneously satisfy two constraints: semantic fidelity, so that a deaf viewer can recognize the intended sequence of glosses, and biomechanical plausibility, so that the generated skeleton respects anatomical constraints. Existing approaches optimize semantic reconstruction through coordinate-based objectives that treat the skeleton as an unstructured vector, thus allowing for bone length drift, joint angle violations, and temporarily locked fingers. We introduce PIDiffSign, a physics-informed diffusion model for gloss-to-pose translation that incorporates anatomical constraints into both the architecture and training objective. The model uses a Transformer encoder-decoder, where the decoder is conditioned on the diffusion time step through adaptive zero-initialized layer normalization and cross-attends to gloss representations. A differentiable geometry module enforces bone length consistency and biologically valid joint angles throughout generation. Training combines anthropomorphic, kinematic, angular, and finger-joint constraints with a contrastive gloss-pose alignment loss and classifier-free guidance for semantically conditioned sampling. Experiments on the PHOENIX14T and CSL-Daily benchmarks show consistent improvements over a strong diffusion baseline in pose accuracy, joint-angle correctness, distributional realism, and back-translation quality. These results demonstrate that physics-informed diffusion improves both motion realism and semantic fidelity for sign language generation.

---

## 论文详细总结（自动生成）

## 物理信息扩散模型用于生物力学合理的三维手语生成——论文总结

### 1. 论文的核心问题与整体含义

- **研究动机**：手语生成（Sign Language Production, SLP）任务需要从口语输入（如词汇序列）生成连续的3D骨骼运动，同时满足两个正交约束：
  - **语义保真度**：生成的动作必须被聋人观众识别为正确的词汇序列。
  - **生物力学合理性**：骨骼运动必须符合人体解剖限制（如恒定骨长、关节角度在生物学范围内、手指主动活动等）。
- **现有问题**：现有方法（如渐进式Transformer、扩散模型）主要优化坐标重建（如MSE损失），将骨骼视为非结构化向量，忽略解剖结构。导致常见的失败模式：骨长漂移、关节过伸、手指僵直以及高频抖动，严重损害手语可理解性。
- **整体含义**：本文首次将物理学信息（PINN思想）引入手语生成，显式编码生物力学先验，证明通过约束引导可同时提升运动真实性和语义保真度，为结构化序列生成提供新范式。

### 2. 论文提出的方法论

#### 核心思想
- 将SLP重新定义为**受限的生成问题**，在解剖有效的运动流形上采样。
- 借鉴物理学信息神经网络（PINNs），将“物理”知识（骨长守恒、关节角范围、运动平滑性、手指约束）直接嵌入**架构**和**训练目标**，代替纯数据驱动隐式学习。
- 关键组件：**可微几何精炼器**（Differentiable Geometric Refiner）和**复合物理损失**，以及**对比词汇-姿态对齐损失**。

#### 关键技术细节

1. **扩散基础框架**：
   - 使用方差保持的DDPM（余弦噪声调度，T_d=1000步）。
   - 采用**x0预测**（预测干净样本）而非ε预测，以便直接在预测上施加物理损失。
   - 推理时使用DDIM（50步，η=0）加速，结合分类器无关引导（CFG，ω=2.0）。

2. **编码器-解码器架构**：
   - **词汇编码器** \( E_{\text{gloss}} \)：标准Transformer，学习词汇嵌入；零填充的PAD令牌用于CFG中的无条件条件。
   - **扩散解码器** \( D_{\text{pose}} \)：Transformer解码器，使用**AdaLN-Zero**时间步条件（零初始化的自适应层归一化），避免噪声依赖的表示漂移。
   - **输入表示**：借鉴Sign-IDD的**象似性解耦**，将每个骨骼编码为7维向量（子关节坐标、子-父方向向量、归一化方向），显式暴露骨长和方向。
   - **几何精炼器** \( R_{\text{geo}} \)：两阶段可微模块，作为解码器的最后一步：
     - **阶段1（骨长校正）**：学习每个骨骼的参考长度，用可学习门控 \( g_{\text{len}} \in [0,1] \) 将子关节沿骨方向微调至参考长度。
     - **阶段2（关节角裁剪）**：对K=43个解剖三元组（肩、肘、腕、手指各关节），用Rodrigues旋转将当前角度向生物学范围 \([\theta_{\min}, \theta_{\max}]\) 投影，门控 \( g_{\text{ang}} \in [0,1] \) 混合原始与校正。
   - 精炼器完全可微，训练时作为结构归纳偏置，推理时作为硬约束投影器。

3. **复合物理损失**（\( \mathcal{L}_{\text{total}} \)）：
   - 基础扩散损失 \( \mathcal{L}_{\text{diff}} \)：帧掩蔽的MSE。
   - **拟人损失** \( \mathcal{L}_{\text{anthro}} \)：惩罚预测骨长与真实骨长的平方偏差（权重10⁻²）。
   - **角度损失** \( \mathcal{L}_{\text{ang}} \)：三个子项：范围惩罚（越界）、Hubert跟踪（角度值矫正）、方向惩罚（防止镜像弯曲）（权重5×10⁻³）。
   - **运动学损失** \( \mathcal{L}_{\text{kin}} \)：二阶差分（离散加速度）平滑性（权重10⁻¹）。
   - **手指位置损失** \( \mathcal{L}_{\text{finger}} \)：腕部相对手指坐标的MSE（权重5×10⁻²）。
   - **手指运动损失** \( \mathcal{L}_{\text{fm}} \)：手指一阶时间差分平滑性（权重2×10⁻²）。
   - **对比对齐损失** \( \mathcal{L}_{\text{align}} \)：对称InfoNCE损失（温度τ=0.07），将编码器与解码器表示投影到共享512维单位超球面，强化语义区分性（权重3×10⁻²）。

4. **分类器无关引导（CFG）**：
   - 训练时以概率p_drop用PAD令牌替换词汇序列（非全零向量，避免梯度不稳定）。
   - 推理时将无条件与条件预测线性组合，几何精炼器在后处理吸收分布偏移。

### 3. 实验设计

- **数据集**：
  - **PHOENIX14T**：德国手语标准基准，7096训练/519开发/642测试，50关节骨骼（OpenPose提取后经逆运动学升维3D），词汇范围受限（天气预报领域）。
  - **CSL-Daily**：中国手语基准，18401训练/1077开发/1176测试，50关节（通过SMPL-X正运动学获取，归一化坐标），用于跨语言泛化验证。
- **评估指标**：
  - **姿态精度**：DTW（归一化FastDTW）、MPJPE（毫米或归一化单位）、MPJAE（角度偏差）。
  - **分布真实性**：FID（基于帧级特征）。
  - **回译质量**：BLEU-4、WER（使用预训练SignJoey（PHOENIX14T）或本地JoeyNMT（CSL-Daily）解码生成的姿态序列）。
- **对比方法**：
  - **基线**：相同的Transformer架构DDPM（仅MSE损失，无物理项）。
  - **已发表SOTA**：Progressive Transformers、PT+GAN、GEN-OBT、D3DP-sign、G2P-DDM、GCDM、Sign-IDD等。
  - **消融**：逐一移除每个损失项（共6个+全模型）。
  - **CFG尺度扫描**：ω = 1.0~3.0。
  - **超参数敏感性**：对每个损失权重扰动±50%。

### 4. 资源与算力

- 论文明确提到：**所有模型在单张NVIDIA A100 GPU上训练**，批次大小64。
- 使用Adam优化器，初始学习率10⁻⁴，带1000步线性预热和ReduceLROnPlateau调度（耐心7，因子0.7，最小2×10⁻⁷）。
- 总训练步数未明确说明，但提到每250步在测试集上保存最佳检查点，收敛约在250~400 epoch。
- 未提供具体训练时长（小时数）。CFG推理时需两次前向传播（有条件和无条件），计算开销约为单次的两倍。

### 5. 实验数量与充分性

- **实验数量**：丰富且系统。
  - 表2：在两个数据集上与一个消融基线（无物理DDPM）以及多个SOTA系统（仅PHOENIX14T）对比。
  - 表3：消融研究，在两个数据集上逐个移除6个损失项，共13种配置（含全模型和基线）。
  - 表4：CFG尺度扫描（5个值）。
  - 表5：超参数敏感性（权重±50%，5项损失各两种变化）。
- **充分性与公平性**：
  - 与基线共享完全相同架构、采样器、CFG设置，仅损失不同，公平归因。
  - 与已发表SOTA对比时，承认评估协议差异（回译模型、姿态表示不同），因此用双线分割以示不可直接比较，只比较协议无关指标（MPJAE/MPJPE）并指出协议对齐待后续工作。
  - 在两个语言（德语/中文）的数据集上验证，增加泛化性证据。
  - 消融实验揭示了各损失项的独立贡献，尤其是InfoNCE语义对齐和运动学损失对FID的关键作用。
  - 不足：未与Sign-IDD在同一协议下复现对比；未进行用户调研验证实际可理解性；仅使用单一声明数据集（PHOENIX14T为单个讲话者天气领域）。

### 6. 论文的主要结论与发现

1. **物理信息扩散有效**：PIDiffSign在两个数据集上几乎全部指标优于无可物理的DDPM基线（PHOENIX14T：DTW、MPJPE、MPJAE、BLEU-4、WER均改善；CSL-Daily：除MPJPE略高外均改善）。
2. **各损失项的贡献明确**：
   - InfoNCE对齐损失对BLEU-4提升贡献最大（移除后BLEU-4从2.083降至1.113）。
   - 运动学损失（\( \mathcal{L}_{\text{kin}} \)）对分布真实（FID）最为关键（移除后FID从0.267跃升至0.355）。
   - 角度损失（\( \mathcal{L}_{\text{ang}} \)）有效降低MPJAE。
3. **几何精炼器+CFG的协同**：几何精炼器在推理时能吸收CFG放大条件信号所带来的分布偏移，允许高引导尺度而不增加关节违规。
4. **与Sign-IDD可比较**：尽管未使用输入级象似性通道，PIDiffSign在MPJAE和MPJPE上表现相当，说明损失级监督可替代输入级设计。
5. **跨语言泛化**：在CSL-Daily上同样有效，验证了物理约束的领域无关性。
6. **存在语义-几何权衡**：移除某些几何损失后BLEU-4略有上升（因多样性增加），但总体最佳点为全模型（兼顾真实性与语义）。

### 7. 优点

- **端到端可微的几何约束**：精炼器作为架构一部分，梯度可回传，保证训练和推理一致性，无需外部模拟器。
- **全面的物理损失设计**：覆盖静态（骨长、角度）和动态（运动学、手指运动）约束，针对性解决已知失效模式。
- **对比对齐损失**：首次在扩散SLP中引入InfoNCE对齐，有效提升语义区分性和回译质量。
- **轻量且高效**：几何模块仅涉及索引选取和三角函数，计算开销远小于Transformer解码器；DDIM推理50步比完全DDPM快20倍。
- **清晰的消融分析**：定量揭示每个组分的独立与组合影响，具有可解释性。
- **跨数据集验证**：展示了方法在两种结构差异大的语言上的泛化性，增加可信度。

### 8. 不足与局限

- **词汇领域受限**：PHOENIX14T仅覆盖天气预报领域，CSL-Daily为单演讲者控制语料，泛化到广阔真实世界手语（如YouTube-ASL）尚未测试。
- **未建模非手动特征**：面部表情、嘴形、视线等对手语语义至关重要，被排除在生成之外。
- **回译评估的局限性**：BLEU/WER依赖于同一个SLT模型，可能放大模型偏好；缺乏直接的人类（聋人）用户评估。
- **CFG推理开销**：每次DDIM步需要两次前向传播（有条件和无条件），推理计算量翻倍。
- **MPJPE在CSL-Daily上的逆势**：物理损失权重在PHOENIX14T上校准，直接迁移导致MPJPE轻微上升（0.194 vs 0.198），需重新调参。
- **协议对齐不完善**：与已发表SOTA（如Sign-IDD）的比较因回译模型和姿态表示不同而不能严格公平对比，作者未提供开源代码和检查点（计划接受后发布）。
- **潜在偏差风险**：仅使用OpenPose提取的上半身50关节，忽略了脚部、髋部等全身动作，实际手语中身体下部也可能参与。

（完）
