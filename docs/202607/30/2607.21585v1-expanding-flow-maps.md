---
title: Expanding Flow Maps
title_zh: 扩展流映射
authors: "Sophia Tang, Pranam Chatterjee"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21585v1"
tags: ["query:3d-motion-generation"]
score: 8.0
evidence: 提出扩展生成流和流图用于生成建模
tldr: 现有流模型受限于固定维度或序列长度，难以处理输出尺寸可变的生成任务。为此提出Expanding Generative Flows (EFlows)，通过扩展插值器逐步增加状态维度，并蒸馏为Expanding Flow Maps (EFMs)实现少步生成。EFMs由扩展算子和传输算子组成，联合扩展与去噪，恢复固定画布流作为特例。方法推广到离散空间，支持可变大小图和序列生成，使输出尺寸成为可学习自由度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21585v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1790, \"height\": 537}, {\"url\": \"assets/figures/arxiv/2607.21585v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 706, \"height\": 584}, {\"url\": \"assets/figures/arxiv/2607.21585v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1675, \"height\": 1639}, {\"url\": \"assets/figures/arxiv/2607.21585v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1677, \"height\": 1633}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21585v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1788, \"height\": 606}, {\"url\": \"assets/tables/arxiv/2607.21585v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 978, \"height\": 425}, {\"url\": \"assets/tables/arxiv/2607.21585v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 744, \"height\": 329}, {\"url\": \"assets/tables/arxiv/2607.21585v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 977, \"height\": 348}, {\"url\": \"assets/tables/arxiv/2607.21585v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1208, \"height\": 298}]"
motivation: 解决现有流模型无法处理输出维度或序列长度可变的问题，例如可变大小图或变长序列生成。
method: 定义扩展插值器逐步增加状态维度，并设计EFMs，通过扩展算子（增广状态空间）与传输算子（推动状态沿插值器前进）组合实现可变尺寸生成。
result: EFMs将扩展插值器蒸馏为高效少步生成模型，在连续和离散模态均有效，且可恢复固定画布流作为特殊情形。
conclusion: EFMs为输出尺寸可变的生成任务提供统一框架，将流动映射推广到可变维度，拓展了生成模型的应用范围。
---

## 摘要
基于流的生成模型在连续和离散状态空间上的快速可控生成方面取得了显著进展，但现有的参数化方法受限于固定维度或固定序列长度。在此，我们引入扩展生成流（EFlows），它通过沿扩展插值器（该插值器通过添加条件噪声来增大状态）在维度递增的分布之间定义流。基于此构造，我们提出扩展流映射（EFMs），这是一类新的流映射，可将扩展插值器蒸馏为高效的少步生成模型。每个EFM将任意两个时间步之间的映射分解为两个可学习操作：扩展算子（基于当前状态用新坐标或标记扩充状态空间）和传输映射（沿插值器将扩展状态向前推进）。组合这些算子可生成一个联合扩展和去噪状态的单一映射，当扩展算子为恒等映射时，可恢复现有的固定画布流和流映射。我们进一步将该框架扩展到离散单纯形，实现可变大小图生成和可变长度序列生成。在连续和离散模态中，我们将EFlows和EFMs建立为一个原则性框架，适用于输出大小本身是学习到的可控自由度的场景。

## Abstract
Flow-based generative models have enabled remarkable progress in fast and controllable generation across continuous and discrete state spaces, yet existing parameterizations are constrained to fixed dimensions or fixed sequence lengths. Here, we introduce Expanding Generative Flows (EFlows), which define flows between distributions of increasing dimensionality along an expanding interpolant that grows the state by augmenting it with conditional noise. Building on this construction, we propose Expanding Flow Maps (EFMs), a new class of flow maps that distill the expanding interpolant into efficient few-step generative models. Each EFM factors the map between any two timesteps into two learnable operations: an expand operator, which augments the state space with new coordinates or tokens conditioned on the current state, and a transport map, which pushes the expanded state forward along the interpolant. Composing these operators yields a single map that jointly expands and denoises the state, recovering existing fixed-canvas flows and flow maps as the special case in which the expand operator is the identity. We further extend the framework to the discrete simplex, enabling variable-size graph generation and variable-length sequence generation. Across both continuous and discrete modalities, we establish EFlows and EFMs as a principled framework for settings in which output size is itself a learned, controllable degree of freedom.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有流模型（flow-based generative models）和流映射（flow maps）受限于固定维度（continuous state space）或固定序列长度（discrete state space），无法处理输出大小本身可变的生成任务（如变分辨率图像、变长序列、可变大小图等）。
- **背景**：流映射（Flow Maps）作为少步生成统一框架，但传统方法必须预定义输出尺寸。作者希望将输出维度或长度也作为模型的“可学习自由度”，从而突破这一限制。
- **整体含义**：提出 Expanding Generative Flows (EFlows) 和 Expanding Flow Maps (EFMs)，在连续和离散状态空间上实现可变维度/可变长度的生成，且支持少步采样。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 定义**扩展插值器（Expanding Interpolant）**：状态维度随时间步 t 非递减，即 d(t) : [0,1] → N，使得源分布（低维）逐步扩大到目标分布（高维）。
- **扩展算子（Expand Operator）Es,t**：将低维状态 xs 通过添加条件噪声 ϵ 扩充到高维空间 Rd(t)，具体形式包括拼接、位置插入、子节点扩展等。
- **传输映射（Transport Map）Xs,t**：在扩充后的空间中，沿插值器 ODE 将状态向前推进。
- **合成**：Φs,t ≡ Xs,t ∘ Es,t，即可联合扩展和去噪。

### 关键技术细节
- **局部时间坐标**：每个新插入的坐标 i 有自己的本地时间 ti = (t - t_insert_i)/(1 - t_insert_i)，使所有坐标在统一局部时钟下退化。
- **分段确定性马尔可夫过程（PDMP）**：将扩展与传输统一为一个跳变-扩散过程，生成器包含传输项和跳变项。
- **Expanding Flow Maps (EFMs)**：通过蒸馏（distillation）将扩展插值器转化为少步生成模型。连续空间用平均速度 vs,t 参数化，离散空间用平均去噪器 ψs,t 参数化。
- **一致条件**：拉格朗日、欧拉、半群条件被扩展到可变维度情形，用于训练 EFM。
- **离散扩展**：引入插入调度 αt、插入头预测期望插入数、二项式采样实现变长图/序列生成。

### 数学形式（文字描述）
- 连续 EFM：Φs,t(xs) = Es,t(xs) + (t - s) vs,t(Es,t(xs))
- 离散 EFM：Φs,t(xs) = (1-t)/(1-s) Es,t(xs) + (t-s)/(1-s) ψs,t(Es,t(xs))

## 3. 实验设计：数据集、benchmark、对比方法

### 实验场景
1. **分子构象生成（连续 3D 坐标）**：
   - 数据集：GEOM-QM9（最多 29 个原子）、GEOM-Drugs（最多 181 个原子）
   - 任务：从粗到细生成全部原子（先重原子骨架，再插入氢原子）
   - 指标：Coverage (COV) 和 Average Minimum RMSD (AMR) 的 Recall/Precision
   - 对比方法：GraphDG、CGCF、ConfVAE、GeoMol、ConfGF、GeoDiff、SubgDiff、MSGEN 等（均需 500-1000 步）
   - 自己方法：EFlow (20-30 步)、EFM (1-4 步)、+R (添加 10 步精炼)

2. **离散分子图生成**：
   - 数据集：QM9（小分子，最多 9 个重原子）
   - 任务：生成节点和边类型
   - 指标：Validity、Uniqueness、Fréchet ChemNet Distance (FCD)
   - 对比方法：DeFoG（多步离散流）、CFM（少步流映射）
   - 自己方法：EFlow (多步)、EFM (1-2 步)

3. **变长语言建模**：
   - 数据集：One Billion Word Benchmark (LM1B)，词汇表 V=30522
   - 任务：无条件文本生成，序列长度可变（最长 128 token）
   - 指标：Generative Perplexity (GPT-2-Large 评估)、熵
   - 对比方法：FLM（固定长度流语言模型）、Duo、MDLM、CFM、FMLM 等蒸馏方法
   - 自己方法：EFlow (64-1024 步)、EFM (1/2/4 步)

### Benchmark
- 分子构象：与多步扩散（500-1000 步）和经典生成模型对比。
- 分子图：与 DeFoG（多步）和 CFM（少步）对比。
- 语言：与 FLM（多步）及多种蒸馏方法（DCD、Di4C 等）对比。

## 4. 资源与算力（文中提及）

- **分子构象**：未明确说明单次训练 GPU 型号和时长，但提到“在单个 NVIDIA A100 GPU 上训练 300 个 epoch”。
- **分子图**：在单块 NVIDIA A100 GPU 上训练，batch size 256，最多 2×10⁶ 步。
- **语言建模**：在 4 块 NVIDIA B200 GPU 上训练，global batch size 512，共 2×10⁵ 步，约需数天（未精确说明）。EFM 蒸馏使用相同配置。
- **总体**：论文未详细列出总 GPU 小时数，但给出了训练配置，表明算力需求适度（A100 级别）。

## 5. 实验数量与充分性

- **实验数量**：涵盖三个模态（连续分子、离散图、离散序列），每个模态有多个 step 设置（1-1024 步），并包含消融（+R 精炼）、蒸馏（EFM vs EFlow）等。
- **充分性**：对比了多种当前 SOTA 方法，指标全面（COV、AMR、FCD、Perplexity、熵、化学性质），并且在同一数据集上重复了标准评估协议（如 GeoDiff 分裂）。实验设计较为客观公平。
- **局限性**：缺少对更大规模数据集（如更长序列或更大分子）的验证；缺少消融研究如不同扩展算子或插入调度的对比。

## 6. 论文的主要结论与发现

- EFlow/EFM 在**少步**（甚至 1-4 步）条件下，性能可与多步扩散（500-1000 步）相媲美甚至超越，尤其在分子构象和分子图任务中。
- 变长生成未降低样本质量：在 LM1B 上，EFlow 在 64-1024 步的 perplexity 低于固定长度 FLM 基线。
- EFM 在离散图生成中 1 步即达到 FCD 0.44（CFM 为 2.14），展示了框架在极端少步场景的优势。
- 框架统一连续和离散模态，输出尺寸成为可学习自由度。

## 7. 优点

- **创新性**：首次提出可变维度的流映射蒸馏框架，统一了连续/离散空间。
- **通用性**：显式处理输出尺寸可变问题，可扩展到图像、音频、多模态等未来场景。
- **先进性**：在多个少步生成长率指标上大幅超越基线，尤其在分子图（1 步 FCD 0.44）和分子构象（4 步 COV-R 83.62%）表现突出。
- **理论完备**：定义了扩展插值器、PDMP、一致条件等严格理论，具有数学严谨性。

## 8. 不足与局限

- **实验覆盖有限**：最大数据集规模较小（GEOM-Drugs 最大 181 原子，LM1B 最长 128 token），未验证更大维度（如图像、长文本）。
- **缺乏消融**：未对扩展算子形式、插入调度、局部时间重参数化等关键组件进行系统性消融。
- **一步采样退化**：在语言模型中，EFM 一步采样出现模式崩溃（perplexity 虽低但熵下降），作者归因于训练与采样分布不匹配，但未完全解决。
- **计算开销**：虽然少步推理快，但训练 EFM 需要冻结教师网络并多次前向，计算成本仍较高。
- **偏见风险**：仅评测英文文本、小分子，未覆盖多模态或长程依赖场景，泛化性需进一步验证。

（完）
