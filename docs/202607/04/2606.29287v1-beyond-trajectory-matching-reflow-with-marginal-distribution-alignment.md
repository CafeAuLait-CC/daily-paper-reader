---
title: "Beyond Trajectory Matching: Reflow with Marginal Distribution Alignment"
title_zh: 超越轨迹匹配：基于边缘分布对齐的Reflow
authors: "Chen Wang, Peiran Yun, Pan Xie, Ke Deng"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29287v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 提出结合边际分布对齐的重流方法用于流模型
tldr: 扩散模型加速中，reflow蒸馏通过匹配轨迹简化教师ODE，但轨迹匹配无法完全确定学生模型分布，导致生成质量不稳定。本文提出边际对齐正则化，追踪学生ODE对数密度变化并与冻结教师分数比较，惩罚边际分布差异。证明局部对齐控制最终分布差异，实验有效提升少步生成质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29287v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 593}, {\"url\": \"assets/figures/arxiv/2606.29287v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1445, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2606.29287v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 924, \"height\": 1268}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29287v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1236, \"height\": 311}, {\"url\": \"assets/tables/arxiv/2606.29287v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 618, \"height\": 548}, {\"url\": \"assets/tables/arxiv/2606.29287v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 761, \"height\": 444}, {\"url\": \"assets/tables/arxiv/2606.29287v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1007, \"height\": 459}, {\"url\": \"assets/tables/arxiv/2606.29287v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 724, \"height\": 169}, {\"url\": \"assets/tables/arxiv/2606.29287v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1045, \"height\": 166}]"
motivation: reflow蒸馏存在轨迹匹配不足以确定学生分布的理论局限，学生模型可能损失相同但产生不同边际分布，影响生成质量。
method: 提出边际对齐正则化，通过追踪学生ODE对数密度变化，利用冻结教师模型分数计算正则项，无需额外网络或对抗优化。
result: 在基准模型上实验表明，该方法有效提升少步生成质量，并统一适用于vanilla reflow和piecewise reflow。
conclusion: 局部边际对齐可有效控制学生与教师最终分布差异，为加速扩散生成提供理论保障和实用方案。
---

## 摘要
扩散和连续流生成模型实现了高质量生成，其确定性采样可以表述为求解学得的ODE动力学。然而，精确的ODE离散化通常需要许多步骤，因此实现高效的少步生成成为关键挑战。在加速策略中，基于重流的蒸馏简化了教师ODE轨迹，使得学生模型能够以更少的步骤近似教师传输。我们发现了该范式的理论局限性，即轨迹匹配可能无法完全确定学生模型诱导的分布。具体而言，两个学生模型可以达到相同的轨迹匹配损失，但在每个蒸馏区间端点诱导出不同的边缘分布，这可能导致不同的生成质量。为解决这一局限性，我们引入了一种边缘对齐正则化项，惩罚学生诱导边缘与对应教师边缘在每个蒸馏区间端点处的差异。该正则化项通过跟踪学生模型诱导的ODE沿线的对数密度变化，并评估冻结教师模型的分数来计算，无需辅助可训练网络或对抗优化。所得框架统一适用于重流家族，包括普通重流和分段重流。我们进一步证明了一个 telescoping 全变差界，表明局部边缘对齐控制了最终时刻学生诱导分布与教师诱导分布之间的差异。在基准主干上的实验证明了所提方法在少步生成中的有效性。

## Abstract
Diffusion and continuous-flow generative models achieve high-quality generation, and their deterministic sampling can be formulated as solving learned ODE dynamics. However, accurate ODE discretization often requires many steps, making efficient few-step generation a key challenge. Among acceleration strategies, reflow-based distillation simplifies teacher ODE trajectories so that a student model can approximate the teacher transport with fewer steps. We identify a theoretical limitation of this paradigm, namely that trajectory matching can under-determine the distribution induced by the student model. In particular, two student models can attain the same trajectory-matching loss while inducing different endpoint marginal distributions, which may lead to different generation quality. To address this limitation, we introduce a marginal-alignment regularizer that penalizes the discrepancy between the student-induced marginal and the corresponding teacher marginal at the endpoint of each distillation interval. The regularizer is computed by tracking log-density changes along the ODE induced by the student model and evaluating scores from the frozen teacher model, without requiring auxiliary trainable networks or adversarial optimization. The resulting framework applies uniformly to the reflow family, including vanilla reflow and piecewise reflow. We further prove a telescoping total-variation bound showing that local marginal alignment controls the final-time discrepancy between the student-induced and teacher-induced distributions. Experiments on benchmark backbones demonstrate the effectiveness of the proposed method for few-step generation.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：扩散模型和连续流生成模型通过求解学得的ODE动力学实现高质量生成，但精确离散化需要很多步，导致少步生成成为挑战。Reflow（重流）是一种主流蒸馏策略，它通过简化教师ODE轨迹让学生模型用更少步骤近似教师传输。然而，论文发现轨迹匹配存在理论局限性：两个学生模型可以达到相同的轨迹匹配损失，但在区间端点诱导出不同的边缘分布，从而影响生成质量。因此，需要补充分布级别的监督信号。
- **整体含义**：本文揭示并解决了reflow蒸馏中轨迹匹配不足以确定学生诱导分布的问题，提出边际对齐正则化，从理论到实验证明其有效性，为加速扩散生成提供了更可靠的方案。

#### 2. 方法论
- **核心思想**：在原有轨迹匹配损失（如PeRFlow的分段线性匹配）基础上，增加一个边际对齐正则化项，惩罚学生模型在每个蒸馏区间端点诱导的分布与教师对应边缘分布之间的KL散度。该正则化项通过追踪学生ODE沿线的对数密度变化，并利用冻结教师模型的分数（score）来计算，无需辅助网络或对抗优化。
- **关键技术细节**：
  - 对于第k个时间窗口 \([t_{k-1}, t_k]\)，定义训练协议边际 \(\tilde{p}_\theta^{(k)} = (\Phi_{\vec{f}_\theta, t_{k-1} \to t_k})_\# \pi_{t_{k-1}}\)，教师边际 \(q_{\psi, t_k} = (\Phi_{\vec{g}_\psi, 0 \to t_k})_\# \pi_0\)。边际对齐损失 \(L_{MA}^{(k)} = \mathrm{KL}(\tilde{p}_\theta^{(k)} \| q_{\psi, t_k})\)。
  - 总损失：\(L_{\text{total}} = L_{\text{TM}} + \lambda L_{\text{MA}}\)，其中 \(L_{\text{TM}}\) 是PeRFlow的轨迹匹配损失。
  - 计算 \(L_{MA}^{(k)}\) 时，通过Liouville方程将KL散度转化为对输入噪声的期望：\(L_{MA}^{(k)} = \mathbb{E}_{X \sim \pi_{t_{k-1}}}\left[ \log \pi_{t_{k-1}}(X) + \Delta^{\text{Stu}}_{t_{k-1}\to t_k}(X;\theta) - \log q_{\psi, t_k}(X^{\text{Stu}}_{t_{k-1}\to t_k}(X;\theta)) \right]\)，其中 \(\Delta\) 是沿学生轨迹的对数密度变化（由散度积分得到）。
  - 梯度公式：\(\nabla_\theta L_{MA}^{(k)} = \mathbb{E}_{X \sim \pi_{t_{k-1}}}\left[ \nabla_\theta \Delta^{\text{Stu}}_k - s_\psi(X^{\text{Stu}}_k, t_k)^\top \nabla_\theta X^{\text{Stu}}_k \right]\)，其中 \(s_\psi\) 是教师分数函数。实际使用stop-gradient形式实现。
  - 散度 \(\mathrm{div}_x \vec{f}_\theta\) 通过Hutchinson迹估计器（结合有限差分）近似，学生轨迹和 \(\Delta\) 通过增强ODE（联合位置和对数密度）用RK4求解器积分得到。
- **理论保证**：定理2给出 telescoping 全变差界：\(\mathrm{TV}(p_{\theta, t_K}, q_{\psi, t_K}) \leq \sum_{k=1}^K \sqrt{\frac12 L_{MA}^{(k)}} + \sum_{k=1}^{K-1} \delta_k\)，其中 \(\delta_k = \mathrm{TV}(q_{\psi, t_k}, \pi_{t_k})\)。说明局部边际对齐控制最终分布差距。

#### 3. 实验设计
- **数据集/场景**：
  - 主要实验：文本到图像生成，使用 **COCO-2014** 和 **COCO-2017** 验证集评估FID和CLIP分数。训练数据来自 LAION-Aesthetics v2 5+。
  - 附加实验：CelebA-HQ 256×256 上使用潜在流匹配（LFM）模型。
- **Benchmark**：未统一公布，但对比了多个代表性方法，包括：
  - 训练自由求解器：DPMSolver、DDIM（教师本身）
  - 蒸馏模型：LCM-LoRA、2-ReFlow、Flash Diffusion、PeRFlow、PCM、LCM、SDXL-Lightning、HyperSD、Rectified Diffusion、DMD2等。
- **实验设置**：
  - 骨干：Stable Diffusion v1.5 和 SDXL，均使用K=4窗口，教师用8步DDIM采样得到端点。
  - 学生初始化：从已收敛的PeRFlow检查点开始，进一步微调。
  - 边际对齐损失每9次PeRFlow更新计算一次，权重 \(\lambda=10^{-6}\)。
  - 敏感度分析：CFG尺度w∈[2,7]，λ在[10^{-7},10^{-5}]变化。

#### 4. 资源与算力
- 文档明确说明：
  - 硬件：2×NVIDIA H200 GPU，BF16混合精度。
  - 训练时间：SD 1.5微调5K步约需一天。
  - 边际对齐损失计算约占30%总训练时间。
  - 其他超参数（学习率、权重衰减）遵循Hugging Face训练脚本。

#### 5. 实验数量与充分性
- **主要实验**：表2（SD 1.5在两个COCO子集）和表3（SDXL在两个子集），对比了约10种方法，充分展示了4步采样的FID和CLIP提升。
- **敏感度分析**：图2考察CFG和λ的影响，显示方法对超参数不敏感，且优于PeRFlow基线。
- **附加实验**：CelebA-HQ上两种初始化（从教师初始化、从PeRFlow继续训练），验证了在不同初始条件下的一致性。
- **消融实验**：通过与纯PeRFlow对比（表D.1, D.2），表明继续PeRFlow反而退步，而MA-Reflow持续提升。
- **充分性评价**：实验覆盖了文本到图像和面部图像两种任务，两种骨干，多种对比基线，且有敏感度和消融，较为充分。但未测试K=1（vanilla reflow）或其他噪声调度，也未在其他模态（如视频）验证。

#### 6. 主要结论与发现
- 理论发现：轨迹匹配不能保证学生诱导分布与教师分布一致，存在分布级欠定问题。
- 方法有效性：边际对齐正则化显著提升少步生成质量，在SD 1.5上COCO-2014 FID从12.01降至10.66，SDXL上COCO-2014-10K FID从20.99降至19.17。
- 理论保证：局部KL正则化通过telescoping TV界控制最终分布差距。
- 实用性：无需额外网络、与CFG兼容、对超参数鲁棒。

#### 7. 优点
- **理论贡献**：清晰指出现有reflow蒸馏的理论漏洞，并提供严格证明。
- **方法简洁**：无需引入额外可训练网络或对抗训练，仅利用冻结教师分数和学生ODE轨迹，实现简单。
- **统一性**：框架适用于vanilla reflow和piecewise reflow（PeRFlow），兼容不同噪声调度和预测参数化（通过扩散-流对应关系）。
- **实验完备**：涵盖两种骨干、多个数据集、丰富对比，且包含敏感度分析。
- **可扩展性**：连续速度场学生保留变步长ODE求解能力。

#### 8. 不足与局限
- **实验覆盖有限**：仅评估了K=4的分段情况，未验证K=1（vanilla reflow）或其他窗口划分。也未测试不同噪声调度（如EDM）下的表现。
- **非ODE学生不适用**：对于一致性模型、捷径模型等非连续速度场的学生，该方法需要不同机制。
- **计算开销**：每9次更新计算一次KL，仍占用约30%训练时间；未来可探索更高效的估计方式。
- **应用边界**：主要验证文本到图像和面部图像，未在其他模态（如视频、3D）或更大规模模型（如Stable Diffusion 3）上测试。
- **潜在偏差**：训练数据来自LAION，可能包含社会偏见，但该问题与本文方法正交，论文未讨论。

（完）
