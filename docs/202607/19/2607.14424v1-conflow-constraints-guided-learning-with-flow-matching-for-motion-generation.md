---
title: "ConFlow: Constraints-Guided Learning with Flow Matching for Motion Generation"
title_zh: ConFlow：基于流匹配的约束引导学习用于运动生成
authors: "Nutan Chen, Jianxiang Feng, Marvin Alles, Botond Cseke"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.14424v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 约束引导的流匹配用于机器人动作生成
tldr: 针对机器人运动生成中流匹配训练忽略任务约束的问题，提出ConFlow框架。通过可微约束函数将障碍或代价信息直接融入训练目标，并采用条件高斯过程源分布保证轨迹平滑与边界条件，同时利用不可行演示作为负监督。在双机器人导航任务中，ConFlow相比标准流匹配方法显著降低碰撞率并提升轨迹质量，验证了训练时约束集成可有效缩小生成模型的训练-推理差距。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14424v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1359, \"height\": 649}, {\"url\": \"assets/figures/arxiv/2607.14424v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 771, \"height\": 645}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14424v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1483, \"height\": 389}, {\"url\": \"assets/tables/arxiv/2607.14424v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 724, \"height\": 312}]"
motivation: 现有流匹配训练仅拟合数据分布，未纳入任务特定约束，导致推理时约束满足能力不足。
method: 提出ConFlow，将可微约束函数集成到流匹配训练中，用条件高斯过程源分布处理平滑性及边界，并利用不可行演示进行负监督。
result: 双机器人导航实验显示，ConFlow在有无推理时引导下均比标准流匹配基线碰撞率更低、轨迹质量更高。
conclusion: 训练时约束集成是缩小生成运动模型训练-推理差距的有效途径。
---

## 摘要
近年来，流匹配已成为机器人运动生成生成式建模的突出方法。在其通用形式中，流匹配是一种基于ODE的神经采样器，通过回归与运动样本相关的经验流场作为数据进行训练。然而，在机器人运动生成中，我们经常面临收集数据中可能不存在的额外约束。大多数当前方法在可用数据上训练流，并在推理时使用引导来强制执行特定任务的约束。为了解决这种不匹配，我们提出了**ConFlow**，一个约束引导的流匹配框架，通过可微的障碍函数或成本函数将约束信息直接纳入训练目标。为了解决平滑性和边界条件等设计规范，我们建议用条件高斯过程替换流匹配训练中使用的标准高斯源分布。我们的方法还使用不可行演示作为负监督，在不需要额外专家数据的情况下改善约束满足。在两个机器人导航任务上的实验表明，ConFlow比标准流匹配基线实现了更低的碰撞率和更高的轨迹质量，无论是否使用推理时引导。这些结果验证了训练时约束整合作为缩小生成运动模型中训练-推理差距的有效方法。

## Abstract
In recent years Flow Matching has become a prominent method for generative modeling robot motion generation. In its generic form Flow Matching is an ODE-based neural sampler that is trained by regressing empirical flow fields associated with motion samples as data. However, in robot motion generation we often have additional constraints that might not be present in the collected data. The majority of current approaches train the flow on the available data and use inference-time guidance to enforce task-specific constraints. To address this mismatch, we propose \textbf{ConFlow}, a constraint-guided flow matching framework that incorporates constraint information directly into the training objective via differentiable barrier or cost functions. To address design specifications such as smoothness and boundary conditions, we propose replacing the standard Gaussian source distribution used in flow matching training with a conditional Gaussian Process. Our approach also uses infeasible demonstrations as negative supervision, improving constraint satisfaction without requiring additional expert data. Experiments on a two-robot navigation task demonstrate that ConFlow achieves lower collision rates and higher trajectory quality than standard flow matching baselines, with or without inference-time guidance. These results validate training-time constraint integration as an effective approach to closing the training--inference gap in generative motion models.

---

## 论文详细总结（自动生成）

# 论文总结：ConFlow：基于流匹配的约束引导学习用于运动生成

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：现有流匹配（Flow Matching）模型在机器人运动生成中，训练时仅拟合数据分布，忽略任务特定的约束（如避障、平滑性）。推理时虽可通过引导（guidance）施加约束，但这种“训练-部署不匹配”会导致生成轨迹不可行、偏离分布，且依赖大量昂贵专家演示。
- **核心问题**：如何将部署时的约束需求直接融入训练过程，同时利用非专家（含失败）演示提升数据效率，从而缩小训练-推理差距。
- **整体含义**：提出训练时约束集成范式，可生成更安全、更平滑、更符合任务要求的轨迹，并降低对专家数据的依赖。

## 2. 方法论：核心思想、关键技术细节
### 核心思想
将推理时约束引导**转移至训练目标**，结合**结构化源分布**和**负演示监督**，使流匹配模型内化约束。

### 关键技术细节
1. **约束引导训练目标**：
   - 在标准流匹配损失上增加约束修正项：  
     `LCG = MSE( uθt - (ut + λc·M(x1)·u_ct) )`  
     其中 `u_ct = ∇_xt C(xt)` 为可微约束梯度（如碰撞规避的SoftPlus函数），`M(x1)` 指示目标轨迹是否违反约束（负样本=1，正样本=0）。
   - 约束函数示例（避障）：`Cγ(xt) = -1/γ log(1+exp(-γ(d(xt)-r)))`，提供平滑梯度。

2. **条件高斯过程（CGP）源分布**：
   - 用GP替换标准高斯源，RBF核编码轨迹时间相关性，保证初始样本平滑。
   - 额外条件于轨迹端点（起始/目标状态），强制边界一致性，降低流的学习负担。

3. **负演示利用**：
   - 数据集包含正（可行）和负（违反约束）轨迹，通过掩码 `M(x1)` 仅在负样本上激活约束修正，避免过度正则化正样本。

4. **推理流程**：
   - 从CGP源采样（条件于给定端点），用标准流匹配ODE积分生成轨迹。可额外添加推理时引导（如碰撞梯度）进一步提升安全。

## 3. 实验设计
- **任务场景**：二维双机器人位置交换导航，轨迹含32个航点，4维状态（两个机器人的xy坐标）。数据集由合成轨迹构成，约30%存在机器人-机器人碰撞（负演示）。
- **基准测试**：基于碰撞率（机器人间、机器人与障碍物）评估安全性能；对比方法包括：
  - **ConFlow (D+∪D-)**：本方法，使用正负数据。
  - **ConFlow (D+)**：仅正数据消融。
  - **FM (D+)**：标准流匹配，仅正数据，高斯源。
  - **FM (D+∪D-)**：标准流匹配，混合正负数据。
- **推理设置**：三种模式——无引导、机器人避障引导、障碍物避障引导。
- **额外消融**：源分布对比（高斯 vs 条件GP），衡量碰撞率、平滑度、端点误差。

## 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量及训练总时长。仅给出架构细节：ResMLP（隐藏层1024，4个残差块），AdamW优化器（学习率3e-4，batch size 2048，20001次迭代）。计算资源相关信息缺失。

## 5. 实验数量与充分性
- **实验数量**：主表（表I）对比了4种方法在3种推理设置下的碰撞率（共12个数据点）；表II消融源分布（2种源×4个指标=8个值）；另有一张定性轨迹可视化图（图2）。
- **充分性与公平性**：实验设计较系统，消融了关键组件（负数据、源分布、推理引导），且在同一合成数据集上进行。但**仅在一个2D导航任务上测试**，缺乏真实机器人或更高维系统（如机械臂）的验证，通用性存疑。对比方法均基于相同流匹配骨架，比较公平。

## 6. 主要结论与发现
1. **ConFlow在所有设置下碰撞率最低**，且无需推理引导时（0.016）已优于带引导的标准FM（0.031），证明内化约束有效。
2. **负演示带来额外提升**：ConFlow (D+∪D-) 优于 ConFlow (D+)（如无引导时0.016 vs 0.043）。
3. **条件GP源分布显著改善**：碰撞率降低46%，平滑度中位数降低74%，端点误差降低两个数量级。
4. **标准FM直接混合负数据会导致性能恶化**（碰撞率升高），凸显本方法设计的重要性。

## 7. 优点
- **创新性**：将约束从推理引导内化至训练目标，从根源上缓解训练-部署不匹配，无需额外推理时计算。
- **数据效率**：有效利用低成本负演示（失败案例）作为监督，减少对专家数据的依赖。
- **结构化源分布**：条件GP不仅保证平滑性，还通过固定端点简化流的学习，提升生成质量。
- **实验设计严谨**：充分消融验证各组件贡献，且对比了不同推理引导条件。

## 8. 不足与局限
- **场景单一**：仅验证了合成2D双机器人导航任务，未在真实机器人（如机械臂、人形）或高维状态空间（如关节角）上测试，可推广性未知。
- **约束类型有限**：仅考虑碰撞避障，未涉及运动学/动力学约束、关节限位、力/力矩约束等实际机器人常见问题。
- **负演示获取成本**：假设数据集已包含负样本，但在实际应用中获取/标记负演示仍需额外工作，未讨论自动生成方法。
- **计算开销未分析**：训练时需计算约束梯度（可能涉及求导），论文未比较与标准流匹配的训练时间差异；推理时若额外使用引导仍有成本。
- **未做统计稳定性报告**：实验只给出单次运行结果（256条轨迹），无置信区间或多次重复标准差，结果可靠性存疑。

（完）
