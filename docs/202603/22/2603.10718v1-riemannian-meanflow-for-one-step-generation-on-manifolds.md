---
title: Riemannian MeanFlow for One-Step Generation on Manifolds
title_zh: 黎曼均值流用于流形上的单步生成
authors: "Zichen Zhong, Haoliang Sun, Yukun Zhao, Yongshun Gong, Yilong Yin"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10718v1"
tags: ["query:3d-motion-generation"]
score: 8.0
evidence: 用于流形上单步生成的黎曼均值流
tldr: 针对黎曼流形上生成模型采样效率低的问题，本文提出 Riemannian MeanFlow (RMF)。该方法将 MeanFlow 扩展至流形，通过并行输运定义平均速度场，并推导出黎曼 MeanFlow 恒等式进行内在监督。RMF 采用对数映射切空间表示，避免了复杂的轨迹模拟，实现了高效的一步生成，在球面、环面和 SO(3) 等流形上显著提升了采样速度与质量的平衡。
selection_source: carryover_cache
motivation: 现有的流形流匹配模型在采样时通常依赖复杂的 ODE 数值积分，导致生成过程缓慢且计算成本高昂。
method: 通过并行输运和对数映射切空间表示构建平均速度场，并引入冲突感知多任务学习来稳定优化过程。
result: 在球面、环面和旋转群 SO(3) 等流形上的实验表明，RMF 能够实现高质量的一步采样，并大幅降低采样成本。
conclusion: RMF 成功将 MeanFlow 理论扩展到黎曼流形，为流形上的高效一步生成模型提供了理论基础和实践方案。
---

## 摘要
流匹配（Flow Matching）实现了黎曼流形上生成模型的无模拟训练，但采样通常仍依赖于对概率流常微分方程（ODE）进行数值积分。我们提出了黎曼均值流（Riemannian MeanFlow, RMF），将均值流（MeanFlow）扩展到流形值生成，其中速度位于依赖于位置的切空间中。RMF 通过平行移动（parallel transport）定义了一个平均速度场，并推导出一个黎曼均值流恒等式，将平均速度与瞬时速度联系起来以进行内在监督。我们在对数映射（log-map）切表示中使该恒等式具有实用性，从而避免了轨迹模拟和繁重的几何计算。为了实现稳定优化，我们将 RMF 目标分解为两项，并应用冲突感知多任务学习来减轻梯度干扰。RMF 还支持通过无分类器引导（classifier-free guidance）进行条件生成。在球面、环面和 SO(3) 上的实验证明了具有竞争力的单步采样性能，改善了质量与效率之间的权衡，并显著降低了采样成本。

## Abstract
Flow Matching enables simulation-free training of generative models on Riemannian manifolds, yet sampling typically still relies on numerically integrating a probability-flow ODE. We propose Riemannian MeanFlow (RMF), extending MeanFlow to manifold-valued generation where velocities lie in location-dependent tangent spaces. RMF defines an average-velocity field via parallel transport and derives a Riemannian MeanFlow identity that links average and instantaneous velocities for intrinsic supervision. We make this identity practical in a log-map tangent representation, avoiding trajectory simulation and heavy geometric computations. For stable optimization, we decompose the RMF objective into two terms and apply conflict-aware multi-task learning to mitigate gradient interference. RMF also supports conditional generation via classifier-free guidance. Experiments on spheres, tori, and SO(3) demonstrate competitive one-step sampling with improved quality-efficiency trade-offs and substantially reduced sampling cost.

---

## 论文详细总结（自动生成）

### 论文总结：Riemannian MeanFlow for One-Step Generation on Manifolds

#### 1. 核心问题与整体含义（研究动机和背景）
生成模型（如扩散模型和流匹配）在欧几里得空间已取得巨大成功，但在处理具有特定几何约束的数据（如蛋白质结构、地球物理数据、旋转群等）时，需要扩展到黎曼流形。现有的**黎曼流匹配（RFM）**虽然实现了无模拟训练，但在推理阶段仍需通过数值积分求解常微分方程（ODE），导致采样速度慢、计算成本高。
本文的核心问题是如何将**均值流（MeanFlow）**理论从欧几里得空间扩展到黎曼流形，以实现**单步（One-Step）高质量生成**。挑战在于：流形上的速度向量位于相互独立的切空间中，无法直接进行时间平均。

#### 2. 方法论
RMF 的核心思想是通过几何算子重新定义“平均速度”，并推导出可训练的恒等式。
*   **黎曼平均速度定义**：利用**并行输运（Parallel Transport）**算子，将轨迹上不同时刻的瞬时速度映射到当前状态的切空间中，再进行积分平均。
*   **黎曼均值流恒等式**：推导出一个内在恒等式，将平均速度 $u$ 与瞬时速度 $v$ 以及沿路径的**协变导数（Covariant Derivative）**联系起来。
*   **切空间简化计算**：为了避免复杂的 Christoffel 符号和坐标系计算，RMF 在局部**对数映射（Log-map）**表示下操作，将流形操作转化为切空间内的欧几里得向量运算，从而利用雅可比-向量积（JVP）高效训练。
*   **冲突感知优化（RMF-MT）**：将 RMF 目标函数分解为两个子项。作者发现这两项的梯度在训练中经常发生冲突（余弦相似度为负），因此引入了 **PCGrad（梯度手术）** 算法，将其视为多任务学习问题来稳定优化。
*   **条件生成**：通过在切空间中线性组合条件与非条件预测，实现了流形上的**无分类器引导（CFG）**。

#### 3. 实验设计
*   **数据集与场景**：
    *   **球面（$S^2$）**：地球灾难数据集（火山、地震、洪水、火灾）。
    *   **环面（$T^n$）**：蛋白质（2D）和 RNA（7D）的扭角数据集。
    *   **旋转群（$SO(3)$）**：合成的旋转分布（Cone, Fisher, Line, Swiss Roll）。
    *   **高维扩展**：$S^{d-1}$ 超球面（最高 128 维）的合成实验。
*   **Benchmark 与对比方法**：
    *   对比了 **RFM**（多步流匹配）、**RCT**（黎曼一致性训练）、**GFM**（广义流映射，含 G-PSD/ESD/LSD 三个变体）。
    *   评估指标主要使用 **MMD（最大均值差异）** 来衡量单步生成分布与真实分布的接近程度。

#### 4. 资源与算力
论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但在附录中提供了训练吞吐量的对比：RMF-MT 的训练速度约为 6.42 it/s，虽然比基础 RMF 慢（8.27 it/s），但显著快于强力基准 G-LSD（3.60 it/s）。这表明该方法在计算效率上具有优势。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2 维到 128 维、从简单几何到复杂生物分子数据的多种流形。
*   **消融实验**：包括梯度冲突分析（验证了 PCGrad 的必要性）、CFG 引导效果验证、以及随维度增加的性能扩展性分析。
*   **公平性**：所有数据集均采用标准的 8/1/1 划分，并在测试集上报告结果。对比了当前最先进的单步/少步流形生成模型（如 GFM 和 RCM），实验设计较为客观、全面。

#### 6. 主要结论与发现
*   **单步生成性能强劲**：RMF 在大多数任务中仅需 1 次网络前向计算（1 NFE）即可达到或超过需要多步采样的模型效果。
*   **梯度冲突确实存在**：实验证实了分解后的损失项之间存在严重的梯度干扰，使用多任务优化策略（RMF-MT）能显著提升模型在复杂数据集上的表现。
*   **高维扩展性**：相比于将流形数据视为欧几里得数据的朴素方法，RMF 在高维超球面上表现出极佳的稳定性。
*   **CFG 有效性**：首次展示了在 $SO(3)$ 等复杂流形上，单步模型也能通过 CFG 实现良好的类别控制。

#### 7. 优点
*   **理论完备性**：通过并行输运严格定义了流形上的平均速度，填补了 MeanFlow 在非欧空间的应用空白。
*   **工程实用性**：巧妙避开了 Christoffel 符号，利用 Log-map 和 JVP 简化了实现难度，使其易于集成到现有深度学习框架中。
*   **优化创新**：敏锐察觉到损失函数项之间的梯度冲突，并给出了无需手动调参的自动化解决方案。

#### 8. 不足与局限
*   **依赖闭式解**：该方法目前依赖于具有闭式测地线（Exp/Log 映射）的流形。对于没有闭式解的复杂黎曼流形，计算成本可能会大幅增加。
*   **数据集规模**：在极小数据集（如 Volcano，仅 827 个样本）上，复杂的 RMF-MT 可能会因为过拟合或优化压力导致性能略低于基础 RMF。
*   **应用范围**：虽然在合成数据和生物分子数据上表现良好，但在更大规模的实际工业场景（如高维流形上的视频或复杂 3D 运动生成）中的表现仍有待验证。

（完）
