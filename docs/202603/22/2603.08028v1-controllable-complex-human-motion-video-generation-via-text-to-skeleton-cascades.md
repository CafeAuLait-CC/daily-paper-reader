---
title: Controllable Complex Human Motion Video Generation via Text-to-Skeleton Cascades
title_zh: 通过文本到骨架级联实现可控的复杂人体动作视频生成
authors: "Ashkan Taghipour, Morteza Ghahremani, Zinuo Li, Hamid Laga, Farid Boussaid, Mohammed Bennamoun"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08028v1"
tags: ["query:3d-motion-generation"]
score: 8.0
evidence: 用于生成复杂人体动作序列的文本转骨架模型
tldr: 本研究针对复杂人体动作视频生成中文本描述模糊和骨架序列获取难的问题，提出了一种两阶段级联框架。首先通过自回归模型将文本转化为2D骨架序列，再利用带有自适应层融合编码器的扩散模型生成视频。此外，研究者还发布了一个包含2000个特技动作的合成数据集，实验证明该方法在动作多样性和视频一致性上均优于现有技术。
selection_source: carryover_cache
motivation: 现有的视频扩散模型在生成翻跟头等复杂动作时，面临文本控制精度不足及手动提供骨架序列成本过高的问题。
method: 提出一种由文本转骨架自回归模型和基于DINO-ALF编码器的骨架转视频扩散模型组成的级联框架，并构建了高质量的合成动作数据集。
result: 实验表明，该方法在FID、动作多样性、时间一致性及主体特征保持等多个指标上均达到了领先水平。
conclusion: 该框架通过级联设计有效提升了复杂动作视频的可控性与质量，填补了特技动作数据集的空白。
---

## 摘要
生成诸如空翻、侧手翻和武术等复杂人体动作的视频，对于当前的视频扩散模型而言仍具挑战性。仅使用文本作为条件在细粒度动作控制方面存在时间上的歧义，而显式的基于姿态的控制虽然有效，但需要用户提供完整的骨架序列，这对于长且动态的动作来说制作成本很高。我们提出了一个两阶段级联框架来解决这两个局限性。首先，一个自回归文本到骨架模型通过根据先前生成的姿态预测每个关节，从自然语言描述中生成二维姿态序列。该设计捕捉了复杂动作所需的长期时间依赖性和关节间的协调。其次，一个以姿态为条件的视频扩散模型根据参考图像和生成的骨架序列合成视频。它采用了 DINO-ALF（自适应层融合），这是一种多级参考编码器，能够在剧烈的姿态变化和自遮挡情况下保留外观和服装细节。为了解决复杂人体动作视频生成缺乏公开数据集的问题，我们引入了一个基于 Blender 的合成数据集，包含 2,000 个视频，涵盖了执行杂技和特技动作的多样化角色。该数据集提供了对外观、动作和环境的完全控制。它填补了一项重要空白，因为现有的基准测试在杂技动作方面的代表性显著不足，而从网络收集的数据集则引发了版权和隐私担忧。在我们的合成数据集和 Motion-X Fitness 基准测试上的实验表明，我们的文本到骨架模型在 FID、R-precision 和动作多样性方面优于之前的方法。我们的姿态到视频模型在 VBench 的时间一致性、动作平滑度和主体保留指标上也达到了所有对比方法中的最佳结果。

## Abstract
Generating videos of complex human motions such as flips, cartwheels, and martial arts remains challenging for current video diffusion models. Text-only conditioning is temporally ambiguous for fine-grained motion control, while explicit pose-based controls, though effective, require users to provide complete skeleton sequences that are costly to produce for long and dynamic actions.   We propose a two-stage cascaded framework that addresses both limitations. First, an autoregressive text-to-skeleton model generates 2D pose sequences from natural language descriptions by predicting each joint conditioned on previously generated poses. This design captures long-range temporal dependencies and inter-joint coordination required for complex motions. Second, a pose-conditioned video diffusion model synthesizes videos from a reference image and the generated skeleton sequence. It employs DINO-ALF (Adaptive Layer Fusion), a multi-level reference encoder that preserves appearance and clothing details under large pose changes and self-occlusions.   To address the lack of publicly available datasets for complex human motion video generation, we introduce a Blender-based synthetic dataset containing 2,000 videos with diverse characters performing acrobatic and stunt-like motions. The dataset provides full control over appearance, motion, and environment. It fills an important gap because existing benchmarks significantly under-represent acrobatic motions while web-collected datasets raise copyright and privacy concerns.   Experiments on our synthetic dataset and the Motion-X Fitness benchmark show that our text-to-skeleton model outperforms prior methods on FID, R-precision, and motion diversity. Our pose-to-video model also achieves the best results among all compared methods on VBench metrics for temporal consistency, motion smoothness, and subject preservation.

---

## 论文详细总结（自动生成）

### 论文总结：通过文本到骨架级联实现可控的复杂人体动作视频生成

#### 1. 核心问题与整体含义（研究动机和背景）
该论文针对当前视频生成模型在处理**复杂人体动作**（如空翻、侧手翻、武术等非重复性、高动态动作）时的两大瓶颈：
*   **控制模糊性**：仅靠文本描述（T2V）难以精确指导关节轨迹，导致肢体畸变或动作不连贯。
*   **交互成本高**：现有的姿态引导模型（Pose-to-Video）要求用户提供完整的骨架序列，这对于普通用户而言制作成本极高。
*   **外观漂移**：在剧烈运动和自遮挡情况下，传统的 CLIP 图像特征（全局语义）难以维持衣物纹理和肢体细节的连贯性。

#### 2. 方法论：核心思想与关键技术
论文提出了一个**两阶段级联框架**，将动作规划与外观合成解耦：

*   **第一阶段：文本到骨架生成（Text-to-Skeleton）**
    *   **核心思想**：将人体 2D 骨架坐标离散化为 Token，利用自回归 Transformer 建模动作。
    *   **技术细节**：将 $T$ 帧 $J$ 个关节的坐标归一化并离散化为 256 个 bin。采用“帧优先、关节次之”的顺序序列化。使用预训练的 CLIP Text Encoder 提取文本特征作为前缀，通过自回归方式预测后续坐标 Token，从而捕捉长程时间依赖和关节间的空间协调。

*   **第二阶段：姿态引导的视频生成（Pose-to-Video）**
    *   **核心思想**：基于 Wan2.1 扩散模型，引入增强的外观保持机制。
    *   **DINO-ALF（自适应层融合）**：针对 CLIP 特征在空间细节上的不足，提取 DINOv3 多个层级的 Patch 描述符。通过交叉注意力机制自适应融合浅层（纹理丰富）和深层（语义一致）特征，以应对大变形和自遮挡。
    *   **动作编码**：使用 3D CNN 将渲染后的骨架序列编码为时空对齐的 Motion Tokens。
    *   **训练策略**：采用 LoRA 高效微调，并在训练中加入“姿态增强”（抖动、丢弃、时间偏移）以提高对第一阶段生成误差的鲁棒性。

#### 3. 实验设计
*   **数据集**：
    *   **Text-Pose**：结合了自建的 Blender 合成数据和 Motion-X Fitness 子集，共 8,000 对序列。
    *   **Synthetic Video Dataset**：利用 Blender 构建了包含 2,000 个视频的合成数据集，涵盖特技、杂技等复杂动作，解决了现有数据集（如 TikTok）动作单一且存在版权隐私的问题。
*   **Benchmark 与对比方法**：
    *   **骨架生成对比**：T2M-GPT, PriorMDM, MLD, HumanDreamer。
    *   **视频生成对比**：HumanVid, MimicMotion, Hyper-Motion, UniAnimate-DiT, VACE。
*   **评价指标**：FID, R-precision, Diversity（骨架）；VBench 指标（主体一致性、动作平滑度等）、SSIM、PSNR、FVD（视频）。

#### 4. 资源与算力
论文明确记录了实验所需的算力资源：
*   **第一阶段（Text-to-Skeleton）**：使用 **1 台 NVIDIA A100 GPU** 进行训练。
*   **第二阶段（Pose-to-Video）**：使用 **4 台 NVIDIA A100 GPU**，在 14B 规模的 Wan2.1 模型上进行 LoRA 微调，训练步数为 **40,000 步**，学习率为 $2 \times 10^{-5}$。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的定量分析和定性对比。
*   **消融实验**：非常充分。针对第一阶段的 Token 粒度、解码深度、采样策略，以及第二阶段的 DINO-ALF 有效性、LoRA Rank、动作编码器架构、姿态增强策略等均做了详细对比。
*   **客观性**：通过引入 VBench 等第三方标准化评估工具，并与多种 SOTA 方法在相同基准下对比，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **级联优于直接生成**：先生成骨架再合成视频的路径显著提升了复杂动作的可控性和物理合理性。
*   **DINO-ALF 的必要性**：实验证明 DINO 特征在保持局部细节（如领带、手部）方面远优于 CLIP，尤其是在人体发生剧烈翻转时。
*   **合成数据的价值**：高质量的合成数据不仅能提供完美的标注，还能有效训练模型处理现实中罕见的极端动作。

#### 7. 优点与亮点
*   **解耦设计**：通过中间骨架层，赋予了用户对动作进行手动微调（Editable）的可能性。
*   **DINO-ALF 创新**：提出了一种新颖的多层特征融合方式，解决了视频生成中长期存在的“外观漂移”痛点。
*   **数据集贡献**：填补了学术界在复杂特技动作视频数据集上的空白。

#### 8. 不足与局限
*   **单人限制**：目前仅支持单人动作生成，无法处理多人互动、碰撞或复杂的群体协调。
*   **极端细节丢失**：在极高速旋转或极远景下，手指和面部等高频细节仍可能出现模糊。
*   **误差累积**：虽然加入了鲁棒性训练，但第一阶段骨架生成的解剖学错误仍可能传导至最终视频。

（完）
