---
title: "RoboForge: Physically Optimized Text-guided Whole-Body Locomotion for Humanoids"
title_zh: RoboForge：针对类人机器人的物理优化文本引导全身运动
authors: "Xichen Yuan, Zhe Li, Bofan Lyu, Kuangji Zuo, Yanshuo Lu, Gen Li, Jianfei Yang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17927v2"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 文本引导的人形机器人全身运动生成
tldr: 本研究针对人形机器人文本引导运动生成中物理可行性差的问题，提出了RoboForge框架。该框架通过物理合理性优化（PP-Opt）模块，将扩散生成模型与物理控制器双向耦合：前向优化控制器以抑制漂浮和穿模等伪影，后向利用物理优化数据微调生成器。这种自改进循环使生成器学习到符合物理规律的潜空间，实现了无需显式重定向的高质量全身运动控制。
selection_source: fresh_fetch
motivation: 现有的文本到运动转换流程在人形机器人上常因物理不可行性和接触误差导致运动失真。
method: 引入PP-Opt模块作为耦合接口，通过前向策略蒸馏和后向生成器微调构建双向自改进循环。
result: 在Unitree G1机器人上的实验证明，该方法在跟踪精度和稳定性方面均优于传统的显式重定向基准。
conclusion: RoboForge通过物理约束下的潜空间优化，为实现可部署的文本引导人形机器人全身运动提供了有效方案。
---

## 摘要
虽然生成模型在根据文本生成类人动作方面已变得非常有效，但将这些动作转移到类人机器人上进行物理执行仍然具有挑战性。现有的流程通常受限于重定向（retargeting），其中运动学质量受到物理不可行性、接触转换误差以及现实世界动力学数据高昂成本的影响。我们提出了一个统一的潜空间驱动框架，通过一个无需重定向、物理优化的流程，连接了自然语言与类人机器人的全身运动。我们的核心见解是在物理约束下将生成与控制进行双向耦合，而不是将它们视为独立的阶段。我们引入了一个物理合理性优化（PP-Opt）模块作为耦合接口。在正向路径中，PP-Opt 通过以合理性为中心的奖励来细化教师-学生蒸馏策略，以抑制诸如漂浮、滑步和穿模等伪影。在反向路径中，它将奖励优化的仿真展开转换为高质量的显式运动数据，用于微调运动生成器，使其趋向于更符合物理规律的潜分布。这种双向设计形成了一个自我改进的循环：生成器学习具有物理基础的潜空间，而控制器学习执行具有动力学完整性的潜空间条件行为。在宇树（Unitree）G1 类人机器人上进行的大量实验表明，我们的双向优化提高了跟踪精度和成功率。在 IsaacLab 和 MuJoCo 平台上，隐式潜空间驱动流程在精度和稳定性方面均一致优于传统的显式重定向基准。通过将基于扩散的运动生成与物理合理性优化相结合，我们的框架为实现可部署的文本引导类人机器人智能提供了一条切实可行的路径。

## Abstract
While generative models have become effective at producing human-like motions from text, transferring these motions to humanoid robots for physical execution remains challenging. Existing pipelines are often limited by retargeting, where kinematic quality is undermined by physical infeasibility, contact-transition errors, and the high cost of real-world dynamical data. We present a unified latent-driven framework that bridges natural language and whole-body humanoid locomotion through a retarget-free, physics-optimized pipeline. Rather than treating generation and control as separate stages, our key insight is to couple them bidirectionally under physical constraints.We introduce a Physical Plausibility Optimization (PP-Opt) module as the coupling interface. In the forward direction, PP-Opt refines a teacher-student distillation policy with a plausibility-centric reward to suppress artifacts such as floating, skating, and penetration. In the backward direction, it converts reward-optimized simulation rollouts into high-quality explicit motion data, which is used to fine-tune the motion generator toward a more physically plausible latent distribution. This bidirectional design forms a self-improving cycle: the generator learns a physically grounded latent space, while the controller learns to execute latent-conditioned behaviors with dynamical integrity.Extensive experiments on the Unitree G1 humanoid show that our bidirectional optimization improves tracking accuracy and success rates. Across IsaacLab and MuJoCo, the implicit latent-driven pipeline consistently outperforms conventional explicit retargeting baselines in both precision and stability. By coupling diffusion-based motion generation with physical plausibility optimization, our framework provides a practical path toward deployable text-guided humanoid intelligence.

---

## 论文详细总结（自动生成）

### RoboForge: 针对类人机器人的物理优化文本引导全身运动 —— 论文深度总结

#### 1. 论文的核心问题与研究动机
*   **核心问题**：现有的文本引导运动生成模型（如基于扩散的模型）虽然能产生视觉上流畅的动作，但在人形机器人上执行时面临严重的**物理可行性**挑战。
*   **研究动机**：
    *   **重定向瓶颈**：传统的“生成-解码-重定向-跟踪”流程是断裂的，每一阶段的微小误差在接触转换（如足部着地）时会被放大，导致机器人滑步、穿模或摔倒。
    *   **物理不一致性**：视觉动画模型往往允许足部滑动或穿透地面以换取平滑度，而物理世界要求严格的接触约束。
    *   **数据稀缺**：高质量、符合机器人动力学的真实世界运动数据获取成本极高。

#### 2. 论文提出的方法论
RoboForge 提出了一个**潜空间驱动（Latent-driven）**且**无需重定向（Retarget-free）**的统一框架，其核心是**物理合理性优化（PP-Opt）模块**，实现了生成与控制的双向耦合：

*   **核心思想**：通过“生成 $\rightarrow$ 执行 $\rightarrow$ 过滤 $\rightarrow$ 再生成”的闭环，让生成器学习物理规律，让控制器适应生成器的潜空间。
*   **关键技术细节**：
    *   **前向优化（Forward Optimization）**：利用物理仿真器（IsaacLab）训练跟踪策略。引入**物理合理性奖励函数**，专门惩罚三种非物理伪影：滑步（Skating）、漂浮（Floating）和地面穿透（Penetration）。
    *   **后向微调（Backward Fine-tune）**：将仿真中经过物理优化的运动轨迹转化为高质量数据，通过**运动质量控制（过滤掉失败或不匹配的动作）**，反向微调运动生成器（MG），使其潜空间分布向物理可行区域靠拢。
    *   **潜空间驱动接口**：放弃显式的关节轨迹重定向，直接将生成器的潜向量（Latent）作为控制器的输入，通过教师-学生蒸馏和 DAgger 算法实现稳健的全身控制。

#### 3. 实验设计
*   **硬件平台**：Unitree G1 人形机器人。
*   **仿真环境**：IsaacLab（用于训练和前向优化）和 MuJoCo（用于跨仿真器验证/Sim-to-Sim）。
*   **数据集**：HumanML3D 和 KIT-ML（主流的人体运动-文本数据集）。
*   **Benchmark 与对比方法**：
    *   **生成质量对比**：对比原始 MLD（Motion Latent Diffusion）模型。
    *   **控制接口对比**：对比传统的“显式重定向（GMR）+ 跟踪”流程。
    *   **消融实验**：验证 PP-Opt 模块的有无、以及多轮迭代（1-3轮）对性能的影响。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量以及训练总时长。
*   **实现细节**：提到了将训练好的策略导出为 ONNX 格式，并使用 Unitree SDK 进行实机部署，这暗示了模型具有较好的实时推理性能。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 4 组主要实验：生成质量评估、跟踪策略优化评估、多轮迭代效果评估、以及隐式 vs 显式接口对比。
    *   涵盖了从文本生成指标（R-Precision, FID）到物理指标（Penetrate, Float, Skate）再到控制指标（Success Rate, Tracking Error）的全方位评价。
*   **充分性与公平性**：
    *   实验在两个不同的仿真器（IsaacLab 和 MuJoCo）上进行了验证，证明了方法的泛化性。
    *   采用了 8:2 的训练/测试集划分，并进行了分层类别平衡，实验设计较为客观、严谨。

#### 6. 论文的主要结论与发现
*   **双向优化的必要性**：PP-Opt 不仅提升了控制器的稳定性，还显著改善了生成器的物理合理性（如穿模现象降至 0）。
*   **迭代增益**：多轮“生成-优化-微调”循环能带来累积收益，通常在 3 轮左右达到饱和。
*   **潜空间驱动的优越性**：相比显式重定向，直接使用潜向量引导控制能显著减少运动学漂移，提高在复杂动作（如踢腿、跳跃）下的成功率。

#### 7. 优点与亮点
*   **闭环自改进**：打破了生成与控制的界限，使物理合理性成为两者的共同优化目标。
*   **无需重定向**：有效解决了人形机器人控制中最头疼的运动学匹配问题，简化了部署流程。
*   **实机验证**：在 Unitree G1 上成功演示了多种复杂的全身运动（如武术、标枪投掷、深蹲躲避），具有很强的实用价值。

#### 8. 不足与局限
*   **场景限制**：目前主要针对**平坦地面（Flat-ground）**，未探讨在崎岖地形或楼梯等复杂环境下的表现。
*   **交互缺失**：实验主要集中在徒手运动，尚未涉及与环境物体的复杂交互（如搬运、开门）。
*   **算力透明度**：缺乏训练成本的具体数据，不便于其他研究者评估复现难度。
*   **假设前提**：过滤掉了一些非地面接触的动作（如游泳、拉小提琴），这限制了模型处理全类别动作的能力。

（完）
