---
title: "ST-GDance++: A Scalable Spatial-Temporal Diffusion for Long-Duration Group Choreography"
title_zh: ST-GDance++：一种用于长时群体编舞的可扩展时空扩散模型
authors: "Jing Xu, Weiqiang Wang, Cunjian Chen, Jun Liu, Qiuhong Ke"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.22316v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 用于群舞动作生成的时空扩散模型
tldr: 针对多人舞蹈生成中计算开销随人数和时长呈平方增长、且易发生碰撞的问题，本文提出ST-GDance++框架。该框架通过解耦时空依赖，引入轻量级距离感知图卷积进行空间建模，并结合扩散噪声调度与时间对齐注意力掩码实现流式生成。实验证明，该方法在保持高生成质量的同时，显著降低了推理延迟，提升了长序列生成的扩展性与交互性。
selection_source: fresh_fetch
motivation: 现有模型因双向注意力依赖导致计算效率低且难以处理长序列，且在多人场景下容易出现动作碰撞和协调性差的问题。
method: 提出一种解耦时空依赖的扩散模型，利用轻量级图卷积优化空间交互，并通过改进的注意力掩码支持高效的流式长序列生成。
result: 在AIOZ-GDance数据集上的实验表明，该方法在生成质量上具有竞争力，且推理延迟较现有方法大幅降低。
conclusion: ST-GDance++有效解决了多人长时舞蹈生成的扩展性难题，为电影、游戏等交互式场景下的协同动作生成提供了高效方案。
---

## 摘要
从音乐生成群体舞蹈需要同步多名舞者，同时保持空间协调，这使其在电影制作、游戏和动画等应用中具有高度相关性。最近的群体舞蹈生成模型已取得令人满意的生成质量，但由于双向注意力依赖，它们在交互式场景中仍难以部署。随着舞者数量和序列长度的增加，将音乐条件与动作序列对齐所需的注意力计算呈二次方增长，导致效率降低并增加了动作碰撞的风险。因此，有效地建模密集的时空交互至关重要，然而现有方法往往难以捕捉这种复杂性，导致可扩展性有限且多舞者协调不稳定。为了应对这些挑战，我们提出了 ST-GDance++，这是一个可扩展的框架，它解耦了空间和时间依赖关系，以实现高效且具备碰撞感知能力的群体编舞生成。在空间建模方面，我们引入了轻量级的距离感知图卷积，以捕捉舞者间的关系，同时降低计算开销。在时间建模方面，我们设计了一种扩散噪声调度策略以及一种高效的时间对齐注意力掩码，实现了长动作序列的流式生成，并提高了长时场景下的可扩展性。在 AIOZ-GDance 数据集上的实验表明，与现有方法相比，ST-GDance++ 在显著降低延迟的同时，实现了具有竞争力的生成质量。

## Abstract
Group dance generation from music requires synchronizing multiple dancers while maintaining spatial coordination, making it highly relevant to applications such as film production, gaming, and animation. Recent group dance generation models have achieved promising generation quality, but they remain difficult to deploy in interactive scenarios due to bidirectional attention dependencies. As the number of dancers and the sequence length increase, the attention computation required for aligning music conditions with motion sequences grows quadratically, leading to reduced efficiency and increased risk of motion collisions. Effectively modeling dense spatial-temporal interactions is therefore essential, yet existing methods often struggle to capture such complexity, resulting in limited scalability and unstable multi-dancer coordination. To address these challenges, we propose ST-GDance++, a scalable framework that decouples spatial and temporal dependencies to enable efficient and collision-aware group choreography generation. For spatial modeling, we introduce lightweight distance-aware graph convolutions to capture inter-dancer relationships while reducing computational overhead. For temporal modeling, we design a diffusion noise scheduling strategy together with an efficient temporal-aligned attention mask, enabling stream-based generation for long motion sequences and improving scalability in long-duration scenarios. Experiments on the AIOZ-GDance dataset show that ST-GDance++ achieves competitive generation quality with significantly reduced latency compared to existing methods.

---

## 论文详细总结（自动生成）

以下是对论文《ST-GDance++: A Scalable Spatial-Temporal Diffusion for Long-Duration Group Choreography》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证多舞者空间协调性的同时，高效生成长时（Long-duration）且无碰撞的群体舞蹈动作。
*   **研究背景**：
    *   **计算瓶颈**：现有的群体舞蹈生成模型多采用双向注意力机制，其计算复杂度随舞者数量（N）和序列长度（L）呈二次方增长（$O(N^2 L^2)$），导致长序列生成效率极低。
    *   **协调与碰撞**：在多人场景下，模型难以显式建模舞者间的空间关系，容易导致动作重叠、碰撞或空间身份分配不一致。
    *   **交互性不足**：由于双向依赖，现有模型难以支持低延迟的流式（Streaming）生成。

### 2. 方法论
ST-GDance++ 的核心思想是**时空依赖解耦**，将复杂的全局注意力拆分为轻量级的空间建模和高效的时间建模。
*   **空间建模块 (SMB)**：
    *   采用**距离感知图卷积网络 (GCN)**。将每帧中的舞者视为节点，根据彼此间的欧几里得距离构建加权图。
    *   通过 Top-k 边缘选择策略降低计算开销，显式捕捉舞者间的空间交互，减少碰撞。
*   **时间建模模块**：
    *   **差分注意力 (Differential Attention)**：通过两个并行分支的注意力差值来过滤冗余信息，提高动作细节的判别力。
    *   **对齐注意力掩码 (AAM)**：在交叉注意力中引入掩码，限制每帧动作仅关注局部时间窗内的音乐信号，将复杂度降为线性，并支持流式生成。
    *   **状态空间模型 (SSM)**：集成 Mamba 风格的 SSM 块，以线性复杂度捕捉长程时间依赖。
*   **分段三角噪声调度 (TNS)**：
    *   在训练中引入分段相关的噪声策略，模拟自回归推理过程，增强模型对历史误差的鲁棒性，确保长序列生成的稳定性。
*   **交换模式与重排**：通过初始位置排序和交换类型编码，稳定舞者身份并处理复杂的队形变换。

### 3. 实验设计
*   **数据集**：使用 **AIOZ-GDance**（目前最大的群体舞蹈数据集，包含 6.7 小时 3D 动作数据，涵盖 7 种风格和 16 种音乐流派）。
*   **实验场景**：
    *   **短时生成**（120 帧）与 **长时生成**（720 帧）对比。
    *   **不同舞者数量**（2 到 5 人）的扩展性测试。
*   **对比方法 (Baselines)**：
    *   单人舞蹈模型：EDGE。
    *   群体舞蹈模型：GCD、CoDancers、TCDiff++（SOTA）、ST-GDance（作者之前的会议版本）。
*   **评价指标**：
    *   群体层面：GMR（真实性）、GMC（协调性）、TIF（碰撞频率）。
    *   单人层面：FID（分布距离）、Div（多样性）、PFC（足部滑动）。
    *   效率层面：FLOPs、参数量、训练/推理耗时。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA RTX 4090** 和 **V100** GPU 进行训练和评估。
*   **训练细节**：
    *   优化器：Adam，学习率 $5 \times 10^{-5}$。
    *   Batch Size：根据舞者数量动态调整（2人时为64，5人时为8）。
    *   训练时长：在长序列设置下，每轮（epoch）训练约需 1 分 45 秒（ST-GDance++）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的定量评估，包括短时/长时对比、不同人数的鲁棒性测试。
*   **消融实验**：针对 SMB（空间块）、AAM（注意力掩码）和 TNS（噪声调度）分别做了消融研究，验证了各组件对生成质量和效率的贡献。
*   **充分性评价**：实验设计较为全面，不仅对比了生成质量，还重点分析了计算复杂度（FLOPs）和实际推理延迟，证明了其在工业应用中的可行性。对比基准涵盖了最新的 SOTA 模型，实验结果具有较高的说服力。

### 6. 主要结论
*   **高效性**：ST-GDance++ 将计算复杂度从二次方降至近线性，在长序列生成中，其 FLOPs 和推理时间远低于 TCDiff++ 等 Transformer 架构模型。
*   **高质量**：在保持高生成多样性的同时，显著降低了群体碰撞率（TIF）并提升了动作协调性（GMC）。
*   **稳定性**：TNS 策略有效缓解了长序列生成中的误差累积问题，使得 720 帧以上的生成依然保持连贯。

### 7. 优点
*   **架构创新**：成功将 SSM 和差分注意力引入舞蹈生成领域，解决了长序列建模的效率难题。
*   **流式支持**：通过 AAM 和 TNS 设计，使模型具备了实时交互场景下的流式推理能力。
*   **空间感知**：相比于简单的特征拼接，图卷积显式建模了舞者间距，物理合理性（无碰撞）更强。

### 8. 不足与局限
*   **控制维度单一**：目前主要依赖音乐驱动，缺乏文本描述、特定队形约束或交互式动作编辑等更高级的控制手段。
*   **复杂交互受限**：对于极高动态的交互（如舞者间的快速换位或身体接触类舞蹈），模型表现仍有提升空间，这受限于现有数据集的标注深度。
*   **应用限制**：虽然推理速度快，但在超大规模群体（如数十人）场景下的表现尚未验证。

（完）
