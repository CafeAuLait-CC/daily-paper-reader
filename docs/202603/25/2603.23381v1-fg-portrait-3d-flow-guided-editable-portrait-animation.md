---
title: "FG-Portrait: 3D Flow Guided Editable Portrait Animation"
title_zh: FG-Portrait：3D 流引导的可编辑人像动画
authors: "Yating Xu, Yunqi Miao, Evangelos Ververas, Jiankang Deng, Jifei Song"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23381v1"
tags: ["query:3d-motion-generation"]
score: 8.0
evidence: 扩散模型中 3D 流引导的动作迁移
tldr: 本文提出FG-Portrait，旨在解决人像动画中运动迁移不准确的问题。针对扩散模型缺乏对应关系及2D流估计不准的挑战，该方法引入了基于参数化3D头像模型的几何驱动3D流。通过3D流编码和深度引导采样，将3D先验集成到扩散模型中，实现了高保真的人像动画及灵活的表情、姿态编辑，在身份保持和运动一致性上达到领先水平。
selection_source: fresh_fetch
motivation: 传统扩散模型仅依赖驱动运动而忽略了源图与驱动图的对应关系，且2D流估计在处理复杂运动时往往不够准确。
method: 引入基于3D参数化模型的免学习3D流作为几何先验，并利用深度引导采样和3D流编码实现精确的像素级位移映射。
result: 实验表明，该方法在运动迁移的准确性、身份保持的完整性以及人像的可编辑性方面均优于现有技术。
conclusion: 通过将3D几何先验与扩散模型深度结合，FG-Portrait为高质量、可编辑的人像动画生成提供了有效的解决方案。
---

## 摘要
从驱动人像到源人像的动作迁移仍然是人像动画中的一个关键挑战。目前的基于扩散的方法仅以驱动动作为条件，无法捕捉源到驱动的对应关系，从而导致动作迁移效果欠佳。虽然流估计提供了一种替代方案，但从 2D 输入预测密集对应关系是一个病态问题，且通常会导致不准确的动画。我们通过引入 3D 流（3D flows）来解决这一问题，这是一种无需学习且由几何驱动的动作对应关系，直接从参数化 3D 头部模型计算得出。为了将这种 3D 先验集成到扩散模型中，我们引入了 3D 流编码，为每个目标像素查询潜在的 3D 流，以指示其回到源位置的位移。为了获得与 2D 动作变化对齐的 3D 流，我们进一步提出了深度引导采样，以准确地为每个像素定位对应的 3D 点。除了高保真的人像动画外，我们的模型还支持用户指定的面部表情和头部姿态编辑。大量的实验证明了我们的方法在一致的驱动动作迁移以及忠实的源身份保持方面的优越性。

## Abstract
Motion transfer from the driving to the source portrait remains a key challenge in the portrait animation. Current diffusion-based approaches condition only on the driving motion, which fails to capture source-to-driving correspondences and consequently yields suboptimal motion transfer. Although flow estimation provides an alternative, predicting dense correspondences from 2D input is ill-posed and often yields inaccurate animation. We address this problem by introducing 3D flows, a learning-free and geometry-driven motion correspondence directly computed from parametric 3D head models. To integrate this 3D prior into diffusion model, we introduce 3D flow encoding to query potential 3D flows for each target pixel to indicate its displacement back to the source location. To obtain 3D flows aligned with 2D motion changes, we further propose depth-guided sampling to accurately locate the corresponding 3D points for each pixel. Beyond high-fidelity portrait animation, our model further supports user-specified editing of facial expression and head pose. Extensive experiments demonstrate the superiority of our method on consistent driving motion transfer as well as faithful source identity preservation.

---

## 论文详细总结（自动生成）

### FG-Portrait: 3D Flow Guided Editable Portrait Animation 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在人像动画（Portrait Animation）中实现精确的动作迁移，同时保持源人物的身份特征（Identity Preservation）。
*   **研究背景**：
    *   现有的基于扩散模型（Diffusion Models）的方法通常仅以驱动图像的动作（如地标、原始图像）为条件，缺乏源图像与驱动图像之间的显式对应关系，导致在大姿态或跨身份迁移时效果不佳。
    *   传统的2D流估计（Flow Estimation）在处理3D头部运动时属于病态问题，且依赖大量数据训练，泛化性差。
*   **研究动机**：利用3D参数化模型的几何先验，提供一种“免学习”且几何感知的运动对应关系，以解决2D空间中运动描述不准确的问题。

#### 2. 核心方法论
*   **核心思想**：引入**3D流（3D Flow）**作为运动引导。3D流描述了参数化3D头部模型（FLAME）在源状态和驱动状态之间的顶点位移。
*   **关键技术细节**：
    1.  **3D流计算**：利用FLAME模型分别拟合源图和驱动图，通过保留源图形状参数（$\beta$）并结合驱动图的姿态（$\theta$）和表情（$\psi$）构建目标3D头像。利用表面场（Surface Field）建立目标点到源点的语义对应关系，计算位移向量。
    2.  **3D流编码（3D Flow Encoding）**：为了将3D信息引入2D扩散模型，对目标图像的每个像素进行射线反投影，在3D空间中查询对应的3D流，并将其堆叠为多通道特征图作为ControlNet的输入。
    3.  **深度引导采样（Depth-Guided Sampling）**：为了使3D流与2D像素精确对齐，利用渲染的深度图在目标头像表面附近进行采样，而非在整个射线路径上均匀采样，从而显著提高运动引导的准确性。
    4.  **可编辑性**：由于基于参数化模型，用户可以通过直接调整FLAME的姿态和表情参数，实现前馈式的交互编辑。

#### 3. 实验设计
*   **数据集**：
    *   **VFHQ**：高质量视频会议/采访数据集，用于训练和主要测试。
    *   **FFHQ**：用于测试模型的泛化能力。
*   **实验场景**：
    *   **自我重演（Self-reenactment）**：源图和驱动图来自同一视频。
    *   **跨身份重演（Cross-reenactment）**：源图和驱动图来自不同身份。
*   **对比方法（Baselines）**：
    *   包括基于流预测的方法（EMOPortrait）和基于扩散模型的方法（X-Portrait, Follow-Your-Emoji, Face-Adapter, HunyuanPortrait）。
*   **评价指标**：LPIPS（图像质量）、CSIM（身份保持）、APD（姿态误差）、AED（表情误差）、FID（生成质量）、FVD（视频时间一致性）。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的GPU型号、数量及总训练时长。
*   **实现细节**：提到了使用AdamW优化器，学习率为1e-5，基于Stable Diffusion 1.5内核。在推理阶段，生成10帧视频的显存占用约为34GB左右（根据消融实验表格推断）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在VFHQ上进行了大规模的定量对比（包含自我和跨身份任务）。
    *   进行了详尽的**消融实验**，验证了3D流编码、深度引导采样、采样点数量（N）和采样范围（$\delta$）的有效性。
    *   提供了针对卡通人像的泛化测试。
*   **充分性评价**：实验设计较为全面，涵盖了从定量指标到定性视觉对比的各个维度。通过与多种SOTA（先进技术）方法的对比，客观地展示了该方法在动作精确度（APD/AED）上的显著优势。

#### 6. 主要结论与发现
*   **3D流的优越性**：相比于2D地标或预测的2D流，基于几何的3D流能提供更稳健的运动引导，尤其是在处理大幅度头部转动时。
*   **深度引导的重要性**：深度引导采样是连接3D几何与2D生成的核心，未经深度引导的均匀采样会导致运动迁移精度大幅下降。
*   **身份与动作的平衡**：该方法在保持源人物身份特征的同时，能更忠实地还原驱动图像的表情细节。

#### 7. 论文亮点
*   **免学习的几何先验**：直接利用3D模型的语义对应关系，避免了复杂的流预测网络训练，提高了鲁棒性。
*   **高精度运动控制**：在APD和AED指标上显著优于现有的扩散模型方法，解决了“动作对不齐”的痛点。
*   **灵活的编辑功能**：支持用户通过参数直接控制生成结果，具有很强的实用性。

#### 8. 不足与局限
*   **3D模型限制**：FLAME模型的网格分辨率有限，可能无法捕捉极其细微的面部肌肉运动或褶皱。
*   **特定域偏差**：由于在真人数据集上训练，在处理卡通人像时可能会出现闭眼等伪影（缺乏卡通外观先验）。
*   **预处理依赖**：模型性能高度依赖于第一步3D头像拟合（FLAME Fitting）的准确度，如果拟合失败，后续生成质量会受损。

（完）
