---
title: Teacher-Student Diffusion Model for Text-Driven 3D Hand Motion Generation
title_zh: 文本驱动 3D 手部动作生成的教师-学生扩散模型
authors: "Ching-Lam Cheng, Bin Zhu, Shengfeng He"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24407v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 用于3D手部动作生成的扩散模型
tldr: 针对现有文本驱动3D手部动作生成方法在细节刻画和通用性上的不足，本文提出了一种名为TSHaMo的教师-学生扩散模型框架。该框架通过教师模型利用辅助信号（如MANO参数）提供结构化指导，并结合协同训练策略，使学生模型在推理阶段仅凭文本即可生成高质量、多样化的手部动作。在GRAB和H2O数据集上的实验证明，该方法显著提升了生成动作的真实感和多样性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24407v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative example comparison for hand motion generation from textual prompt.\", \"page\": 4, \"index\": 1, \"width\": 460, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2603.24407v1/fig-002.webp\", \"caption\": \"Fig. 1. Training procedure of our method. The student takes a noisy sample and text embedding to predict the denoised output, while the teacher also uses auxiliary conditions (e.g., 3D joints, MANO parameters, contact maps). Three losses are applied between model predictions and ground truth. The example here uses the MDM backbone.\", \"page\": 3, \"index\": 2, \"width\": 1054, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2603.24407v1/fig-003.webp\", \"caption\": \"Table 1. 3D hand motion generation performance comparison on the GRAB dataset. The best results are highlighted in bold.\", \"page\": 3, \"index\": 3, \"width\": 938, \"height\": 185}]"
motivation: 现有的手部动作生成方法往往忽略手势细节或过度依赖3D物体模型，限制了其在通用场景下的应用。
method: 提出一种教师-学生扩散框架，利用教师模型的辅助信号指导学生模型学习，并在推理时仅使用文本输入。
result: 在GRAB和H2O数据集上的评估显示，该方法在动作质量和多样性方面均优于现有基准。
conclusion: TSHaMo框架在无需测试时提供3D物体信息的情况下，实现了鲁棒且灵活的高质量文本驱动手部动作生成。
---

## 摘要
从自然语言生成逼真的 3D 手部动作对于虚拟现实（VR）、机器人技术和人机交互至关重要。现有方法要么侧重于全身运动，忽略了详细的手势，要么需要显式的 3D 物体网格，从而限制了通用性。我们提出了 TSHaMo，这是一个用于文本驱动手部动作生成的模型无关教师-学生扩散框架。学生模型学习仅从文本合成动作，而教师模型则利用辅助信号（如 MANO 参数）在训练期间提供结构化指导。协同训练策略使学生模型能够从教师模型的中间预测中获益，同时在推理阶段保持仅使用文本。在 GRAB 和 H2O 数据集上使用两种扩散骨干网络进行的评估表明，TSHaMo 一致地提高了动作质量和多样性。消融实验证实了其在利用多种辅助输入方面的鲁棒性和灵活性，且在测试阶段无需 3D 物体。

## Abstract
Generating realistic 3D hand motion from natural language is vital for VR, robotics, and human-computer interaction. Existing methods either focus on full-body motion, overlooking detailed hand gestures, or require explicit 3D object meshes, limiting generality. We propose TSHaMo, a model-agnostic teacher-student diffusion framework for text-driven hand motion generation. The student model learns to synthesize motions from text alone, while the teacher leverages auxiliary signals (e.g., MANO parameters) to provide structured guidance during training. A co-training strategy enables the student to benefit from the teacher's intermediate predictions while remaining text-only at inference. Evaluated using two diffusion backbones on GRAB and H2O, TSHaMo consistently improves motion quality and diversity. Ablations confirm its robustness and flexibility in using diverse auxiliary inputs without requiring 3D objects at test time.

---

## 论文详细总结（自动生成）

以下是对论文《Teacher-Student Diffusion Model for Text-Driven 3D Hand Motion Generation》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何仅通过自然语言描述生成逼真且多样化的 3D 手部动作。
*   **研究背景**：
    *   现有的全身动作生成方法（Text-to-Body）往往忽略了手势的精细细节。
    *   现有的手-物交互生成方法（Hand-Object Interaction）通常依赖于显式的 3D 物体网格（Mesh）作为输入，这限制了其在仅有文本描述的通用场景（如 AR/VR 交互、手势通信）中的应用。
    *   **动机**：开发一种无需物体模型、仅凭文本即可生成高质量手部动作的通用框架。

### 2. 方法论
*   **核心思想**：提出 **TSHaMo** 框架，采用“教师-学生”架构的扩散模型。在训练阶段利用丰富的辅助信息指导学习，在推理阶段仅保留文本输入。
*   **关键技术细节**：
    *   **学生模型 (Student)**：仅接收文本提示（Text Prompt）和噪声动作，学习生成 3D 手部动作。
    *   **教师模型 (Teacher)**：除了文本，还接收辅助信号（如 MANO 参数、3D 关节、接触图等）。教师模型在训练中提供更结构化的监督。
    *   **协同训练策略 (Co-training)**：
        *   学生模型同时向地面真值（Ground Truth）和教师模型的预测结果学习。
        *   损失函数包含：学生与 GT 的差异、教师与 GT 的差异，以及学生与教师预测之间的蒸馏损失（$L_{S \to T}$）。
    *   **模型无关设计 (Model-Agnostic)**：该框架可集成到不同的扩散骨干网络中（如 MDM 或 StableMoFusion）。
    *   **推理阶段**：教师模型被移除，学生模型独立完成从文本到动作的生成，确保了部署的灵活性。

### 3. 实验设计
*   **数据集**：
    *   **GRAB**：全身抓取物体的数据集（仅提取手部动作）。
    *   **H2O**：双手操纵物体的数据集。
*   **对比基准 (Baselines)**：
    *   **T2M**：基于 VAE 的动作生成方法。
    *   **MDM**：基于 Transformer 的扩散模型。
    *   **StableMoFusion**：基于 U-Net/RetNet 的高效扩散模型。
*   **评估指标**：Top-k 动作分类准确率（语义对齐度）、KID（分布真实感）、Diversity（生成多样性）。

### 4. 资源与算力
*   **训练参数**：扩散步数 $T_{max}=1000$，训练 1000 个 epoch，学习率 $10^{-5}$，采用余弦退火调度。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集（GRAB, H2O）上进行了完整测试。
    *   对比了三种不同架构的基准模型，并验证了 TSHaMo 在不同骨干网络上的普适性。
    *   **消融实验**：针对不同的辅助条件类型（MANO、3D/2D 关节、掩码等）以及教师指导强度 $\lambda$ 进行了详细分析。
*   **充分性评价**：实验设计较为充分，通过多次运行（20次）取平均值及 95% 置信区间，保证了结果的客观性。通过重新训练基准模型以适应手部数据，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：TSHaMo 在所有数据集和骨干网络上均显著提升了 Top-k 准确率，并降低了 KID 值，说明生成的动作既符合语义又更加逼真。
*   **辅助信号的作用**：MANO 参数结合接触图（Contact Maps）作为辅助输入时效果最佳。
*   **指导强度**：适度的教师指导（$\lambda=0.3$）能显著提升性能，但过强的指导反而会限制模型的泛化能力。

### 7. 优点
*   **无需物体模型**：解决了推理阶段对 3D 物体网格的依赖，极大地扩展了应用场景。
*   **灵活性高**：作为一种通用的训练策略，可以轻松嵌入现有的各种扩散模型架构中。
*   **辅助学习**：巧妙利用训练时的“特权信息”（Privileged Information）来增强推理时的弱输入模型。

### 8. 不足与局限
*   **文本复杂度**：目前的实验主要基于动作标签（Action Labels），对于复杂、长篇或具有时序逻辑的文本指令（如“先抓起杯子再喝水”）的生成能力尚待验证。
*   **物理约束**：虽然引入了接触图，但在完全脱离物体模型的情况下，生成动作的物理合理性（如穿透、悬空）仍存在潜在风险。
*   **算力透明度**：缺乏对训练资源消耗的量化描述，不便于评估其训练成本。

（完）
