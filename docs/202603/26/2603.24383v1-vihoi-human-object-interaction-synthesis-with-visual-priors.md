---
title: "ViHOI: Human-Object Interaction Synthesis with Visual Priors"
title_zh: ViHOI：基于视觉先验的人物交互合成
authors: "Songjin Cai, Linjie Zhong, Ling Guo, Changxing Ding"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24383v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 基于扩散的生成模型用于3D人机交互动作生成
tldr: 针对3D人机交互（HOI）生成中仅靠文本难以描述物理约束的问题，本文提出ViHOI框架。该框架利用大语言视觉模型（VLM）从2D图像中提取丰富的视觉和文本先验，并通过Q-Former适配器将高维特征压缩为先验Token，辅助扩散模型生成。实验证明，该方法在多个基准测试中达到SOTA水平，并展现出卓越的泛化能力。
selection_source: fresh_fetch
motivation: 传统的文本驱动HOI生成难以准确描述复杂的物理约束和交互细节。
method: 提出一种基于扩散模型的框架，利用VLM提取2D图像先验，并通过解耦策略和Q-Former适配器增强生成质量。
result: 在多个基准测试中超越了现有方法，实现了更真实、符合物理规律的3D交互生成。
conclusion: 通过引入视觉先验，ViHOI显著提升了HOI合成的质量和对未知物体及交互类别的泛化能力。
---

## 摘要
生成逼真且符合物理规律的三维人物交互（HOI）仍然是动作生成领域的一个关键挑战。一个主要原因是仅凭文字描述这些物理约束是非常困难的。为了解决这一局限性，我们提出了一种新的范式：从易于获取的二维图像中提取丰富的交互先验。具体而言，我们引入了 ViHOI，这是一个新颖的框架，使基于扩散的生成模型能够利用来自二维图像的丰富的、特定于任务的先验，从而提高生成质量。我们利用大型视觉语言模型（VLM）作为强大的先验提取引擎，并采用层解耦策略来获取视觉和文本先验。同时，我们设计了一个基于 Q-Former 的适配器，将 VLM 的高维特征压缩为紧凑的先验标记（tokens），这显著促进了我们扩散模型的条件训练。我们的框架在来自数据集的动作渲染图像上进行训练，以确保视觉输入与动作序列之间的严格语义对齐。在推理过程中，它利用由文本生成图像模型合成的参考图像，以提高对未见物体和交互类别的泛化能力。实验结果表明，ViHOI 达到了最先进的性能，在多个基准测试中优于现有方法，并展示了卓越的泛化能力。

## Abstract
Generating realistic and physically plausible 3D Human-Object Interactions (HOI) remains a key challenge in motion generation. One primary reason is that describing these physical constraints with words alone is difficult. To address this limitation, we propose a new paradigm: extracting rich interaction priors from easily accessible 2D images. Specifically, we introduce ViHOI, a novel framework that enables diffusion-based generative models to leverage rich, task-specific priors from 2D images to enhance generation quality. We utilize a large Vision-Language Model (VLM) as a powerful prior-extraction engine and adopt a layer-decoupled strategy to obtain visual and textual priors. Concurrently, we design a Q-Former-based adapter that compresses the VLM's high-dimensional features into compact prior tokens, which significantly facilitates the conditional training of our diffusion model. Our framework is trained on motion-rendered images from the dataset to ensure strict semantic alignment between visual inputs and motion sequences. During inference, it leverages reference images synthesized by a text-to-image generation model to improve generalization to unseen objects and interaction categories. Experimental results demonstrate that ViHOI achieves state-of-the-art performance, outperforming existing methods across multiple benchmarks and demonstrating superior generalization.

---

## 论文详细总结（自动生成）

以下是对论文《ViHOI: Human-Object Interaction Synthesis with Visual Priors》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：生成逼真且符合物理规律的三维人物交互（HOI）动作极具挑战。传统的“文本驱动”生成方式存在严重的“一对多”问题，因为简单的文本（如“搬起箱子”）无法提供物体的精确形状、尺寸以及人机接触点的几何先验。
*   **核心思想**：提出一种名为 **ViHOI** 的新范式，即**从 2D 图像中提取丰富的交互先验**来辅助 3D 动作生成。通过利用视觉语言模型（VLM）捕捉图像中的空间关系和几何细节，弥补文本描述的抽象性。

### 2. 方法论
*   **VLM 先验提取器**：使用预训练的 **Qwen2.5-VL** 作为引擎。
    *   **层解耦策略**：研究发现 VLM 不同层对模态的关注度不同。ViHOI 从第 3 层提取**视觉先验（$E_v$）**以保留几何细节，从第 12 层提取**文本先验（$E_t$）**以获取深层语义。
    *   **指令设计**：通过结构化 Prompt 引导 VLM 关注物体形状、尺寸和接触区域。
*   **Q-Former 适配器**：由于 VLM 输出的特征维度高且长度可变，设计了基于 Q-Former 的适配器，通过可学习的 Query 进行交叉注意力运算，将高维特征压缩为紧凑的**先验 Token**。
*   **视觉感知生成器**：基于扩散 Transformer（DiT）架构。将压缩后的视觉和文本 Token 作为条件注入到扩散模型的自注意力层中，指导噪声到动作序列的逆向去噪过程。
*   **训练与推理流程**：
    *   **训练阶段**：直接从数据集的真值（GT）动作中渲染出起始、中间、结束三帧图像作为视觉输入，确保语义严格对齐。
    *   **推理阶段**：利用文本生成图像模型（如 Nano Banana）合成三帧参考图，从而利用 T2I 模型中蕴含的海量世界知识来增强对未知物体的泛化能力。

### 3. 实验设计
*   **数据集**：
    *   **FullBodyManipulation**：包含 15 个物体、17 个受试者的大规模交互数据集。
    *   **BEHAVE**：包含 20 个物体、8 个受试者的真实场景交互数据集。
    *   **3D-FUTURE**：用于测试在完全未见过的家具物体上的泛化表现。
*   **对比方法**：MDM（通用动作生成）、CHOIS（路径引导）、ROG（几何关系引导）、SemGeoMo（语义与几何引导）。
*   **评价指标**：MPJPE（关节位置误差）、接触精度/召回率/F1 分数、FID（动作分布距离）、R-precision（文本匹配度）、Foot Sliding（足部滑动）和穿透分数。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA RTX 3090 GPU** 上运行。
*   **推理开销**：VLM 提取特征的额外开销约为 **0.65s**，推理时调用 T2I 模型 API 约需 **7.20s**。
*   **训练细节**：训练时冻结 VLM 参数，仅训练 Q-Former 适配器和动作生成器部分。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了完整的定量对比。
    *   针对“未见物体（Unseen Objects）”进行了专门的泛化性实验。
    *   **消融实验**：对比了不同 VLM 层组合、Q-Former 与简单池化的效果、以及 VLM 与 CLIP 文本编码器的差异。
    *   **用户研究**：邀请 20 名参与者对生成的语义一致性和交互自然度进行主观评分。
*   **充分性评价**：实验设计客观且充分。通过将 ViHOI 作为“即插即用”模块集成到 MDM、ROG 和 CHOIS 等多个基准模型中，有力证明了该方法的通用性和有效性。

### 6. 主要结论与发现
*   **视觉先验的优越性**：引入 2D 图像先验显著降低了物体漂移、悬浮和穿透等物理不合理现象。
*   **泛化能力强**：得益于 T2I 模型和 VLM 的世界知识，ViHOI 在处理训练集中未出现的物体类别时，表现远超仅依赖文本的模型。
*   **鲁棒性**：即使推理时生成的参考图像存在轻微伪影或渲染瑕疵，模型依然能提取出正确的交互逻辑，说明适配器成功捕捉了高层语义而非低层像素。

### 7. 优点（亮点）
*   **范式创新**：首次系统性地提出利用 2D 图像作为 3D HOI 生成的显式先验，并解决了推理时图像来源的问题。
*   **即插即用**：该框架不改变原有的扩散模型核心逻辑，具有极强的适配性。
*   **天然对齐**：利用 VLM 内部的图文共处理机制，实现了视觉先验与文本指令的自然语义对齐。

### 8. 不足与局限
*   **细粒度缺失**：受限于现有数据集的标注，模型目前无法生成精细的手指动作（如捏、拨等微操）。
*   **推理延迟**：推理过程中引入 T2I 生成步骤会显著增加等待时间，不适合实时性要求极高的场景。
*   **依赖性**：生成质量在一定程度上受限于 T2I 模型生成参考图的准确性。

（完）
