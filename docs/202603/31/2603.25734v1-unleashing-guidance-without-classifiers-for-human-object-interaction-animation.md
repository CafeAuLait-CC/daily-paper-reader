---
title: Unleashing Guidance Without Classifiers for Human-Object Interaction Animation
title_zh: 释放用于人-物交互动画的无分类器引导
authors: "Ziyin Wang, Sirui Xu, Chuan Guo, Bing Zhou, Jiangshan Gong, Jian Wang, Yu-Xiong Wang, Liang-Yan Gui"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25734v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 基于扩散模型的人机交互动画生成
tldr: 本研究针对人机交互（HOI）动画生成中依赖手工先验的局限，提出了名为 LIGHT 的数据驱动框架。该方法基于扩散强制思想，将表示分解为特定模态组件并采用异步去噪调度，使去噪程度较高的模态通过交叉注意力引导噪声较大的模态，实现了无需辅助分类器的引导。LIGHT 能够捕捉内在的接触感知信息，并在合成几何增强训练下表现出极强的泛化性，显著提升了交互动画的真实感与接触质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25734v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of LIGHT. Left: Training. We form different modalities, e.g., body, hand, and object, each diffused with its own noise level. After adding modal-wise and frame-wise rotary positional encodings, the tokens are processed by a shared Transformer decoder and an MLP head to predict clean motion. Right: Inference. We compare a uniform schedule that denoises all modalities synchronously with a staged schedule that keeps one modality cleaner from the uniform run.\", \"page\": 4, \"index\": 1, \"width\": 826, \"height\": 490}]"
motivation: 传统方法生成人机交互动画时过度依赖手工设计的接触先验或运动约束，难以应对复杂的动态交互和多样的物体几何形状。
method: 提出 LIGHT 框架，通过为不同模态分配独立的异步去噪调度，利用去噪节奏产生的差异在交叉注意力机制中实现模态间的自动引导。
result: 实验表明，这种基于节奏的引导机制在接触保真度和生成真实感上优于传统方法，并对未见过的物体和任务展现出更强的泛化能力。
conclusion: 该研究证明了通过异步去噪节奏产生的引导可以有效替代手工约束，为实现高质量、具有几何不变性的交互动画生成提供了新路径。
---

## 摘要
生成逼真的人-物交互（HOI）动画仍然具有挑战性，因为它需要对动态人体动作和多样的物体几何形状进行联合建模。先前的基于扩散的方法通常依赖于手工设计的接触先验或人为施加的运动学约束，以提高接触质量。我们提出了 LIGHT，这是一种数据驱动的替代方案，其中引导源自去噪步调（denoising pace）本身，从而减少了对手工设计先验的依赖。基于扩散强迫（diffusion forcing），我们将表示分解为特定模态的组件，并通过异步去噪调度分配个性化的噪声水平。在这种范式中，较清晰的组件通过交叉注意力引导噪声较大的组件，从而在无需辅助分类器的情况下产生引导。我们发现这种数据驱动的引导本质上具有接触感知能力，并且当训练通过广泛的合成物体几何形状进行增强时，这种引导可以得到进一步加强，从而促进接触语义对几何多样性的不变性。大量实验表明，与传统的无分类器引导相比，步调诱导的引导能更有效地反映接触先验的优势，同时实现更高的接触保真度、更逼真的 HOI 生成以及对未知物体和任务更强的泛化能力。

## Abstract
Generating realistic human-object interaction (HOI) animations remains challenging because it requires jointly modeling dynamic human actions and diverse object geometries. Prior diffusion-based approaches often rely on hand-crafted contact priors or human-imposed kinematic constraints to improve contact quality. We propose LIGHT, a data-driven alternative in which guidance emerges from the denoising pace itself, reducing dependence on manually designed priors. Building on diffusion forcing, we factor the representation into modality-specific components and assign individualized noise levels with asynchronous denoising schedules. In this paradigm, cleaner components guide noisier ones through cross-attention, yielding guidance without auxiliary classifiers. We find that this data-driven guidance is inherently contact-aware, and can be enhanced when training is augmented with a broad spectrum of synthetic object geometries, encouraging invariance of contact semantics to geometric diversity. Extensive experiments show that pace-induced guidance more effectively mirrors the benefits of contact priors than conventional classifier-free guidance, while achieving higher contact fidelity, more realistic HOI generation, and stronger generalization to unseen objects and tasks.

---

## 论文详细总结（自动生成）

### 论文总结：释放用于人-物交互动画的无分类器引导 (LIGHT)

#### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖手工设计的物理约束（如反向运动学 IK）或外部预训练分类器的情况下，生成具有高物理保真度（无穿透、无漂浮、接触准确）的人-物交互（HOI）动画。
*   **研究背景**：现有的基于扩散模型的生成方法在处理复杂动态交互时，常出现手部抓握落空、物体穿模或滑动等现象。传统解决方案要么依赖繁琐的外部先验，要么牺牲了生成的通用性。
*   **整体含义**：本文提出了一种名为 **LIGHT** 的框架，通过“去噪节奏”的差异化，让模型在生成过程中实现自我引导，从而在纯数据驱动的范式下提升交互的真实感。

#### 2. 方法论
*   **核心思想**：利用**异步去噪调度（Asynchronous Denoising Schedules）**产生引导。将人体、手部和物体分解为独立的模态组件，并为它们分配不同的噪声水平。较清晰（去噪较快）的模态通过交叉注意力机制引导噪声较大（去噪较慢）的模态。
*   **关键技术细节**：
    *   **模态分解（Token Separation）**：将 HOI 序列分解为身体（Body）、手部（Hand）和物体（Object）三组 Token。
    *   **步调诱导引导（Pace-induced Guidance）**：在推理时运行两个分支：
        1.  **统一调度分支（Uniform Schedule）**：所有模态同步去噪。
        2.  **阶段性调度分支（Staged Schedule）**：使某一模态（如人体）保持比另一模态（如物体）更清晰的状态。
    *   **引导公式**：最终预测值由“条件分支”与“参考分支”的差值加权得到，类似于无分类器引导（CFG），但这种引导是基于模态间的信息差异（即“软”引导）。
    *   **接触感知形状谱增强**：通过优化算法将现有的交互动作迁移到具有不同几何形状的新物体上，同时保持接触点语义不变，增强模型对未见物体的泛化能力。

#### 3. 实验设计
*   **数据集**：主要在 **InterAct**（大规模全身体交互数据集）上进行，并涵盖了 **BEHAVE**、**OMOMO** 和 **GRAB** 等子集。
*   **Benchmark 与对比方法**：
    *   对比了四种最先进的基准方法：**HOI-Diff**、**CHOIS**、**InterDiff** 和 **Text2HOI**。
    *   评估指标包括：R-Precision（文本对齐）、FID（真实感）、Multi-modal Distance、Diversity（多样性），以及物理指标：FSR（足部滑动比）、Pene（穿透率）、Contact Ratio（接触率）。
*   **场景**：涵盖了从简单的抓取到复杂的搬运、放置、旋转等动态交互任务。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU**。
*   **训练时长**：模型在约 **24 小时**内完成收敛。
*   **实现细节**：采用了混合精度训练（FP16）和 Flash Attention 技术以优化性能。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比、定性可视化、消融实验（针对模态分离、引导强度 $\omega_2$、去噪偏移量 $\delta$ 等参数）以及用户主观研究（15人参与）。
*   **充分性与客观性**：
    *   实验不仅在已知物体上测试，还专门设置了“类内未见物体”和“跨类别未见物体”的泛化性测试。
    *   通过计算引导方向与“减小穿透梯度”的相关性，客观分析了 LIGHT 引导为何比传统 CFG 更有效。
    *   实验设计考虑了不同数据集的特性，对比公平且评估维度全面。

#### 6. 主要结论与发现
*   **引导有效性**：异步去噪产生的“步调诱导引导”比传统的基于文本 Dropout 的 CFG 更能捕捉细粒度的接触细节。
*   **模态分离的重要性**：将手部独立建模能显著提升抓握动作的精确度。
*   **泛化能力**：通过形状谱增强，模型能够成功将学习到的交互语义迁移到从未见过的物体几何形状上。
*   **物理质量**：LIGHT 在减少穿透和提升接触稳定性方面显著优于现有的基于分类器引导的方法。

#### 7. 优点（亮点）
*   **无需外部先验**：摆脱了对 IK 约束或物理模拟器的依赖，实现了纯数据驱动的物理感知生成。
*   **创新的引导机制**：将“扩散强迫（Diffusion Forcing）”理论扩展为一种通用的引导框架，具有很强的启发性。
*   **鲁棒的增强策略**：提出的接触感知增强方法有效解决了 HOI 数据稀缺导致的泛化难题。

#### 8. 不足与局限
*   **推理开销**：由于需要运行多个去噪分支，推理速度较慢（单次推理约 72 秒），难以满足实时交互需求。
*   **场景限制**：目前仅支持单人与单个物体的交互，尚未扩展到多人或复杂静态环境（如家具布局）的交互。
*   **物理极限**：虽然减少了穿透，但在极端动态下仍可能无法完全符合严格的物理定律（如重力平衡）。

（完）
