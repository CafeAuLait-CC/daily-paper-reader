---
title: Teacher-Student Diffusion Model for Text-Driven 3D Hand Motion Generation
title_zh: 用于文本驱动 3D 手部动作生成的教师-学生扩散模型
authors: "Ching-Lam Cheng, Bin Zhu, Shengfeng He"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24407v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 用于文本驱动3D手部动作生成的教师-学生扩散模型
tldr: 本研究针对从文本生成逼真3D手部动作的挑战，提出了TSHaMo框架。现有方法往往忽视手部细节或依赖3D物体模型，限制了通用性。TSHaMo采用教师-学生扩散模型架构，教师模型利用辅助信号（如MANO参数）提供结构化指导，学生模型则仅通过文本进行推理。通过协同训练策略，该方法在GRAB和H2O数据集上显著提升了生成动作的质量与多样性，且在推理阶段无需额外辅助输入。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24407v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative example comparison for hand motion generation from textual prompt.\", \"page\": 4, \"index\": 1, \"width\": 460, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2603.24407v1/fig-002.webp\", \"caption\": \"Fig. 1. Training procedure of our method. The student takes a noisy sample and text embedding to predict the denoised output, while the teacher also uses auxiliary conditions (e.g., 3D joints, MANO parameters, contact maps). Three losses are applied between model predictions and ground truth. The example here uses the MDM backbone.\", \"page\": 3, \"index\": 2, \"width\": 1054, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2603.24407v1/fig-003.webp\", \"caption\": \"Table 1. 3D hand motion generation performance comparison on the GRAB dataset. The best results are highlighted in bold.\", \"page\": 3, \"index\": 3, \"width\": 938, \"height\": 185}]"
motivation: 现有的文本驱动动作生成方法在处理精细手部姿态时存在局限，且往往过度依赖推理阶段难以获取的3D物体信息。
method: 提出一种教师-学生扩散框架，通过协同训练让学生模型在仅使用文本输入的情况下，学习教师模型从辅助信号中提取的结构化知识。
result: 在GRAB和H2O数据集上的实验表明，该方法在多种扩散模型骨干网络上均能显著提高手部动作生成的质量和多样性。
conclusion: TSHaMo框架证明了利用辅助信号指导训练可以有效提升纯文本驱动的手部动作生成效果，具有较强的鲁棒性和灵活性。
---

## 摘要
从自然语言生成逼真的 3D 手部动作对于虚拟现实（VR）、机器人技术和人机交互至关重要。现有方法要么专注于全身运动而忽略了详细的手势，要么需要显式的 3D 物体网格，从而限制了通用性。我们提出了 TSHaMo，一种用于文本驱动手部动作生成的模型无关（model-agnostic）教师-学生扩散框架。学生模型学习仅根据文本合成动作，而教师模型则利用辅助信号（如 MANO 参数）在训练期间提供结构化指导。协同训练策略使学生模型能够从教师模型的中间预测中获益，同时在推理阶段保持仅使用文本输入。在 GRAB 和 H2O 数据集上使用两种扩散骨干网络进行评估，结果表明 TSHaMo 持续提升了动作的质量和多样性。消融实验证实了其在不依赖测试时 3D 物体的情况下，使用多种辅助输入的鲁棒性和灵活性。

## Abstract
Generating realistic 3D hand motion from natural language is vital for VR, robotics, and human-computer interaction. Existing methods either focus on full-body motion, overlooking detailed hand gestures, or require explicit 3D object meshes, limiting generality. We propose TSHaMo, a model-agnostic teacher-student diffusion framework for text-driven hand motion generation. The student model learns to synthesize motions from text alone, while the teacher leverages auxiliary signals (e.g., MANO parameters) to provide structured guidance during training. A co-training strategy enables the student to benefit from the teacher's intermediate predictions while remaining text-only at inference. Evaluated using two diffusion backbones on GRAB and H2O, TSHaMo consistently improves motion quality and diversity. Ablations confirm its robustness and flexibility in using diverse auxiliary inputs without requiring 3D objects at test time.

---

## 论文详细总结（自动生成）

以下是对论文《Teacher-Student Diffusion Model for Text-Driven 3D Hand Motion Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何仅通过自然语言描述生成逼真且符合语义的 3D 手部动作。
*   **研究背景**：
    *   现有的全身动作生成方法（Text-to-Body）往往忽略了手部精细的动作细节。
    *   现有的手-物交互生成方法（Hand-Object Interaction）通常依赖于精确的 3D 物体网格（Mesh）作为输入，但在 AR/VR 或手势通信等实际场景中，往往只有文本指令，缺乏物体模型。
*   **研究动机**：提出一种无需物体模型、仅靠文本驱动，且能保持高精度手势细节的通用生成框架。

### 2. 论文提出的方法论
*   **核心思想**：采用**教师-学生（Teacher-Student）扩散模型架构**。在训练阶段，利用丰富的辅助信息（如物体接触图、MANO 参数）指导模型学习；在推理阶段，学生模型仅需文本即可生成高质量动作。
*   **关键技术细节**：
    *   **教师模型 (Teacher)**：输入包括文本、扩散步数以及**辅助信号**（如 MANO 参数、3D 关节坐标、接触图等）。它作为一个“专家”，利用这些额外信息预测更准确的去噪结果。
    *   **学生模型 (Student)**：输入仅包含文本、扩散步数和加噪后的动作。其目标是模仿教师模型的输出。
    *   **协同训练策略 (Co-training)**：
        *   学生模型损失函数：包含与真实值（GT）的去噪损失，以及与教师模型预测结果之间的蒸馏损失（$L_{S \to T}$）。
        *   教师模型损失函数：仅包含与真实值的去噪损失。
    *   **模型无关性 (Model-Agnostic)**：该框架可集成到不同的扩散模型骨干网络中（如基于 Transformer 的 MDM 或基于 U-Net 的 StableMoFusion）。
    *   **推理过程**：仅保留学生模型，实现纯文本驱动的生成。

### 3. 实验设计
*   **数据集**：
    *   **GRAB**：全身抓取物体数据集，仅提取手部动作。
    *   **H2O**：双手操作物体数据集。
*   **Benchmark 与对比方法**：
    *   **T2M**：基于 VAE 的人体动作生成方法。
    *   **MDM**：基于 Transformer 的扩散模型。
    *   **StableMoFusion**：基于 U-Net/RetNet 的高效扩散模型。
*   **评估指标**：Top-k 动作分类准确率（衡量语义一致性）、KID（衡量生成分布的真实性）、Diversity（衡量生成动作的多样性）。

### 4. 资源与算力
*   **训练参数**：扩散步数 $T_{max}=1000$，训练 1000 个 epoch，学习率 $10^{-5}$，采用余弦退火调度。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集（GRAB, H2O）上进行了完整对比。
    *   针对两种不同的扩散模型骨干（MDM, StableMoFusion）验证了框架的通用性。
    *   **消融实验**：测试了不同辅助信号（MANO、3D 关节、2D 关节、掩码、接触图）对性能的影响，以及教师指导权重 $\lambda$ 的敏感性分析。
*   **充分性评价**：实验设计较为充分，通过 20 次运行取平均值及 95% 置信区间，保证了结果的客观性和统计学意义。

### 6. 主要结论与发现
*   **性能提升**：TSHaMo 在所有基准模型上均显著提升了生成动作的准确率和真实感（KID 显著降低）。
*   **辅助信号价值**：即使在推理时不使用物体信息，训练时引入“手-物接触图”和“MANO 参数”能极大增强模型对物理合理性的理解。
*   **指导强度**：适度的教师指导（$\lambda=0.3$）效果最好，过强的指导反而会限制模型的生成能力。

### 7. 优点
*   **灵活性高**：作为一种插件式框架，可以轻松提升现有扩散模型的性能，而不改变其推理结构。
*   **摆脱物体依赖**：解决了推理阶段必须提供 3D 物体模型的局限性，扩大了应用范围。
*   **语义对齐强**：通过教师模型的结构化引导，生成的动作与文本描述的动作类别匹配度更高。

### 8. 不足与局限
*   **文本复杂度有限**：目前主要针对短语/动作标签（如“抓取杯子”），尚未探索长文本或具有复杂时序逻辑的指令。
*   **辅助信息获取**：虽然推理不需要辅助信息，但训练阶段仍依赖高质量的标注数据（如接触图），这在某些野外数据集上难以获取。
*   **物理约束**：虽然引入了接触图引导，但在完全没有物体输入的推理阶段，仍可能出现轻微的悬空或穿模等物理不自然现象。

（完）
