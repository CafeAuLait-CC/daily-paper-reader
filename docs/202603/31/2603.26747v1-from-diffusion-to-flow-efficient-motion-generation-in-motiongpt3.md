---
title: "From Diffusion To Flow: Efficient Motion Generation In MotionGPT3"
title_zh: 从扩散到流：MotionGPT3 中的高效运动生成
authors: "Jaymin Ban, JiHong Jeon, SangYeop Jeong"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.26747v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 比较用于 3D 人体动作生成的扩散与整流流
tldr: 本研究探讨了在MotionGPT3框架下，将扩散模型替换为修正流（Rectified Flow）目标函数对文本驱动动作生成的影响。通过在HumanML3D数据集上的受控实验，研究发现修正流在训练收敛速度、推理效率及动作质量方面均优于传统扩散模型，为连续潜空间动作生成提供了更高效的先验选择，显著优化了生成效率与质量的权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26747v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of text-to-motion generation methods on HumanML3D. Arrows indicate whether higher (↑) or lower (↓) values are better. Real denotes statistics computed from groundtruth motion data. Bold values indicate the best performance for each metric.\", \"page\": 5, \"index\": 1, \"width\": 823, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2603.26747v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of the MotionGPT-3 architecture with alternative motion priors. The dualstream GPT-2 backbone produces text-conditioned motion latents, which are fed into either a\", \"page\": 2, \"index\": 2, \"width\": 582, \"height\": 906}]"
motivation: 旨在验证在图像和音频领域表现优异的修正流目标函数是否能提升文本驱动动作生成的效率和质量。
method: 在保持模型架构和训练协议不变的前提下，对比研究了MotionGPT3中扩散模型与修正流两种生成目标的性能差异。
result: 实验表明修正流在更少的训练轮次下即可达到更优性能，且在推理阶段具有更好的采样效率和稳定性。
conclusion: 修正流目标函数的优势可成功迁移至动作生成领域，是构建高效连续潜空间动作先验的重要方向。
---

## 摘要
最近的文本驱动运动生成方法涵盖了基于离散标记的方法和连续潜空间公式。MotionGPT3 是后一种范式的典范，它结合了学习到的连续运动潜空间和基于扩散的先验，用于文本条件下的合成。虽然在图像和音频生成中，修正流（rectified flow）目标函数最近表现出优于扩散模型的收敛性和推理时间特性，但这些优势是否能直接迁移到运动生成领域仍不明确。在这项工作中，我们在 MotionGPT3 框架内进行了一项受控实证研究，比较了扩散目标和修正流目标。通过保持模型架构、训练协议和评估设置不变，我们分离了生成目标对训练动态、最终性能和推理效率的影响。在 HumanML3D 数据集上的实验表明，修正流在更少的训练轮次内收敛，更早达到强大的测试性能，并在相同条件下达到或超过了基于扩散的运动质量。此外，基于流的先验在广泛的推理步数范围内表现出稳定的行为，并以更少的采样步数实现了具有竞争力的质量，从而改善了效率与质量之间的权衡。总之，我们的结果表明，修正流目标函数的几个已知优势确实扩展到了连续潜空间文本到运动生成中，强调了运动先验中训练目标选择的重要性。

## Abstract
Recent text-driven motion generation methods span both discrete token-based approaches and continuous-latent formulations. MotionGPT3 exemplifies the latter paradigm, combining a learned continuous motion latent space with a diffusion-based prior for text-conditioned synthesis. While rectified flow objectives have recently demonstrated favorable convergence and inference-time properties relative to diffusion in image and audio generation, it remains unclear whether these advantages transfer cleanly to the motion generation setting. In this work, we conduct a controlled empirical study comparing diffusion and rectified flow objectives within the MotionGPT3 framework. By holding the model architecture, training protocol, and evaluation setup fixed, we isolate the effect of the generative objective on training dynamics, final performance, and inference efficiency. Experiments on the HumanML3D dataset show that rectified flow converges in fewer training epochs, reaches strong test performance earlier, and matches or exceeds diffusion-based motion quality under identical conditions. Moreover, flow-based priors exhibit stable behavior across a wide range of inference step counts and achieve competitive quality with fewer sampling steps, yielding improved efficiency--quality trade-offs. Overall, our results suggest that several known benefits of rectified flow objectives do extend to continuous-latent text-to-motion generation, highlighting the importance of the training objective choice in motion priors.

---

## 论文详细总结（自动生成）

这篇论文对文本驱动的人体动作生成模型 **MotionGPT3** 进行了深入的实证研究，探讨了将生成先验从传统的**扩散模型（Diffusion）**切换为**修正流（Rectified Flow）**后的性能表现。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在连续潜空间的文本到动作生成任务中，修正流（Rectified Flow）目标函数是否比扩散模型（Diffusion）更具优势？
*   **研究背景**：
    *   动作生成目前分为离散标记（如 VQ-VAE + LLM）和连续潜空间（如 VAE + Diffusion）两种范式。MotionGPT3 属于后者，避免了离散化带来的量化误差。
    *   修正流在图像和音频领域已证明具有更快的收敛速度和更高的推理效率，但在动作生成领域的迁移效果尚不明确。
*   **整体含义**：通过受控实验验证生成目标函数（Objective）的选择对动作生成模型性能的决定性影响。

### 2. 方法论
*   **核心思想**：保持 MotionGPT3 的架构（GPT-2 文本编码器 + 连续动作 VAE）完全不变，仅将动作先验的训练目标从扩散模型替换为修正流。
*   **关键技术细节**：
    *   **扩散先验（基准）**：采用 DDPM 目标，学习预测添加到动作潜变量中的高斯噪声 $\epsilon$。推理时通过随机去噪过程进行采样。
    *   **修正流先验（改进）**：
        *   **线性插值**：构建噪声 $x_1$ 到数据 $x_0$ 的直接轨迹：$x_t = (1-t)x_1 + tx_0$。
        *   **速度预测**：模型学习预测恒定的速度场 $v_{true} = x_0 - x_1$。
        *   **损失函数**：最小化预测速度与真实速度之间的均方误差 $L_{flow} = E[\|v_\theta(x_t, t, c) - v_{true}\|^2]$。
        *   **采样**：推理时使用确定性的 ODE 求解器（如 Euler 方法），沿着直线轨迹从噪声传输到数据。
    *   **时间步采样**：修正流采用了 logit-normal 分布采样时间步 $t$，以提高训练稳定性。

### 3. 实验设计
*   **数据集**：**HumanML3D**（目前人体动作生成领域最主流的标准数据集）。
*   **Benchmark（基准）**：
    *   **内部对比**：Diffusion-based MotionGPT3 vs. Flow-based MotionGPT3。
    *   **外部对比**：与 T2M-GPT、MoMask、MotionGPT (v1)、DiverseMotion 等 SOTA 方法进行对比。
*   **评估指标**：
    *   **R-Precision (Top 1/2/3)**：衡量文本与动作的语义一致性。
    *   **FID**：衡量生成动作分布与真实分布的接近程度。
    *   **Matching Score**：文本与动作嵌入之间的欧氏距离。
    *   **Diversity & Multi-modality**：衡量生成动作的多样性。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 5090** GPU。
*   **训练时长**：在相同配置下，扩散模型和修正流模型各需约 **13 小时**。
*   **训练参数**：Batch size 为 50，使用 AdamW 优化器，学习率为 $2 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验设置**：进行了严谨的受控变量实验（Controlled Study），确保架构、超参数、数据集完全一致，仅改变生成目标。
*   **充分性分析**：
    *   **训练动态实验**：监控了 200 个 Epoch 的验证集指标变化，展示了收敛曲线。
    *   **推理效率实验**：绘制了 Pareto 曲线，对比了不同采样步数（2 到 15 步）下的质量与耗时。
    *   **测试集评估**：在最佳 Checkpoint 下进行了全面的指标对比。
    *   **结论**：实验设计客观、公平，充分证明了修正流在收敛速度和推理效率上的优势。

### 6. 主要结论与发现
*   **更快的收敛**：修正流在约 **54 个 Epoch** 时即可达到最佳性能，而扩散模型需要约 **143 个 Epoch**。
*   **性能提升**：在最佳状态下，修正流模型的 R-Precision 提升了约 3%，FID 降低了约 5%。
*   **推理效率极高**：修正流仅需 **4 步** 采样即可达到近乎峰值的质量，而扩散模型至少需要 8 步。
*   **稳定性强**：修正流对采样步数的敏感度较低，在极低步数下表现更稳健。
*   **计算开销低**：由于 ODE 求解器结构简单，修正流的单步推理延迟比扩散模型低 15-20%。

### 7. 优点
*   **受控实验严谨**：成功分离了“生成目标”这一变量，为动作生成模型的优化提供了清晰的指导。
*   **实用性强**：证明了修正流是延迟敏感型应用（如实时具身智能、交互式动画）的更优选择。
*   **效率与质量双赢**：不仅推理快，且在语义对齐和动作质量指标上均优于扩散模型。

### 8. 不足与局限
*   **框架局限性**：研究仅基于 MotionGPT3 这一种架构，尚不确定结论是否能完美推广到其他更大型或不同结构的动作模型。
*   **端到端瓶颈**：虽然动作先验部分变快了，但整个流水线（文本编码、VAE 解码）仍占据较大耗时，限制了整体速度的提升上限。
*   **数据集单一**：仅在 HumanML3D 上进行了验证，未在 KIT-ML 等其他数据集上进行交叉验证。

（完）
