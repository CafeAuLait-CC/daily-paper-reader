---
title: "UniMotion: A Unified Framework for Motion-Text-Vision Understanding and Generation"
title_zh: UniMotion：一个统一的人体运动-文本-视觉理解与生成框架
authors: "Ziyi Wang, Xinshun Wang, Shuang Chen, Yang Cong, Mengyuan Liu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22282v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 统一的人体动作生成框架
tldr: UniMotion是首个实现人体动作、文本和图像统一理解与生成的框架。针对现有模型模态受限及离散量化导致的连续性缺失问题，该研究将动作视为连续模态，通过CMA-VAE和对称双路径嵌入器在LLM中构建平行路径。引入DPA蒸馏视觉先验并利用LRA自监督预训练解决冷启动，在7项跨模态任务中达到SOTA，显著提升了复杂组合任务的表现。
selection_source: fresh_fetch
motivation: 现有统一模型通常只处理部分模态且依赖离散标记化，导致量化误差并破坏了动作的时间连续性。
method: 采用CMA-VAE构建连续运动路径，并结合双后验KL对齐（DPA）与潜空间重建对齐（LRA）来增强模态间的语义校准。
result: 在涉及动作、文本和图像的七项理解、生成及编辑任务中，UniMotion均实现了最先进的性能表现。
conclusion: 该研究证明了将动作作为一等连续模态处理的有效性，为多模态人体运动研究提供了强大的统一基准。
---

## 摘要
我们提出了 UniMotion，据我们所知，这是首个在单一架构内同时实现人体运动、自然语言和 RGB 图像理解与生成的统一框架。现有的统一模型仅能处理受限的模态子集（例如运动-文本或静态姿态-图像），且主要依赖离散化分词，这会引入量化误差并破坏时间连续性。UniMotion 通过一个核心原则克服了这两个局限性：将运动视为与 RGB 具有同等地位的一等公民连续模态。一种新型的跨模态对齐运动变分自编码器（CMA-VAE）和对称双路径嵌入器在共享的 LLM 主干网络中为运动和 RGB 构建了平行的连续路径。为了在推理阶段无需图像的情况下将视觉语义先验注入运动表示，我们提出了双后验 KL 对齐（DPA），将视觉融合编码器更丰富的后验知识蒸馏到仅运动编码器中。为了解决冷启动问题——即仅靠文本监督过于稀疏，难以校准新引入的运动路径——我们进一步提出了潜空间重构对齐（LRA），这是一种自监督预训练策略，利用密集的运动潜变量作为明确的条件来共同校准嵌入器、主干网络和流头部（flow head），为所有下游任务建立稳定的运动感知基础。UniMotion 在涵盖三种模态之间任意到任意理解、生成和编辑的七项任务中均达到了最先进的性能，在跨模态组合任务上表现出尤为强大的优势。

## Abstract
We present UniMotion, to our knowledge the first unified framework for simultaneous understanding and generation of human motion, natural language, and RGB images within a single architecture. Existing unified models handle only restricted modality subsets (e.g., Motion-Text or static Pose-Image) and predominantly rely on discrete tokenization, which introduces quantization errors and disrupts temporal continuity. UniMotion overcomes both limitations through a core principle: treating motion as a first-class continuous modality on equal footing with RGB. A novel Cross-Modal Aligned Motion VAE (CMA-VAE) and symmetric dual-path embedders construct parallel continuous pathways for Motion and RGB within a shared LLM backbone. To inject visual-semantic priors into motion representations without requiring images at inference, we propose Dual-Posterior KL Alignment (DPA), which distills a vision-fused encoder's richer posterior into the motion-only encoder. To address the cold-start problem -- where text supervision alone is too sparse to calibrate the newly introduced motion pathway -- we further propose Latent Reconstruction Alignment (LRA), a self-supervised pre-training strategy that uses dense motion latents as unambiguous conditions to co-calibrate the embedder, backbone, and flow head, establishing a stable motion-aware foundation for all downstream tasks. UniMotion achieves state-of-the-art performance across seven tasks spanning any-to-any understanding, generation, and editing among the three modalities, with especially strong advantages on cross-modal compositional tasks.

---

## 论文详细总结（自动生成）

### UniMotion: 统一的人体运动-文本-视觉理解与生成框架论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在单一架构中实现人体运动（Motion）、自然语言（Text）和图像（Vision/RGB）的统一理解与生成。
*   **研究背景**：现有的多模态大模型（MLLM）在处理人体动作时存在两个主要缺陷：
    1.  **模态覆盖受限**：大多仅支持“运动-文本”或“静态姿态-图像”的子集，缺乏三模态的深度融合。
    2.  **离散化弊端**：普遍依赖离散标记化（如 VQ-VAE），这会引入不可逆的量化误差，破坏动作的时间连续性和结构保真度。
*   **动机**：将运动提升为与 RGB 平等的“一等公民”连续模态，构建一个能够处理任意模态间转换（Any-to-Any）的通用系统。

#### 2. 论文提出的方法论
*   **核心思想**：采用全连续表示范式，通过对称的连续路径处理运动和视觉信息，并利用自监督学习校准跨模态对齐。
*   **关键技术细节**：
    *   **CMA-VAE（跨模态对齐运动 VAE）**：将变长运动序列编码为连续潜空间。
    *   **DPA（双后验 KL 对齐）**：在训练时使用“视觉融合编码器”引导“纯运动编码器”，通过模式寻求（Mode-seeking）的逆向 KL 散度，将视觉语义先验蒸馏至运动表示中，使模型在推理时无需图像也能感知视觉特征。
    *   **双路径嵌入器（Dual-Path Embedder）**：设计语义分支（提取高层抽象）和生成分支（保留精细运动细节），实现语义理解与生成任务的结构化解耦。
    *   **LRA（潜空间重构对齐）**：针对文本监督稀疏导致的“冷启动”问题，提出自监督预训练策略。利用密集的运动潜变量作为条件进行自我重构，校准 LLM 主干、嵌入器和流头部（Flow Head）。
    *   **架构优化**：基于 Show-o2，引入**模态路由 LoRA**（独立适配不同模态）和**混合注意力机制**（兼顾文本的自回归特性与运动的全局双向交互）。

#### 3. 实验设计
*   **数据集**：
    *   **运动/文本**：HumanML3D（T2M, M2T, 预测）、MotionFix（动作编辑）。
    *   **视觉/运动/文本**：Human3.6M（V2M, V2T, 图像编辑）、MoVid（视频理解）、3DPW（跨域泛化测试）。
*   **Benchmark 与任务**：涵盖 7 项任务：文本转运动 (T2M)、运动转文本 (M2T)、运动预测、运动编辑、视觉转运动 (V2M)、视觉转文本 (V2T) 以及运动引导图像编辑 (MGIE)。
*   **对比方法**：包括 MotionGPT、MG-MotionLLM、UniPose、HMVLM、Show-o2 等 SOTA 模型。

#### 4. 资源与算力
*   **硬件环境**：论文明确指出所有训练均在 **4 × NVIDIA A6000 GPU** 上完成。
*   **训练细节**：采用了多阶段渐进式训练（从 VAE 预训练到 LRA 校准，再到多模态对齐和全任务微调），总步数超过 90 万步。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量实验**：在 7 个核心任务上进行了详尽的指标对比（如 R-Precision, FID, MPJPE, BertScore 等）。
    *   **消融实验**：针对 DPA、LRA、双路径嵌入器设计、连续 vs 离散表示、注意力机制、LoRA 路由等关键组件进行了深入验证。
    *   **定性分析**：提供了大量的可视化结果，包括动作重建质量的频域和时域分析。
*   **充分性评价**：实验设计非常全面且客观。通过跨数据集（如 3DPW）的零样本测试验证了泛化性，并使用统一的 269 维运动表示确保了不同任务间的公平对比。

#### 6. 论文的主要结论与发现
*   **连续表示的优越性**：相比离散的 VQ-VAE，连续的 CMA-VAE 在动作重建精度（APE 降低约 80%）和下游任务表现上均有显著提升。
*   **统一框架的协同效应**：多任务联合训练产生了正向迁移，统一模型在 T2M 等任务上的表现甚至优于单任务模型。
*   **对齐策略的有效性**：LRA 解决了运动路径的初始化难题，而 DPA 成功将视觉几何先验注入了运动编码中。
*   **SOTA 性能**：UniMotion 是首个覆盖全部 7 项三模态任务的框架，并在所有任务中均达到或超过了现有专门模型的水平。

#### 7. 优点（亮点）
*   **首创性**：首次实现了运动-文本-视觉三模态在 LLM 架构下的深度统一。
*   **架构对称美学**：将运动与视觉对称处理，消除了模态间的表示鸿沟。
*   **创新的对齐机制**：DPA 和 LRA 从语义和几何两个维度解决了跨模态校准的难题。
*   **端到端能力**：支持运动引导的图像编辑，无需中间骨架渲染或文本中介。

#### 8. 不足与局限
*   **计算开销**：基于 1.5B 参数的主干网络，推理成本相对较高，难以在资源受限的设备上实时运行。
*   **数据偏差**：视觉-运动对齐主要依赖 Human3.6M 等室内受控环境数据集，在极端遮挡或复杂室外场景下的鲁棒性仍有待进一步验证。
*   **视频推理限制**：目前的视觉输入主要基于单帧或稀疏采样帧，对于长视频的复杂时序推理能力尚有提升空间。

（完）
