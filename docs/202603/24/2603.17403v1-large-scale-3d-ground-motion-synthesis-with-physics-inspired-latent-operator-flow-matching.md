---
title: Large-Scale 3D Ground-Motion Synthesis with Physics-Inspired Latent Operator Flow Matching
title_zh: 基于物理启发式潜算子流匹配的大规模三维地震动合成
authors: "Yaozhong Shi, Grigorios Lavrentiadis, Konstantinos Tsalouchidis, Zachary E. Ross, David McCallen, Caifeng Zou, Kamyar Azizzadenesheli, Domniki Asimaki"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17403v1"
tags: ["query:3d-motion-generation"]
score: 8.0
evidence: 用于3D地面运动合成的潜算子流匹配
tldr: 地震灾害分析需要大规模且具有时空相干性的地面运动模拟，但传统物理模拟计算成本极高。本文提出 GMFlow，一种受物理启发的潜在算子流匹配框架，能够根据物理参数生成大规模区域地面运动时程。在旧金山湾区模拟中，该方法在数秒内生成了超过 900 万个网格点的空间相干运动，比传统模拟快一万倍，为分布式基础设施的快速风险评估提供了新途径。
selection_source: fresh_fetch
motivation: 传统的基于物理的地震模拟计算量巨大，难以满足分布式基础设施对大规模、多场景不确定性量化的实时需求。
method: 提出了一种名为 GMFlow 的物理启发式潜在算子流匹配框架，通过条件物理参数生成具有时空相干性的地面运动场。
result: 在旧金山湾区案例中，GMFlow 实现了比传统物理模拟快一万倍的生成速度，并能处理超过 900 万个网格点的大规模数据。
conclusion: 该研究不仅加速了地震灾害评估，还为科学领域中大规模时空物理场的网格无关生成建模提供了通用框架。
---

## 摘要
地震灾害分析和空间分布基础设施（如电网和能源管道网络）的设计，需要具有真实频率成分和时空相干性的特定场景地震动时程。然而，利用基于物理的模拟生成不确定性量化所需的大型集成，计算强度大，在工程工作流中不切实际。为了应对这一挑战，我们引入了 Ground-Motion Flow (GMFlow)，这是一个受物理启发的潜算子流匹配框架，可根据物理参数生成真实的、大规模区域地震动时程。在旧金山湾区的模拟地震场景中经过验证，GMFlow 在几秒钟内生成了超过 900 万个网格点的空间相干地震动，比模拟工作流实现了 10,000 倍的加速，这为分布式基础设施的快速且具有不确定性意识的灾害评估开辟了道路。更广泛地说，GMFlow 推进了与网格无关的函数生成建模，并有可能扩展到不同科学领域的大规模时空物理场合成。

## Abstract
Earthquake hazard analysis and design of spatially distributed infrastructure, such as power grids and energy pipeline networks, require scenario-specific ground-motion time histories with realistic frequency content and spatiotemporal coherence. However, producing the large ensembles needed for uncertainty quantification with physics-based simulations is computationally intensive and impractical for engineering workflows. To address this challenge, we introduce Ground-Motion Flow (GMFlow), a physics-inspired latent operator flow matching framework that generates realistic, large-scale regional ground-motion time-histories conditioned on physical parameters. Validated on simulated earthquake scenarios in the San Francisco Bay Area, GMFlow generates spatially coherent ground motion across more than 9 million grid points in seconds, achieving a 10,000-fold speedup over the simulation workflow, which opens a path toward rapid and uncertainty-aware hazard assessment for distributed infrastructure. More broadly, GMFlow advances mesh-agnostic functional generative modeling and could potentially be extended to the synthesis of large-scale spatiotemporal physical fields in diverse scientific domains.

---

## 论文详细总结（自动生成）

以下是对论文《Large-Scale 3D Ground-Motion Synthesis with Physics-Inspired Latent Operator Flow Matching》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何快速生成具有真实频率成分和时空相干性的大规模、三维区域地震动（Ground-Motion）时程，以支持分布式基础设施（如电网、管网）的地震风险评估。
*   **研究动机**：
    *   **传统模拟局限**：基于物理的数值模拟（如求解弹性动力学方程）虽然准确，但计算成本极高，难以生成不确定性量化所需的大规模场景集成。
    *   **现有AI模型不足**：现有的生成模型多将地震动视为孤立站点的1D时间序列，忽略了区域内的空间相关性，且难以处理数百万网格点的大规模场生成。
    *   **数据稀缺**：真实强震记录稀疏且分布不均，难以覆盖所有可能的震级和震源场景。

### 2. 方法论：GMFlow 框架
GMFlow 采用了一种**两阶段、函数空间（Function-space）**的生成策略：
*   **核心思想**：利用“物理启发式子空间”分解任务。先生成捕捉大尺度物理结构的低频波场，再通过超分辨率算子恢复高频细节。
*   **关键技术细节**：
    1.  **物理对齐子空间（Physics-Aligned Subspace）**：设定截止频率（如0.75Hz），将复杂的全波场分解为低频分量（物理规律更稳健）和高频分量。
    2.  **自编码算子（AENO）**：使用 U-型神经算子（UNO）将低频波场压缩到紧凑的潜在空间（Latent Space）。
    3.  **条件潜在流匹配（Conditional Latent Flow Matching）**：在潜在空间中，利用整流流（Rectified Flow）技术，根据震级、震源位置等物理参数，将高斯噪声变换为对应的潜在表示。
    4.  **超分辨率神经算子（SNO）**：采用残差傅里叶神经算子（rFNO），将生成的低频波场提升至目标分辨率的全频带波场。
*   **算法流程**：输入物理条件 $c$ → 流匹配生成潜在码 $z_1$ → 解码为低频波场 $u_f$ → 超分辨率重建全频带波场 $u$。

### 3. 实验设计
*   **数据集**：基于旧金山湾区（SFBA）三维速度模型生成的 5,300 个高保真模拟事件。
    *   **Mw 4.4**：2,100 个点源地震。
    *   **Mw 6.0/7.0**：3,200 个海沃德断层（Hayward Fault）有限破裂事件。
*   **基准（Benchmark）**：使用 SW4 平台进行的物理数值模拟结果作为真值（Ground Truth）。
*   **对比与评估指标**：
    *   **强度指标**：峰值地面速度（PGV）。
    *   **频率指标**：傅里叶振幅谱（FAS）。
    *   **时空相干性**：峰值归一化互相关（NCC）及时间滞后图。
    *   **物理一致性**：震级缩放规律（Magnitude Scaling）。

### 4. 资源与算力
*   **GMFlow 训练（单台工作站 GPU: NVIDIA RTX 6000 Ada）**：
    *   **SNO 训练**：58.5 小时。
    *   **AENO 训练**：36.3 小时。
    *   **流匹配训练**：5.8 小时。
*   **推理效率**：在单 GPU 上仅需 **0.386 小时** 即可生成全部 5,300 个事件（每个事件包含约 940 万个标量值）。
*   **物理模拟对比（基准）**：生成相同数据集需要约 **4,000 NVIDIA A100 GPU 小时** 和 **6,200 CPU 小时**。
*   **加速比**：实现了约 **10,000 倍** 的推理加速。

### 5. 实验数量与充分性
*   **实验规模**：使用了 5,000 个训练样本和 300 个测试样本（涵盖不同震级）。
*   **消融实验**：
    *   对比了不同参数规模（16M vs 128M）的流匹配模型。
    *   测试了不同低频截止频率（0.5Hz 到 1.0Hz）对生成质量的影响。
*   **充分性评价**：实验设计较为全面，不仅验证了单点波形，还通过虚拟台阵剖面验证了波场传播的物理一致性。通过震级缩放实验验证了模型在未见震级下的插值能力，客观地展示了模型在处理复杂有限破裂场景时的挑战。

### 6. 主要结论
*   **极速生成**：GMFlow 能够在数秒内生成覆盖全区域、数百万网格点的空间相干地震动。
*   **物理一致性**：生成的波场准确捕捉了波前几何、盆地放大效应和破裂方向性。
*   **不确定性感知**：模型能够量化由于源复杂性引起的 aleatory 不确定性，震级越大，预测带宽越宽。
*   **零样本超分**：作为函数算子模型，它支持在推理时直接在比训练网格更细的网格上进行查询。

### 7. 优点与亮点
*   **首创性**：据称是首个直接在函数空间中制定的、用于大规模三维地震动合成的生成模型。
*   **网格无关性**：基于神经算子的设计使其不依赖于特定离散化，具有极高的灵活性。
*   **物理启发式架构**：通过低频/高频分解，降低了高维数据生成的难度，同时保证了长周期物理特性的准确性。
*   **开源贡献**：公开了约 3.3 TB 的大规模地震模拟数据集，对社区贡献巨大。

### 8. 不足与局限
*   **高频偏差**：在处理 Mw 7.0 等大震级有限破裂事件时，模型在 0.5-1.0 Hz 范围内存在系统性的振幅低估（需通过后处理校准缓解）。
*   **几何限制**：当前实现基于 FFT 的 FNO，仍依赖于规则网格，尚未完全发挥处理复杂地形或不规则观测站点的潜力（作者指出未来将引入 MINO 等架构）。
*   **频率上限**：目前仅验证至 1 Hz，而工程应用往往需要更高频（>5 Hz）的信息，这受限于训练数据的物理模拟精度。

（完）
