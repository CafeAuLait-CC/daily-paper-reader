---
title: "Sculpt4D: Generating 4D Shapes via Sparse-Attention Diffusion Transformers"
title_zh: Sculpt4D：通过稀疏注意力扩散 Transformer 生成 4D 形状
authors: "Minghao Yin, Wenbo Hu, Jiale Xu, Ying Shan, Kai Han"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21592v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 通过稀疏注意力扩散 Transformer 生成 4D 形状
tldr: "Sculpt4D是一个原生4D生成框架，旨在解决动态3D合成中的时间伪影和高计算成本问题。它基于预训练的Hunyuan3D 2.1，通过引入块稀疏注意力机制，在锚定初始帧以保持物体一致性的同时，利用时间衰减掩码捕捉运动动态。该方法不仅实现了高保真度的时空建模，还将计算开销降低了56%，在保持时间连贯性方面达到了SOTA水平。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21592v1/fig-001.webp\", \"caption\": \"Figure 3. Qualitative comparison of 4D mesh generation. We compare Sculpt4D against V2M4 [5] and L4GM [29]. Given an input image (left), we show two generated views per method. Top and bottom rows correspond to time frames Time 1 and Time 2, respectively.\", \"page\": 6, \"index\": 1, \"width\": 923, \"height\": 831}, {\"url\": \"assets/figures/arxiv/2604.21592v1/fig-002.webp\", \"caption\": \"Figure 2. An overview of our 4D generation framework. Conditioned on an image sequence, we use Consistent Surface Sampling (Sec. 3.1) to acquire both sharp edge points and random surface points, which a vector set VAE [13, 58] encodes into shape latents. These latents are processed by 4D DiT blocks, which use cross-attention for image conditioning and our novel Block Sparse Attention (Sec. 3.3). This sparse attention, guided by a composite mask (Temporal Sparse and First-Frame Anchor), efficiently captures motion while ensuring identity consistency. Finally, a decoder produces the final mesh sequence from the denoised latents.\", \"page\": 3, \"index\": 2, \"width\": 1031, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2604.21592v1/fig-003.webp\", \"caption\": \"Figure 5. Mesh sequences generated from in the wild data.\", \"page\": 8, \"index\": 3, \"width\": 496, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.21592v1/fig-004.webp\", \"caption\": \"Figure 6. Qualitative results of textured mesh sequences.\", \"page\": 8, \"index\": 4, \"width\": 446, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.21592v1/fig-005.webp\", \"caption\": \"Figure 4. Additional qualitative results of our method. Each row displays one of six diverse 4D results across six time frames. For each frame, the main image shows View 1, with View 2 (top-left) and the input image (bottom-left) as insets.\", \"page\": 7, \"index\": 5, \"width\": 958, \"height\": 637}]"
motivation: 现有的4D生成模型面临时间伪影严重、计算需求极高以及高质量4D训练数据稀缺的挑战。
method: 在预训练3D扩散Transformer基础上，提出一种块稀疏注意力机制，通过初始帧锚定和时间衰减掩码实现高效的时空建模。
result: "该方法在实现高保真、时间连贯的4D合成的同时，成功将网络总计算量降低了56%。"
conclusion: Sculpt4D为高效且可扩展的4D形状生成提供了新路径，并在该领域确立了新的领先标准。
---

## 摘要
近期 3D 生成建模的突破在静态形状合成方面取得了显著进展，但高保真动态 4D 生成仍难以实现，受限于时间伪影和极高的计算需求。我们提出了 Sculpt4D，这是一个原生的 4D 生成框架，它将高效的时间建模无缝集成到预训练的 3D 扩散 Transformer（Hunyuan3D 2.1）中，从而缓解了 4D 训练数据的稀缺问题。其核心是一种块稀疏注意力（Block Sparse Attention）机制，通过锚定初始帧来保持物体的一致性，同时通过时间衰减稀疏掩码捕捉丰富的运动动态。该设计能够高保真地建模复杂的时空依赖关系，同时避开了全注意力机制的二次方开销，并将网络总计算量减少了 56%。因此，Sculpt4D 在时间连续的 4D 合成方面达到了新的最先进水平，并为高效且可扩展的 4D 生成指明了方向。

## Abstract
Recent breakthroughs in 3D generative modeling have yielded remarkable progress in static shape synthesis, yet high-fidelity dynamic 4D generation remains elusive, hindered by temporal artifacts and prohibitive computational demand. We present Sculpt4D, a native 4D generative framework that seamlessly integrates efficient temporal modeling into a pretrained 3D Diffusion Transformer (Hunyuan3D 2.1), thereby mitigating the scarcity of 4D training data. At its core lies a Block Sparse Attention mechanism that preserves object identity by anchoring to the initial frame while capturing rich motion dynamics via a time-decaying sparse mask. This design faithfully models complex spatiotemporal dependencies with high fidelity, while sidestepping the quadratic overhead of full attention and reducing network total computation by 56%. Consequently, Sculpt4D establishes a new state-of-the-art in temporally coherent 4D synthesis and charts a path toward efficient and scalable 4D generation.

---

## 论文详细总结（自动生成）

这是一份关于学术论文 **《Sculpt4D: Generating 4D Shapes via Sparse-Attention Diffusion Transformers》** 的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在计算资源有限且 4D 训练数据稀缺的情况下，生成高保真、时间连贯的动态 3D 形状（即 4D 资产）。
*   **研究背景**：
    *   静态 3D 生成已取得突破（如 Hunyuan3D），但 4D 生成仍面临挑战。
    *   **现有方案缺陷**：基于分数蒸馏采样（SDS）的方法速度极慢且不稳定；“视频转 4D”的两阶段方法容易累积误差导致几何抖动；直接扩展 3D DiT 到 4D 会面临全注意力机制带来的二次方计算开销（$O(T \times P)^2$）。
*   **整体含义**：Sculpt4D 旨在通过将高效的时间建模集成到预训练的 3D 扩散 Transformer 中，实现一种原生、高效且可扩展的 4D 生成框架。

### 2. 提出方法论
Sculpt4D 的核心思想是利用预训练 3D 模型的空间先验，并通过创新的稀疏注意力机制扩展其时间维度。
*   **时间一致性潜空间编码（Temporally Consistent Latent Encoding）**：
    *   **表面采样一致性**：为了防止 VAE 编码产生的几何抖动，通过重心坐标（Barycentric coordinates）将第一帧的采样点传播到后续帧，并投影到水密网格表面，确保点对点的时空对应。
    *   **共享噪声重参数化**：在 VAE 编码过程中，所有帧共享同一个随机噪声向量 $\epsilon$，消除随机性导致的时间不连续。
*   **4D-DiT 架构**：
    *   在预训练的 Hunyuan3D 2.1 块中插入时间自注意力层，并使用 1D 旋转位置编码（RoPE）处理时间维度。
*   **块稀疏注意力机制（Block Sparse Attention）—— 核心创新**：
    *   **第一帧锚点（First-Frame Anchor）**：所有帧的所有 token 都会关注第一帧的 token。这为序列提供了全局身份（Identity）参考，防止长时间序列中的形状漂移。
    *   **时间衰减稀疏掩码（Time-decaying Sparse Mask）**：引入随时间距离增加而增大的步幅（Stride）。近距离帧使用密集注意力捕捉精细运动，远距离帧通过稀疏采样捕捉语义上下文。
    *   **空间对应保持**：该掩码设计确保了空间位置相同的块在远距离依然能相互关注，维持了物体的结构一致性。

### 3. 实验设计
*   **数据集**：从 Objaverse 中筛选出的 13k 个 4D 动画对象。
*   **Benchmark 与对比方法**：
    *   **对比方法**：L4GM（基于高斯投影）、V2M4（网格重建）、GVFD（变形场扩散）以及基础模型 Hunyuan3D 的逐帧生成版本。
    *   **评估指标**：几何质量（Chamfer Distance, IoU, F-Score）、视觉质量（LPIPS, CLIP score）和时间连贯性（FVD）。
*   **测试场景**：涵盖了 Objaverse 测试集、DAVIS 真实世界视频（In-the-wild）以及长序列生成（最高 64 帧）。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 96GB 显存的 GPU**（推测为 NVIDIA A100 或 H100）。
*   **训练配置**：训练迭代次数为 24k 次，Batch Size 为 32（每条序列 16 帧），训练耗时约 **3 天**。
*   **推理效率**：生成一个完整序列约需 **7 分钟**，显著快于对比方法（如 V2M4 的 45 分钟）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量对比**：在 50 个 Objaverse 模型上进行了标准化的几何指标测试。
    *   **消融实验**：设计了 7 组变体，分别验证了时间一致性采样、共享噪声、第一帧锚点、稀疏掩码策略（固定步幅 vs 衰减步幅）的有效性。
    *   **计算分析**：详细对比了 8、16、32 帧下稀疏注意力与全注意力的 FLOPs 消耗。
*   **充分性评价**：实验设计较为全面，不仅涵盖了合成数据，还通过真实视频验证了泛化能力，并探讨了模型在训练长度之外（Zero-shot）的扩展表现，逻辑严密且客观。

### 6. 主要结论与发现
*   **性能领先**：Sculpt4D 在几何精度和时间连贯性上均显著优于现有 SOTA 方法。
*   **计算高效**：块稀疏注意力机制将网络总计算量降低了 **56%**，且随着序列增长，计算优势更加明显（32 帧时仅为全注意力的 26.8%）。
*   **长序列能力**：尽管只在 16 帧数据上训练，模型能稳定生成 64 帧的高质量动画，证明了稀疏注意力机制的鲁棒性。

### 7. 优点（亮点）
*   **原生 4D 建模**：不同于后处理对齐，Sculpt4D 在 DiT 内部学习时空依赖，生成的运动更自然。
*   **创新的注意力设计**：结合了“锚点”和“步幅衰减”的思想，巧妙平衡了显存占用与生成质量。
*   **VAE 优化**：识别并解决了 VAE 随机性导致的 4D 抖动问题，这对后续研究具有借鉴意义。

### 8. 不足与局限
*   **数据依赖**：虽然缓解了数据稀缺，但模型性能仍高度依赖于 Objaverse 动画的质量和多样性。
*   **纹理对齐**：目前的纹理生成仍依赖于拓扑对齐和后处理传播，并非完全端到端的 4D 纹理生成。
*   **拓扑限制**：虽然能处理一定的拓扑变化，但对于极度复杂的拓扑断裂或融合，其表现仍有待进一步验证。

（完）
