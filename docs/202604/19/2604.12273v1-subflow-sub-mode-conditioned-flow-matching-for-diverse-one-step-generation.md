---
title: "SubFlow: Sub-mode Conditioned Flow Matching for Diverse One-Step Generation"
title_zh: SubFlow：用于多样化单步生成的子模态条件流匹配
authors: "Yexiong Lin, Jia Shi, Shanshan Ye, Wanyu Wang, Yu Yao, Tongliang Liu"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12273v1"
tags: ["query:3d-motion-generation"]
score: 8.0
evidence: 用于生成建模和多样性改进的流匹配方法
tldr: 针对流匹配在少步生成中存在的多样性缺失问题，本文提出了SubFlow。研究发现，传统方法因MSE损失导致的“平均失真”会使模型忽略低密度子模式。SubFlow通过语义聚类将类别分解为细粒度子模式，并以此为条件进行训练，使分布近似单峰，从而在单步生成中恢复完整的模式覆盖。该方法具有即插即用特性，在ImageNet-256上显著提升了生成多样性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12273v1/fig-001.webp\", \"caption\": \"Table 1: Class-conditional ImageNet-256 generation results. NFE denotes the number of function evaluations (sampling steps). Results in the Multi-step Diffusion/Flows section are cited from [63], and all other results are evaluated using officially released checkpoints. SubFlow consistently improves Recall (diversity) when integrated into different one-step baselines, while maintaining competitive FID and Precision.\", \"page\": 6, \"index\": 1, \"width\": 822, \"height\": 653}, {\"url\": \"assets/figures/arxiv/2604.12273v1/fig-002.webp\", \"caption\": \"Figure 1: Toy experiment on a 4-peak Gaussian mixture target with imbalanced subclusters (majority weight 0.35, minority weight 0.15 per class). (a) Traditional flow matching (100 inference steps) covers all modes. (b) MeanFlow suffers from severe mode collapse, concentrating only on dominant subclusters and losing diversity. (c) In contrast, our SubFlow successfully restores full mode coverage in a single step by decomposing each class into fine-grained sub-modes and conditioning the flow on sub-mode indices.\", \"page\": 2, \"index\": 2, \"width\": 1045, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.12273v1/fig-003.webp\", \"caption\": \"Table 2: Effect of the number of sampling steps (NFE) on MeanFlow-B/2 with and without SubFlow (𝐾=5). Multi-step integration could improve FID but Recall slightly decreases, confirming that reduced discretization error amplifies rather than corrects the dominant-mode bias. SubFlow provides consistent Recall gains at all step counts.\", \"page\": 7, \"index\": 3, \"width\": 436, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2604.12273v1/fig-004.webp\", \"caption\": \"Figure 3: Qualitative comparison between MeanFlow (left) and MeanFlow with SubFlow (right) on ImageNet-256. Each row shows five samples from the same class. Green boxes highlight samples where SubFlow produces visibly higher image quality with sharper details and fewer artifacts.\", \"page\": 7, \"index\": 4, \"width\": 894, \"height\": 468}, {\"url\": \"assets/figures/arxiv/2604.12273v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of SubFlow. (a) Offline pre-processing: semantic features are extracted from the training images and clustered within each class, yielding sub-mode assignments {𝑘𝑖 } and empirical priors 𝑝 (𝑘 | 𝑐). (b) Training: the vector field 𝑣𝜃 (𝑥𝑡 , 𝑡, 𝑐, 𝑘) is optimized with the SubFlow objective. For classifier-free guidance, only the class label 𝑐 is randomly dropped, while the sub-mode index 𝑘 is always retained. (c) Inference: given a target class 𝑐, a sub-mode index 𝑘 is sampled from 𝑝 (𝑘 | 𝑐), and the conditioned vector field generates a sample associated with the selected sub-mode.\", \"page\": 4, \"index\": 5, \"width\": 860, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2604.12273v1/fig-006.webp\", \"caption\": \"Figure 6: t-SNE visualization of DINOv3 features for ImageNet classes at opposite ends of the HDBSCAN clustering spectrum. Classes in (a) are well served by standard class conditioning, while classes in (b) suffer from averaging distortion that SubFlow’s sub-mode conditioning resolves.\", \"page\": 12, \"index\": 6, \"width\": 953, \"height\": 353}]"
motivation: 现有的少步流匹配模型因MSE目标的平均失真效应，导致样本集中于高密度模式而忽略稀有子模式，造成严重的多样性退化。
method: 提出SubFlow框架，通过语义聚类将类别分解为细粒度的子模式，并将子模式索引作为条件引入流匹配训练，以消除平均失真。
result: 在ImageNet-256实验中，SubFlow在保持图像质量的同时显著提升了生成多样性（Recall），且能无缝集成至多种单步生成模型。
conclusion: SubFlow通过子模式条件化有效解决了单步生成中的多样性缺失问题，为实现高质量且多样化的快速生成提供了通用且高效的方案。
---

## 摘要
流匹配（Flow matching）已成为一种强大的生成框架，近期的少步（few-step）方法在推理加速方面取得了显著成效。然而，我们发现了一个关键但被忽视的局限性：这些模型存在严重的样本多样性退化问题，样本往往集中在主导模态（dominant modes）上，而忽略了目标分布中稀有但有效的变体。我们将这种退化归因于“平均失真”（averaging distortion）：当使用均方误差（MSE）目标进行训练时，类别条件流会学习类内子模态的频率加权平均值，导致模型过度表征高密度模态，而系统性地忽略低密度模态。为解决这一问题，我们提出了 SubFlow（子模态条件流匹配），通过语义聚类将每个类别分解为细粒度的子模态，并将流以子模态索引为条件，从而消除平均失真。由于每个条件子分布近似为单峰分布（unimodal），学习到的流能够准确地针对各个模态而无平均失真，从而在单步推理中恢复完整的模态覆盖。至关重要的是，SubFlow 完全是即插即用的：它可以无缝集成到现有的单步模型（如 MeanFlow 和 Shortcut Models）中，无需任何架构修改。在 ImageNet-256 上的广泛实验表明，SubFlow 在保持竞争力的图像质量（FID）的同时，显著提升了生成多样性（Recall），证实了其在不同单步生成框架中的广泛适用性。项目主页：https://yexionglin.github.io/subflow。

## Abstract
Flow matching has emerged as a powerful generative framework, with recent few-step methods achieving remarkable inference acceleration. However, we identify a critical yet overlooked limitation: these models suffer from severe diversity degradation, concentrating samples on dominant modes while neglecting rare but valid variations of the target distribution. We trace this degradation to averaging distortion: when trained with MSE objectives, class-conditional flows learn a frequency-weighted mean over intra-class sub-modes, causing the model to over-represent high-density modes while systematically neglecting low-density ones. To address this, we propose SubFlow, Sub-mode Conditioned Flow Matching, which eliminates averaging distortion by decomposing each class into fine-grained sub-modes via semantic clustering and conditioning the flow on sub-mode indices. Each conditioned sub-distribution is approximately unimodal, so the learned flow accurately targets individual modes with no averaging distortion, restoring full mode coverage in a single inference step. Crucially, SubFlow is entirely plug-and-play: it integrates seamlessly into existing one-step models such as MeanFlow and Shortcut Models without any architectural modifications. Extensive experiments on ImageNet-256 demonstrate that SubFlow yields substantial gains in generation diversity (Recall) while maintaining competitive image quality (FID), confirming its broad applicability across different one-step generation frameworks. Project page: https://yexionglin.github.io/subflow.