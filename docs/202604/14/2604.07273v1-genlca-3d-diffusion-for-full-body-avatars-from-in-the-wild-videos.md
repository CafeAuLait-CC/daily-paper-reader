---
title: "GenLCA: 3D Diffusion for Full-Body Avatars from In-the-Wild Videos"
title_zh: GenLCA：基于野外视频的全身化身 3D 扩散模型
authors: "Yiqian Wu, Rawal Khirodkar, Egor Zakharov, Timur Bagautdinov, Lei Xiao, Zhaoen Su, Shunsuke Saito, Xiaogang Jin, Junxuan Li"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07273v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 用于生成和驱动全身人体化身的 3D 扩散模型
tldr: GenLCA 是一种基于扩散模型的全身 3D 化身生成框架，旨在从文本或图像生成高保真且可动画化的数字人。该研究通过将预训练重建模型作为 3D 分词器，将大规模真实视频转化为结构化 3D 标记，并引入可见性感知训练策略解决数据不完整问题。GenLCA 在生成质量和泛化性上表现卓越，支持精细的动画控制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07273v1/fig-001.webp\", \"caption\": \"Fig. 5. 3D avatars generated by GenLCA from texts. All results are generated with CFG scale = 5.0, 50 sampling steps, and animated with random poses.\", \"page\": 6, \"index\": 1, \"width\": 852, \"height\": 614}, {\"url\": \"assets/figures/arxiv/2604.07273v1/fig-002.webp\", \"caption\": \"Fig. 6. We compare our GenLCA with SOTA methods, including SDS based approaches: TADA [Liao et al. 2024], HumanGaussian [Liu et al. 2024], and DreamWaltz-G [Huang et al. 2025a], and text conditioned 3D human diffusion models, TeRA [Wang et al. 2025b] and SIGMAN [Yang et al. 2025]. We use the same text prompt as input. In addition to full-body renderings, we also provide zoomed-in views for comparison of facial regions.\", \"page\": 7, \"index\": 2, \"width\": 744, \"height\": 757}, {\"url\": \"assets/figures/arxiv/2604.07273v1/fig-003.webp\", \"caption\": \"Table 1. Comparison of training datasets for 3D human diffusion models. Our dataset contains the largest number of identities and the most realistic data among existing methods. “∗” indicates that the corresponding model is trained on each sub-dataset individually, rather than on a mixture of them.\", \"page\": 3, \"index\": 3, \"width\": 760, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2604.07273v1/fig-004.webp\", \"caption\": \"Fig. 7. We conduct ablation studies by individually removing the visibility-aware training, learnable placeholder, and in-the-wild training data components to demonstrate the effectiveness of each.\", \"page\": 9, \"index\": 4, \"width\": 1050, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2604.07273v1/fig-005.webp\", \"caption\": \"Fig. 4. (A) The tokens accurately reconstruct the visible regions of the images. After filtering out all invalid tokens and retaining only the valid ones, the rendered results still achieve high-quality reconstruction. (B) We present the rendered tokens in canonical space (1st row), where blurry regions are highlighted with blue boxes and transparent regions with yellow boxes. The visibility mask (2nd row) separates the valid and invalid regions.\", \"page\": 5, \"index\": 5, \"width\": 502, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2604.07273v1/fig-006.webp\", \"caption\": \"Fig. 3. (A) Training pipeline of GenLCA. During training, the high dimensional GS tokens are first encoded into compact GS latent by the compressor encoder. For conditional inputs, we use CLIP [Radford et al. 2021] to extract text embeddings and DINOv2 [Oquab et al. 2024] to extract scribble and body part embeddings. To prevent the training process from being affected by corrupted information, we replace invalid regions (as indicated by the visibility mask) with learnable placeholder features and employ a masked loss. (B) Detailed architecture of the compressor. The compressor’s encoder and decoder consist of MLPs for downsampling or upsampling, combined with self-attention blocks for feature fusion. Positional encoding is applied within each block. (C) Detailed architecture of the GenLCA block. We adapt the MMDiT block as the basic block of GenLCA. Each GenLCA block takes the query points, time step, latent features, and conditional features as inputs. Separate branches are used to process latent and conditional features, and positional encoding is added to the latent features.\", \"page\": 5, \"index\": 6, \"width\": 1050, \"height\": 423}]"
motivation: 现有的 3D 化身生成方法受限于高质量 3D 数据的匮乏，难以有效利用大规模真实世界视频进行训练。
method: 将预训练重建模型作为 3D 分词器，并结合可见性感知扩散训练策略，在不完整的 2D 视频数据上训练 3D 扩散模型。
result: 该方法生成的全身化身在真实感、多样性和动画保真度上均大幅领先现有方案。
conclusion: GenLCA 证明了通过 3D 分词化和可见性感知训练，可以有效利用大规模 2D 视频数据构建高性能的 3D 生成模型。
---

## 摘要
我们提出了 GenLCA，这是一种基于扩散的生成模型，用于根据文本和图像输入生成和编辑具有照片级真实感的全身化身（avatars）。生成的化身忠实于输入，同时支持高保真的面部和全身动画。其核心思想是一种新颖的范式，能够从部分可观测的 2D 数据中训练全身 3D 扩散模型，从而使训练数据集能够扩展到数百万个真实世界的视频。这种可扩展性使 GenLCA 具有卓越的写实感和泛化能力。具体而言，我们通过将预训练的前馈化身重建模型重新利用为可动画化的 3D 分词器（tokenizer），将非结构化的视频帧编码为结构化的 3D 标记（tokens），从而扩大了数据集规模。然而，大多数真实视频仅提供身体部位的部分观测，导致 3D 标记中出现过度的模糊或透明伪影。为了解决这一问题，我们提出了一种新颖的可见性感知扩散训练策略，用可学习的标记替换无效区域，并仅在有效区域上计算损失。随后，我们在标记数据集上训练一个基于流（flow-based）的扩散模型，从而本质上保持了预训练化身重建模型所提供的写实感和可动画性。我们的方法有效地实现了利用大规模真实视频数据在原生 3D 空间中训练扩散模型。我们通过多样化且高保真的生成和编辑结果证明了该方法的有效性，其表现大幅优于现有解决方案。项目页面见 https://onethousandwu.com/GenLCA-Page。

## Abstract
We present GenLCA, a diffusion-based generative model for generating and editing photorealistic full-body avatars from text and image inputs. The generated avatars are faithful to the inputs, while supporting high-fidelity facial and full-body animations. The core idea is a novel paradigm that enables training a full-body 3D diffusion model from partially observable 2D data, allowing the training dataset to scale to millions of real-world videos. This scalability contributes to the superior photorealism and generalizability of GenLCA. Specifically, we scale up the dataset by repurposing a pretrained feed-forward avatar reconstruction model as an animatable 3D tokenizer, which encodes unstructured video frames into structured 3D tokens. However, most real-world videos only provide partial observations of body parts, resulting in excessive blurring or transparency artifacts in the 3D tokens. To address this, we propose a novel visibility-aware diffusion training strategy that replaces invalid regions with learnable tokens and computes losses only over valid regions. We then train a flow-based diffusion model on the token dataset, inherently maintaining the photorealism and animatability provided by the pretrained avatar reconstruction model. Our approach effectively enables the use of large-scale real-world video data to train a diffusion model natively in 3D. We demonstrate the efficacy of our method through diverse and high-fidelity generation and editing results, outperforming existing solutions by a large margin. The project page is available at https://onethousandwu.com/GenLCA-Page.

---

## 论文详细总结（自动生成）

以下是对论文《GenLCA: 3D Diffusion for Full-Body Avatars from In-the-Wild Videos》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何利用大规模、非结构化的真实世界（In-the-wild）单目视频，训练一个能够生成高保真、可动画驱动的全身 3D 数字人扩散模型？
*   **研究背景**：
    *   现有的 3D 数字人生成方法通常依赖于合成数据（存在领域差异，真实感差）或小规模的多视角采集数据（泛化性不足）。
    *   虽然互联网上有数以百万计的单目视频，但由于缺乏准确的 3D 标注且存在严重的遮挡（部分可观测性），直接将其用于 3D 扩散模型训练极具挑战。

### 2. 方法论
GenLCA 的核心思想是将 3D 生成任务转化为在“3D 标记（Token）”空间中的扩散过程，并解决数据不完整性问题。
*   **3D 化身分词器（3D Avatar Tokenizer）**：
    *   采用预训练的重建模型（LCA），将输入的 2D 视频帧编码为 $N=8192$ 个结构化的 3D 标记。每个标记对应人体模板上的一个查询点，并最终解码为 3D Gaussian Splatting (3DGS) 属性。
*   **标记压缩器（Token Compressor）**：
    *   为了提高训练效率，使用一个基于自注意力机制的编码器-解码器结构，将高维的 3D 标记（1024 维）压缩到紧凑的潜空间（Latent Space，8 维）。
*   **可见性感知训练策略（Visibility-aware Training）**：
    *   **可见性掩码（Visibility Mask）**：针对单目视频中不可见的区域（如背面或被遮挡的下半身），计算每个标记的可见性。
    *   **可学习占位符（Learnable Placeholder）**：在训练扩散模型时，用统一的可学习特征替换无效/不可见区域的潜变量。
    *   **掩码损失（Masked Loss）**：仅在有效（可见）区域计算扩散损失，避免模型学习到重建模型在不可见区域产生的模糊或透明伪影。
*   **生成模型架构**：
    *   采用基于流（Flow-based）的扩散模型（Rectified Flow），使用 28 层双流 MMDiT 块。
    *   支持多模态输入：文本（CLIP 嵌入）、涂鸦（Scribble）和身体部位图像（DINOv2 嵌入）。

### 3. 实验设计
*   **数据集**：
    *   **野外数据**：约 111 万个单目人体视频。
    *   **采集数据**：约 4000 个高质量多视角或手机拍摄的全身旋转视频。
    *   **测试集**：从采集数据中抽取 1000 个身份进行评估。
*   **对比方法（Benchmark）**：
    *   **基于 SDS 优化的方法**：TADA, HumanGaussian, DreamWaltz-G。
    *   **3D 扩散模型**：TeRA, SIGMAN。
*   **评估指标**：BLIP-VQA、Text CLIP Score（语义一致性）；CLIB-FIQA、HyperIQA（图像质量）；FID（分布相似度）；以及 30 人参与的用户主观研究。

### 4. 资源与算力
*   **压缩器训练**：使用 32 张 NVIDIA A100 GPU，训练时长为 1 天。
*   **GenLCA 扩散模型训练**：使用 64 张 NVIDIA A100 GPU，训练时长为 4 天。
*   **推理效率**：生成一个化身约需 12 秒（50 步采样），远快于基于 SDS 优化的方法（需 1-3 小时）。

### 5. 实验数量与充分性
*   **实验规模**：论文在超过 110 万个身份的大规模数据集上进行了训练，这是目前 3D 数字人生成领域最大的训练规模之一。
*   **消融实验**：针对“可见性感知训练”、“可学习占位符”以及“野外数据规模”分别进行了对比实验，证明了每个组件对消除伪影和提升泛化性的必要性。
*   **客观性**：通过定量指标、定性视觉对比和双盲用户研究多维度评估，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **规模效应**：将 3D 扩散模型扩展到百万级真实数据规模，能显著提升生成化身的真实感和多样性。
*   **处理不完整数据的有效性**：通过可见性感知策略，模型可以从只有局部观测的视频中学习到全局的语义关联（例如，从正面照片推断出合理的背面细节）。
*   **多模态控制**：GenLCA 实现了文本、图像和涂鸦的无缝融合，支持高精度的局部编辑。

### 7. 优点
*   **数据利用率极高**：首次证明了可以有效利用海量“不完美”的单目视频来训练原生 3D 扩散模型。
*   **生成质量卓越**：生成的化身在面部细节、服装纹理和解剖结构上均优于现有 SOTA 方法。
*   **原生 3D 属性**：相比 2D 提升（Lifting）方法，GenLCA 在 3D 空间直接生成，避免了多视角不一致和过度饱和问题。

### 8. 不足与局限
*   **动画变形限制**：由于继承了重建模型的线性混合蒙皮（LBS）驱动方式，在处理极端姿势或宽松衣物时，可能会出现不自然的拉伸或穿模。
*   **依赖分词器质量**：生成效果的上限受限于预训练 LCA 重建模型的表现，若分词器无法捕捉某些复杂几何，扩散模型也难以生成。
*   **计算成本**：尽管推理快，但百万级数据的预处理（分词化）和扩散模型训练需要庞大的算力支撑。

（完）
