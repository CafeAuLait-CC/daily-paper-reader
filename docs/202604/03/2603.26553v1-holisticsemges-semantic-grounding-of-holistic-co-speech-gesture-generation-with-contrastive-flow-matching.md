---
title: "HolisticSemGes: Semantic Grounding of Holistic Co-Speech Gesture Generation with Contrastive Flow-Matching"
title_zh: HolisticSemGes：基于对比流匹配的全身协同语音手势生成的语义对齐
authors: "Lanmiao Liu, Esam Ghaleb, Aslı Özyürek, Zerrin Yumak"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26553v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 用于协同语音手势生成的流匹配
tldr: 针对协同语音手势生成中语义关联性弱和跨模态不一致的问题，本文提出HolisticSemGes模型。该模型引入对比流匹配机制，利用不匹配的音视频文本作为负样本，引导模型学习语义相关的稀疏动作而非仅是节奏性动作。通过构建统一的潜在空间，实现了文本、音频与全身动作的跨模态一致性，在BEAT2和SHOW数据集上达到SOTA水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26553v1/fig-001.webp\", \"caption\": \"Fig. 2: Our architecture comprises two synergistic modules: (a) Semantics-Aware Composite Module (SACM), which anchors audio, textual, and motion features within an aligned semantic space to ensure cross-modal consistency; and (b) Multimodal Conditioning Module, which leverages a learned velocity field conditioned on multimodal priors and stochastic seed latents to synthesize expressive, kinematically coherent holistic gestures.\", \"page\": 6, \"index\": 1, \"width\": 716, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2603.26553v1/fig-002.webp\", \"caption\": \"Fig. 4: Qualitative comparison of semantic gesture generation on BEAT2. Our model synthesizes context-grounded gestures that precisely map to linguistic semantics: discourse marker “Well”, deictic pointing for “for me” rhythmic denial for “never” and iconic explanation for “because”. In contrast, baselines typically collapse into semantically neutral, rhythm-driven arm swings, failing to capture the distinct communicative functions of each clause.\", \"page\": 13, \"index\": 2, \"width\": 652, \"height\": 540}, {\"url\": \"assets/figures/arxiv/2603.26553v1/fig-003.webp\", \"caption\": \"Fig. 3: Overview of the proposed semantic-aware contrastive flow matching framework for gesture generation. Audio and text transcripts are encoded into a shared semantic representation where congruent and incongruent gesture latents form contrastive pairs. Contrastive flow matching then learns motion trajectories aligned with the correct semantic context while diverging from mismatched inputs, producing coherent fullbody gestures.\", \"page\": 8, \"index\": 3, \"width\": 529, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2603.26553v1/fig-004.webp\", \"caption\": \"Table 2: Ablation study on BEAT2 and SHOW datasets. We analyze the contribution of each component, including the Multimodal Semantics-Aware Composite Module (SACM) under different modality settings, and the Contrastive Flow Matching objective compared to Standard Flow Matching.\", \"page\": 12, \"index\": 4, \"width\": 714, \"height\": 394}]"
motivation: 现有方法依赖预定义规则且缺乏负样本训练，导致生成的手势往往只有节奏感而缺乏深层语义关联和全身协调性。
method: 提出对比流匹配方法，通过负样本约束速度场，并利用余弦和对比目标将文本、音频及全身运动嵌入统一的复合潜在空间。
result: 实验和用户研究表明，该方法在BEAT2和SHOW两个主流数据集上均显著优于现有的最先进模型。
conclusion: 本研究证明了对比学习与流匹配结合能有效提升手势生成的语义准确性与跨模态连贯性。
---

## 摘要
尽管协同语音手势生成领域已取得显著进展，但生成具有语义关联的全身手势仍是一项挑战。现有方法依赖于外部语义检索方法，由于其对预定义语言规则的依赖，限制了泛化能力。基于流匹配的方法取得了不错的效果；然而，由于网络仅使用语义一致的样本进行优化，未接触负样本，导致模型倾向于学习节奏性手势，而非形象手势（iconic）和隐喻手势（metaphoric）等稀疏动作。此外，由于对身体部位进行孤立建模，大多数方法无法保持跨模态一致性。我们提出了一种基于对比流匹配的协同语音手势生成模型，该模型将不匹配的音频-文本条件作为负样本，训练速度场遵循正确的运动轨迹，同时排斥语义不一致的轨迹。我们的模型通过余弦和对比目标将文本、音频和全身运动嵌入到一个复合潜空间中，从而确保跨模态连贯性。大量的实验和用户研究表明，我们提出的方法在 BEAT2 和 SHOW 两个数据集上优于现有最先进的方法。

## Abstract
While the field of co-speech gesture generation has seen significant advances, producing holistic, semantically grounded gestures remains a challenge. Existing approaches rely on external semantic retrieval methods, which limit their generalisation capability due to dependency on predefined linguistic rules. Flow-matching-based methods produce promising results; however, the network is optimised using only semantically congruent samples without exposure to negative examples, leading to learning rhythmic gestures rather than sparse motion, such as iconic and metaphoric gestures. Furthermore, by modelling body parts in isolation, the majority of methods fail to maintain crossmodal consistency. We introduce a Contrastive Flow Matching-based co-speech gesture generation model that uses mismatched audio-text conditions as negatives, training the velocity field to follow the correct motion trajectory while repelling semantically incongruent trajectories. Our model ensures cross-modal coherence by embedding text, audio, and holistic motion into a composite latent space via cosine and contrastive objectives. Extensive experiments and a user study demonstrate that our proposed approach outperforms state-of-the-art methods on two datasets, BEAT2 and SHOW.

---

## 论文详细总结（自动生成）

以下是对论文《HolisticSemGes: Semantic Grounding of Holistic Co-Speech Gesture Generation with Contrastive Flow-Matching》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在生成全身协同语音手势（包括身体、手部、面部）时，既保证动作的**真实连贯性**，又实现深层的**语义对齐**（即手势能表达出言语的具体含义，而非仅仅随节奏摆动）。
*   **研究背景与动机**：
    *   **局部对齐的局限**：现有方法多采用分部位建模，导致不同身体部位（如手和头）在表达同一语义时出现不一致。
    *   **缺乏负样本约束**：主流生成模型（如扩散模型、流匹配）仅使用正样本训练，导致模型倾向于生成通用的“节奏手势”（Beat Gestures），而难以捕捉稀疏但关键的“形象手势”（Iconic）或“隐喻手势”（Metaphoric）。
    *   **过度依赖外部规则**：部分语义增强方法依赖预定义的语言规则或检索机制，泛化能力受限。

### 2. 方法论
论文提出了 **HolisticSemGes** 框架，包含两个核心模块：
*   **语义感知复合模块 (SACM)**：
    *   **核心思想**：将音频、文本和**全身复合运动**嵌入到一个统一的共享潜空间中。
    *   **技术细节**：不同于以往的分部位对齐，该模块将手、上肢、下肢、面部的潜变量拼接成一个复合表示。利用 BERT（文本）和 HuBERT（音频）提取特征，通过**序列级余弦对齐**和 **CLIP 风格的 InfoNCE 对齐**损失函数，强制运动特征与语义特征在空间上靠近。
*   **对比流匹配 (Contrastive Flow Matching, CFM)**：
    *   **核心思想**：在流匹配（Flow Matching）的训练过程中引入“负样本”约束。
    *   **算法流程**：模型学习一个条件速度场（Velocity Field）。在训练时，除了让预测的速度场趋近于真实的运动轨迹（正样本），还引入不匹配的音频-文本对作为负样本。
    *   **公式逻辑**：损失函数 $L_{CFM}$ 包含两部分：第一项吸引预测速度向正确轨迹靠拢，第二项（带有权重 $\lambda$）排斥那些与错误语义相关的轨迹。这增强了生成路径的定向稳定性，避免了语义模糊。

### 3. 实验设计
*   **数据集**：
    *   **BEAT2**：大规模多模态动捕数据集（60小时，25名演讲者），包含高精度的 SMPL-X 全身参数。
    *   **SHOW**：视听同步数据集（26.9小时，4名演讲者）。
*   **Benchmark 与对比方法**：
    *   对比了 **TalkSHOW**、**EMAGE**、**RAGGesture**、**GestureLSM** 和 **SemTalk** 等当前最先进（SOTA）的全身手势生成模型。
*   **评价指标**：
    *   **FGD (Fréchet Gesture Distance)**：衡量生成手势与真实分布的相似度（真实性）。
    *   **BC (Beat Consistency)**：衡量手势与语音节奏的同步性。
    *   **Diversity**：衡量生成动作的多样性。
    *   **用户研究**：30名受试者对自然度、多样性、语义对齐度进行评分。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A100 GPU**。
*   **训练时长**：
    *   第一阶段（运动表示学习/RVQ-VAE）：800 epochs，约 **140 小时**。
    *   第二阶段（语义生成器训练）：1000 epochs，约 **63 小时**。
*   **参数设置**：Batch size 为 64（第一阶段）和 128（第二阶段）。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流数据集上进行了全量对比，涵盖了多说话人泛化实验。
*   **消融实验**：设计了多组消融，包括：(1) 移除 SACM 模块；(2) 仅保留单模态（仅文本或仅音频）对齐；(3) 将对比流匹配（CFM）替换为标准流匹配（FM）。
*   **充分性评价**：实验设计非常充分且客观。通过定量指标、定性可视化（针对特定语义词如 "Well", "me", "never" 的手势分析）以及人类主观评价，全方位验证了 CFM 和 SACM 的有效性。

### 6. 主要结论与发现
*   **性能领先**：HolisticSemGes 在所有核心指标（FGD, BC, Diversity）上均显著优于现有 SOTA 模型。
*   **语义增强**：引入对比学习后，模型能生成更具指示性（Deictic）和形象性的手势，而不仅仅是简单的摆动。
*   **跨模态一致性**：复合潜空间有效地解决了身体不同部位在表达语义时的脱节问题。
*   **流匹配优势**：相比扩散模型，流匹配在保持高质量生成的条件下，推理效率更高且训练更稳定。

### 7. 优点与亮点
*   **创新性地引入对比流匹配**：首次将对比学习的负样本思想引入流匹配的速度场学习中，有效解决了生成模型中的“语义平均化”问题。
*   **全身整体建模**：通过复合模块实现了真正的全身协同，而非部位堆叠。
*   **端到端语义对齐**：无需复杂的外部检索或语言学规则，通过特征空间对齐即可实现良好的语义接地（Grounding）。

### 8. 不足与局限
*   **计算开销**：虽然比扩散模型快，但流匹配在推理时仍需进行 ODE 数值积分，计算量高于简单的自回归模型。
*   **负样本构建**：目前负样本主要通过批次内随机打乱（In-batch shuffling）获得，未来可能需要更精细的“硬负样本”（Hard Negatives）挖掘技术来进一步提升语义辨析力。
*   **依赖预训练模型**：模型性能在很大程度上受限于 BERT 和 HuBERT 等预训练编码器的特征质量。

（完）
