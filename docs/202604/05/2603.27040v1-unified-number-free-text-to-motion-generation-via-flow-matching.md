---
title: Unified Number-Free Text-to-Motion Generation Via Flow Matching
title_zh: 基于流匹配的统一不限人数文本到动作生成
authors: "Guanhe Huang, Oya Celiktutan"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.27040v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 通过流匹配进行文本到动作生成
tldr: 针对现有文本生成动作模型在处理变动人数时效率低且易累积误差的问题，本文提出统一动作流（UMF）框架。该框架通过统一潜空间整合异构数据，利用金字塔动作流（P-Flow）进行高效的先验生成，并结合半噪声动作流（S-Flow）实现自适应反应生成。UMF作为一种通用模型，在多人数动作生成任务中表现出色，显著提升了生成质量与效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27040v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative evaluation on the InterHuman test sets. ± indicates a 95% confidence interval and→ means the closer to ground truth the better. Boldface indicates the best result, while underline refers to the second best.\", \"page\": 7, \"index\": 1, \"width\": 852, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2603.27040v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison (zoom into see it better) between FreeMotion [12] and UMF. Red circles demonstrate successful cases, while Blue circles show failure cases.\", \"page\": 7, \"index\": 2, \"width\": 1059, \"height\": 577}, {\"url\": \"assets/figures/arxiv/2603.27040v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of the Unified Motion Flow (UMF) architecture. The UMF framework consists of three stages. (A) Unified motion VAE: A motion VAE with latent adapters encodes raw motions from heterogeneous datasets (e.g., HumanML3D [13], InterHuman [32]) into a regularized multi-token latent representation (Z). (B) P-Flow motion prior generation: The Pyramid Flow Transformer synthesizes the latent motion prior (Ž) based on noisy latent motion and text conditions. The P-Flow operates hierarchically based on the timestep t ∼ (0, 1): it processes downsampled, low-resolution latents for t < p and switches to original-resolution latents for t > p, mitigating multi-token computational overheads. (C) S-Flow reaction generation: Based on the previously generated latent {Ži, . . . , Ž1}, the context adapter generates the context motion C. Then the Semi-Noise Flow transformer predicts the reaction latent (W̌ ) by jointly modeling context reconstruction and reaction transformation, alleviating the error accumulation from previously generated motion.\", \"page\": 4, \"index\": 3, \"width\": 881, \"height\": 475}]"
motivation: 现有的多智能体动作生成方法多采用自回归模型，存在计算效率低、误差累积以及难以泛化到变动人数的问题。
method: 提出由P-Flow（分层分辨率生成先验）和S-Flow（联合概率路径生成反应）组成的UMF框架，并利用统一潜空间桥接异构数据集。
result: 实验结果和用户调研表明，UMF在多人数文本生成动作任务上具有极高的有效性，能够生成高质量且协调的动作。
conclusion: UMF通过流匹配技术实现了高效、灵活且无人数限制的文本到动作生成，为多智能体协同动作合成提供了新的通用方案。
---

## 摘要
生成模型在固定数量智能体的动作合成方面表现出色，但在处理可变数量智能体时难以泛化。基于有限的特定领域数据，现有方法采用自回归模型递归地生成动作，这存在效率低下和误差累积的问题。我们提出了统一动作流（Unified Motion Flow, UMF），它由金字塔动作流（Pyramid Motion Flow, P-Flow）和半噪声动作流（Semi-Noise Motion Flow, S-Flow）组成。UMF 将不限人数的动作生成分解为一个单阶段的动作先验生成阶段和多阶段的反应生成阶段。具体而言，UMF 利用统一的潜空间来弥合异构动作数据集之间的分布差异，从而实现有效的统一训练。对于动作先验生成，P-Flow 在以不同噪声水平为条件的层级分辨率上运行，从而减轻了计算开销。对于反应生成，S-Flow 学习一条联合概率路径，自适应地执行反应变换和上下文重建，从而缓解了误差累积。广泛的实验结果和用户研究证明了 UMF 作为一种通用的文本驱动多人物动作生成模型的有效性。项目主页：https://githubhgh.github.io/umf/。

## Abstract
Generative models excel at motion synthesis for a fixed number of agents but struggle to generalize with variable agents. Based on limited, domain-specific data, existing methods employ autoregressive models to generate motion recursively, which suffer from inefficiency and error accumulation. We propose Unified Motion Flow (UMF), which consists of Pyramid Motion Flow (P-Flow) and Semi-Noise Motion Flow (S-Flow). UMF decomposes the number-free motion generation into a single-pass motion prior generation stage and multi-pass reaction generation stages. Specifically, UMF utilizes a unified latent space to bridge the distribution gap between heterogeneous motion datasets, enabling effective unified training. For motion prior generation, P-Flow operates on hierarchical resolutions conditioned on different noise levels, thereby mitigating computational overheads. For reaction generation, S-Flow learns a joint probabilistic path that adaptively performs reaction transformation and context reconstruction, alleviating error accumulation. Extensive results and user studies demonstrate UMF' s effectiveness as a generalist model for multi-person motion generation from text. Project page: https://githubhgh.github.io/umf/.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Unified Motion Flow (UMF)** 的框架，旨在解决文本驱动下生成任意人数（Number-Free）人体动作的难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的文本到动作生成模型大多局限于固定人数（如单人或双人），在处理变动人数（如拥挤场景）时泛化能力差。
*   **研究动机**：
    *   **数据稀缺性**：多人物交互数据集（如 InterHuman）规模远小于单人数据集（如 HumanML3D），且交互任务更复杂。
    *   **技术缺陷**：现有的不限人数生成方法多采用自回归递归生成，存在**计算效率低**和**误差累积**（Error Accumulation）的问题，导致生成的动作随人数增加而逐渐失真。
    *   **异构性挑战**：不同数据集的动作表示不统一，难以直接进行联合训练。

### 2. 方法论：核心思想与关键技术
UMF 框架将生成过程解耦为“单次动作先验生成”和“多次反应生成”两个阶段，核心组件包括：

*   **统一潜空间（Unified Latent Space）**：
    *   通过一个带有**潜层适配器（Latent Adapter）**的多标记（Multi-token）VAE，将异构的单人和多人数据集映射到统一的正则化潜空间。
    *   这种设计在保证重建精度的同时，通过压缩语义空间降低了生成难度。
*   **金字塔动作流（P-Flow, Pyramid Motion Flow）**：
    *   用于生成第一个人的“动作先验”。
    *   **核心思想**：利用流匹配（Flow Matching）在不同噪声水平下采用分层分辨率。在噪声较大的早期阶段使用低分辨率采样，在后期阶段切换到高分辨率。
    *   **优势**：显著降低了多标记表示带来的计算开销，提高了推理速度。
*   **半噪声动作流（S-Flow, Semi-Noise Motion Flow）**：
    *   用于生成后续智能体的“反应动作”。
    *   **核心思想**：学习一条**联合概率路径**，同时进行“反应变换”（从已有上下文到目标动作）和“上下文重建”（从噪声到上下文）。
    *   **优势**：不再将前序动作视为静态条件，而是作为流路径的起点，增强了模型对前序误差的容忍度，有效缓解了自回归过程中的误差累积。

### 3. 实验设计
*   **数据集**：
    *   **InterHuman**：主要的多人交互基准。
    *   **HumanML3D**：用于提供单人动作先验。
    *   **InterHuman-AS**：带有角色（发起者/反应者）标注的交互数据集。
*   **对比方法**：
    *   **通用模型**：FreeMotion（目前最先进的不限人数框架）。
    *   **专用模型**：TEMOS, T2M, MDM, InterGen, InterMask, TIMotion 等。
*   **评价指标**：FID（真实性）、R-Precision（文本匹配度）、MM Dist（多模态距离）、Diversity（多样性）。

### 4. 资源与算力
*   **训练细节**：使用 AdamW 优化器，学习率 $10^{-4}$。
*   **训练时长**：VAE 阶段训练 6000 epoch，P-Flow 和 S-Flow 阶段各训练 2000 epoch。
*   **算力资源**：文中提到使用了 King's CREATE 高性能计算集群（HPC），但未明确给出具体的 GPU 型号和数量。
*   **批大小**：VAE 阶段为 128，流匹配阶段为 64。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流交互基准上进行了详尽的定量对比。
    *   **消融实验**：针对异构先验、潜层适配器、多标记机制、P-Flow 的分层结构、S-Flow 的联合路径等关键组件均做了对比测试。
    *   **用户研究**：邀请 30 名用户对 20 组随机采样的多人（N > 2）生成结果进行主观评价。
*   **充分性评价**：实验设计较为全面，不仅涵盖了标准的双人交互，还通过 Zero-shot（零样本）方式测试了 3 人及以上（如 4 人对练、5 人跆拳道）的复杂场景，验证了模型的泛化能力。

### 6. 主要结论与发现
*   **性能领先**：UMF 在 InterHuman 基准上达到了 SOTA 性能（FID 4.772），显著优于之前的通用框架 FreeMotion。
*   **效率提升**：P-Flow 的金字塔结构使推理速度提升了近 5 倍，同时降低了计算量（FLOPs）。
*   **误差缓解**：S-Flow 的联合路径设计证明了在自回归生成中，通过“上下文重建”辅助训练能有效抑制长序列生成的漂移现象。
*   **零样本泛化**：模型虽然主要在双人数据上训练，但能成功推广到 10 人左右的中等规模群体交互。

### 7. 优点与亮点
*   **高效性**：通过 P-Flow 解决了多标记潜空间带来的计算瓶颈。
*   **鲁棒性**：S-Flow 的半噪声设计是处理自回归误差累积的一个创新视角。
*   **通用性**：成功整合了单人与多人异构数据，使模型能够学习到更丰富的动作原语。
*   **灵活性**：支持不限人数的生成，且在多人场景下依然保持物理合理性（如减少了人物穿模现象）。

### 8. 不足与局限
*   **规模限制**：虽然号称“不限人数”，但实验表明其主要适用于中等规模群体（约 10 人），对于超大规模人群（如 100 人以上的密集人群动态）仍有力不从心之处。
*   **中心化依赖**：生成逻辑仍依赖于一个“主要代理（Primary Agent）”作为交互中心，对于去中心化的复杂群体行为建模可能存在偏差。
*   **视觉先验缺失**：目前仅依赖文本，未来若能引入大规模视频扩散模型的视觉先验，可能会进一步提升动作的细腻程度。

（完）
