---
title: Teacher-Student Diffusion Model for Text-Driven 3D Hand Motion Generation
title_zh: 文本驱动的三维手部动作生成的教师-学生扩散模型
authors: "Ching-Lam Cheng, Bin Zhu, Shengfeng He"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24407v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 文本驱动3D手部动作生成的扩散框架
tldr: 针对现有文本驱动3D手部动作生成方法在细节刻画和通用性上的不足，本文提出了TSHaMo框架。该框架采用教师-学生扩散模型结构，教师模型利用辅助信号提供结构化指导，学生模型仅凭文本生成动作。通过协同训练，学生模型在推理阶段无需3D物体即可生成高质量、多样化的手部动作，在GRAB和H2O数据集上表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24407v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative example comparison for hand motion generation from textual prompt.\", \"page\": 4, \"index\": 1, \"width\": 460, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2603.24407v1/fig-002.webp\", \"caption\": \"Fig. 1. Training procedure of our method. The student takes a noisy sample and text embedding to predict the denoised output, while the teacher also uses auxiliary conditions (e.g., 3D joints, MANO parameters, contact maps). Three losses are applied between model predictions and ground truth. The example here uses the MDM backbone.\", \"page\": 3, \"index\": 2, \"width\": 1054, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2603.24407v1/fig-003.webp\", \"caption\": \"Table 1. 3D hand motion generation performance comparison on the GRAB dataset. The best results are highlighted in bold.\", \"page\": 3, \"index\": 3, \"width\": 938, \"height\": 185}]"
motivation: 现有方法在生成手部动作时往往忽略细节或过度依赖3D物体模型，限制了其在复杂交互场景中的通用性。
method: 提出一种教师-学生扩散框架，利用教师模型的辅助信号引导学生模型学习，实现推理阶段仅凭文本输入生成动作。
result: 在GRAB和H2O数据集上的评估显示，TSHaMo在动作质量和多样性上均优于现有基准，且对不同辅助输入具有鲁棒性。
conclusion: TSHaMo框架通过创新的协同训练机制，有效解决了文本驱动手部动作生成中对外部3D模型依赖的问题。
---

## 摘要
从自然语言生成逼真的三维手部动作对于虚拟现实（VR）、机器人技术和人机交互至关重要。现有方法要么侧重于全身运动而忽略了详细的手势，要么需要显式的三维物体网格，从而限制了通用性。我们提出了 TSHaMo，这是一个用于文本驱动手部动作生成的模型无关（model-agnostic）教师-学生扩散框架。学生模型学习仅从文本合成动作，而教师模型则利用辅助信号（如 MANO 参数）在训练期间提供结构化指导。协同训练策略使学生模型能够从教师模型的中间预测中获益，同时在推理阶段保持仅使用文本。通过在 GRAB 和 H2O 数据集上使用两种扩散骨干网络进行评估，TSHaMo 一致地提高了动作的质量和多样性。消融实验证实了其在利用多种辅助输入方面的鲁棒性和灵活性，且在测试阶段无需三维物体。

## Abstract
Generating realistic 3D hand motion from natural language is vital for VR, robotics, and human-computer interaction. Existing methods either focus on full-body motion, overlooking detailed hand gestures, or require explicit 3D object meshes, limiting generality. We propose TSHaMo, a model-agnostic teacher-student diffusion framework for text-driven hand motion generation. The student model learns to synthesize motions from text alone, while the teacher leverages auxiliary signals (e.g., MANO parameters) to provide structured guidance during training. A co-training strategy enables the student to benefit from the teacher's intermediate predictions while remaining text-only at inference. Evaluated using two diffusion backbones on GRAB and H2O, TSHaMo consistently improves motion quality and diversity. Ablations confirm its robustness and flexibility in using diverse auxiliary inputs without requiring 3D objects at test time.

---

## 论文详细总结（自动生成）

### 论文总结：TSHaMo - 文本驱动三维手部动作生成的教师-学生扩散模型

#### 1. 论文的核心问题与整体含义
*   **研究背景**：生成逼真的三维手部动作在虚拟现实（VR）、机器人和人机交互中至关重要。
*   **核心问题**：现有的文本驱动动作生成方法存在两极分化：一是侧重全身动作（如 MDM），往往忽略了手部精细动作；二是侧重手物交互（HOI），这类方法通常依赖于精确的三维物体网格（Object Mesh），限制了其在无物体场景或通用环境下的应用。
*   **研究动机**：开发一种无需物体模型、仅凭文本输入即可生成高质量、精细化手部动作的通用框架。

#### 2. 论文提出的方法论
*   **核心思想**：提出 **TSHaMo** 框架，采用**教师-学生（Teacher-Student）**架构。在训练阶段，利用包含丰富信息的辅助信号（如 MANO 参数、接触图）指导模型学习；在推理阶段，学生模型仅需文本即可生成动作。
*   **关键技术细节**：
    *   **教师模型 (Teacher)**：输入包括文本提示、时间步嵌入以及**辅助信号**（如 MANO 参数、3D 关节、接触图等）。它负责提供结构化的监督信号。
    *   **学生模型 (Student)**：输入仅为文本提示、时间步嵌入和加噪动作。其目标是模仿教师的输出并逼近真实值（Ground Truth）。
    *   **协同训练策略**：
        *   学生模型损失函数：$L_S = L_{S \to GT} + \lambda L_{S \to T}$（结合了真实值损失和教师引导损失）。
        *   教师模型损失函数：$L_T = L_{T \to GT}$。
    *   **模型无关性 (Model-Agnostic)**：该框架可集成到不同的扩散模型骨干中（如基于 Transformer 的 MDM 或基于 U-Net 的 StableMoFusion）。
    *   **辅助信号处理**：在训练中均匀采样 5 帧辅助特征，投影后与文本嵌入拼接，增强教师模型的感知能力。

#### 3. 实验设计
*   **数据集**：
    *   **GRAB**：全身抓取物体数据集，仅提取手部动作。
    *   **H2O**：双手操纵物体数据集。
*   **Benchmark 与对比方法**：
    *   对比了 **T2M**（基于 VAE）、**MDM**（基于 Transformer 扩散模型）和 **StableMoFusion**（基于 U-Net 扩散模型）。
*   **评估指标**：Top-k 动作分类准确率（Top-1/2/3）、KID（衡量分布真实度）、Diversity（多样性）。

#### 4. 资源与算力
*   **训练参数**：扩散步数 $T_{max}=1000$，训练 1000 个 epoch，学习率 $10^{-5}$，采用余弦退火调度。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集（GRAB, H2O）上进行了完整测试。
    *   针对两种不同的扩散模型骨干（MDM, StableMoFusion）验证了框架的通用性。
    *   **消融实验**：测试了不同辅助信号类型（MANO、3D/2D 关节、手部掩码、接触图）的影响，以及引导权重 $\lambda$ 的敏感度。
*   **充分性评价**：实验设计较为全面，通过跨模型、跨数据集的对比验证了方法的有效性。所有指标均取 20 次运行的平均值并附带 95% 置信区间，体现了实验的客观性和严谨性。

#### 6. 论文的主要结论与发现
*   **性能提升**：TSHaMo 在所有基准模型上均实现了性能增益。在 GRAB 数据集上，Top-1 准确率显著提升；在 H2O 数据集上，KID 指标大幅下降，说明生成的动作更接近真实分布。
*   **辅助信号的作用**：结合 MANO 参数和接触图作为辅助信号时效果最佳。即使是较弱的辅助信号（如 2D 关节）也能提供正向引导。
*   **推理优势**：尽管训练时使用了复杂的辅助信息，但推理时仅需文本，保证了部署的灵活性。

#### 7. 优点
*   **通用性强**：作为一种“插件式”框架，可以轻松提升现有扩散模型的表现，而不改变其推理结构。
*   **摆脱物体依赖**：解决了以往手部动作生成必须依赖 3D 物体模型的问题，扩大了应用范围。
*   **质量与多样性兼顾**：在提高动作准确性的同时，通过扩散模型的特性保持了生成动作的多样性。

#### 8. 不足与局限
*   **文本输入简单**：目前的实验主要基于动作标签（Action Labels）作为文本提示，对于复杂的长句或具有时序逻辑的指令（如“先握拳再张开”）的理解能力尚待验证。
*   **辅助信息获取**：虽然推理不需要辅助信息，但训练阶段仍依赖于高质量的 MANO 标注和接触图数据，这在某些野外数据集上可能难以获得。
*   **物理约束**：虽然引入了接触图引导，但在完全无物体的生成中，如何保证手部动作在物理意义上的合理性（如避免手指自穿透）仍有提升空间。

（完）
