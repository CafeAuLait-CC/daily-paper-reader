---
title: Unleashing Guidance Without Classifiers for Human-Object Interaction Animation
title_zh: 释放人物-物体交互动画中的无分类器引导
authors: "Ziyin Wang, Sirui Xu, Chuan Guo, Bing Zhou, Jiangshan Gong, Jian Wang, Yu-Xiong Wang, Liang-Yan Gui"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25734v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 基于扩散模型的人机交互动画生成
tldr: 本研究针对生成逼真的人机交互（HOI）动画中依赖手工约束的难题，提出了LIGHT框架。该方法基于扩散强制机制，通过为不同模态分配异步去噪进度，使较清晰的组件通过交叉注意力引导较模糊的组件，实现无需辅助分类器的数据驱动引导。实验证明，这种进度诱导引导能有效捕捉接触语义，在接触保真度、生成真实感及泛化能力上均优于传统方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25734v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of LIGHT. Left: Training. We form different modalities, e.g., body, hand, and object, each diffused with its own noise level. After adding modal-wise and frame-wise rotary positional encodings, the tokens are processed by a shared Transformer decoder and an MLP head to predict clean motion. Right: Inference. We compare a uniform schedule that denoises all modalities synchronously with a staged schedule that keeps one modality cleaner from the uniform run.\", \"page\": 4, \"index\": 1, \"width\": 826, \"height\": 490}]"
motivation: 现有的HOI动画生成方法过度依赖手工设计的接触先验或运动学约束，难以处理复杂的交互和多样的物体几何。
method: 提出LIGHT框架，利用异步去噪进度让不同模态互为引导，并通过合成几何增强训练以提升接触语义的鲁棒性。
result: 实验表明，该方法在接触保真度和动画真实感方面显著优于传统无分类器引导，并展现出强大的跨物体泛化能力。
conclusion: 通过调整去噪节奏产生的内在引导是一种比手工先验更有效的HOI建模方式，为实现通用的交互动画生成提供了新思路。
---

## 摘要
生成逼真的人物-物体交互（HOI）动画仍然具有挑战性，因为它需要对动态人体动作和多样的物体几何形状进行联合建模。先前的基于扩散的方法通常依赖于手工设计的接触先验或人为施加的运动学约束来提高接触质量。我们提出了 LIGHT，这是一种数据驱动的替代方案，其中引导源自去噪步调（denoising pace）本身，从而减少了对手动设计先验的依赖。基于扩散强制（diffusion forcing），我们将表示分解为特定模态的组件，并使用异步去噪调度分配个性化的噪声水平。在这种范式中，较清晰的组件通过交叉注意力引导噪声较大的组件，从而在无需辅助分类器的情况下产生引导。我们发现这种数据驱动的引导本质上具有接触感知能力，并且当训练通过广泛的合成物体几何形状进行增强时，可以得到进一步提升，从而促进接触语义对几何多样性的不变性。大量实验表明，与传统的无分类器引导相比，步调诱导的引导能更有效地反映接触先验的优势，同时实现更高的接触保真度、更逼真的 HOI 生成以及对未见物体和任务更强的泛化能力。

## Abstract
Generating realistic human-object interaction (HOI) animations remains challenging because it requires jointly modeling dynamic human actions and diverse object geometries. Prior diffusion-based approaches often rely on hand-crafted contact priors or human-imposed kinematic constraints to improve contact quality. We propose LIGHT, a data-driven alternative in which guidance emerges from the denoising pace itself, reducing dependence on manually designed priors. Building on diffusion forcing, we factor the representation into modality-specific components and assign individualized noise levels with asynchronous denoising schedules. In this paradigm, cleaner components guide noisier ones through cross-attention, yielding guidance without auxiliary classifiers. We find that this data-driven guidance is inherently contact-aware, and can be enhanced when training is augmented with a broad spectrum of synthetic object geometries, encouraging invariance of contact semantics to geometric diversity. Extensive experiments show that pace-induced guidance more effectively mirrors the benefits of contact priors than conventional classifier-free guidance, while achieving higher contact fidelity, more realistic HOI generation, and stronger generalization to unseen objects and tasks.

---

## 论文详细总结（自动生成）

### 论文总结：释放人物-物体交互动画中的无分类器引导 (LIGHT)

#### 1. 核心问题与整体含义
生成逼真的人物-物体交互（HOI）动画（如搬椅子、举箱子）极具挑战，因为需要同时建模人体动作与多样化的物体几何。
*   **研究动机**：现有的基于扩散模型的方法通常依赖**手工设计的先验**（如接触约束、逆运动学 IK）或**外部分类器引导**。这些方法设计繁琐、计算效率低，且难以泛化到未见过的物体。
*   **核心目标**：提出一种名为 **LIGHT** 的纯数据驱动框架，通过调整去噪步调（Denoising Pace）产生内在引导，无需任何手工约束或辅助分类器即可实现高保真的交互生成。

#### 2. 方法论
LIGHT 的核心思想是利用**异步去噪进度**来实现模态间的相互引导。
*   **模态分离（Token Separation）**：将 HOI 序列分解为人体躯干、手部和物体三个独立的模态组件。
*   **异步去噪调度（Asynchronous Schedules）**：基于“扩散强制”（Diffusion Forcing）机制，在推理时为不同模态分配不同的噪声水平。
    *   **阶段性调度（Staged Schedule）**：让某一模态（如人体）保持比另一模态（如物体）更“干净”的状态。
    *   **步调诱导引导（Pace-induced Guidance）**：通过对比“同步去噪”和“异步去噪”的预测差异，产生一种类似于无分类器引导（CFG）的效果。较清晰的组件通过交叉注意力机制引导噪声较大的组件，从而自动修正接触错误（如穿透或漂浮）。
*   **接触感知形状光谱增强**：为了提升泛化性，研究者通过优化算法将原始交互动作迁移到大量具有相似语义但几何形状不同的新物体上，训练模型学习接触语义对几何变化的“不变性”。

#### 3. 实验设计
*   **数据集**：主要在 **InterAct** 数据集（目前最全面的 HOI 数据集，整合了 BEHAVE, OMOMO 等）上进行。
*   **Benchmark 与对比方法**：
    *   对比了 **HOI-Diff**（依赖辅助模块）、**CHOIS**、**InterDiff**（依赖运动学预测器）和 **Text2HOI**。
*   **评估指标**：
    *   **生成质量**：FID、多样性（Diversity）。
    *   **文本一致性**：R-Precision、多模态距离（MM Dist）。
    *   **物理保真度**：足部滑动率（FSR）、穿透率（Pene）、接触比例（Contact Ratio）以及接触精确度/召回率（Cprec/Crec/CF1）。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU**。
*   **训练时长**：模型在约 **24 小时**内收敛。
*   **技术细节**：采用了混合精度训练（FP16）和 Flash Attention 以优化效率。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比，并在 InterAct、BEHAVE 和 OMOMO 多个子集上验证了性能。
*   **消融实验**：非常充分，涵盖了模态分离策略、引导强度（$\omega_2$）、去噪偏移量（$\delta$）、数据增强的效果以及引导方向的梯度分析。
*   **客观性**：除了自动指标，还进行了 **15 人的用户研究**，对比了基准方法和引导机制的有效性。此外，还测试了模型在未见物体（跨类别）上的泛化能力，实验设计严谨且公平。

#### 6. 主要结论与发现
*   **引导源自步调**：异步去噪产生的“软引导”比传统的文本硬丢弃（CFG）更能捕捉底层的接触细节。
*   **接触感知**：LIGHT 产生的引导方向与减少穿透的梯度方向高度一致，证明其内在学习到了物理接触规律。
*   **泛化性**：通过形状光谱增强，模型能够成功处理训练集中从未出现过的物体类别。
*   **性能领先**：在所有关键指标（尤其是接触保真度和文本对齐度）上均显著优于现有的 SOTA 方法。

#### 7. 优点（亮点）
*   **优雅的引导机制**：将引导机制与扩散过程本身的步调结合，摆脱了对繁琐手工约束（如 IK）的依赖。
*   **模态解耦**：通过分离手部和身体 token，显著提升了精细抓取动作的准确性。
*   **数据增强策略**：提出了一种保持接触语义的物体替换优化方法，有效解决了 HOI 数据稀缺的问题。

#### 8. 不足与局限
*   **场景复杂度限制**：目前仅针对**单人单物**交互，尚未扩展到多物体或复杂环境（如障碍物）交互。
*   **推理开销**：由于需要执行两个耦合的去噪路径（Uniform 和 Staged），推理时间比普通扩散模型更长（约为基准方法的 4-5 倍）。
*   **环境感知缺失**：模型未显式建模静态场景几何，可能在复杂的室内布局中出现局限。

（完）
