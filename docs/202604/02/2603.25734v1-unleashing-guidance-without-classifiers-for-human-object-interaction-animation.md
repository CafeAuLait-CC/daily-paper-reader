---
title: Unleashing Guidance Without Classifiers for Human-Object Interaction Animation
title_zh: 释放用于人-物交互动画的无分类器引导
authors: "Ziyin Wang, Sirui Xu, Chuan Guo, Bing Zhou, Jiangshan Gong, Jian Wang, Yu-Xiong Wang, Liang-Yan Gui"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25734v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 基于扩散的人机交互动画方法
tldr: 本研究针对人机交互（HOI）动画生成中依赖手工先验的局限，提出了名为 LIGHT 的数据驱动框架。该方法基于扩散强制思想，将表示分解为特定模态组件并采用异步去噪调度。通过让去噪程度较高的模态在交叉注意力机制中引导噪声较大的模态，实现了无需辅助分类器的引导。LIGHT 能够捕捉内在的接触感知信息，并在合成几何增强训练下表现出极强的泛化性，显著提升了交互动画的真实感与接触质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25734v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of LIGHT. Left: Training. We form different modalities, e.g., body, hand, and object, each diffused with its own noise level. After adding modal-wise and frame-wise rotary positional encodings, the tokens are processed by a shared Transformer decoder and an MLP head to predict clean motion. Right: Inference. We compare a uniform schedule that denoises all modalities synchronously with a staged schedule that keeps one modality cleaner from the uniform run.\", \"page\": 4, \"index\": 1, \"width\": 826, \"height\": 490}]"
motivation: 现有人机交互动画生成方法过度依赖手工设计的接触先验，难以应对复杂的动态交互和多样的物体几何形状。
method: 提出 LIGHT 框架，利用异步去噪调度产生的“节奏引导”，使不同模态在去噪过程中通过交叉注意力实现互引导。
result: 实验证明该方法在接触保真度和生成真实感上优于传统引导方式，并对未见过的物体和任务具有更强的泛化能力。
conclusion: 数据驱动的去噪节奏引导能有效替代手工约束，为高质量、高泛化性的人机交互动画生成提供了新范式。
---

## 摘要
生成逼真的人-物交互（HOI）动画仍然具有挑战性，因为它需要对动态人体动作和多样的物体几何形状进行联合建模。先前的基于扩散的方法通常依赖于手工设计的接触先验或人为施加的运动学约束，以提高接触质量。我们提出了 LIGHT，这是一种数据驱动的替代方案，其中引导源自去噪步调（denoising pace）本身，从而减少了对手动设计先验的依赖。基于扩散强迫（diffusion forcing），我们将表示分解为特定模态的组件，并分配具有异步去噪计划的个性化噪声水平。在这种范式中，较清晰的组件通过交叉注意力引导噪声较大的组件，从而在无需辅助分类器的情况下产生引导。我们发现这种数据驱动的引导本质上具有接触感知能力，并且当训练通过广泛的合成物体几何形状进行增强时，这种引导可以得到进一步加强，从而促进接触语义对几何多样性的不变性。大量实验表明，与传统的无分类器引导（classifier-free guidance）相比，步调诱导的引导能更有效地反映接触先验的优势，同时实现更高的接触保真度、更逼真的 HOI 生成，以及对未知物体和任务更强的泛化能力。

## Abstract
Generating realistic human-object interaction (HOI) animations remains challenging because it requires jointly modeling dynamic human actions and diverse object geometries. Prior diffusion-based approaches often rely on hand-crafted contact priors or human-imposed kinematic constraints to improve contact quality. We propose LIGHT, a data-driven alternative in which guidance emerges from the denoising pace itself, reducing dependence on manually designed priors. Building on diffusion forcing, we factor the representation into modality-specific components and assign individualized noise levels with asynchronous denoising schedules. In this paradigm, cleaner components guide noisier ones through cross-attention, yielding guidance without auxiliary classifiers. We find that this data-driven guidance is inherently contact-aware, and can be enhanced when training is augmented with a broad spectrum of synthetic object geometries, encouraging invariance of contact semantics to geometric diversity. Extensive experiments show that pace-induced guidance more effectively mirrors the benefits of contact priors than conventional classifier-free guidance, while achieving higher contact fidelity, more realistic HOI generation, and stronger generalization to unseen objects and tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Unleashing Guidance Without Classifiers for Human-Object Interaction Animation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
生成逼真的人-物交互（HOI）动画极具挑战，因为需要同时建模动态人体动作和多样的物体几何形状。
*   **研究动机**：现有的基于扩散模型的方法通常依赖于**手工设计的接触先验**（如运动学约束、逆运动学 IK）或**外部任务分类器**（如接触回归器）来确保交互的真实性。这些方法要么设计繁琐、泛化性差，要么计算效率低下。
*   **核心目标**：提出一种名为 **LIGHT**（Learning Implicit Guidance for Human-object inTeraction）的框架，旨在摆脱对手工先验的依赖，通过数据驱动的方式，从去噪过程本身的“步调差”中提取引导信号，实现高保真、高泛化性的交互生成。

### 2. 方法论：核心思想与关键技术
LIGHT 的核心在于利用**异步去噪调度（Asynchronous Denoising Schedules）**产生的“步调诱导引导”。

*   **模态分解与 Token 化**：将 HOI 表示分解为人体（Body）、手部（Hand）和物体（Object）三个独立的模态组件。
*   **异步去噪（基于 Diffusion Forcing）**：在训练时为不同模态分配独立的噪声水平 $\lambda$。在推理时，设置两个并行的路径：
    1.  **均匀调度（Uniform Schedule）**：所有模态同步去噪，作为基础参考。
    2.  **阶段调度（Staged Schedule）**：使某一模态（如人体）保持比其他模态（如物体）更“干净”的状态。
*   **步调诱导引导（Pace-induced Guidance）**：利用较清晰的模态组件通过 Transformer 的交叉注意力机制来引导噪声较大的组件。这种“软引导”类似于无分类器引导（CFG），但它作用于底层接触细节而非全局分布。
*   **接触感知形状光谱增强**：为了提高泛化性，研究者通过优化算法将原始交互序列迁移到具有不同几何形状的同类物体上，同时保持接触语义不变。这教会了模型：接触关系应独立于物体的具体几何细节。

### 3. 实验设计
*   **数据集**：主要在 **InterAct**（大规模全方位 HOI 数据集）上进行，并对比了其子集 **BEHAVE** 和 **OMOMO**。
*   **Benchmark 与对比方法**：
    *   **对比基准**：HOI-Diff（带分类器引导）、CHOIS、InterDiff（物理信息扩散）、Text2HOI。
    *   **评估指标**：除了常规的 R-Precision（文本对齐）、FID（真实感）、Diversity（多样性）外，还引入了 **FSR**（足部滑动比）、**Pene**（穿透率）、**Contact**（接触率）等物理一致性指标。
*   **场景**：涵盖了复杂的动态交互，如搬运椅子、移动显示器、背背包、烹饪等。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A100 GPU**。
*   **训练细节**：采用混合精度训练（FP16）和 Flash Attention。
*   **时长**：在单张 A100 GPU 上，模型通常在 **24 小时内**即可收敛。
*   **推理效率**：由于需要并行运行调度路径，推理速度较慢（64 个样本约需 72 秒，而基线方法约 15 秒）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的定量对比（表 1、D、E）和定性可视化。
*   **消融实验**：非常充分，涵盖了：
    *   模态分离策略（人体/手部/物体是否分离）的影响。
    *   引导强度 $\omega_2$ 和去噪偏移量 $\delta$ 的超参数搜索。
    *   数据增强对未见物体（类内及跨类）泛化能力的提升。
    *   不同模态组合作为引导源的效果对比。
*   **客观性**：引入了 15 人的用户研究（User Study），从人类感知角度验证了生成质量，实验设计较为客观公平。

### 6. 主要结论与发现
*   **引导有效性**：步调诱导引导（LIGHT）在减少物体漂移、穿透和接触不稳方面显著优于传统的 CFG。
*   **接触感知**：分析表明，LIGHT 的引导方向与“减少穿透”的梯度方向高度相关，证明其学到了内在的物理约束。
*   **泛化能力**：通过形状光谱增强，模型能够成功处理训练集中未见过的物体几何形状。
*   **手部建模**：将手部作为独立 Token 处理能显著提升精细抓取动作的准确性。

### 7. 优点（亮点）
*   **无需分类器**：摆脱了繁琐的手工先验和额外的分类器训练，实现了一种优雅的、纯数据驱动的引导机制。
*   **原理创新**：将 Diffusion Forcing 扩展为一种通用的引导框架，为扩散模型的研究提供了新思路。
*   **高质量生成**：在处理复杂、长程的“文本-动作”对齐时，生成的交互动作比现有基线更自然、物理上更合理。

### 8. 不足与局限
*   **单物体限制**：目前仅支持单人与单个物体的交互，无法处理多物体或复杂场景环境。
*   **环境缺失**：未考虑静态环境（如墙壁、地板障碍物）对动作的影响。
*   **推理开销**：由于需要多次前向传播来计算引导梯度，推理时间比普通扩散模型长 4-5 倍，难以满足实时应用需求。
*   **物理极限**：虽然减少了穿透，但在极端动态下仍可能出现细微的物理违和感，尚未完全达到物理引擎模拟的精度。

（完）
