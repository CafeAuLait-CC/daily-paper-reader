---
title: Next-Scale Autoregressive Models for Text-to-Motion Generation
title_zh: 用于文本到动作生成的下一尺度自回归模型
authors: "Zhiwei Zheng, Shibo Jin, Lingjie Liu, Mingmin Zhao"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03799v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 用于文本到人体序列动作生成的自回归模型
tldr: 本文针对文本生成动作任务中自回归模型与时间结构不匹配的问题，提出了MoScale框架。该框架摒弃了传统的逐Token预测，转而采用从粗到细的“下一尺度”预测机制，通过分层细化捕捉全局语义和局部细节。此外，引入了跨尺度和尺度内的细化策略以增强鲁棒性。实验表明，MoScale在性能上达到SOTA，且具有极高的训练效率和零样本泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03799v1/fig-001.webp\", \"caption\": \"Table 1. Performance on HumanML3D. We report the average result over 20 runs with 95% confidence interval. Bold for the best and underline for the second. → indicates that values closer to real motion correspond to better results. S is the total steps across all scales.\", \"page\": 6, \"index\": 1, \"width\": 1036, \"height\": 434}, {\"url\": \"assets/figures/arxiv/2604.03799v1/fig-002.webp\", \"caption\": \"Table 2. Performance on KIT-ML test set. Bold for the best result and underline for the second best.\", \"page\": 7, \"index\": 2, \"width\": 1032, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2604.03799v1/fig-003.webp\", \"caption\": \"Figure 4. Motion editing results. MoScale achieves better instruction adherence and retains unedited motion (shown in gray).\", \"page\": 8, \"index\": 3, \"width\": 1036, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.03799v1/fig-004.webp\", \"caption\": \"Figure 2. Overview of MoScale. (a) MoScale encodes motion sequences into discrete tokens from coarse to fine through multi-scale quantization. (b) It autoregressively predicts tokens at the next scale, conditioned on the prefix and text inputs, using hierarchical scalewise causal attention. (c) Within each scale, MoScale performs temporal refinement to further improve token quality and consistency.\", \"page\": 4, \"index\": 4, \"width\": 1027, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2604.03799v1/fig-005.webp\", \"caption\": \"Figure 6. Visualization of coarse-to-fine representation of motion with our Residual VQVAE.\", \"page\": 12, \"index\": 5, \"width\": 860, \"height\": 396}]"
motivation: 传统的自回归模型逐Token预测方式难以有效捕捉动作生成所需的复杂长程时间结构。
method: 提出一种从粗到细的分层自回归框架，结合跨尺度分层细化和尺度内双向重预测机制进行动作生成。
result: 该方法在文本生成动作任务上取得了SOTA性能，展现出优异的训练效率、模型扩展性及零样本泛化能力。
conclusion: MoScale通过建立因果层次结构，为长程且结构复杂的动作生成提供了一种高效且稳健的新范式。
---

## 摘要
自回归（AR）模型具有训练稳定且高效的优点，但标准的下一标记（next-token）预测与文本条件动作生成所需的时间结构并不匹配。我们引入了 MoScale，这是一种下一尺度（next-scale）自回归框架，能够以从粗到细的时间分辨率分层生成动作。通过在最粗尺度上提供全局语义并逐步进行细化，MoScale 建立了一种更适合长程动作结构的因果层次体系。为了提高在有限文本-动作数据下的鲁棒性，我们进一步结合了跨尺度分层细化以改进各尺度的初始预测，以及尺度内时间细化以进行选择性的双向重新预测。MoScale 以极高的训练效率实现了 SOTA 级别的文本到动作生成性能，能够随模型规模有效扩展，并能零样本泛化到多种动作生成和编辑任务中。

## Abstract
Autoregressive (AR) models offer stable and efficient training, but standard next-token prediction is not well aligned with the temporal structure required for text-conditioned motion generation. We introduce MoScale, a next-scale AR framework that generates motion hierarchically from coarse to fine temporal resolutions. By providing global semantics at the coarsest scale and refining them progressively, MoScale establishes a causal hierarchy better suited for long-range motion structure. To improve robustness under limited text-motion data, we further incorporate cross-scale hierarchical refinement for improving per-scale initial predictions and in-scale temporal refinement for selective bidirectional re-prediction. MoScale achieves SOTA text-to-motion performance with high training efficiency, scales effectively with model size, and generalizes zero-shot to diverse motion generation and editing tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Next-Scale Autoregressive Models for Text-to-Motion Generation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的自回归（AR）模型在文本生成动作任务中，通常采用“下一标记预测（Next-Token Prediction）”机制。这种机制倾向于利用动作的短期可预测性（局部动力学），而难以捕捉文本描述中的全局语义结构（如“做两次开合跳”中的次数，或“转身、捡东西、再转身”的序列逻辑）。
*   **研究动机**：
    *   **AR模型的局限**：局部相关性过强，导致长程语义对齐效果差。
    *   **扩散/掩码模型的局限**：虽然能处理全局，但在数据量有限的情况下，难以从无序的去噪过程中建立稳健的全局语义支架。
    *   **目标**：提出一种既能保持自回归训练稳定性，又能对齐动作时间结构的“下一尺度（Next-Scale）”生成范式。

### 2. 方法论
MoScale 引入了从粗到细的因果层次结构，核心组件包括：
*   **多尺度动作表示（Residual VQ-VAE）**：使用残差向量量化技术，将动作编码为 $K$ 个尺度的离散标记。最粗尺度捕捉全局轨迹和姿态，细尺度逐步补充残差细节。
*   **下一尺度自回归（Next-Scale AR）**：模型不再逐个预测 Token，而是以尺度为单位进行预测。给定文本和前 $k-1$ 个尺度的所有标记，预测第 $k$ 个尺度的完整标记序列。
*   **跨尺度分层细化（Cross-Scale Hierarchical Refinement）**：在训练中对粗尺度预测引入随机扰动（Corruption），强迫细尺度模型学习如何纠正前序尺度的误差，从而缓解自回归模型常见的“曝光偏差”问题。
*   **尺度内时间细化（In-Scale Temporal Refinement）**：在每个尺度内部，利用双向注意力机制进行类似扩散模型的“掩码-再预测（Mask-and-Repredict）”，通过多次迭代提升局部动作的平滑度和一致性。

### 3. 实验设计
*   **数据集**：
    *   **HumanML3D**：目前最大的文本-动作数据集（约1.4万个动作，4.5万条描述）。
    *   **KIT-ML**：较小规模的数据集（约3.9千个动作）。
*   **Benchmark 与对比方法**：
    *   对比了自回归模型（T2M-GPT, ParCo）、扩散模型（MDM, ReMoDiffuse, MotionDiffuse）和掩码 Transformer（MMM, MoMask, MoMask++）。
*   **评估指标**：R-Precision（文本-动作对齐度）、FID（动作质量）、Diversity（多样性）、MM-Dist（多模态距离）以及人工评价（User Study）。

### 4. 资源与算力
*   **训练时长**：论文图3显示，MoScale 的训练效率极高。在 HumanML3D 上，不同规模的模型（Tiny 到 Large）训练时间大约在 **10 到 50 小时**之间。
*   **推理速度**：使用 KV 缓存，在 18 步细化下推理时间为 0.28s，单步模式下仅为 0.08s。
*   **硬件说明**：文中未明确给出具体的 GPU 型号和数量，但强调了其训练效率优于 ParCo 等复杂的自回归变体。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了标准 Benchmark 的全量对比。
    *   **消融实验**：详细测试了分层细化（HR）、时间细化（TR）、扰动率（$\gamma_{max}$）、Transformer 深度、迭代次数以及 CFG Scale 的影响。
    *   **复杂性分析**：利用 LLM 将测试集分为低、中、高三种文本复杂度，验证了 MoScale 在处理复杂长指令时的显著优势。
*   **公平性**：采用了标准的 20 次运行取平均值及 95% 置信区间的做法，并引入了基于 VLM（视觉语言模型）的客观评分和人工双盲评审，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **性能领先**：MoScale 在两个主流数据集上均刷新了 SOTA 记录，尤其在 R-Precision（对齐度）上提升明显。
*   **分层机制的威力**：消融实验证明，**“下一尺度”的分层因果结构**是提升文本对齐能力的核心因素，而尺度内细化主要提升局部平滑度。
*   **零样本能力**：MoScale 无需额外训练即可直接应用于动作编辑、补全（Inpainting）、外推（Outpainting）和持续生成任务。
*   **扩展性**：模型性能随参数量和深度增加而稳定提升，展现了良好的 Scaling Law 特性。

### 7. 优点
*   **语义对齐精准**：通过分层预测，成功解决了传统 AR 模型“顾近失远”的问题，能准确执行“跳两次”等计数或时序指令。
*   **训练高效**：相比扩散模型，自回归训练收敛更快，且通过单次前向传播即可实现多尺度监督。
*   **多功能性**：统一了生成与编辑任务，零样本泛化能力强。
*   **鲁棒性**：跨尺度细化策略有效解决了分层生成中的误差累积问题。

### 8. 不足与局限
*   **推理开销**：虽然比扩散模型快，但由于引入了尺度内迭代细化，其推理速度略慢于最简单的单轮掩码模型（如 MoMask）。
*   **数据依赖**：尽管在小数据集上表现优异，但其分层量化（VQ-VAE）的质量仍受限于原始数据的分布，对于极度罕见的动作可能存在量化损失。
*   **硬件细节缺失**：未提供具体的算力消耗清单（如 TFLOPS 或具体显存占用），不便于开发者精确评估部署成本。

（完）
