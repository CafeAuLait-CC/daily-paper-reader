---
title: Next-Scale Autoregressive Models for Text-to-Motion Generation
title_zh: 用于文本到动作生成的下一尺度自回归模型
authors: "Zhiwei Zheng, Shibo Jin, Lingjie Liu, Mingmin Zhao"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03799v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 用于文本到动作生成的下一尺度自回归框架
tldr: 本研究针对自回归模型在文本生成动作任务中难以捕捉时间结构的问题，提出了MoScale框架。该框架采用“下一尺度”预测机制，从粗到细分层生成动作，通过粗尺度提供全局语义并逐步细化。此外，引入跨尺度分层细化和尺度内时间细化技术，增强了模型在有限数据下的鲁棒性。MoScale在保持高效训练的同时达到了SOTA性能，并具备出色的零样本泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03799v1/fig-001.webp\", \"caption\": \"Table 1. Performance on HumanML3D. We report the average result over 20 runs with 95% confidence interval. Bold for the best and underline for the second. → indicates that values closer to real motion correspond to better results. S is the total steps across all scales.\", \"page\": 6, \"index\": 1, \"width\": 1036, \"height\": 434}, {\"url\": \"assets/figures/arxiv/2604.03799v1/fig-002.webp\", \"caption\": \"Table 2. Performance on KIT-ML test set. Bold for the best result and underline for the second best.\", \"page\": 7, \"index\": 2, \"width\": 1032, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2604.03799v1/fig-003.webp\", \"caption\": \"Figure 4. Motion editing results. MoScale achieves better instruction adherence and retains unedited motion (shown in gray).\", \"page\": 8, \"index\": 3, \"width\": 1036, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.03799v1/fig-004.webp\", \"caption\": \"Figure 2. Overview of MoScale. (a) MoScale encodes motion sequences into discrete tokens from coarse to fine through multi-scale quantization. (b) It autoregressively predicts tokens at the next scale, conditioned on the prefix and text inputs, using hierarchical scalewise causal attention. (c) Within each scale, MoScale performs temporal refinement to further improve token quality and consistency.\", \"page\": 4, \"index\": 4, \"width\": 1027, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2604.03799v1/fig-005.webp\", \"caption\": \"Figure 6. Visualization of coarse-to-fine representation of motion with our Residual VQVAE.\", \"page\": 12, \"index\": 5, \"width\": 860, \"height\": 396}]"
motivation: 传统的自回归模型采用逐标记预测，难以有效对齐文本驱动动作生成所需的长程时间结构。
method: 提出了MoScale框架，通过从粗到细的时间分辨率分层生成动作，并结合跨尺度和尺度内的细化机制。
result: 该方法在文本生成动作任务上取得了SOTA性能，具有极高的训练效率和良好的模型扩展性。
conclusion: MoScale证明了分层自回归架构在处理复杂动作结构方面的优越性，并能泛化至多种动作编辑任务。
---

## 摘要
自回归（AR）模型提供了稳定且高效的训练，但标准的下一标记（next-token）预测与文本条件动作生成所需的时间结构并不完全匹配。我们引入了 MoScale，这是一种下一尺度（next-scale）自回归框架，能够以从粗到细的时间分辨率分层生成动作。通过在最粗尺度上提供全局语义并逐步进行细化，MoScale 建立了一种更适合长程动作结构的因果层级。为了提高在有限文本-动作数据下的鲁棒性，我们进一步结合了跨尺度层级细化以改进每个尺度的初始预测，以及尺度内时间细化以进行选择性的双向重新预测。MoScale 以极高的训练效率实现了最先进的（SOTA）文本到动作生成性能，能够随模型规模有效扩展，并能零样本泛化到多种动作生成和编辑任务中。

## Abstract
Autoregressive (AR) models offer stable and efficient training, but standard next-token prediction is not well aligned with the temporal structure required for text-conditioned motion generation. We introduce MoScale, a next-scale AR framework that generates motion hierarchically from coarse to fine temporal resolutions. By providing global semantics at the coarsest scale and refining them progressively, MoScale establishes a causal hierarchy better suited for long-range motion structure. To improve robustness under limited text-motion data, we further incorporate cross-scale hierarchical refinement for improving per-scale initial predictions and in-scale temporal refinement for selective bidirectional re-prediction. MoScale achieves SOTA text-to-motion performance with high training efficiency, scales effectively with model size, and generalizes zero-shot to diverse motion generation and editing tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Next-Scale Autoregressive Models for Text-to-Motion Generation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的自回归（AR）模型在文本驱动的动作生成任务中，往往只能捕捉局部动作的连贯性，而难以维持长程的全局语义结构（例如“做两次开合跳”或复杂的动作序列）。
*   **研究动机**：
    *   **自回归模型的局限**：标准的“下一标记预测（Next-token prediction）”容易让模型走捷径，即仅根据极短的历史预测下一帧，忽略了文本中的全局指令。
    *   **扩散/掩码模型的不足**：扩散模型和掩码 Transformer 虽然能通过双向上下文优化局部细节，但在初始生成阶段缺乏因果层级，难以建立稳健的全局语义框架。
    *   **数据稀缺性**：现有的文本-动作数据集规模较小，模型容易在复杂指令下失效。

### 2. 方法论
MoScale 提出了一个**从粗到细（Coarse-to-fine）的因果层级框架**，核心组件包括：
*   **下一尺度自回归（Next-Scale AR）**：
    *   放弃逐标记预测，改为逐尺度预测。
    *   使用**残差向量量化（Residual VQ-VAE）**将动作编码为 $K$ 个不同时间分辨率的尺度。
    *   最粗糙的尺度决定全局结构，后续尺度逐步细化残差细节。
*   **跨尺度层级细化（Cross-scale Hierarchical Refinement）**：
    *   在训练时对粗尺度的预测引入扰动（随机替换标记），强制细尺度学习如何纠正前序尺度的误差，从而缓解自回归模型常见的“曝光偏差（Exposure Bias）”。
*   **尺度内时间细化（In-scale Temporal Refinement）**：
    *   在每个尺度内部，利用双向注意力机制进行类似扩散模型的“掩码-再预测（Mask-and-repredict）”，通过多次迭代提升动作的平滑度和局部保真度。
*   **文本融合**：使用预训练的 T5 编码器提取特征，通过交叉注意力机制在所有尺度上保持一致的语义引导。

### 3. 实验设计
*   **数据集**：
    *   **HumanML3D**：目前最主流的数据集，包含约 1.45 万个动作序列和 4.5 万条文本描述。
    *   **KIT-ML**：较小规模的数据集，包含约 3900 个动作。
*   **评估指标**：R-Precision（文本-动作对齐度）、FID（动作质量/真实性）、Multimodal Distance（语义距离）、Diversity（多样性）。
*   **对比方法**：
    *   **自回归类**：T2M-GPT, ParCo, AttT2M。
    *   **扩散模型类**：MDM, MLD, MotionDiffuse, ReMoDiffuse。
    *   **掩码 Transformer 类**：MMM, MoMask, MoMask++。

### 4. 资源与算力
*   **训练时长**：论文图 3 显示，MoScale 的训练效率极高。MoScale-L（大模型）在 HumanML3D 上达到收敛约需 **20-30 小时**，显著快于 ParCo 等复杂的自回归变体。
*   **推理速度**：在开启 18 步细化时推理耗时 0.28s，仅进行单步尺度预测时仅需 0.08s。
*   **硬件说明**：文中未明确给出具体的 GPU 型号和数量，但强调了其架构在标准 Transformer 硬件上的高效性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定量对比（Table 1 & 2），覆盖了过去三年的主流 SOTA 模型。
    *   **消融实验**（Table 4 & 5）：详细验证了跨尺度细化（HR）、时间细化（TR）、扰动率（$\gamma_{max}$）、模型深度以及迭代次数对结果的影响。
    *   **文本复杂度分析**：专门针对“高难度”文本指令进行了分级评估，证明了 MoScale 在处理复杂逻辑时的优越性。
*   **主观评估**：组织了 20 人参与的用户研究，针对文本对齐和动作质量进行了偏好投票。
*   **充分性评价**：实验设计非常全面，不仅涵盖了标准生成任务，还通过零样本（Zero-shot）实验展示了模型在动作编辑、补全（Inpainting）和外推（Outpainting）方面的泛化能力，客观性较强。

### 6. 主要结论与发现
*   **性能领先**：MoScale 在 HumanML3D 和 KIT-ML 上的 R-Precision 和语义距离指标均刷新了 SOTA 记录。
*   **层级因果的重要性**：实验证明，**分层预测路径**是提升文本对齐能力的核心驱动力，而时间细化主要贡献于动作的局部平滑度。
*   **扩展性**：模型性能随参数量（Tiny 到 Large）和 Transformer 深度增加而稳步提升，符合 Scaling Law。
*   **零样本能力**：无需针对性训练，模型即可通过掩码策略完成高质量的动作编辑任务。

### 7. 优点
*   **语义对齐极佳**：通过先定全局再定细节的策略，解决了自回归模型容易“跑题”的顽疾。
*   **训练效率高**：相比扩散模型，自回归训练更简单且收敛更快。
*   **鲁棒性强**：跨尺度细化机制有效解决了推理时的误差累积问题。
*   **多功能性**：一个模型统一了生成、编辑、补全等多种下游任务。

### 8. 不足与局限
*   **推理延迟**：虽然比扩散模型快，但由于引入了尺度内迭代细化，其推理速度慢于纯粹的掩码 Transformer（如 MoMask）。
*   **数据依赖**：尽管在小数据上表现优异，但其性能上限仍受限于现有动作捕捉数据集的动作多样性。
*   **计算开销**：多尺度表示增加了序列长度，对于超长动作序列的生成，显存占用可能会显著增加。

（完）
