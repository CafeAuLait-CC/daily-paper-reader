---
title: Data-driven Head Motion Generation through Natural Gaze-Head Coordination
title_zh: 基于数据驱动的自然目光-头部协调的头部运动生成
authors: "Xiaohan Liu, Yilin Wen, Yusuke Sugano"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25810v1"
tags: ["query:3d-motion-generation"]
score: 8.0
evidence: 基于条件VAE的数据驱动头部运动生成
tldr: 从大规模野外人脸视频数据出发，提出首种数据驱动方法建模凝视与头部运动的时序协调。该方法利用现成外观凝视估计器自动提取自然多样的运动数据，并构建生成式条件变分自编码器捕捉其概率相关性与时序动态。实验表明该方法可生成合理多样的头部运动，并在凝视控制视频生成中产生更自然的效果。贡献在于首次将凝视-头部协调建模应用于视频生成，提升了真实感。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25810v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 777, \"height\": 679}, {\"url\": \"assets/figures/arxiv/2605.25810v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 626}, {\"url\": \"assets/figures/arxiv/2605.25810v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 866, \"height\": 451}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25810v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 270}, {\"url\": \"assets/tables/arxiv/2605.25810v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 857, \"height\": 210}]"
motivation: 现有方法缺乏对凝视与头部自然协调的建模，导致面部视频生成中头部运动僵硬、不真实。
method: 提出自动流水线提取凝视与头部运动，利用条件变分自编码器建模概率相关性，生成多样且合理的凝视条件头部运动。
result: 人类评估和定量比较显示，所提方法生成的头部运动更自然，且显著优于基线方法。
conclusion: 验证了数据驱动凝视-头部协调建模的有效性，为生成更真实的凝视控制视频提供新方向。
---

## 摘要
我们提出了首个数据驱动的方法，从大规模的真实世界面部视频中建模时间上的目光-头部协调。为了获得可推广学习的训练数据，我们提出了一种自动流水线，使用现成的基于外观的目光估计器提取自然且多样的目光和头部运动。为了捕捉目光-头部协调的概率相关性和时间动态，我们在生成条件变分自编码器上构建模型，以生成合理且多样的目光条件下的头部运动。我们进一步将框架应用于目光控制的面部视频生成，实现了与输入目光相关的自然逼真的头部运动视频生成——这是之前未曾强调的方面。人工评估和定量比较证明了我们方法的有效性并验证了我们的设计选择，评估者对我们的方法相比基线方法显示出统计上显著的偏好。

## Abstract
We present the first data-driven approach to model temporal gaze-head coordination from large-scale in-the-wild facial videos. To obtain training data for generalizable learning, we propose an automatic pipeline that extracts natural yet diverse gaze and head motions with off-the-shelf appearance-based gaze estimators. To capture the probabilistic correlation and temporal dynamics of gaze-head coordination, we build our model on a generative conditional Variational Autoencoder for plausible yet diverse gaze-conditioned head motion generations. We further apply our framework to gaze-controlled facial video generation, where we enable video generation with natural and realistic head motion correlated to the input gaze - an aspect that has not been emphasized before. Human evaluation and quantitative comparisons demonstrate our method's effectiveness and validate our design choices, with evaluators showing statistically significant preference for our approach over baseline methods.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：人类视觉注意依赖眼神与头部运动的精细协调（gaze-head coordination），但现有 gaze-controlled facial video generation 方法通常将头部姿态视为独立控制变量，忽略了这种内在协调，导致生成的头部运动僵硬、不自然，尤其在动态 gaze 转移时表现明显。
- **背景与动机**：以往研究多依赖受控实验室环境、移动眼动仪或 VR 头显采集数据，硬件限制大、数据多样性差，难以建模 gaze-head coordination 的概率性关系与时序动态。本文首次提出**数据驱动方法**，从大规模野外人脸视频中自动提取 gaze 和 head 运动，并通过生成模型捕捉其协调模式，从而生成更自然的 gaze-conditioned head motion。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 利用**自动流水线**从野生视频中高效提取多样化、自然的 gaze 和 head pose 数据（无需专用硬件）。
- 构建**条件变分自编码器（cVAE）**，同时建模 gaze-head coordination 的概率分布性（distributional mapping）和时序动态。

### 关键技术细节
1. **数据提取流水线**（图1）：
   - 使用现成的基于外观的 gaze 估计器（[10]）提取 head pose 和 gaze direction（pitch & yaw）。
   - 对检测到的 face 进行规范化处理，消除 head pose 和相机位置带来的几何变化。
   - 数据过滤：排除佩戴太阳镜、多人脸、检测失败/不稳定、相机抖动/场景快速变化（使用 Glasses Detector、MediaPipe、PySceneDetect）。
2. **cVAE 模型架构**（图2）：
   - 编码器 \( \mathcal{E} \) 和解码器 \( \mathcal{D} \) 均使用 **GRU** 处理时序，捕获连续运动模式。
   - 输入序列 \( g_{1:T} \)（gaze directions）和真实 head motions \( h_{1:T} \)，编码器输出隐变量 \( z \) 的分布参数 \( (\mu, \sigma) \)，采样 \( z \sim \mathcal{N}(\mu, \sigma) \)。
   - 解码器基于 \( z \)、gaze 输入和上下文历史（前次生成的 head motion）逐帧自回归生成 \( \hat{h}_{1:T} \)。
   - 长期生成采用**自回归方式**：将上一迭代的最后两帧 head pose 作为下一迭代的上下文条件 \( c_1, c_2 \)。
3. **损失函数**：
   \[
   \mathcal{L} = \|\hat{h}_{1:T} - h_{1:T}\|^2 + \lambda \mathcal{L}_{\text{KL}}
   \]
   包括重建误差（L2 loss）和 KL 散度正则项（使隐空间接近标准正态），\( \lambda \) 平衡两项。
4. **推理**：从 \( \mathcal{N}(0,1) \) 采样 \( z \)，解码器自回归生成；初始上下文设为零。

### 实现细节
- 序列长度：12 frames（约2.4秒），帧率5 FPS。
- 隐变量维度 \( d=128 \)。
- 训练 60,000 steps，batch size 64，Adam 优化器，学习率 \( 5\times10^{-5} \)。
- 使用 KL 权重 annealing、context dropout、feature dropout 等正则化技巧。
- 视频生成集成：将 head motion 生成器与预训练 ST-ED [2]（基于 ETH-XGaze）结合，进行 gaze-controlled 面部视频合成。

## 3. 实验设计：数据集、基准（benchmark）、对比方法

- **数据集**：CelebV-Text [11] 大规模野生人脸视频数据集。
  - 训练集：45,806 个视频（经过自动流水线提取和过滤后）。
  - 测试集：676 个视频（来自未见过的 subject），所有视频对齐到 25 FPS。
- **评估协议**：每个测试视频的 gaze 序列作为输入，随机生成 30 个 head motion 序列，评估合理性和多样性。
- **对比方法**：
  1. **Constant Head Motion**：固定头部姿态（不运动）。
  2. **Mirror Gaze Inputs**：直接将 gaze direction 复制为 head pose（简单确定性规则）。
  3. **w/o Temporal Modeling**（消融变体）：去除 GRU 和 context conditioning 的 cVAE 变体。
  4. 另外，人类评估中还包括 **Driven by Real Motion**（使用真实 head motion 驱动的生成）作为对比条件。
- **评估指标**：
  - **合理性**：Angular Error（3D角度差）、Correlation（时序相关性，分 pitch/yaw）、Average Variance Error (AVE)、Smoothness（三阶导数的 jerk）。
  - **多样性**：Average Pairwise Distance (APD) [18]。
  - **人类评估**：4 位专家评估 8 对随机顺序、counter-balanced 的视频对，打分 -2（强烈偏好基线）到 2（强烈偏好我们的方法）。

## 4. 资源与算力

- **论文未明确说明训练所用的 GPU 型号、数量和总训练时长**。
- 仅提到训练步数 60,000，batch size 64，学习率 \(5\times10^{-5}\)，但未提供具体硬件信息。

## 5. 实验数量与充分性

- **实验数量**：主体实验包括与两个确定性基线（Constant Head Motion、Mirror Gaze Inputs）的定量比较（表1）；一个消融实验（w/o Temporal Modeling）；以及人类评估（表2，四种对比条件）。另提供定性对比（图3）。
- **充分性评估**：
  - **优点**：包含定量指标覆盖合理性和多样性；人类评估采用统计学检验（Wilcoxon signed-rank test），结果显著；消融实验验证了时序建模的关键性。
  - **不足**：
    - 基线方法过于简单（仅固定头部或镜像 gaze），未与现有更先进的 head motion generation 方法（如基于 GAN、扩散模型的方法）对比，削弱了说服力。
    - 仅使用单一数据集（CelebV-Text），缺乏跨数据集或跨场景验证，泛化性存疑。
    - 人类评估仅 4 位专家、8 对比较，样本量较小，可能存在偏差。

## 6. 论文的主要结论与发现

- **主要结论**：提出的数据驱动 cVAE 框架能够有效建模 gaze-head coordination 的概率性和时序动态，生成合理且多样的 gaze-conditioned head motion。
- **定量结果**：与 Mirror Gaze Inputs 相比，Angular Error（best）降低 55.3%，Smoothness 提升 72.4%；相关性（pitch & yaw）显著提高，AVE 降低 30%。与 Constant Head Motion 相比同样显著。
- **消融结果**：去除时序建模后，Angular Error（best）升高 44.0%，jerk 增大 8.5 倍，相关性大幅下降，AVE 上升 54.8%，并出现明显抖动。人类评估中，完整模型获得最高偏好得分（1.625，p=0.008）。
- **人类评估**：我们的方法在自然性和 gaze 协调方面显著优于 Mirror Gaze Inputs 和 w/o Temporal Modeling，与 Constant Head Motion 相比无显著差异（平衡结果，说明最小头部运动在某些场景下可接受，但我们的方法在需要运动时更自然）。
- **应用演示**：将生成 head motion 与 ST-ED 结合生成的面部视频，在 gaze 转移时头部运动更自然。

## 7. 优点

1. **首创性**：第一个数据驱动建模 gaze-head coordination 的工作，填补了该领域空白。
2. **高效数据采集**：提出的自动流水线无需专用硬件，可大规模从野生视频中提取自然多样的 gaze 和 head 运动，具有泛化潜力。
3. **模型设计合理**：cVAE + GRU 架构同时捕获分布概率性和时序动态；自回归上下文使长期生成平滑。
4. **多维度评估**：同时采用多项定量指标（角度误差、相关性、方差、平滑度、多样性）和人类评估，验证了方法的有效性和设计选择。
5. **实际应用价值**：集成到 gaze-controlled 面部视频生成中，提升了头部运动的自然感，具有虚拟人、数字人类等应用前景。

## 8. 不足与局限

- **实验覆盖有限**：
  - 基线仅包含简单规则（固定头部、镜像 gaze），未与 SOTA 头部运动生成模型（如基于自回归、扩散模型或 VAE 的其他工作）对比。
  - 仅在一个数据集（CelebV-Text）上评估，缺乏多样性（如不同光照、姿态、民族文化、年龄等）验证；未报告泛化到其他数据集（如 VoxCeleb、FaceForensics）的结果。
  - 人类评估样本量小（4 人，8 对比），可能导致偏好偏差；未提供离群值分析或多轮独立测试。
- **消融实验单一**：仅去除了时序建模，未对 GRU 层数、隐变量维度、序列长度等超参数进行系统消融。
- **视频生成集成初步**：论文承认“integration with facial video generation remains preliminary”，仅使用了预训练 ST-ED，未端到端联合训练；生成的面部视频质量受限于 ST-ED 能力，可能影响最终感知自然度。
- **资源与可复现性**：未提供训练所需的 GPU 型号、数量、时间等算力信息，不利于复现和公平比较。
- **简化假设**：模型仅处理 pitch 和 yaw 两个自由度，未考虑 roll 或更复杂的头部姿态动作；gaze 和 head 的时序关系可能简化（仅 5 FPS，2.4s 窗口），可能遗漏细微协调模式。
- **应用限制**：目前方法只生成头部运动，未同步生成其他面部运动（嘴唇、眉毛等），离真正的全脸动画仍有距离。

---

（完）
