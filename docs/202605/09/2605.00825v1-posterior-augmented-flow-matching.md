---
title: Posterior Augmented Flow Matching
title_zh: 后验增强流匹配
authors: "George Stoica, Sayak Paul, Matthew Wallingford, Vivek Ramanujan, Abhay Nori, Winson Han, Ali Farhadi, Ranjay Krishna, Judy Hoffman"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00825v1"
tags: ["query:3d-motion-generation"]
score: 7.5
evidence: 后验增强流匹配以防止高维数据中的流崩溃
tldr: 本研究针对流匹配（Flow Matching）在高维图像生成中因监督信号稀疏导致的“流崩溃”和泛化差等问题，提出了后验增强流匹配（PAFM）。该方法通过将单目标监督替换为对多个潜在目标的后验期望，利用重要性采样构建候选目标混合体，从而显著降低训练梯度的方差。实验证明，PAFM在多种模型架构和基准测试中均能有效提升生成质量，且计算开销极小。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00825v1/fig-001.webp\", \"caption\": \"Figure C.1: Posterior augmented flow matching learns the target distribution better under data sparsity. We train two rectified flow models to generate two crescent moon distributions, under different data sparsity regimes. Column titles indicate the total number of training data points given to each model. Rows indicate the source used for generating targets: the “Ground Truth” distribution (top), the trained “FM” model (middle), and the trained “PAFM” model (bottom). Each plot shows kernel density estimates (KDE) for the generated target distribution.\", \"page\": 15, \"index\": 1, \"width\": 776, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2605.00825v1/fig-002.webp\", \"caption\": \"Figure 2: Posterior augmented flow matching is more robust than flow matching. We train two rectified flow models to generate two crescent moon distributions (left). Second-left: a model trained with FM generates many points in between the two moons. Second-right: the same model trained with PAFM has far less of an issue. Right: PAFM estimates the true velocity field across t significantly better.\", \"page\": 7, \"index\": 2, \"width\": 781, \"height\": 190}]"
motivation: 传统的流匹配在处理高维数据时，由于每个训练样本仅监督单一轨迹，容易导致梯度方差过大及模型对特定源-目标配对的过度记忆。
method: 提出PAFM框架，通过重要性采样将单目标监督扩展为对给定中间状态下多个有效目标补全的后验期望。
result: 在ImageNet和CC12M等基准上，PAFM在不同模型规模和架构下均实现了高达3.4的FID提升，且几乎不增加计算负担。
conclusion: PAFM作为一种理论完备的流匹配泛化方法，通过聚合多条可能轨迹的信息，有效缓解了流崩溃问题并增强了模型的泛化能力。
---

## 摘要
流匹配 (Flow Matching, FM) 训练一个随时间变化的向量场，将样本从简单的先验分布传输到复杂的数据分布。然而，对于高维图像，每个训练样本仅监督单一的轨迹和中间点，导致训练信号极其稀疏且具有高方差。这种约束不足的监督可能导致流崩溃 (flow collapse)，即学习到的动力学记住了特定的源-目标配对，将多样化的输入映射到过度相似的输出，从而无法泛化。我们提出了后验增强流匹配 (Posterior-Augmented Flow Matching, PAFM)，这是 FM 的一种具有理论基础的泛化形式，它将单目标监督替换为给定中间状态和条件下有效目标补全的近似后验期望。PAFM 将这一难以处理的后验分解为：(i) 假设终点下中间状态的似然，以及 (ii) 该终点在条件下的先验概率，并使用重要性采样方案构建多个候选目标的混合。我们证明了 PAFM 产生了原始 FM 目标的无偏估计量，同时通过聚合每个中间状态的许多可能延续轨迹的信息，显著降低了训练期间的梯度方差。最后，我们展示了 PAFM 在不同模型规模 (SiT-B/2 和 SiT-XL/2)、不同架构 (SiT 和 MMDiT) 以及类别和文本条件基准 (ImageNet 和 CC12M) 中，相比 FM 在 FID50K 指标上提升了高达 3.4，且计算开销的增加可以忽略不计。代码：https://github.com/gstoica27/PAFM.git。

## Abstract
Flow matching (FM) trains a time-dependent vector field that transports samples from a simple prior to a complex data distribution. However, for high-dimensional images, each training sample supervises only a single trajectory and intermediate point, yielding an extremely sparse and high-variance training signal. This under-constrained supervision can cause flow collapse, where the learned dynamics memorize specific source-target pairings, mapping diverse inputs to overly similar outputs, failing to generalize. We introduce Posterior-Augmented Flow Matching (PAFM), a theoretically grounded generalization of FM that replaces single-target supervision with an expectation over an approximate posterior of valid target completions for a given intermediate state and condition. PAFM factorizes this intractable posterior into (i) the likelihood of the intermediate under a hypothesized endpoint and (ii) the prior probability of that endpoint under the condition, and uses an importance sampling scheme to construct a mixture over multiple candidate targets. We prove that PAFM yields an unbiased estimator of the original FM objective while substantially reducing gradient variance during training by aggregating information from many plausible continuation trajectories per intermediate. Finally, we show that PAFM improves over FM by up to 3.4 FID50K across different model scales (SiT-B/2 and SiT-XL/2), different architectures (SiT and MMDiT), and in both class and text conditioned benchmarks (ImageNet and CC12M), with a negligible increase in the compute overhead. Code: https://github.com/gstoica27/PAFM.git.