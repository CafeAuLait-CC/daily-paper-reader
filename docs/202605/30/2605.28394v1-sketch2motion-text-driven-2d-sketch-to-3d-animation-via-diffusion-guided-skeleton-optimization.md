---
title: "Sketch2Motion: Text-driven 2D Sketch to 3D Animation via Diffusion-guided Skeleton Optimization"
title_zh: Sketch2Motion：基于扩散引导骨架优化的文本驱动二维草图到三维动画
authors: "Gaurav Rai, Ojaswa Sharma"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28394v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 扩散引导的骨架运动合成用于三维动画
tldr: 2D手绘草图动画面临遮挡和运动映射难题。提出Sketch2Motion框架，利用扩散引导的骨架优化，结合文本到视频扩散模型和运动感知得分蒸馏采样，无需配对数据即可生成动画。通过物理约束和弹簧质量模拟增强运动合理性。实验证明生成动画时间连贯且文本对齐，适用于多种角色类型，优于传统方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28394v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1678, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2605.28394v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1790, \"height\": 713}, {\"url\": \"assets/figures/arxiv/2605.28394v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1388, \"height\": 924}, {\"url\": \"assets/figures/arxiv/2605.28394v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1877, \"height\": 1027}, {\"url\": \"assets/figures/arxiv/2605.28394v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 910, \"height\": 696}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28394v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 2079}, {\"url\": \"assets/tables/arxiv/2605.28394v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 845, \"height\": 226}, {\"url\": \"assets/tables/arxiv/2605.28394v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 856, \"height\": 225}]"
motivation: 现有2D草图动画方法局限于特定运动类型，缺乏通用框架；遮挡和运动映射问题未有效解决。
method: 提出扩散引导的骨架优化框架，结合文本到视频扩散模型通过运动感知得分蒸馏采样优化，并应用物理约束和弹簧质量模拟。
result: 生成动画时间连贯、文本对齐，优于缺乏生成先验或物理约束的基线方法，支持双足、四足等角色。
conclusion: Sketch2Motion提供通用、全可微分、模块化框架，实现2D草图到逼真3D动画的转换，代码和数据集将公开。
---

## 摘要
二维手绘草图的动画为视觉交流提供了有效媒介。然而，这些草图带来了挑战，特别是在处理遮挡和准确映射运动方面。虽然三维动画自然解决了这些挑战，但估计三维运动仍然是一项非常复杂的任务。最近将二维草图转换为三维动画的方法主要集中于特定类型的运动，如双足运动和面部表情。我们提出了Sketch2Motion，一个基于扩散引导的骨架运动合成框架，结合了经典角色动画流程与深度生成先验。我们的方法使用骨架变换表示运动，并通过线性混合蒙皮将其传播到网格变形。为了引导生成的动画朝向真实且语义上有意义的运动，我们通过运动感知分数蒸馏采样（MoSDS）集成了文本到视频扩散模型，从而无需配对运动数据即可进行优化。此外，我们应用了受物理启发的平滑性、拓扑和接触约束来稳定优化并保持运动合理性。进一步地，我们集成了一个弹簧质量模拟器来引入二次运动效果。所提出的框架是通用的、完全可微的、模块化的，并且兼容双足、四足和非生命关节角色。实验证明，我们的方法生成了时间上连贯、与文本对齐的动画，优于缺乏生成先验或显式物理约束的基线运动迁移方法。我们将公开我们的代码和数据集。

## Abstract
Animation of 2D hand-drawn sketches provides an effective medium for visual communication. However, these sketches pose challenges, particularly in handling occlusions and accurately mapping motion. While 3D animation naturally addresses these challenges, estimating 3D motion remains a very complex task. Recent approaches to converting 2D sketches to 3D animations have mainly focused on specific types of motion, such as bipedal movements and facial expressions. We propose Sketch2Motion, a diffusion-guided framework for skeleton-based motion synthesis that combines classical character animation pipelines with deep generative priors. Our method represents motion using skeletal transformations, which are propagated to mesh deformations via linear blend skinning. To guide the resulting animation toward realistic and semantically meaningful motion, we integrate a text-to-video diffusion model via motion-aware score-distillation sampling (MoSDS), enabling optimization without paired motion data. Additionally, we apply physics-inspired smoothness, topological, and contact constraints to stabilize optimization and preserve motion plausibility. Further, we integrate a spring-mass simulator to introduce secondary motion effects. The proposed framework is generalized, fully differentiable, modular, and compatible with biped, quadruped, and non-living articulated characters. Experiments demonstrate that our approach produces temporally coherent, text-aligned animations that outperform baseline motion transfer methods that lack generative priors or explicit physical constraints. We will make our code and dataset publicly available.

---

## 论文详细总结（自动生成）

# Sketch2Motion：基于扩散引导骨架优化的文本驱动二维草图到三维动画 — 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：从二维手绘草图生成三维动画面临两大挑战：
  - **维度鸿沟**：草图缺乏深度、表面几何和一致拓扑，难以推断合理的三维结构与运动。
  - **运动映射困难**：遮挡、自交、细节不完整等问题使部件分割和运动解释复杂化。
- **现有局限**：传统方法局限于特定运动类型（如双足行走、面部表情）或依赖模板/用户交互；现代学习式运动合成依赖大规模配对数据，难以泛化到草图风格化的角色。
- **研究动机**：利用预训练文本到视频扩散模型作为强语义先验，结合经典骨架动画流程，实现无需配对数据的、通用且物理合理的从草图到三维动画的自动生成。

## 2. 论文提出的方法论

### 核心思想
- 将2D草图膨胀为3D网格，自动提取骨架与蒙皮权重；用**骨架变换**表示运动，通过**线性混合蒙皮（LBS）** 变形网格。
- 引入**运动感知得分蒸馏采样（MoSDS）**，从冻结的文本到视频扩散模型中提取梯度，直接优化骨架运动参数，无需配对运动数据。
- 结合**物理启发的正则化**（时间平滑、关节范围、接触约束）和**弹簧质量模拟器**（二次运动），生成稳定、真实的动画。

### 关键技术细节
1. **骨架表示**
   - 关节层次树结构，参数化：全局根平移 \( t_{\text{root}} \in \mathbb{R}^{T \times 3} \)、关节旋转 \( R \in \mathbb{R}^{T \times J \times 3} \)（轴角表示）、局部偏移 \( \delta \in \mathbb{R}^{T \times J \times 3} \)（非刚性二次运动）。
2. **运动初始化**
   - 根据骨架形态（双足/四足等）和文本动作类别，用正弦基函数生成初始周期性运动轨迹。
   - 通过**NURBS曲线**（非均匀有理B样条）将密集轨迹投影到低维流形，减少优化复杂度，保留时间平滑性和关键相变（如脚触地的高权重点）。
3. **前向运动学与蒙皮**
   - 可微前向运动学：递归计算全局变换，包含局部偏移。
   - 线性混合蒙皮：根据预计算权重加权变形顶点。
4. **扩散引导（MoSDS）**
   - 渲染帧序列，编码为视频潜在 \( z \)，添加噪声后输入冻结的文本到视频扩散模型。
   - MoSDS 将噪声残差分解为**外观梯度**（时间均值）和**运动梯度**（帧间偏差），加权组合以抑制外观扰动、聚焦运动优化。
   - 通过代理MSE损失将梯度回传到骨架参数。
5. **物理正则化**
   - **时间平滑**：速度损失 + 加速度损失。
   - **拓扑约束**：关节活动范围限制（如脊柱0.4 rad，膝/肘1.5 rad） + 双足对称性损失。
   - **循环一致性**：首尾帧位置与速度匹配。
   - **环境交互**：地面穿透惩罚 + 脚接触损失（动态检测触地阶段并惩罚水平滑动）。
6. **二次运动模拟**
   - 自动分割网格为刚性区域（由LBS驱动）和动态区域（如头发、尾巴）。
   - 弹簧质量系统：位置弹簧力、结构弹簧力、阻尼、重力，半隐式欧拉积分，速度/位移钳位防止撕裂。
   - 最终位置通过混合权重 \( w_i \) 插值刚性目标与模拟位置。

### 算法流程（文字说明）
1. 3D初始化：从2D草图膨胀网格，提取骨架和蒙皮权重。
2. 运动初始化：根据语义类别生成NURBS参数初值；初始化弹簧质量模拟器。
3. 对于每轮迭代：
   - 通过前向运动学计算全局关节变换（含局部偏移）。
   - 线性混合蒙皮得到变形网格。
   - 弹簧质量模拟输出二次运动网格。
   - 可微渲染得到2D帧序列。
   - 计算MoSDS梯度，更新骨架参数和局部偏移。
   - 计算物理正则化损失并反向传播。
4. 返回优化后的动画网格序列。

## 3. 实验设计

### 数据集
- **自建数据集**：100张手绘草图，覆盖三类：
  - 双足（40张）、四足（40张）、非生命物体（20张）。
- 使用 Step1X-3D [16] 将2D草图膨胀为纹理3D网格，用 Puppeteer [47] 自动提取骨架和蒙皮权重。

### 基准方法
- **AnimateAnyMesh** [11]：基于扩散先验和变形场的任意网格动画方法。
- **BiMotion** [12]：B样条运动表示的文本驱动3D角色生成。
- （注：AKD [13] 和 AnimaX [35] 因代码未公开无法直接对比。）

### 评估指标
- **文本-视频对齐（T2VA）** ↑：使用X-CLIP计算文本嵌入与帧嵌入的余弦相似度。
- **网格拉普拉斯失真（MLD）** ↓：度量变形网格与静止网格的局部几何结构保持程度。
- **多视图特征一致性（MVFC）** ↑：不同视角渲染的特征（CLIP）间平均余弦相似度，反映3D一致性。

### 对比结果
- 定性：图4显示 AnimateAnyMesh 缺乏骨架约束导致结构不一致；BiMotion 运动幅度小；Sketch2Motion 生成物理合理、文本对齐的动画。
- 定量（表I）：
  - T2VA：Ours 0.2120 > BiMotion 0.1891 > AnimateAnyMesh 0.1967
  - MLD：Ours 0.3523（低于 AnimateAnyMesh 0.6796，高于 BiMotion 0.0102*（*几乎无运动））
  - MVFC：Ours 0.8933 最高

### 消融实验
- 设置：去除运动初始化、去除物理正则化、去除二次运动。
- 结果（表II、图5）：
  - 无初始化：漂浮、脚滑、抖振。
  - 无物理正则化：高频抖动、脚滑、关节超限、步态不对称。
  - 无二次运动：僵硬，缺乏跟随和重叠动作。
  - 完整方法在所有指标上最优或接近最优。

## 4. 资源与算力

- 论文在“实现细节”中说明：优化使用 AdamW（权重衰减 1e-5），不同参数学习率（旋转 1.5e-2，根平移 2.0e-2，偏移 5.0e-3），序列帧数 T=48，生成约需 **5分钟**。
- **未明确说明**：GPU 型号、数量、训练过程（如有）的耗时。仅在推理优化阶段给出时间估计。

## 5. 实验数量与充分性

- **实验组数**：
  - 与两种基线方法的定性 + 定量比较（含三个指标）。
  - 三组消融实验（w/o init, w/o physics, w/o secondary motion）。
  - 自建 100 草图数据集覆盖三类角色。
- **充分性评价**：
  - 对比方法数量有限（仅两个可运行），但这两个方法分别代表不同范式（变形场 vs 隐式运动），具有一定代表性。
  - 消融实验设计清晰，验证了每个核心组件的必要性。
  - 评估指标（T2VA、MLD、MVFC）从语义、几何、3D一致性三个维度覆盖，较为全面。
  - 但缺乏用户研究或对运动自然度的主观评分。
  - 未与更多最近的文本到4D方法（如 AnimateAnyMesh 以外的其他4D方法）对比，部分原因在于代码不可用，但可视为局限。

## 6. 论文的主要结论与发现

- Sketch2Motion 实现了从2D草图到3D动画的自动、文本驱动生成，无需配对运动数据，泛化到双足、四足和非生命角色。
- MoSDS 结合运动/外观梯度解耦，有效利用视频扩散先验驱动骨架优化，避免纹理伪影。
- 物理正则化（平滑、关节限制、接触等）对于稳定优化、防止脚滑和关节超限至关重要。
- 弹簧质量模拟器为动画增添生动感（如尾巴摆动、帽子振动）。
- 在定量指标上全面优于 AnimateAnyMesh 和 BiMotion，尤其在多视图一致性上显著提升。

## 7. 优点

- **通用性**：支持双足、四足、非生命物体，无需重新训练。
- **无需配对数据**：利用预训练扩散模型作为先验，克服了三维运动数据匮乏问题。
- **可微分与模块化**：整个流程（前向运动学、蒙皮、渲染、物理模拟）可微，便于端到端优化。
- **物理合理性**：引入接触、关节限制、二次运动等物理约束，生成更真实的动画。
- **时间连贯性**：NURBS 初始化 + 平滑正则化 + MoSDS 解耦，有效抑制闪烁。
- **开源承诺**：代码和数据集将公开，促进复现和后续研究。

## 8. 不足与局限

- **依赖扩散模型质量**：生成运动的多样性、复杂动作的表现受限于预训练文本到视频模型的训练分布和能力。
- **计算成本高**：每序列优化约5分钟（48帧），对于高分辨率网格或更长序列可能不实用；未报告GPU规格，难以量化。
- **物理模型简化**：仅使用弹簧质量系统和约束，未显式建模全物理动力学（如刚体碰撞、摩擦、肌肉力），细微伪影（轻微滑步）仍可能出现。
- **骨架依赖重建质量**：草图膨胀到3D网格的误差（如 Step1X-3D）会传播到动画阶段。
- **单角色局限**：当前框架不支持多角色交互或动态环境。
- **实验对比不够广泛**：仅与两个基线方法比较；更多近期方法（如 AnimaX、AKD）因代码未开源而缺失。
- **未报告方差或显著性检验**：定量结果是单次运行？指标是否有标准差？论文未提及，影响结果可靠性判断。

（完）
