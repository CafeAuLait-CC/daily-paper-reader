---
title: Multi-Scale Generative Modeling with Heat Dissipation Flow Matching
title_zh: 基于热耗散流匹配的多尺度生成建模
authors: "Jun Ma, Hanquan Zhang, Yanjun Qin, Haoyuan Guan, Ke Zhang"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.19371v1"
tags: ["query:3d-motion-generation"]
score: 8.0
evidence: 热耗散流匹配将基于模糊的破坏集成到流匹配中用于生成建模。
tldr: 扩散模型在图像生成中广泛应用，主流方法基于噪声退化与去噪，而另一分支使用模糊作为退化方式，通过多尺度先验更好地保留色彩预算与细节。然而，基于模糊的模型仍局限于SDE框架，未融入ODE框架如流匹配（FM），且经典逆热耗散过程面临不适定问题，同时在高维回归上也存在困难。本文提出热耗散流匹配（HDFM），将连续模糊（热耗散）过程引入FM以注入多尺度先验，通过对齐插值热耗散路径解决不适定性，并采用x预测缓解高维回归难题。实验表明HDFM从模糊和x预测中受益，性能在多个数据集上超越基线方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19371v1/fig-001.webp\", \"caption\": \"Figure 6: Qualitative Results. Some examples are selected on ImageNet256.\", \"page\": 6, \"index\": 1, \"width\": 508, \"height\": 508}, {\"url\": \"assets/figures/arxiv/2605.19371v1/fig-002.webp\", \"caption\": \"Figure 2: The Architecture of HDFM. The learned model can be viewed as a stack of ViT [Dosovitskiy et al., 2021] blocks, augmented with a lightweight LayerSync regularization constraint.\", \"page\": 2, \"index\": 2, \"width\": 527, \"height\": 467}, {\"url\": \"assets/figures/arxiv/2605.19371v1/fig-003.webp\", \"caption\": \"Table 2: Quantitative comparison across datasets.\", \"page\": 7, \"index\": 3, \"width\": 508, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2605.19371v1/fig-004.webp\", \"caption\": \"Figure 4: HDFM transport trajectories in a toy experiment. Left: trajectories of particles in the data space, exhibiting curved paths. Right: trajectories of the same particles in the DCT (frequency) domain, which become approximately straight lines, consistent with the linear velocity property in the frequency domain (the proof see Linear velocity in the frequency domain).\", \"page\": 5, \"index\": 4, \"width\": 508, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2605.19371v1/fig-005.webp\", \"caption\": \"Figure 5: Spectral discrepancy during sampling. Left: Frequency ratio curves for JiT (full noise), HDFM, and HDFM-blur (pure blurring), where HDFM exhibits an intermediate behavior between the two extremes. Right: Attenuating the blur strength in HDFM by a factor r shifts the curve toward JiT, validating a smooth interpolation from blur-dominant to noise-dominant sampling. These suggest that HDFM can get a hybrid ability to recover low-frequency structure from noise and high-frequency details from blur.\", \"page\": 5, \"index\": 5, \"width\": 504, \"height\": 242}]"
motivation: 基于模糊的扩散模型尚未融入流匹配（FM）等ODE框架，且逆热耗散过程存在不适定问题和高维回归困难。
method: 提出热耗散流匹配（HDFM），将连续模糊过程引入FM，通过对齐插值热耗散路径和采用x预测来解决现有问题。
result: HDFM在玩具实验和消融研究中一致受益于模糊和x预测，在多个数据集上性能优于大多数基线方法。
conclusion: HDFM成功将模糊过程集成到流匹配框架中，有效解决了不适定性和高维回归难题，提升了生成性能。
---

## 摘要
扩散模型在图像生成中广泛应用，多数基于噪声的破坏与去噪。另一分支则使用模糊作为主要破坏方式，通过提供多尺度先验来更好地保留色彩预算与多尺度细节。然而，基于模糊的模型仍停留在基于随机微分方程的框架中，并未整合到基于常微分方程的框架（如流匹配）中。同时，在基于模糊的公式中，经典的反热耗散过程面临不适定挑战。此外，在数据流形假设下，从高维噪声（或速度）空间回归模糊图像也十分困难。我们提出热耗散流匹配，将连续模糊（热耗散）过程引入流匹配以注入多尺度先验。该方法通过对齐插值的热耗散路径来解决不适定性，并采用$x$预测来缓解高维回归困难。玩具实验与消融研究表明，热耗散流匹配能持续从模糊与$x$预测中受益。在所有数据集上，其性能均优于大多数基线方法。

## Abstract
Diffusion models are widely used in image generation, with most relying on noise-based corruption and denoising. A distinct branch instead uses blur as the main corruption, preserving better color budgets and multi-scale detail by providing multi-scale priors. However, blur-based models remain in SDE-based frameworks and are not integrated into ODE-based frameworks, such as Flow Matching (FM). Meanwhile, in the blur-based formulation, the classical inverse heat-dissipation (IHD) process faces an ill-posed challenge. Moreover, under the data-manifold assumption, regressing blurred images from high-dimensional noise (or velocity) space is also difficult. We propose Heat Dissipation Flow Matching (HDFM), which introduces a continuous blurred (heat-dissipation) process into FM to inject multi-scale priors. HDFM aligns an interpolated heat-dissipation path to address ill-posedness and adopts $x$-prediction to mitigate high-dimensional regression difficulty. Toy experiments and ablation studies show that HDFM consistently benefits from both blur and $x$-prediction. The performance of HDFM outperforms most baseline methods on all datasets.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化的详细总结。

### **论文总结：Multi-Scale Generative Modeling with Heat Dissipation Flow Matching**

#### **1. 核心问题与整体含义**
*   **研究背景**：扩散模型在图像生成领域占据主导，主流方法（如DDPM）基于**噪声添加与去除**的随机过程。另一分支（如IHDM、Blurring Diffusion）则探索使用**图像模糊（热耗散）** 作为前向过程，因其能更好地保留色彩和多尺度结构先验。然而，这类方法存在三个关键问题：
    1.  **框架局限**：主要基于**随机微分方程（SDE）**，未能有效融入**常微分方程（ODE）** 框架（如Flow Matching），后者采样速度更快。
    2.  **不适定问题**：经典的逆热耗散过程在数学上是**不适定的**，微小的扰动在反向过程中会被指数级放大，影响生成稳定性。
    3.  **高维回归困难**：在“数据存在于低维流形”的假设下，从充满高维噪声的速度场直接预测模糊图像（一个相对低维的目标）非常困难，导致训练不稳定。
*   **研究动机**：旨在将模糊过程的多尺度优势与流匹配（FM）的高效ODE框架相结合，同时解决上述不适定性和高维回归难题，从而提升生成模型的性能与效率。

#### **2. 方法论：Heat Dissipation Flow Matching (HDFM)**
*   **核心思想**：在流匹配框架中，设计一条连接噪声与**时变模糊图像**的插值路径，从而显式地注入从粗到细的多尺度生成先验。
*   **关键技术细节**：
    *   **路径设计**：定义 `z_t = t * u_t + (1-t) * ε`。其中，`u_t` 是原始数据 `x` 在热时间 `τ(t) = -log(t)` 下的模糊状态。当 `t` 从0到1变化时，路径从纯噪声平滑过渡到清晰图像，且模糊程度由强变弱。
    *   **时间重参数化**：使用 `t = exp(-τ)` 将热时间 `τ` 映射到流时间 `t`。这**将指数级模糊衰减转化为幂律形式**，有效缓解了原始逆热耗散过程的不适定性（因为当 `t→0` 时，`t*u_t → 0`，路径由噪声主导）。
    *   **X-Prediction 目标**：网络不直接预测噪声 `ε` 或速度 `v`，而是预测清晰的原始数据 `x`，然后通过确定性的热耗散算子 `H` 计算出对应的模糊状态 `u_t` 和拉普拉斯项 `Δu_t`。这**将学习目标对齐到低维数据流形上**，避免了直接在高维空间回归的困难。
    *   **速度场推导**：通过微分路径公式，得到真实速度场为 `v* = (u_t - z_t)/s(t) - Δu_t`。其中 `Δu_t` 项是关键，忽略它会导致训练与采样不一致。
    *   **工程优化**：
        *   **LayerSync 正则化**：引入轻量级正则项，通过余弦对齐增强网络中间层特征的一致性。
        *   **区间分类器无关引导（CFG）**：仅在中间时间步 `(t_min, t_max)` 应用强CFG（`α > 1`），在两端使用 `α=1`，以平衡语义控制与细节质量。
        *   **自适应校正融合**：根据局部残差能量动态调整拉普拉斯校正项 `Δu_t` 的强度 `β`，提升有限步采样下的稳定性。

#### **3. 实验设计**
*   **数据集**：ImageNet 256x256, ImageNet 128x128, Places365。
*   **评估指标**：Fréchet Inception Distance (FID, 越低越好), Inception Score (IS, 越高越好)，以及定性视觉对比。
*   **对比方法**：
    *   **基于模糊/热耗散的方法**：IHDM (经典逆热耗散), Blurring Diffusion。
    *   **非模糊的流匹配方法**：JiT (作为主干网络和主要对比基线)。
    *   **其他先进方法**：f-DM-DS (在ImageNet256上对比)。

#### **4. 资源与算力**
*   **文中未明确说明**具体的GPU型号、数量、训练时长或总计算开销。论文提到实验使用了公开的JiT代码及其默认的B/16配置作为主干网络，并遵循了其发布代码的默认超参数。

#### **5. 实验数量与充分性**
*   **实验组别较为全面**，主要包括：
    1.  **玩具实验**：在可控的2D螺旋数据集嵌入高维空间的情境下，验证了在热耗散设置下`x-pred`相比`v-pred`的显著优势，并可视化了HDFM在频率域中的线性传输轨迹。
    2.  **主实验**：在三个标准图像数据集上与多个基线方法进行定量（FID, IS）和定性比较。
    3.  **消融实验**：
        *   `x-pred` vs `v-pred`：在真实图像数据集上验证`x-pred`的有效性。
        *   **频谱分析实验**：通过分析采样过程中高低频能量比，验证HDFM融合了噪声（恢复低频结构）和模糊（恢复高频细节）的混合能力。
        *   **参数分析**：系统研究了CFG引导强度和噪声尺度对生成效果的影响。
*   **评价**：实验设计**较为充分和客观**。通过与同主干（JiT）的对比，能清晰分离出所提“热耗散路径”和“x-prediction”带来的增益。玩具实验为方法论的核心动机（流形假设、高维回归）提供了直观证据。消融实验覆盖了核心组件和关键超参数。

#### **6. 主要结论与发现**
*   HDFM成功地将连续的多尺度热耗散路径引入流匹配框架。
*   通过时间重参数化和`x-prediction`，有效缓解了逆热耗散的不适定性和高维回归难题。
*   在多个图像生成基准测试中，HDFM的性能**优于或持平于**现有的基于模糊的扩散模型和先进的流匹配方法（JiT）。
*   模糊过程为生成模型提供了有益的归纳偏置，与噪声过程形成互补。

#### **7. 优点**
*   **理论创新**：巧妙地将空间PDE（热方程）转化为ODE框架下的可处理形式，并给出了路径的连续性方程证明和频率域线性速度的解释。
*   **实验设计巧妙**：通过高维嵌入的玩具实验，清晰、有力地验证了“数据流形假设”下`x-prediction`的必要性，增强了论文的说服力。
*   **工程实践扎实**：不仅提出了核心算法，还配套了LayerSync正则化、区间CFG、自适应校正等实用技巧，提升了方法的稳定性和最终性能。
*   **兼容性强**：方法兼容条件生成和分类器无关引导。

#### **8. 不足与局限**
*   **计算开销**：相比纯噪声的流匹配，HDFM在训练和推理中需要额外计算DCT/IDCT以及拉普拉斯项 `Δu_t`，**增加了计算成本**。
*   **超参数敏感性**：方法引入了新的超参数，如CFG应用区间 `[t_min, t_max]` 和自适应校正参数，需要调优以获得最佳性能。
*   **理论深度**：虽然缓解了不适定性，但对新路径的数学性质（如概率密度演化）的深入理论分析相对有限。
*   **应用范围**：目前仅在图像生成领域验证，在其他模态（如视频、3D形状）上的有效性有待探索。其多尺度先验在需要严格结构保持的任务（如某些医学图像生成）中的表现也需要进一步评估。
*   **对比基线**：主要与同类方法（IHDM, Blurring）和主干方法（JiT）对比，与更广泛的SOTA扩散模型（如DiT系列）的全面性能对比略显不足。

（完）
