---
title: "DrawMotion: Generating 3D Human Motions by Freehand Drawing"
title_zh: DrawMotion：通过手绘草图生成3D人体动作
authors: "Tao Wang, Lei Jin, Zhihua Wu, Qiaozhi He, Jiaming Chu, Yu Cheng, Junliang Xing, Jian Zhao, Shuicheng Yan, Li Wang"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.20955v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 基于扩散的框架，从文本和绘图生成3D人体动作
tldr: "针对文本生成人体动作任务中用户难以用文字精确描述意图的问题，本文提出DrawMotion框架，通过手绘简笔画提供空间控制，结合文本语义条件生成3D人体动作。该方法引入多条件融合模块，在扩散过程中整合条件并降低计算复杂度，同时支持无训练引导以对齐用户意图。实验表明手绘方式可节省约46.7%的用户时间，有效提升动作生成的准确性和效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20955v1/fig-001.webp\", \"caption\": \"TABLE III: Comparison on the HumanML3D test set. We mark the best result as red and the second best one as blue . Arrows indicate the desired direction of metrics: ↓ (lower is better), ↑ (higher is better), and → (closer to real data is better).\", \"page\": 11, \"index\": 1, \"width\": 1006, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.20955v1/fig-002.webp\", \"caption\": \"TABLE IV: Comparison on the KIT-ML test set.\", \"page\": 11, \"index\": 2, \"width\": 1006, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.20955v1/fig-003.webp\", \"caption\": \"TABLE V: Comparison of diffusion-based motion edit methods on T2M and KIT datasets. R-prec (top3) denotes Rprecision (top3).\", \"page\": 11, \"index\": 3, \"width\": 524, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2605.20955v1/fig-004.webp\", \"caption\": \"TABLE II: Hyperparameter analysis of Intermediate Feature Guidance (IFG) on KIT-ML dataset. Here, repeat denotes the number of SGD iterations, lr is the learning rate of this update, Nth layer specifies the selected MCM layer for guidance, ϵMD is the Mahalanobis distance threshold used for clipping abnormal updates, and λ is the clip scale.\", \"page\": 10, \"index\": 4, \"width\": 520, \"height\": 573}, {\"url\": \"assets/figures/arxiv/2605.20955v1/fig-005.webp\", \"caption\": \"Fig. 8: Visual comparison between ReModiffuse, StickMotion, and DrawMotion: 1) This user attempted to make the generated trajectory resemble the emblem from Naruto and specified that, at a designated position along the trajectory, the action should involve raising the left hand high. 2) This user simply wrote the letter ”m”, without specifying a stickman. (see the animation on GitHub).\", \"page\": 14, \"index\": 5, \"width\": 529, \"height\": 761}, {\"url\": \"assets/figures/arxiv/2605.20955v1/fig-006.webp\", \"caption\": \"Fig. 7: Visualization of DrawMotion (see the animation on GitHub).\", \"page\": 13, \"index\": 6, \"width\": 1052, \"height\": 1381}, {\"url\": \"assets/figures/arxiv/2605.20955v1/fig-007.webp\", \"caption\": \"Fig. 3: The DrawMotion framework consists of the diffusion process (left) and the network structure (right). 1) The diffusion process includes a forward and a reverse process. In the forward process, original motions are augmented with Gaussian noise and fed into DrawMotion, which learns to predict the added noise based on textual descriptions and hand-drawn sketches. In the reverse process, user-provided textual descriptions and hand-drawn sketches are input into DrawMotion, enabling the gradual generation of motion sequences using the predicted noise. 2) In the DrawMotion architecture, both the stickman encoder and the text encoder are frozen, while the remaining modules are trainable. Encoded inputs are processed by multiple Multi-Condition Modules (MCMs) to get the final output.\", \"page\": 5, \"index\": 7, \"width\": 1060, \"height\": 586}]"
motivation: 用户仅用文本难以精确描述期望的人体动作，需要更直观的控制方式。
method: 提出基于扩散的DrawMotion框架，结合文本和手绘简笔画条件，通过多条件融合模块实现细粒度动作生成。
result: "手绘方法使用户生成符合想象的动作时间减少约46.7%，定量实验和用户研究验证了有效性。"
conclusion: DrawMotion通过手绘条件提供直观空间控制，能更高效地生成符合用户意图的3D人体动作。
---

## 摘要
文本到动作生成技术旨在将文本描述转化为人体动作，但其面临一个挑战：用户往往难以仅通过文字精确传达他们想要的动作。为解决这一问题，本文提出了DrawMotion，一个高效的基于扩散模型的多条件生成框架。DrawMotion同时基于传统的文本条件和一个新颖的手绘草图条件来生成动作，这两个条件分别对生成动作提供语义和空间层面的控制。具体而言，我们从三个角度解决细粒度动作生成任务：1）手绘草图条件。为了准确捕捉用户意图而无需繁琐的文本输入，我们开发了一种算法，能够自动生成适应不同数据集格式的手绘火柴人草图；2）多条件融合。我们提出了一个多条件模块（MCM），将其集成到扩散过程中，使模型能够利用所有可能的条件组合，同时相比传统方法降低了计算复杂度；3）免训练引导。值得注意的是，DrawMotion中的MCM确保其中间特征位于连续空间内，这使得分类器引导梯度能够更新特征，从而在保持保真度的同时，使生成的动作与用户意图对齐。定量实验和用户研究表明，在生成符合用户想象的动作时，手绘草图方法将用户所需时间减少了约46.7%。代码、演示和相关数据已在https://github.com/InvertedForest/DrawMotion公开。

## Abstract
Text-to-motion generation, which translates textual descriptions into human motions, faces the challenge that users often struggle to precisely convey their intended motions through text alone. To address this issue, this paper introduces DrawMotion, an efficient diffusion-based framework designed for multi-condition scenarios. DrawMotion generates motions based on both a conventional text condition and a novel hand-drawing condition, which provide semantic and spatial control over the generated motions, respectively. Specifically, we tackle the fine-grained motion generation task from three perspectives: 1) freehand drawing condition. To accurately capture users' intended motions without requiring tedious textual input, we develop an algorithm to automatically generate hand-drawn stickman sketches across different dataset formats; 2) multi-condition fusion. We propose a Multi-Condition Module (MCM) that is integrated into the diffusion process, enabling the model to exploit all possible condition combinations while reducing computational complexity compared to conventional approaches; and 3) training-free guidance. Notably, the MCM in DrawMotion ensures that its intermediate features lie in a continuous space, allowing classifier-guidance gradients to update the features and thereby aligning the generated motions with user intentions while preserving fidelity. Quantitative experiments and user studies demonstrate that the freehand drawing approach reduces user time by approximately 46.7% when generating motions aligned with their imagination. The code, demos, and relevant data are publicly available at https://github.com/InvertedForest/DrawMotion.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化的深入总结。

### 1. 论文的核心问题与整体含义
*   **核心问题**：传统的文本到动作生成技术存在瓶颈，用户仅通过文字描述难以精确、高效地传达其想象中的复杂人体动作细节（如肢体姿态、运动轨迹）。
*   **研究动机**：为了提供比文本更直观、更细粒度的控制方式，降低用户表达意图的门槛和时间成本，提升动作生成的准确性和用户体验。
*   **整体含义**：本文提出了一种名为 **DrawMotion** 的新型生成框架，它创新性地引入了**手绘草图**（包括轨迹和火柴人姿态）作为控制条件，与文本描述相结合，共同指导3D人体动作的生成，旨在实现语义和空间层面的双重精确控制。

### 2. 论文提出的方法论
*   **核心思想**：构建一个基于扩散模型的多条件生成框架，同时接受文本（语义）和手绘（空间）输入，通过高效的模块融合二者信息，并在推理阶段利用模型内部特征的特性进行免训练优化，以精确满足用户约束。
*   **关键技术细节**：
    1.  **手绘表示生成与编码**：
        *   **数据生成**：提出了**火柴人生成算法**，用于自动从现有3D动作数据集中生成多样化的手绘风格火柴人草图，解决了真实手绘数据匮乏的问题。
        *   **编码器**：使用预训练并冻结的编码器将火柴人草图编码为紧凑的嵌入。引入“候选损失”来应对草图到3D姿态映射中的歧义性（如左右肢体重叠）。
        *   **轨迹编码**：用户绘制的2D轨迹被重采样后，通过一个可训练的1D CNN编码。
    2.  **多条件融合模块**：
        *   **MCM结构**：取代了传统基于掩码的自注意力方法。MCM包含**绘图解码器**（使用标准点积注意力处理局部姿态和轨迹信息）和**文本解码器**（使用高效注意力处理全局语义信息），以及一个**潜在编码器**进行特征再融合。
        *   **高效融合**：通过沿批次维度分割数据并共享解码器计算，仅用两次前向传播即可得到全部四种条件组合（文本+绘图、仅文本、仅绘图、无条件）的输出，降低了计算复杂度。
    3.  **免训练引导**：
        *   **中间特征引导**：关键发现是MCM的中间特征空间具有**连续性**（通过PCA和特征扰动实验验证），而非离散聚类。这使得可以直接对这些特征进行梯度更新。
        *   **算法流程**：在扩散去噪过程的某些步骤中，从特征中解码出预测动作，计算其与用户空间约束（如轨迹）的损失，并将梯度反向传播至中间特征进行更新。
        *   **马氏距离裁剪**：为防止梯度更新使特征偏离有效分布，提出使用马氏距离进行裁剪，确保生成动作的保真度。

### 3. 实验设计
*   **数据集**：在两个主流人体动作生成数据集上进行评估：**HumanML3D** 和 **KIT-ML**。
*   **评测基准**：
    *   **文本对齐指标**：R Precision（Top1/2/3）、MM Dist、FID、多样性、多模态性。
    *   **空间对齐指标**：**StiSim**（与输入火柴人的相似度）、**Traj.Err**（生成轨迹与输入轨迹的误差）。
*   **对比方法**：
    *   **文本到动作SOTA方法**：ReMoDiffuse, T2M-GPT, MotionDiffuse, MDM, Guo et al.等。
    *   **动作编辑方法**：OmniControl, DNO, PriorMDM, CondMDI, GMD, MDM等。
    *   **自身前序工作**：StickMotion（仅支持固定位置的火柴人，无显式轨迹控制）。

### 4. 资源与算力
*   论文中明确提到：训练使用了 **4 块 NVIDIA 4090 GPU**，批次大小为1024。
*   **未明确说明**：总训练时长、单卡显存占用、推理阶段的详细硬件配置和耗时（仅给出了不同`repeat`参数下的相对时间比较）。

### 5. 实验数量与充分性
*   **实验组数**：实验设计较为全面，主要包括：
    1.  **主实验**：在两个数据集上与多个SOTA方法进行定量对比（表III, IV）。
    2.  **动作编辑任务对比**：与专门的编辑方法在轨迹误差和FID上对比（表V），并比较了内存和耗时（表VI）。
    3.  **消融实验**：
        *   条件解码器结构（点积 vs. 高效注意力）（表VII）。
        *   MCM模块结构有效性（表VIII）。
        *   条件混合策略（表IX）。
        *   输入火柴人数量的影响（表X）。
        *   IFG超参数分析（表II）和时间消耗分析（表XI）。
    4.  **用户研究**：招募20名志愿者，从时间成本和满意度两方面对比DrawMotion、StickMotion和纯文本方法。
    5.  **定性可视化**：展示了多种复杂轨迹和姿态约束下的生成结果（图7, 8）。
*   **充分性与客观性**：实验设计**较为充分和客观**。涵盖了标准评测、与各类相关方法的公平对比、详尽的模块消融、参数分析以及主观用户评估。评测指标兼顾生成质量、多样性和对输入条件的对齐度，能够全面反映方法性能。

### 6. 论文的主要结论与发现
1.  **有效性**：DrawMotion在保持与文本描述语义对齐的同时，能显著提升生成动作与手绘空间约束（轨迹、姿态）的对齐精度（StiSim, Traj.Err指标领先）。
2.  **效率**：手绘交互方式比撰写详细文本描述**平均节省约46.7%的用户时间**，且用户满意度最高。
3.  **技术贡献**：提出的MCM模块在融合多条件时更高效；发现的中间特征连续性使得免训练引导（IFG）成为可能，该引导能有效平衡约束满足与动作自然度。
4.  **性能**：在文本到动作生成任务上，DrawMotion达到了与最先进方法相当的总体性能，同时在空间控制方面具有显著优势。

### 7. 优点
1.  **创新性交互方式**：首次将手绘草图系统性地作为动作生成的条件，提供了一种直观、细粒度的空间控制接口，直击文本描述的痛点。
2.  **高效的多条件融合设计**：MCM模块设计巧妙，通过特征偏移相加和批次操作，高效实现了多种条件组合的建模，降低了计算开销。
3.  **免训练引导的洞察**：对模型中间特征空间连续性的发现与利用是一个亮点，使得可以在不增加训练成本的前提下，在推理阶段进行精确的约束优化。
4.  **全面的实验验证**：不仅进行了标准的定量评估，还通过详尽的消融实验和用户研究，扎实地验证了各个组件的有效性和整体方法的实用性。

### 8. 不足与局限
1.  **用户责任与冲突**：方法的灵活性也带来了责任转移。当用户输入的轨迹、姿态与文本描述在物理或语义上**存在严重冲突**时，模型可能无法生成合理结果，或保真度下降。论文指出这需要用户自行保证输入的合理性。
2.  **应用限制**：目前主要关注单人、无交互的原子动作生成。对于更复杂的**多人交互、人-物交互**场景，当前的手绘条件定义和模型可能无法直接适用。
3.  **算力信息不完整**：缺乏完整的训练时长和推理端到端延迟报告，不利于后续研究进行复现或成本评估。
4.  **草图表示的局限性**：依赖于预定义的火柴人模型（六条单笔画），虽然简化了输入，但也可能限制了用户表达更复杂或非标准姿态的能力。

（完）
