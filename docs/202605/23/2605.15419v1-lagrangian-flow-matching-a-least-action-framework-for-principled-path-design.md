---
title: "Lagrangian Flow Matching: A Least-Action Framework for Principled Path Design"
title_zh: 拉格朗日流匹配：一种用于原理性路径设计的最小作用量框架
authors: "Shukai Du, Junzhe Zhang, Yiming Li"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15419v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 提出拉格朗日流匹配，一种基于物理的生成建模框架。
tldr: 本文提出了一种基于物理原理的拉格朗日流匹配框架，用于改进生成模型中的概率路径设计。现有方法通常采用直线路径，限制了动力学的多样性。受经典力学最小作用量原理启发，该框架通过最小化一般拉格朗日量的作用量来联合确定概率路径和速度场，并证明了其等价于静态最优传输问题。实验表明，新框架能产生有意义的动力学变化，且性能与现有条件流匹配模型相当。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15419v1/fig-001.webp\", \"caption\": \"Figure 9: Uncurated CIFAR-10 samples from each flow-matching variant after 400,000 training steps, generated with EMA weights and the DOPRI5 adaptive solver from a fixed noise seed x0 „ N p0, Iq. The harmonic family interpolates between OT-CFM (ω Ñ 0) and OT-SI (ω “ π{2) as ω increases. OT-Aniso uses the data-fit anisotropic harmonic potential described in Sec. H.4.\", \"page\": 26, \"index\": 1, \"width\": 829, \"height\": 622}, {\"url\": \"assets/figures/arxiv/2605.15419v1/fig-002.webp\", \"caption\": \"Figure 1: Lagrangian flow matching unifies trajectory selection (top row) and probability-path construction (bottom row) under the leastaction principle, linked by the static–dynamic equivalence (Thm. 2.1). The kinetic Lagrangian (left column) yields straight-line trajectories and recovers OT-based flow matching; a general Lagrangian (right column) yields curved trajectories and induces new probability paths.\", \"page\": 2, \"index\": 2, \"width\": 574, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2605.15419v1/fig-003.webp\", \"caption\": \"Figure 4: Mini-batch harmonic flow matching (ω “ 1) from an 8-Gaussian-mixture source (olive) to a double-moon target (blue), swept over OT batch size n P t1, 10, 25, 50, 100u. Increasing n tightens the empirical coupling toward the OT solution: compare the many-to-many fan-out at n “ 1 to the bundled, mode-to-region routing at n “ 100. Trajectory curvature is set by ω and is preserved across all panels — the two axes (n for the coupling, L for the matched-pair trajectory) act independently.\", \"page\": 7, \"index\": 3, \"width\": 826, \"height\": 195}, {\"url\": \"assets/figures/arxiv/2605.15419v1/fig-004.webp\", \"caption\": \"Figure 8: Sample quality vs. inference budget. We plot the 2-Wasserstein distance W2 between generated and target samples against the number of function evaluations (NFE) at inference, across five sourceÑtarget settings (rows) and three ODE integrators (columns). OT-CFM and OT-Harmonic at ω “ 10´3 are visually indistinguishable, consistent with the harmonic interpolant degenerating to the affine one as ω Ñ 0. OT-Harmonic at ω “ 1 matches or improves on this shared baseline at every NFE budget, with the largest gains in the low-NFE regime where step error dominates. Shaded bands show ˘1 standard deviation across five training seeds.\", \"page\": 24, \"index\": 4, \"width\": 826, \"height\": 899}, {\"url\": \"assets/figures/arxiv/2605.15419v1/fig-005.webp\", \"caption\": \"Figure 6: CIFAR-10 images generated by OT-CFM (left) and OT-Harmonic (right) with frequency ω “ 0.001.\", \"page\": 9, \"index\": 5, \"width\": 542, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2605.15419v1/fig-006.webp\", \"caption\": \"Figure 3: Mini-batch harmonic flow matching trajectories from a 8-Gaussian-mixture to a doublemoon target, swept over ω P p0, π{2s and compared against OT-CFM and the stochastic interpolant. Endpoints (black) are matched by quadratic-cost OT and connected along least-action trajectories (olive); the data manifold is shown in blue. See Examples 2.4 and 2.5 for the limiting cases.\", \"page\": 5, \"index\": 6, \"width\": 826, \"height\": 217}]"
motivation: 现有流匹配方法中的概率路径设计（如直线路径）过于简单，限制了动力学的表达能力。
method: 提出拉格朗日流匹配框架，通过最小化一般拉格朗日量的作用量来联合优化概率路径和速度场。
result: 该框架推导出新的概率路径和速度场，在实验中诱导有意义的动力学变化，且性能保持竞争力。
conclusion: 拉格朗日流匹配为概率路径设计提供了基于物理原理的灵活框架，扩展了现有方法。
---

## 摘要
流匹配通过回归一个目标速度场来训练神经速度场，该目标速度场与连接简单初始分布到数据分布的指定概率路径相关联。一个核心设计选择是路径本身。现有构造，包括基于整流和最优传输的路径，沿着耦合端点之间的直线传输样本，因此仅覆盖了狭窄的动力学类别。我们观察到，这对应于经典力学中最小作用量原理的最简单情况，其中动能拉格朗日量产生自由粒子的直线轨迹。基于这一观察，我们提出了拉格朗日流匹配，这是一个基于物理的框架，其中概率路径和速度场通过最小化一般拉格朗日量的作用量来确定，同时满足连续性方程和指定的端点。我们证明这一动力学问题存在一个等效的静态最优传输（OT）表述，从而产生了一类无需模拟的训练目标，其中基于OT的流匹配作为动能特例被恢复，而三角方差保持扩散路径作为谐振子情况被恢复。更一般的拉格朗日量产生了新的概率路径和速度场，数值实验表明，它们在学习的动力学中引起了有意义的变化，同时与现有的条件流匹配模型相比仍具有竞争力。

## Abstract
Flow matching trains a neural velocity field by regression against a target velocity associated with a prescribed probability path connecting a simple initial distribution to the data distribution. A central design choice is the path itself. Existing constructions, including rectified and optimal-transport-based paths, transport samples along straight lines between coupled endpoints and thus cover only a narrow class of dynamics. We observe that this corresponds to the simplest case of the least-action principle in classical mechanics, in which the kinetic Lagrangian yields free-particle straight-line trajectories. Building on this observation, we propose Lagrangian flow matching, a physics-based framework in which the probability path and velocity field are determined by minimizing the action of a general Lagrangian subject to the continuity equation and the prescribed endpoints. We show that this dynamic problem admits an equivalent static optimal transport (OT) formulation, yielding a family of simulation-free training objectives that recover OT-based flow matching as the kinetic special case and the trigonometric variance-preserving diffusion path as the harmonic-oscillator case. More general Lagrangians give rise to new probability paths and velocity fields, and numerical experiments show that they induce meaningful changes in the learned dynamics while remaining competitive with existing conditional flow matching models.

---

## 论文详细总结（自动生成）

### **论文总结：拉格朗日流匹配**

#### **1. 核心问题与整体含义**
*   **研究动机**：现有的流匹配生成模型（如OT-CFM、Rectified Flow）在训练时，需要预先指定一个从噪声分布到数据分布的概率路径。当前主流方法（基于最优传输或独立耦合）都默认使用**端点间的直线路径**来构造该路径。这虽然简单高效，但极大地限制了可表达的动力学类别，可能并非最优。
*   **核心思想**：受经典力学中**最小作用量原理**的启发，论文提出一个更普适的框架。不再手动指定直线路径，而是通过**最小化一个广义拉格朗日量的作用量**，在满足连续性方程和端点分布的约束下，**联合推导出最优的概率路径和对应的速度场**。这为路径设计提供了一个基于物理原理的、可解释的灵活空间。

#### **2. 方法论**
*   **核心思想**：将生成建模中的概率路径设计问题，形式化为一个**拉格朗日最优传输**问题。目标是找到满足边界条件（`p0=p_init`, `p1=p_data`）的概率密度流 `{p_t}` 和速度场 `{v_t}`，以最小化作用量 `∫∫ L(x, v_t(x), t) p_t(x) dx dt`。
*   **关键技术细节**：
    1.  **静态-动态等价（Theorem 2.1）**：这是方法的理论基石。它证明上述动态优化问题，等价于一个**静态最优传输**问题，其传输成本 `c_L(x0, x1)` 恰好是拉格朗日量在两点间**最小作用量轨迹**上的积分。这使得复杂的动态优化可转化为更易处理的静态耦合问题。
    2.  **闭式轨迹与免模拟训练**：对于可分离的拉格朗日量 `L(x,v)=K(v)-V(x)`（如动能+势能），两点间的最小作用量轨迹（即**测地线**）可通过求解欧拉-拉格朗日方程得到闭式解。训练时，只需先求解静态OT得到端点配对 `(x0, x1)`，然后使用闭式轨迹 `γ(t)` 及其导数 `γ'(t)` 作为回归目标，**完全避免了在训练循环中求解ODE**，保持了流匹配的高效性。
    3.  **谐振子拉格朗日量家族**：论文重点研究了一类重要的特例：`L_ω(x,v) = 1/2||v||^2 - 1/2 ω^2 ||x||^2`。其测地线是正弦/余弦插值。参数 `ω` 控制曲率：`ω→0` 时退化为直线（恢复OT-CFM）；`ω=π/2` 时变为三角插值（联系到方差保持扩散路径）。这形成了一个连续的路径家族。

#### **3. 实验设计**
*   **数据集/场景**：
    1.  **2D合成数据**：评估路径几何性质（如8高斯混合到双月形）。
    2.  **单细胞轨迹插值**（CITE-seq， Multiome， Embryoid Body）：评估在时序数据上的分布插值能力。
    3.  **CIFAR-10图像生成**：评估在标准图像生成基准上的性能。
*   **Benchmark与对比方法**：主要与两种先进的流匹配方法对比：
    *   **OT-CFM**：基于最优传输的直线路径流匹配。
    *   **OT-SI**：基于最优传输的随机插值器（使用三角路径）。
    *   **自身变体**：论文提出的 `OT-Harmonic`（`ω=0.001, 1, π/2`）以及各向异性变体 `OT-Aniso`。

#### **4. 资源与算力**
论文在附录H.5中提及，所有实验均在 **NVIDIA H100 GPUs (80GB)** 上运行。未明确说明使用的GPU数量。对于CIFAR-10实验，训练步数为 **400,000步**（作者指出此预算低于原文对比的OT-CFM论文的500,000步，旨在所有方法间进行公平比较）。

#### **5. 实验数量与充分性**
*   **实验数量**：较为充分。在三个不同维度和性质的任务上进行了验证，每个任务都进行了多方法对比。
*   **客观性与公平性**：
    *   **控制变量**：强调所有对比方法使用相同的网络架构、优化器、训练步数和评估协议。
    *   **统计可靠性**：关键结果（如Wasserstein距离、FID）均报告了**多次随机种子实验的均值与标准差**（通常为5次）。
    *   **消融研究**：在2D实验中系统研究了**批量大小 `n`**（控制耦合近似程度）和**频率 `ω`**（控制路径曲率）的影响（见图4）。
    *   **推理成本分析**：绘制了样本质量（W2距离）与函数评估次数（NFE）的关系曲线，全面评估了不同ODE求解器下的性能-效率权衡。

#### **6. 主要结论与发现**
1.  **框架有效性**：拉格朗日流匹配框架成功地将基于物理的路径设计引入流匹配，能够生成**有意义且非平凡的弯曲概率路径**。
2.  **恢复现有方法**：理论证明了OT-CFM（`ω→0`）和三角VP路径（`ω=π/2`）是该框架的特例，并通过实验验证了数值上的连续性。
3.  **性能表现**：
    *   在**2D合成数据**上，`ω=1` 的谐波流通常能取得比两个端点基线（OT-CFM, OT-SI）更好的分布匹配效果。
    *   在**单细胞插值**和**CIFAR-10**上，`ω→0`（近似OT-CFM）的变体表现最为稳健和优秀。
    *   **没有单一的 `ω` 在所有任务上占优**，这表明拉格朗日量是一种**归纳偏置**，其最优选择依赖于数据几何。
4.  **各向异性潜力有限**：在CIFAR-10上，基于PCA拟合的各向异性势能并未带来显著增益，推测是因为图像像素空间缺乏强的低维几何结构。

#### **7. 优点**
*   **原理驱动**：为生成模型中的路径设计提供了一个优雅的、基于经典物理原理的理论框架，提升了可解释性。
*   **理论严谨**：建立了静态-动态等价定理，将创新想法扎实地落地为可计算的训练目标。
*   **保持高效**：尽管框架更通用，但通过利用闭式测地线，**保留了流匹配“免模拟训练”的核心计算优势**。
*   **统一视角**：统一并解释了多种现有路径设计（直线、三角），并将其纳入一个连续谱中。
*   **实验全面**：评估层次清晰，从可视化分析到标准基准，从路径能量到样本质量，验证维度多样。

#### **8. 不足与局限**
*   **拉格朗日量形式受限**：目前主要适用于可分离形式 `L=K-V` 且能给出闭式测地线的拉格朗日量，限制了框架的完全通用性。
*   **势能 `V` 是预设而非学习的**：论文中的势能（如谐振子频率 `ω`、各向异性矩阵 `A`）是作为超参数预设或通过简单启发式方法（如PCA）拟合的。**联合学习势能与速度场**是一个未被探索但更有潜力的方向。
*   **仅限确定性动力学**：框架基于连续性方程，属于**确定性流**模型。如何将其扩展至**随机动力学**（如连接薛定谔桥或扩散模型）是未来的工作。
*   **实验覆盖范围**：虽然包含了图像生成，但仅限于CIFAR-10。在更高分辨率图像（如ImageNet）、文本到图像生成或其他模态（如视频、分子）上的有效性尚未验证。
*   **最优拉格朗日量的选择**：如何根据数据或任务自动选择或学习最合适的拉格朗日量/势能，目前仍是一个开放问题。

（完）
