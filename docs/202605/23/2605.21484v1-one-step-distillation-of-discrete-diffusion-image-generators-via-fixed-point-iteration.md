---
title: One-Step Distillation of Discrete Diffusion Image Generators via Fixed-Point Iteration
title_zh: 基于定点迭代的离散扩散图像生成器一步蒸馏法
authors: "Chaoyang Wang, Yunhai Tong"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21484v1"
tags: ["query:3d-motion-generation"]
score: 8.0
evidence: 离散扩散模型蒸馏用于高效生成
tldr: 离散扩散模型在图像生成方面表现出色，但其解码过程缓慢且需要迭代。现有的一步蒸馏方法要么需要训练额外的分数网络增加计算量，要么引入复杂的参数化和多阶段流程导致优化碎片化。本文提出了固定点蒸馏（FPD），一种端到端框架，通过部分破坏学生模型的一步草稿并用教师模型单步精炼来构建局部校正目标。为了在语义空间计算训练目标，将离散标记提升为连续特征并应用多带宽漂移损失迭代累积校正。通过直通估计器在离散瓶颈处反向传播，确保训练和推理在相同码本流形上进行，同时将连续梯度传回学生逻辑值。该框架还可选择性地结合无条件对抗目标以增强感知真实性。在类别和文本条件生成上的评估验证了该框架的有效性。FPD在单步推理中实现了有竞争力的视觉保真度和结构对齐，缩小了与多步教师模型的差距，同时优于现有离散蒸馏基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21484v1/fig-001.webp\", \"caption\": \"Figure 3: Training strategy comparisons. We compare the multi-step teacher baselines with our singlestep FPD variants, demonstrating the superiority of the Straight-Through Estimator in preserving structural integrity, and the perceptual enhancements introduced by the auxiliary GAN loss.\", \"page\": 8, \"index\": 1, \"width\": 822, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2605.21484v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of the proposed distillation framework. The student model processes a masked initialization zinit to output logits ℓθ. Via a Straight-Through Estimator (STE), these discrete outputs are decoded into a continuous image xθ and mapped to a feature space by a frozen backbone Φ. Concurrently, the student’s token draft is partially re-masked (Mr) and refined by the frozen teacher. The decoded teacher features serve as positive targets, while cyclically shifted in-batch student features act as negatives to compute the drift loss Ldrift. An auxiliary GAN loss Lgan is applied to xθ for enhanced perceptual quality. Black lines denote forward computation paths, while red lines highlight the differentiable gradient flow that safely bypasses the discrete bottleneck. The backbone and decoder share the same weights.\", \"page\": 4, \"index\": 2, \"width\": 816, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2605.21484v1/fig-003.webp\", \"caption\": \"Table 1: Comparison of various text-to-image generation models on the GenEval benchmark. The symbols ↑ and ↓ indicate that higher and lower scores are preferable, respectively. “#Model Params” is the inference parameter count.\", \"page\": 7, \"index\": 3, \"width\": 826, \"height\": 465}, {\"url\": \"assets/figures/arxiv/2605.21484v1/fig-004.webp\", \"caption\": \"Table 2: Quantitative results on class-conditional ImageNet-256 with MaskGit teacher.\", \"page\": 7, \"index\": 4, \"width\": 627, \"height\": 399}, {\"url\": \"assets/figures/arxiv/2605.21484v1/fig-005.webp\", \"caption\": \"Table 4: Feature space ablations on GenEval. (a) Representation space and kernel bandwidths for the drift objective. (b) Layer selection and spatial granularity of the frozen feature backbone (DINOv3-B, 12 layers). Patch Grid indicates whether features are kept on a 4×4 spatial grid or globally average-pooled into a single vector.\", \"page\": 9, \"index\": 5, \"width\": 785, \"height\": 181}]"
motivation: 解决离散扩散模型解码慢、现有一步蒸馏方法计算开销大或优化碎片化的问题。
method: 提出固定点蒸馏（FPD），通过部分破坏学生草稿并用教师单步精炼构建校正目标，结合多带宽漂移损失和直通估计器实现端到端训练。
result: FPD在单步推理中达到有竞争力的视觉保真度和结构对齐，优于现有离散蒸馏基线。
conclusion: FPD有效缩小了单步生成与多步教师模型的性能差距，为离散扩散模型的高效生成提供了新方案。
---

## 摘要
离散扩散模型在视觉合成方面表现出色，但依赖于缓慢的迭代解码。现有的一步蒸馏方法试图绕过这一瓶颈，要么通过训练辅助评分网络（这实际上使计算量翻倍），要么通过引入专门的参数化和多阶段流水线（这会分散优化过程）。本文提出定点蒸馏（FPD），这是一种端到端框架，通过部分破坏学生模型的一步草稿并用单步教师模型进行精炼，从而构建局部修正目标。为了在语义有意义的空间中计算训练目标，我们将离散标记提升为连续特征，并应用多带宽漂移损失来迭代累积这些修正。为了通过离散瓶颈进行反向传播，我们采用直通估计器，在前向传播期间将精确硬采样标记馈送给教师模型和解码器，确保训练和推理在相同的码本流形上进行，同时将连续梯度路由回学生逻辑值。这种完全可微的路径还容纳了一个可选的无条件对抗目标，以增强感知真实性。在类别条件和文本条件生成上的评估验证了我们框架的有效性。FPD在单步推理中实现了具有竞争力的视觉保真度和结构对齐，缩小了与多步教师模型的差距，同时优于现有的离散蒸馏基线。

## Abstract
Discrete diffusion models excel at visual synthesis but rely on slow, iterative decoding. Existing single-step distillation methods attempt to bypass this bottleneck, either by training auxiliary score networks that effectively double compute, or by introducing specialized parameterizations and multi-stage pipelines that fragment optimization. In this paper, we introduce Fixed-Point Distillation (FPD), an end-to-end framework that constructs local correction targets by partially corrupting the student's one-step draft and refining it with a single teacher step. To compute the training objective in a semantically meaningful space, we lift discrete tokens into continuous features and apply a multi-bandwidth drift loss that iteratively accumulates these corrections. To backpropagate through the discrete bottleneck, we employ a straight-through estimator that feeds exact hard-sampled tokens to the teacher and decoder during the forward pass, ensuring that training and inference operate on the same codebook manifold, while routing continuous gradients back to the student logits. This fully differentiable pathway additionally accommodates an optional unconditional adversarial objective to enhance perceptual realism. Evaluations on both class- and text-conditional generation validate the effectiveness of our framework. FPD achieves competitive visual fidelity and structural alignment within a single inference step, narrowing the gap to multi-step teachers while outperforming existing discrete distillation baselines.