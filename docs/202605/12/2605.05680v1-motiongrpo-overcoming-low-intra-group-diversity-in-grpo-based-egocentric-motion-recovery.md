---
title: "MotionGRPO: Overcoming Low Intra-Group Diversity in GRPO-Based Egocentric Motion Recovery"
title_zh: MotionGRPO：克服基于 GRPO 的第一人称视角运动恢复中组内多样性低的问题
authors: "Nanjie Yao, Junlong Ren, Wenhao Shen, Hao Wang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05680v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 使用基于扩散的方法和强化学习后训练进行全身3D人体动作恢复
tldr: 本研究针对头戴设备信号恢复3D人体动作时局部关节精度不足的问题，提出了MotionGRPO框架。该框架将扩散模型采样建模为马尔可夫决策过程，并利用强化学习算法GRPO进行后训练优化。通过引入混合奖励机制和噪声注入策略，有效解决了策略优化中的梯度消失问题，显著提升了动作恢复的视觉逼真度与精确度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05680v1/fig-001.webp\", \"caption\": \"Figure 2. Method Overview. Given the input head trajectory signals H1:T cpf from HMD, we employ them as conditions for a motion diffusion model to recover human motion. To address low intra-group diversity, we obtain diverse motion outputs through SDE-based sampling and noise injection on trajectory conditions. Based on these outputs, we utilize a proposed hybrid reward mechanism for comprehensive reward calculation: at global visual level, a trajectory-conditioned perceptual model via spatial-temporal attention assesses visual plausibility; at the local joint level, we enforce explicit joint constraints to align generated results with GT. Finally, we calculate advantages and use GRPO to update the model parameters, providing guidance for both visual plausibility and joint accuracy.\", \"page\": 3, \"index\": 1, \"width\": 996, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2605.05680v1/fig-002.webp\", \"caption\": \"Figure 4. Qualitative Comparison and Visualization. The first row presents a qualitative comparison with the most competitive baseline on the AMASS dataset. Red boxes highlight failure cases, such as ground penetration and inaccurate joint positions. The second row visualizes the results of MOTIONGRPO on the ADT dataset, demonstrating its generalization capability to real-world scenarios.\", \"page\": 8, \"index\": 2, \"width\": 1017, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2605.05680v1/fig-003.webp\", \"caption\": \"Figure 5. Additional Qualitative Comparisons. We provide more qualitative comparisons with the most competitive baseline.\", \"page\": 12, \"index\": 3, \"width\": 1017, \"height\": 488}]"
motivation: 现有的基于扩散的方法在从头戴设备信号恢复动作时，往往因侧重全局分布匹配而导致局部关节重建精度较低。
method: 提出MotionGRPO框架，结合混合奖励机制与噪声注入策略，利用GRPO算法对扩散采样过程进行强化学习优化。
result: 实验结果表明，MotionGRPO在保持高视觉保真度的同时，达到了当前最先进的性能水平。
conclusion: 通过在扩散模型中引入强化学习指导和多样性增强策略，可以有效提升第一人称视角下人体动作恢复的精确性。
---

## 摘要
本文研究了从头戴式设备信号中恢复全身 3D 人体运动。现有的基于扩散的方法通常依赖于全局分布匹配，导致局部关节重建误差。我们提出了 MotionGRPO，这是一个利用强化学习后训练将细粒度引导注入扩散过程的新型框架。在技术上，我们将扩散采样建模为一个通过组相对策略优化（GRPO）进行优化的马尔可夫决策过程。为此，我们引入了一种混合奖励机制，结合了用于全局视觉合理性的学习条件感知模型和用于局部关节精度的显式约束。我们的核心技术见解是，基于扩散的恢复中的策略优化由于有限的组内样本多样性而面临梯度消失问题。为了解决这个问题，我们进一步引入了一种噪声注入策略，显式地增加了样本方差并稳定了学习。大量实验表明，MotionGRPO 实现了最先进的性能，并具有卓越的视觉保真度。

## Abstract
This paper studies full-body 3D human motion recovery from head-mounted device signals. Existing diffusion-based methods often rely on global distribution matching, leading to local joint reconstruction errors. We propose MotionGRPO, a novel framework leveraging reinforcement learning post-training to inject fine-grained guidance into the diffusion process. Technically, we model diffusion sampling as a Markov decision process optimized via Group Relative Policy Optimization (GRPO). To this end, we introduce a hybrid reward mechanism that combines a learned conditioned perceptual model for global visual plausibility and explicit constraints for local joint precision. Our key technical insight is that policy optimization in diffusion-based recovery suffers from vanishing gradients due to limited intra-group sample diversity. To address this, we further introduce a noise-injection strategy that explicitly increases sample variance and stabilizes learning. Extensive experiments demonstrate that MotionGRPO achieves state-of-the-art performance with superior visual fidelity

---

## 论文详细总结（自动生成）

这是一份关于论文《MotionGRPO: Overcoming Low Intra-Group Diversity in GRPO-Based Egocentric Motion Recovery》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：从头戴式设备（HMD，如 Project Aria）的稀疏信号（仅头部轨迹）恢复全身 3D 人体运动是 VR/AR 领域的关键挑战。
*   **核心问题**：
    *   **局部精度不足**：现有的基于扩散模型（Diffusion）的方法侧重于全局分布匹配，导致局部关节重建误差大。
    *   **视觉伪影**：生成的动作常伴有滑步（Foot Skating）、动作抖动（Jitter）和地面穿透（Ground Penetration）等物理不合理现象。
    *   **RL 优化难题**：直接将强化学习（RL）应用于此类重建任务时，由于输入条件（头部轨迹）极强，导致生成的候选样本组内多样性极低，使得 GRPO 算法因优势函数计算失效而出现梯度消失。

### 2. 方法论
*   **核心思想**：将扩散模型的采样过程建模为马尔可夫决策过程（MDP），利用 **GRPO（组相对策略优化）** 进行后训练（Post-training），通过混合奖励机制引导模型生成既符合物理规律又精确对齐关节的动作。
*   **关键技术细节**：
    *   **混合奖励机制（Hybrid Reward）**：
        *   **视觉级奖励**：训练一个基于 Transformer 的轨迹条件感知模型，通过在线对比学习（主动合成硬负样本）来识别滑步、抖动等伪影。
        *   **关节级奖励**：设定显式的几何约束，包括旋转误差（Rrot）、位置误差（Rpos）、对齐后位置误差（R'pos）和速度误差（Rvel）。
    *   **噪声注入策略（Noise-Injection）**：为了解决“组内多样性低”的问题，在训练阶段向输入条件（头部轨迹）注入时间平滑的 **Perlin 噪声**。这模拟了分布外输入，增加了模型输出的方差，从而稳定了 GRPO 的优势函数计算。
    *   **算法流程**：基于 SDE 的采样生成多个候选动作组，计算组内相对奖励（Advantage），通过 GRPO 更新扩散模型参数。

### 3. 实验设计
*   **数据集/场景**：
    *   **AMASS**：主要训练集和评估集，包含大规模人体运动捕捉数据。
    *   **RICH**：用于评估模型在复杂场景（如地面交互）下的泛化能力。
    *   **ADT (Aria Digital Twins)**：用于真实世界场景的定性评估。
*   **Benchmark 与对比方法**：
    *   对比了 **EgoEgo** 和 **EgoAllo**（当前 SOTA）等基于扩散的方法。
    *   在消融实验中对比了 **直接微调（FT）** 和 **直接偏好优化（DPO）** 等后训练范式。

### 4. 资源与算力
*   **硬件环境**：服务器配备 AMD EPYC 7542 处理器和 **8 台 NVIDIA RTX A6000 GPU (48GB VRAM)**。
*   **训练时长**：
    *   **感知模型预训练**：约 8 GPU 小时，占用 47.2 GB 显存。
    *   **GRPO 后训练**：约 **72 GPU 小时**（约 2000 次迭代/3 个 Epoch），占用 47.5 GB 显存。
*   **推理效率**：推理时关闭噪声注入和奖励模型，速度与基础扩散模型持平（约 1.14s 处理 128 帧）。

### 5. 实验数量与充分性
*   **实验规模**：在 AMASS 和 RICH 两个主流数据集上进行了详尽的定量评估。
*   **消融实验**：涵盖了 Vanilla GRPO、视觉奖励、Perlin 噪声注入、硬负样本合成等多个模块的有效性验证。
*   **客观性**：通过 5 次随机种子运行取平均值，并对比了多种后训练范式（FT, DPO），实验设计较为严谨、公平，充分证明了所提策略在解决多样性坍塌方面的必要性。

### 6. 主要结论与发现
*   **性能提升**：MotionGRPO 在所有指标上均刷新了 SOTA。在 AMASS 上，MPJPE 从 124.9mm 降至 114.2mm；在视觉质量上，滑步和地面穿透现象显著减少。
*   **多样性是关键**：实验证实，在受限的重建任务中，人为引入噪声以提升组内多样性是 RL 优化成功的先决条件。
*   **奖励协同**：关节级奖励提升精度，视觉级奖励滤除伪影，两者结合优于单一奖励。

### 7. 优点
*   **创新性解决 RL 瓶颈**：针对重建任务中 GRPO 梯度消失的问题，提出了简单有效的噪声注入方案。
*   **细粒度控制**：通过后训练而非重新训练，成功将非微分的几何约束注入到预训练的扩散模型中。
*   **视觉保真度高**：引入的感知模型能有效捕捉标准损失函数难以量化的视觉瑕疵（如滑步）。

### 8. 不足与局限
*   **环境假设**：目前假设地面为平坦平面，无法处理斜坡、楼梯或复杂的家具交互。
*   **训练成本**：GRPO 需要在训练时进行大量采样，计算开销显著高于标准监督学习。
*   **泛化范围**：虽然在 Transformer 架构上验证有效，但尚未在其他扩散骨干（如 U-Net 或 Flow-matching 模型）上进行广泛测试。

（完）
