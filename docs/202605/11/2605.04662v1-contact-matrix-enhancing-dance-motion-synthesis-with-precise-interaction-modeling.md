---
title: "Contact Matrix: Enhancing Dance Motion Synthesis with Precise Interaction Modeling"
title_zh: 接触矩阵：通过精确交互建模增强舞蹈动作合成
authors: "Xuhai Chen, Zhi Cen, Huaijin Pi, Sida Peng, Xiaowei Zhou, Yong Liu"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04662v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 舞蹈动作合成与逼真人体运动数据生成
tldr: 本研究针对双人舞中反应性动作生成的挑战，提出了一种新型两阶段框架。第一阶段通过带有独立身体部位编码器和联合解码器的VQ-VAE增强动作表征能力；第二阶段引入感知接触的扩散模型，通过联合生成动作与人际接触矩阵，实现显式的交互建模。实验证明，该方法在动作质量、交互保真度和节奏同步性方面均显著优于现有模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04662v1/fig-001.webp\", \"caption\": \"Figure 1. We tackle interaction-aware reactive motion generation. First, we introduce (b) PartFusion-VQ to prevent unnatural poses, as highlighted by the green circle in (a) Duolando [45]. Second, in (c), we incorporate a contact matrix to guide motion refinement, correcting unrealistic joint configurations (red circles).\", \"page\": 1, \"index\": 1, \"width\": 458, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2605.04662v1/fig-002.webp\", \"caption\": \"Figure 2. Architecture of PartFusion-VQ. Js, Es, and Zs (s ∈ {U,D,L,R}) represent the joint number, encoder, and codebook of each body part (upper body, lower body, left hand, right hand), respectively. T and T ′ denote the input and encoded sequence lengths. C is the feature dimension, and Dp, Dr , Dg denote the decoders for 3D joint positions, rotations, and global translation.\", \"page\": 4, \"index\": 2, \"width\": 496, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2605.04662v1/fig-003.webp\", \"caption\": \"Figure 3. Overview of the pipeline. We propose a diffusion model named RCDiff to generate reactive motion in latent space. Em denotes the four PartFusion-VQ encoders; Ed, Ec, and Ea denote the encoders for global trajectory, contact matrix, and music, respectively. Dm, Dd, and Dc denote the corresponding decoders. All encoders and decoders except Ea are frozen during diffusion training.\", \"page\": 5, \"index\": 3, \"width\": 727, \"height\": 364}]"
motivation: 针对双人舞中高质量数据稀缺、动作模式复杂以及人际交互细节难以精确建模的问题。
method: 提出一种结合了多部位VQ-VAE和感知接触扩散模型的两阶段框架，通过接触矩阵引导精确的交互动力学。
result: 实验结果显示，该方法在FID和BED等关键指标上均优于Duolando，显著提升了动作的真实感和同步性。
conclusion: 通过引入接触矩阵进行显式交互建模，该框架有效解决了双人舞生成中的约束难题，为复杂人体交互合成提供了新思路。
---

## 摘要
生成逼真的反应性动作（即一个人对他人固定动作做出反应）具有挑战性，因为存在严格的交互约束和有限的可行解空间。本文关注一个典型场景：双人舞，该场景下高质量数据稀缺，动作模式复杂，且人体交互细节错综复杂且丰富。为了应对这些挑战，我们提出了一个新颖的两阶段框架。在第一阶段，我们引入了一种具有独立身体部位编码器和联合解码器的动作 VQ-VAE，通过专用码本增强表示能力，同时在解码过程中动态建模身体部位间的依赖关系，从而防止生成的动作出现不一致。在第二阶段，我们提出了一种用于反应性动作生成的接触感知扩散模型，该模型联合生成动作和个体间的接触矩阵，实现了显式的交互建模，并在采样过程中为更精确且受约束的交互动力学提供引导。实验表明，我们的方法优于 Duolando，具有更低的 FID_k (8.89 vs. 25.30) 和 FID_cd (8.01 vs. 9.97)，以及更高的 BED (0.4606 vs. 0.2858)，表明交互保真度和节奏同步性得到了提升。

## Abstract
Generating realistic reactive motions, in which one person reacts to the fixed motions of others, is challenging due to strict interaction constraints and a limited feasible solution space. This paper focuses on a typical scenario: duet dance, where high-quality data is scarce, motion patterns are complex, and the details of human interactions are both intricate and abundant. To tackle these challenges, we propose a novel two-stage framework. In the first stage, we introduce a motion VQ-VAE with separate body-part encoders and a joint decoder, enabling specialized codebooks to enhance representation capacity while dynamically modeling dependencies across body parts during decoding, thereby preventing inconsistencies in the generated motions. In the second stage, we propose a contact-aware diffusion model for reactive motion generation that jointly generates motion and a contact matrix between individuals, enabling explicit interaction modeling and providing guidance toward more precise and constrained interaction dynamics during sampling. Experiments show that our method outperforms Duolando with lower $\text{FID}_k$ (8.89 vs. 25.30) and $\text{FID}_{cd}$ (8.01 vs. 9.97), as well as a higher BED (0.4606 vs. 0.2858), indicating improved interaction fidelity and rhythmic synchronization.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Contact Matrix** 的新型两阶段框架，旨在解决双人舞蹈中“反应性动作生成”（Reactive Motion Generation）的难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在给定“领舞者”动作和背景音乐的情况下，生成一个既符合物理规律、又与领舞者有精确肢体交互的“跟舞者”动作。
*   **研究背景**：
    *   **交互约束严苛**：双人舞涉及频繁且复杂的身体接触，微小的位置偏差都会导致穿模或接触落空，破坏真实感。
    *   **数据稀缺性**：高质量的双人交互运动捕获数据远少于单人动作数据。
    *   **现有方法缺陷**：以往方法要么将身体部位拆分导致整体动作不协调（如“断手”现象），要么忽略了显式的接触建模，导致交互显得“虚假”。

### 2. 方法论：核心思想与关键技术
论文提出了一个两阶段框架，核心在于**局部细节的独立表征**与**全局交互的显式引导**。

*   **第一阶段：PartFusion-VQ（部位融合向量量化自动编码器）**
    *   **核心思想**：针对数据稀缺问题，采用分部位编码以增强表达能力，但通过“共享解码器”来保证全身协调。
    *   **技术细节**：将人体分为上身、下身、左手、右手四个部分，分别使用独立的编码器和码本（Codebook）。在解码阶段，将四个部位的离散特征融合后输入统一的解码器，重建 3D 关节位置、旋转和全局位移。
*   **第二阶段：RCDiff（接触感知扩散模型）**
    *   **核心思想**：在潜在空间（Latent Space）中联合生成动作、轨迹和**接触矩阵（Contact Matrix）**。
    *   **关键技术**：
        1.  **接触矩阵建模**：定义一个 $T \times 23 \times 23$ 的矩阵，显式记录跟舞者与领舞者关节间的接触状态。
        2.  **接触引导采样（Contact-guided Sampling）**：在扩散模型的推理（Denoising）过程中，利用预测的接触矩阵作为约束项。如果矩阵显示某两个关节应接触，则通过梯度修正引导模型减小这两个关节的空间距离。
    *   **输入条件**：领舞者动作特征、音乐音频特征（通过 Librosa 提取并经 Transformer 编码）。

### 3. 实验设计
*   **数据集**：**DD100**。这是一个高质量双人舞数据集，包含 10 种舞种，总计约 3.24 小时录制数据，采用 SMPL-X 模型表示。
*   **Benchmark 与对比方法**：
    *   **对比方法**：Duolando（目前最强的双人舞生成模型）、InterFormer（基于 Transformer 的交互模型）、ReGenNet（基于扩散模型的反应生成模型）。
    *   **评估指标**：FID（动作真实感）、Diversity（多样性）、FID_cd（交互距离保真度）、BED（节奏同步度）、BAS（音乐对齐度）以及 CF（接触频率）。

### 4. 资源与算力
*   **训练配置**：
    *   PartFusion-VQ 训练了 500 个 epoch，batch size 为 128。
    *   RCDiff 扩散模型训练了 150 个 epoch，batch size 为 16。
    *   优化器使用 Adam 和 AdamW。
*   **算力说明**：**论文未明确说明具体的 GPU 型号、数量及总训练时长**。这在复现性评估上是一个小缺憾。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量对比**：在 DD100 全集上进行了详尽的指标测试，结果显示其 FID_k 指标（8.89）远优于 Duolando（25.30）。
    *   **消融实验**：分别验证了 PartFusion-VQ（对比单 VQ 和纯独立 VQ）和接触矩阵（对比无接触信息、无引导采样）的有效性。
    *   **用户研究**：邀请 25 名不同背景的志愿者对生成动作的自然度、协调性进行主观评分。
*   **充分性评价**：实验设计较为全面，涵盖了从底层重建误差到高层感知质量的多个维度，消融实验有力地证明了各模块的贡献，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **接触矩阵是关键**：显式预测接触状态并将其用于推理引导，能显著提升交互的物理真实感，减少“空气舞”现象。
*   **部位融合优于完全拆分**：PartFusion-VQ 成功解决了部位独立建模带来的关节畸形问题，同时保留了对复杂动作的建模精度。
*   **扩散模型优势**：相比于 Duolando 使用的 GPT 类自回归模型，基于扩散模型的 RCDiff 在处理长序列稳定性和动作平滑度上表现更好。

### 7. 优点与亮点
*   **创新性建模**：首次在双人反应性动作生成中引入“接触矩阵”并实现推理引导。
*   **结构设计巧妙**：PartFusion-VQ 兼顾了局部灵活性与全局一致性，有效应对了小样本复杂动作的学习难题。
*   **交互质量高**：在 BED（节奏同步）和 FID_cd（交互保真度）上的提升非常显著，生成的动作更具“双人一体”的视觉效果。

### 8. 不足与局限
*   **手部简化**：为了降低计算复杂度，接触矩阵将手指关节合并为一个手部关节，这限制了对极精细交互（如指尖触碰）的建模。
*   **数据依赖**：虽然 PartFusion-VQ 缓解了数据压力，但模型性能仍高度依赖于 DD100 这种高质量标注数据，在更泛化的场景下表现未知。
*   **实时性未讨论**：扩散模型的多步迭代采样通常计算开销较大，论文未讨论该框架在 AR/VR 等需要实时交互场景下的推理延迟。

（完）
