---
title: Latent Process Generator Matching
title_zh: 潜在过程生成器匹配
authors: "Lukas Billera, Hedwig Nora Nordlinder, Ben Murrell"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.20547v1"
tags: ["query:3d-motion-generation"]
score: 8.0
evidence: 针对流匹配和扩散风格生成模型的通用框架
tldr: 现有流匹配和扩散生成模型常依赖辅助随机动力学训练，但辅助状态在生成时难以采样。本文提出潜在过程生成器匹配框架，将观测状态视为可处理马尔可夫过程的确定性投影，并学习图像空间上具有相同单时间边际分布的随机过程生成器。该方法统一并推广了离散潜在过程结果，将生成器匹配从静态潜在变量扩展到丰富的时间相关潜在条件过程。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20547v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1625, \"height\": 1119, \"label\": \"Figure\"}]"
motivation: 现有生成模型依赖辅助动态导致生成阶段不可采样，需统一框架处理时间依赖潜在条件。
method: 将观测状态定义为可处理马尔可夫过程的确定性投影，并学习图像空间生成器以匹配投影过程的单时间边际分布。
result: 框架统一并推广了离散潜在过程结果，扩展了生成器匹配至时间相关潜在条件过程。
conclusion: 提出潜在过程生成器匹配，为复杂潜在条件生成提供一般性理论支持。
---

## 摘要
许多最近的流匹配和扩散类生成模型在训练过程中依赖于辅助随机动力学：模拟一个更丰富的过程来定义条件目标，但辅助状态在生成时要么难以采样，要么根本不是期望输出的一部分。现有的生成器匹配理论形式化了静态潜在随机变量的条件化，并且最近的一些论文证明了特定增广状态构造的投影结果的特例。我们引入了潜在过程生成器匹配，这是一个通用框架，将观测到的生成状态视为一个可处理的马尔可夫过程 $Y_t$ 的确定性像 $X_t=Φ(Y_t)$。我们表明，在这种设置下，可以在像空间上学习一个随机过程的生成器，该生成器具有与投影过程相同的一时间边际分布。这概括并包含了文献中的离散潜在过程结果，并将生成器匹配从静态潜在变量扩展到丰富的时间依赖潜在条件过程家族。

## Abstract
Many recent flow-matching and diffusion-style generative models rely on auxiliary stochastic dynamics during training: a richer process is simulated to define conditional targets, but the auxiliary state is either intractable to sample at generation time or simply not part of the desired output. Existing Generator Matching theory formalises conditioning on static latent random variables, and several recent papers prove special cases of projection results for particular augmented-state constructions. We introduce latent process generator matching, a general framework that treats the observed generative state as a deterministic image $X_t=Φ(Y_t)$ of a tractable Markov process $Y_t$. We show that in this setting one may learn the generator of a stochastic process on the image space which has the same one-time marginal distributions as the projected process. This generalizes and subsumes the discrete latent process results from the literature, and extends Generator Matching from static latent variables to a rich family of time-dependent latent conditional processes.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

现有流匹配（flow matching）和扩散类生成模型在训练时常常通过模拟一个更丰富的随机过程（称为辅助动力学）来定义条件训练目标。然而，该辅助状态在生成阶段要么难以采样，要么不是最终输出的一部分。此前对于这类“投影”（projection）情况的理论证明都是针对特定结构（如离散潜在变量）逐一给出的，缺乏统一框架。本文旨在提出一个通用理论——**潜在过程生成器匹配**（Latent Process Generator Matching），将生成器匹配方法从静态潜在变量扩展至任意时间依赖的潜在过程，并证明通过合适的投影映射，可以在观测空间学习一个具有与投影过程相同的一时间边际分布的生成器，从而统一并推广了多个现有工作（Edit Flows、OneFlow、Flowception、Branching Flows、Any-Order Masked Diffusion等）。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将观测的生成状态 $X_t$ 视为一个可处理马尔可夫过程 $Y_t$ 经过确定性映射 $\Phi$ 的像，即 $X_t = \Phi(Y_t)$。在假设 $Y_t$ 是Feller过程、$\Phi$ 可测且满足一定正则性条件下，可以定义一个在像空间 $X$ 上的生成器 $L_t$，使得 $X_t$ 的一时间边际分布与 $\Phi(Y_t)$ 相同。
- **关键技术细节**：
  - **Pushforward Kolmogorov Forward Equation**（定理3.2）：给出 $L_t f(x_t) = \mathbb{E}[W_t(f\circ\Phi)(Y_t) \mid \Phi(Y_t)=x_t]$，并证明它满足KFE，从而像空间的过程具有与投影过程相同的边际分布。
  - **线性参数化**（定义3.1、3.8）：生成器可表示为 $L_t f(x) = \langle K_{t,x} f, F_t(x)\rangle$，其中$F_t(x)$是可学习的参数向量。 
  - **潜在过程条件生成器匹配损失**（定义3.10）：利用条件生成器 $L_t^{y_t}$ 及其线性参数化 $F_t^{y_t}$，通过Bregman散度构建损失 $\mathcal{L}_{\text{cgm}}$，并证明梯度等价于边际生成器损失 $\mathcal{L}_{\text{gm}}$（定理3.12）。因此训练时仅需从联合过程 $(X_t, Z_t)$ 采样，使用当前潜在状态 $Z_t$ 计算目标，但网络只接收 $X_t$ 作为输入。
- **示例**：一维切换扩散（§3.7.1）展示了离散潜在过程如何被整合。蛋白结构生成（§4）展示了非投影映射（链级刚体运动与残基内部自由度分离）的潜在应用。

### 3. 实验设计：使用的数据集/场景、benchmark、对比方法

论文在“实验”方面主要提供了两个定性/概念性示例：
- **一维切换扩散示例**（§3.7.1）：固定端点 $x_0, x_1$，$Z_t$ 为 $\{\pm1\}$ 的CTMC，$X_t$ 为布朗桥方向随 $Z_t$ 切换。没有使用真实数据集，仅用于验证理论——训练网络仅输入 $X_t$，生成时无需 $Z_t$，仍能匹配目标分布。没有对比其他方法。
- **蛋白结构生成**（§4）：提出一个利用非投影 $\Phi$（链旋转+内部旋转）的框架，但并未实现或实验，仅作为未来工作的概念设计。
- **未提及标准benchmark数据集**（如CIFAR、MNIST等）或与Flow Matching、Diffusion等方法的量化对比。因此实验部分非常薄弱，理论验证为主。

### 4. 资源与算力

论文**没有提及**任何关于GPU型号、数量、训练时长或计算资源的信息。所有示例都是低维或概念性的，不需要大规模算力。

### 5. 实验数量与充分性

- **实验数量**：仅有一个一维数值验证例（图1），以及一个未付诸实施的蛋白生成概念例。没有消融实验、多数据集对比、大规模生成质量评估。
- **充分性**：实验极其有限，仅能说明理论在简化设定下可行，远不足以证明方法在实际生成任务中的有效性。公平性和客观性无法评估（因为没有对比基线）。论文的主要贡献是理论，而非实证验证。

### 6. 论文的主要结论与发现

- 正式提出潜在过程生成器匹配框架，统一并推广了多个先前的特例（Edit Flows等）。
- 证明只要投影映射 $\Phi$ 满足一定条件，可以在像空间学习一个与投影过程边际匹配的生成器，且训练可通过条件生成器匹配损失实现梯度保真。
- 发现即使 $\Phi$ 光滑、底过程正则，推前过程也可能不满足KFE唯一性（例3.7.2），因此需要额外假设（Assumption A.19）。
- 提供一个一维示例验证了方法有效性，展示潜在离散过程被成功边际化，生成器仅在观测空间运行。

### 7. 优点：方法或实验设计上的亮点

- **理论统一性**：将先前零散的特例统一在一个通用数学框架下（Feller过程、线性参数化、Bregman散度），使方法可适用于连续、离散、流形值等多种潜在过程。
- **泛化能力**：不仅支持投影映射（$\Phi=\pi_X$），还支持一般非投影映射，如蛋白结构的链级刚体运动，打开新应用可能。
- **梯度保真**：严格证明条件损失与边际损失梯度相等，因而训练时无需知道难以计算的边际目标。
- **概念示例**：一维切换扩散直观展示了潜在过程如何被整合，理论分析清晰。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验严重不足**：缺乏标准生成任务（图像、分子、蛋白质）的实验验证，未与Flow Matching、Diffusion、Generator Matching等进行性能和计算效率对比。仅有玩具例，无法评估实际效能。
- **理论假设限制**：需要被推前过程 $Y_t$ 满足Feller性、$\Phi$ 使 $f\circ\Phi$ 处于生成元定义域、KFE唯一性等条件，这些在实际复杂模型中可能不成立或难以验证。例3.7.2就显示即使是光滑映射也可能破坏唯一性。
- **可扩展性质疑**：对于高维连续潜在过程（如 $Z_t$ 为复杂扩散过程）如何高效采样条件目标 $F_t^{y_t}$ 未讨论；训练损失中的Bregman散度选择、神经网络架构等实际问题未被触及。
- **应用限制**：蛋白质结构生成仅为概念，未实现。离散潜在过程变体（如Edit Flows）已被先前工作独立证明，本文的统一理论虽然优雅，但并未带来立即可用的新算法优势。

（完）
