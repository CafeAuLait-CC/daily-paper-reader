---
title: "MotionHiFlow: Text-to-motion via hierarchical flow matching"
title_zh: MotionHiFlow：通过分层流匹配实现文本生成动作
authors: "Heng Li, Xiaotong Lin, Ling-An Zeng, Yulei Kang, Shuai Li, Jian-Fang Hu"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23264v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 用于文本到动作生成的层级流匹配框架
tldr: 针对文本生成动作任务中单时间尺度建模导致语义对齐和时序连贯性不足的问题，本文提出MotionHiFlow框架。该方法受人类认知启发，采用分层流匹配技术，从低时间尺度捕捉宏观语义，到高时间尺度细化动作细节。通过跨尺度转换过程和拓扑感知VAE，实现了高精度的语义对齐与细腻的动作生成，在主流数据集上达到SOTA水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23264v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparisons with the current state-of-the-art methods on the HumanML3D (upper half) and KIT-ML (lower half) datasets. Symbol “±” denotes a 95% confidence interval. Text in bold and underline denote the best and second-best results, respectively.\", \"page\": 7, \"index\": 1, \"width\": 1023, \"height\": 698}, {\"url\": \"assets/figures/arxiv/2604.23264v1/fig-002.webp\", \"caption\": \"Figure 2. Overview of our MotionHiFlow, which progressively generates motion from low to high temporal scales across multiple stages. The early stages mainly capture high-level semantics and coarse motion structures, while later stages model fine-grained temporal details via cross-scale transition and flow operators. The points along the gradient-colored dashed line in the inset (bottom left) of (a) and in (b) denotes a linear interpolation between its endpoints. Down/Up denotes downsampling/upsampling, respectively.\", \"page\": 3, \"index\": 2, \"width\": 1021, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2604.23264v1/fig-003.webp\", \"caption\": \"Figure 4. Visual comparisons between different methods given three distinct text descriptions. Only key frames are displayed, with arrows indicating the character’s movement direction. Green lines denote correct directions, while red lines indicate incorrect directions that do not match the text content. Refer to the demo video for complete motion clips and more visualization results.\", \"page\": 8, \"index\": 3, \"width\": 1034, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2604.23264v1/fig-004.webp\", \"caption\": \"Figure 3. Illustration of two main components in our TMDiT. (a) The TMDiT block employs two separate streams that independently process motion and text features, while self-attention and shared parameters enables information exchange between streams. (b) The Joint RoPE integrates rotations derived from temporal displacement, relative spatial coordinates, and the human body topologies. Here, xj and yj denote the j-th joint’s coordinates in the xy-plane.\", \"page\": 5, \"index\": 4, \"width\": 930, \"height\": 341}]"
motivation: 现有的文本生成动作方法多采用单时间尺度建模，难以兼顾宏观语义对齐与微观动作细节的连贯性。
method: 提出一种分层流匹配框架，结合跨尺度转换过程、文本动作扩散Transformer及拓扑感知VAE，实现从粗到细的动作生成。
result: 在HumanML3D和KIT-ML基准测试中取得了最先进的性能，消融实验验证了分层设计和关键组件的有效性。
conclusion: MotionHiFlow通过模拟人类认知的层次化建模，显著提升了生成动作的语义一致性与物理真实感。
---

## 摘要
文本生成动作旨在生成与输入文本紧密对齐、同时保持物理合理性且富含细粒度细节的3D人体动作。尽管最近的方法可以产生复杂且自然的运动，但它们通常仅在单一时间尺度上运行，这限制了语义对齐和时间连贯性。受人类认知系统中复杂动作是分层构思而非单一时间尺度这一事实的启发，我们提出了 MotionHiFlow，这是一个分层流匹配框架，通过构建从低到高时间尺度的流路径来逐步生成动作。低尺度的流捕捉高层语义和粗略的动作结构，而高尺度的流则细化时间细节。为了连接跨尺度的流，我们引入了一种新颖的跨尺度过渡过程，确保了连续性并保持了噪声一致性。此外，通过集成文本-动作扩散 Transformer 和拓扑感知动作 VAE，MotionHiFlow 通过关节感知位置编码和骨骼拓扑显式建模了关节间的结构依赖关系，从而实现了精确的语义对齐和细粒度的动作细节。在 HumanML3D 和 KIT-ML 基准测试上的广泛实验证明了其达到了最先进的性能，消融实验证实了分层设计和关键组件的有效性。代码可在 https://github.com/ai-lh/MotionHiFlow 获取。

## Abstract
Text-to-motion generation aims to generate 3D human motions that are tightly aligned with the input text while remaining physically plausible and rich in fine-grained detail. Although recent approaches can produce complex and natural movements, they usually operate at only one temporal scale, which limits both semantic alignment and temporal coherence. Inspired by the fact that complex motions are conceptualized hierarchically rather than at a single temporal scale in the human cognitive system, we propose \textit{MotionHiFlow}, a hierarchical flow matching framework to generate motion progressively by constructing flow path from low to high temporal scales. The flows at lower scales capture high-level semantics and coarse motion structures, while flows at higher scales refine temporal details. To link the flows across scales, we introduce a novel cross-scale transition process, ensuring continuity and preserving noise consistency. Furthermore, by integrating a Text-Motion Diffusion Transformer and a topology-aware Motion VAE, MotionHiFlow explicitly models structural dependencies among joints via joint-aware positional encoding and skeletal topology, enabling precise semantic alignment alongside fine-grained motion details. Extensive experiments on HumanML3D and KIT-ML benchmarks demonstrate state-of-the-art performance, with ablation studies confirming the effectiveness of the hierarchical design and key components. Code is available at https://github.com/ai-lh/MotionHiFlow.

---

## 论文详细总结（自动生成）

这是一份关于论文《MotionHiFlow: Text-to-Motion via Hierarchical Flow Matching》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的文本生成动作（Text-to-Motion）方法大多在**单一时间尺度**上建模。这导致模型难以同时兼顾宏观的语义对齐（如“走一个圆圈”）和微观的动作细节（如手指或足部的细微摆动）。
*   **研究动机**：受人类认知系统启发，人类构思动作是层次化的（先确定关键姿态框架，再填充动态过渡）。作者通过实验发现，即使将动作大幅下采样（只保留20%帧），其语义信息依然稳健。因此，提出一种**从粗到细（Coarse-to-Fine）**的生成策略，能有效提升动作的连贯性与语义准确度。

### 2. 方法论：MotionHiFlow 框架
*   **核心思想**：采用**分层流匹配（Hierarchical Flow Matching）**技术，在多个时间尺度上逐步构建生成路径。
*   **关键技术细节**：
    *   **分层流匹配 (HFM)**：将生成过程分为 $K$ 个阶段。早期阶段在低时间尺度运行，捕捉高层语义；后续阶段逐步增加时间分辨率，细化动作。
    *   **跨尺度转换过程 (Cross-scale Transition)**：为了解决多尺度间的噪声不一致问题，设计了“去噪（外推）- 上采样 - 重构噪声（内插）”三步走方案，确保不同分辨率间的平滑过渡。
    *   **文本-动作扩散 Transformer (TMDiT)**：采用双流架构分别处理文本和动作特征，通过参数共享机制（前几层独立，后几层共享）增强模态融合。
    *   **关节感知位置编码 (Joint RoPE)**：将旋转位置编码扩展到骨骼拓扑上，同时考虑时间偏移、空间相对坐标和运动学树深度，增强空间结构感。
    *   **拓扑感知 Motion VAE**：利用图卷积网络（GCN）显式建模人体骨骼拓扑，将动作压缩至潜在空间。

### 3. 实验设计
*   **数据集**：使用了两个主流基准数据集：**HumanML3D**（约4.5万个动作-文本对）和 **KIT-ML**（约6.3千个动作-文本对）。
*   **对比方法**：涵盖了近年来最先进的（SOTA）模型，包括基于 VAE 的 TEMOS、基于自回归的 T2M-GPT、基于扩散模型的 ReMoDiffuse、MotionDiffuse，以及基于掩码建模的 MoMask、BAMM 和 MoGenTS 等。
*   **评价指标**：R-Precision（Top 1/2/3）、FID（分布相似度）、Multimodal Distance（多模态距离）和 Diversity（多样性）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及总训练时长。
*   **训练细节**：Motion VAE 训练了 300,000 步（Batch Size 256）；TMDiT 训练了 200,000 步（Batch Size 64）。使用了 AdamW 优化器和多步学习率衰减策略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个核心数据集上进行了完整的定量对比。
    *   进行了详尽的**消融实验**，验证了分层尺度数量（从1个到4个尺度）、TMDiT 模块、拓扑感知 VAE 以及 Joint RoPE 的有效性。
    *   包含**用户主观研究**（20名参与者对100组动作进行投票），对比了真实感和语义对齐度。
*   **充分性评价**：实验设计非常充分且客观。通过 20 次重复实验并提供 95% 置信区间，确保了结果的统计学意义。对比维度涵盖了定量指标、定性可视化和人类主观感受。

### 6. 主要结论与发现
*   **性能领先**：MotionHiFlow 在所有核心指标（尤其是 FID 和 R-Precision）上均刷新了 SOTA 记录。
*   **分层优势**：实验证明 3 阶段分层（尺度为 1/3, 2/3, 1）效果最佳，过多的层次边际收益递减。
*   **语义对齐**：低时间尺度的粗略动作对文本语义的捕捉更为稳健，这为后续细化提供了良好的全局约束。

### 7. 优点与亮点
*   **认知启发**：将人类“由粗到细”的认知逻辑引入流匹配框架，逻辑自洽且效果显著。
*   **数学严谨性**：提出的跨尺度转换算法解决了多尺度生成中常见的噪声偏移问题，保证了 ODE 轨迹的确定性。
*   **结构感知**：Joint RoPE 和拓扑 VAE 的结合，使得模型对人体解剖学结构的理解比纯 Transformer 架构更深。

### 8. 不足与局限
*   **计算开销**：虽然流匹配推理效率较高，但分层生成意味着需要多次调用模型，相比单阶段方法，推理延迟可能会有所增加（文中未详细对比推理耗时）。
*   **数据依赖**：模型性能高度依赖于高质量的文本-动作标注，对于极其复杂或罕见的动作描述，分层策略的提升空间可能受限。
*   **硬件信息缺失**：未公布具体算力消耗，不便于其他研究者评估复现成本。

（完）
