---
title: "Sketch2Colab: Sketch-Conditioned Multi-Human Animation via Controllable Flow Distillation"
title_zh: Sketch2Colab：通过可控流蒸馏实现草图引导的多人动画
authors: "Divyanshu Daiya, Aniket Bera"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02190v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 使用修正流蒸馏进行3D多人动作生成
tldr: Sketch2Colab 是一种将 2D 分镜草图转化为 3D 多人协作动画的新框架。针对扩散模型在处理复杂交互约束时效率低且一致性差的问题，该方法通过将扩散先验蒸馏为潜空间中的修正流，并结合可微能量函数与连续时间马尔可夫链（CTMC）规划器，实现了对角色、关节、时序及物理接触的精细控制。实验证明，该方法在保持高感知质量的同时，显著提升了约束遵循能力和推理速度。
selection_source: fresh_fetch
motivation: 传统的扩散模型在处理复杂的多人交互约束时，面临训练成本高、推理速度慢以及难以精确遵循细粒度控制指令的问题。
method: 该方法将草图驱动的扩散先验蒸馏为潜空间修正流，并结合可微能量引导与 CTMC 规划器来生成符合物理规律且时序协调的交互动作。
result: 在 CORE4D 和 InterHuman 数据集上的实验表明，该方法在约束遵循度和感知质量上达到了 SOTA 水平，且推理速度显著优于扩散模型基准。
conclusion: Sketch2Colab 为基于草图的多人 3D 动画生成提供了一种高效且高度可控的解决方案，有效平衡了生成质量与交互约束的精确性。
---

## 摘要
我们提出了 Sketch2Colab，它能将分镜脚本风格的二维草图转化为连贯的、感知物体的三维多人运动，并实现对智能体、关节、时序和接触点的细粒度控制。传统的基于扩散的运动生成器在逼真度方面已取得长足进步；然而，精确遵循丰富的交互约束通常需要大量的训练和/或昂贵的后验引导，且在强多实体条件约束下性能往往会下降。相比之下，Sketch2Colab 首先学习草图驱动的扩散先验，然后将其蒸馏到在潜空间中运行的高效修正流（rectified-flow）学生模型中，以实现快速、稳定的采样。作用于关键帧、轨迹和物理约束的可微能量直接塑造了学生模型的传输场，引导样本生成既忠实于分镜脚本又符合物理规律的运动。为了捕捉协调的交互，我们利用连续时间马尔可夫链（CTMC）规划器增强了连续流，该规划器负责调度触摸、抓取和交接等离散事件，通过调节动力学产生清晰且相位协调的“人-物-人”协作。在 CORE4D 和 InterHuman 数据集上的实验表明，Sketch2Colab 在约束遵循和感知质量方面达到了最先进水平，同时推理速度显著快于纯扩散模型基准。

## Abstract
We present Sketch2Colab, which turns storyboard-style 2D sketches into coherent, object-aware 3D multi-human motion with fine-grained control over agents, joints, timing, and contacts. Conventional diffusion-based motion generators have advanced realism; however, achieving precise adherence to rich interaction constraints typically demands extensive training and/or costly posterior guidance, and performance can degrade under strong multi-entity conditioning. Sketch2Colab instead first learns a sketch-driven diffusion prior and then distills it into an efficient rectified-flow student operating in latent space for fast, stable sampling. Differentiable energies over keyframes, trajectories, and physics-based constraints directly shape the student's transport field, steering samples toward motions that faithfully satisfy the storyboard while remaining physically plausible. To capture coordinated interaction, we augment the continuous flow with a continuous-time Markov chain (CTMC) planner that schedules discrete events such as touches, grasps, and handoffs, modulating the dynamics to produce crisp, well-phased human-object-human collaborations. Experiments on CORE4D and InterHuman show that Sketch2Colab achieves state-of-the-art constraint adherence and perceptual quality while offering significantly faster inference than diffusion-only baselines.

---

## 论文详细总结（自动生成）

### 论文总结：Sketch2Colab - 通过可控流蒸馏实现草图引导的多人动画

#### 1. 核心问题与研究动机
*   **核心问题**：如何将直观但稀疏的 **2D 分镜草图（Storyboard）** 高效且精确地转化为符合物理规律、时序协调的 **3D 多人与物体交互（HOH）动画**。
*   **研究动机**：
    *   **现有方法局限**：传统的文本驱动生成（Text-to-Motion）缺乏空间和时序上的精确控制；而基于扩散模型（Diffusion）的控制方法在处理复杂多人交互时，往往需要昂贵的后验引导（Posterior Guidance），导致推理速度慢且难以满足硬性约束（如精确的接触和避障）。
    *   **草图优势**：草图能自然地表达关键帧、关节轨迹和物体路径，是动画师更理想的交互媒介。
    *   **技术目标**：在保证生成质量的同时，提升对多人、多关节、精确时序和物理接触的细粒度控制能力，并加快采样速度。

#### 2. 方法论
Sketch2Colab 的核心思想是将扩散模型的生成能力与修正流（Rectified Flow）的高效性相结合，并引入离散事件规划。
*   **扩散到流的蒸馏（Flow Distillation）**：
    *   首先训练一个强大的草图驱动扩散模型作为“教师”。
    *   将其概率流速度场蒸馏到一个潜空间（Latent Space）中的“学生”修正流模型中，实现更直观、更快速的样本传输。
*   **双空间约束引导（Dual-Space Conditioning）**：
    *   **潜空间锚点**：利用对齐的 2D/3D 编码器，确保生成的运动保持在合理的运动流形内。
    *   **原始空间能量引导**：定义可微能量函数（针对关键帧、轨迹、接触一致性、碰撞避免等），通过学习到的雅可比代理（Jacobian Surrogate）将梯度传回潜空间，直接塑造传输场。
*   **CTMC 离散事件规划器**：
    *   引入**连续时间马尔可夫链（CTMC）**来调度离散交互事件（如接近、抓取、交接）。
    *   CTMC 根据当前状态预测相位概率，动态调节连续流的子场和接触能量权重，从而产生清晰、无抖动的相位切换。
*   **Lyapunov 势能函数**：学习一个势能场，引导学生模型向满足约束的方向演化，确保采样的稳定性。

#### 3. 实验设计
*   **数据集**：
    *   **CORE4D**：包含 998 条多人与物体交互序列，侧重于协作搬运和重排任务。
    *   **InterHuman**：包含 6,022 条多人交互序列，用于增强交互的多样性。
*   **基准方法（Benchmarks）**：
    *   **Retrieval-INT**：基于检索的方法。
    *   **Sketch2Anim-INT**：将单人草图生成模型扩展至多人交互场景的适配版。
    *   **COLLAGE Teacher**：作为教师模型的层级潜空间扩散模型。
*   **评估指标**：逼真度（FID、足部滑动）、控制精度（2D/3D 关键帧与轨迹误差）、交互质量（物体位置误差、锚点误差、穿透率）以及文本-动作对齐度。

#### 4. 资源与算力
*   **训练细节**：
    *   教师模型训练：400k 步，Batch Size 64。
    *   学生模型蒸馏与优化：500k 步，Batch Size 64。
    *   优化器：AdamW，学习率 $10^{-4}$。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量，但从训练步数和模型复杂度（4层时间 U-Net）来看，属于中等规模的深度学习训练需求。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流交互数据集上进行了详尽测试，并利用 682 张手绘草图进行了真实感验证。
*   **消融实验**：设计了 7 组消融实验，分别验证了能量引导、CTMC 规划、潜空间锚点、时间相位偏差等模块的贡献。
*   **鲁棒性测试**：针对不同程度的草图噪声（20% 到 80%）进行了性能衰减测试。
*   **评价**：实验设计全面，对比了检索、扩散及适配后的 SOTA 方法，涵盖了定量指标与定性可视化，客观性较强。

#### 6. 主要结论与发现
*   **性能领先**：Sketch2Colab 在所有关键指标上均优于纯扩散模型基准，FID 降低了约 23%，轨迹控制精度提升了约 30%-40%。
*   **速度优势**：通过流蒸馏，仅需 30-60 个采样步数即可生成高质量动作，显著快于传统扩散模型。
*   **交互质量**：CTMC 规划器有效解决了多人协作中的相位漂移问题，使接触和交接动作更加干脆、准确。
*   **双空间引导的必要性**：实验证明，仅靠轨迹引导会损失关键帧精度，而加入能量引导是实现物理安全（防穿透）的关键。

#### 7. 优点
*   **高效性**：将复杂的后验引导转化为速度场的一部分，平衡了生成速度与控制精度。
*   **混合建模**：巧妙地结合了连续流（动作姿态）与离散链（交互逻辑），解决了动画生成中“软”动作与“硬”事件的耦合难题。
*   **直观控制**：支持分镜级别的细粒度控制，非常符合专业动画师的工作流。

#### 8. 不足与局限
*   **类别限制**：模型性能受限于训练集（如 CORE4D）中的物体类别，对未知物体的泛化能力尚待验证。
*   **交互复杂度**：目前主要集中在 1对1 的手-物交互，对于更复杂的多人同时操作同一物体的极端情况覆盖有限。
*   **依赖性**：学生模型的质量高度依赖于预训练的扩散教师模型，若教师模型存在模式崩溃，学生模型难以自行纠正。

（完）
