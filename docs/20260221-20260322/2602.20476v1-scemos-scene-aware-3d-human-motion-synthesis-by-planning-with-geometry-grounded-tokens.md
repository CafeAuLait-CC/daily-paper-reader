---
title: "SceMoS: Scene-Aware 3D Human Motion Synthesis by Planning with Geometry-Grounded Tokens"
title_zh: SceMoS：通过几何接地标记进行规划的场景感知 3D 人体运动合成
authors: "Anindita Ghosh, Vladislav Golyanik, Taku Komura, Philipp Slusallek, Christian Theobalt, Rishabh Dabral"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20476v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 合成文本驱动的3D人体动作
tldr: SceMoS 提出了一种基于 2D 场景表示的 3D 人体运动合成框架，旨在解决文本驱动下人体与复杂场景交互的语义意图与物理可行性问题。该方法通过解耦全局规划（利用俯视图 BEV）与局部执行（利用局部高度图），在降低计算开销的同时，实现了高保真度且符合物理约束的运动生成，在 TRUMANS 基准上达到了 SOTA 性能并显著减少了参数量。
selection_source: fresh_fetch
motivation: 现有的 3D 场景运动合成方法过度依赖计算昂贵的 3D 体素或点云数据，且难以高效平衡高层语义规划与底层物理接触推理。
method: 该框架将全局规划与局部执行解耦，分别利用 BEV 图像进行路径规划，并结合局部高度图训练几何感知的运动分词器以嵌入物理约束。
result: "在 TRUMANS 基准测试中，SceMoS 在运动真实感和接触准确性上均取得最优表现，同时将场景编码的训练参数量降低了 50% 以上。"
conclusion: 研究证明结构化的 2D 场景线索是全 3D 监督的强力替代方案，能有效引导 3D 人机交互合成并实现效率与保真度的平衡。
---

## 摘要
在真实场景中合成文本驱动的 3D 人体运动，需要同时学习语义意图（例如“走向沙发”）和物理可行性（例如避免碰撞）。当前的方法通常采用生成式框架，同时学习高层规划和底层接触推理，并依赖于计算成本昂贵的 3D 场景数据，如点云或体素占用网格。我们提出了 SceMoS，这是一个场景感知运动合成框架，它证明了在物理接地的运动合成中，结构化的 2D 场景表示可以作为全 3D 监督的强大替代方案。SceMoS 利用轻量级 2D 线索将全局规划与局部执行解耦，并依赖于：(1) 一个以文本为条件的自回归全局运动规划器，该规划器使用从场景高处渲染并由 DINOv2 特征编码的鸟瞰图 (BEV) 图像作为场景表示；(2) 一个通过条件 VQ-VAE 训练的几何接地运动分词器，它利用 2D 局部场景高度图将表面物理特性直接嵌入到离散词汇表中。这种 2D 分解实现了效率与保真度之间的权衡：BEV 语义捕捉空间布局和可供性 (affordance) 以进行全局推理，而局部高度图则在无需全 3D 体积推理的情况下确保了细粒度的物理遵循。SceMoS 在 TRUMANS 基准测试中实现了最先进的运动逼真度和接触准确度，将场景编码的可训练参数数量减少了 50% 以上，表明 2D 场景线索可以有效地为 3D 人体-场景交互提供物理接地。

## Abstract
Synthesizing text-driven 3D human motion within realistic scenes requires learning both semantic intent ("walk to the couch") and physical feasibility (e.g., avoiding collisions). Current methods use generative frameworks that simultaneously learn high-level planning and low-level contact reasoning, and rely on computationally expensive 3D scene data such as point clouds or voxel occupancy grids. We propose SceMoS, a scene-aware motion synthesis framework that shows that structured 2D scene representations can serve as a powerful alternative to full 3D supervision in physically grounded motion synthesis. SceMoS disentangles global planning from local execution using lightweight 2D cues and relying on (1) a text-conditioned autoregressive global motion planner that operates on a bird's-eye-view (BEV) image rendered from an elevated corner of the scene, encoded with DINOv2 features, as the scene representation, and (2) a geometry-grounded motion tokenizer trained via a conditional VQ-VAE, that uses 2D local scene heightmap, thus embedding surface physics directly into a discrete vocabulary. This 2D factorization reaches an efficiency-fidelity trade-off: BEV semantics capture spatial layout and affordance for global reasoning, while local heightmaps enforce fine-grained physical adherence without full 3D volumetric reasoning. SceMoS achieves state-of-the-art motion realism and contact accuracy on the TRUMANS benchmark, reducing the number of trainable parameters for scene encoding by over 50%, showing that 2D scene cues can effectively ground 3D human-scene interaction.

---

## 论文详细总结（自动生成）

### 论文总结：SceMoS - 基于几何接地标记规划的场景感知 3D 人体运动合成

#### 1. 核心问题与研究动机
在现实 3D 场景中生成由文本驱动的人体动作（如“走到沙发旁坐下”）面临两大挑战：一是**语义意图的规划**（理解空间布局），二是**物理可行性的约束**（避免穿模、确保足部接触）。
目前的主流方法依赖昂贵的 3D 表示（如体素网格或点云），计算开销巨大，且往往将高层规划与底层物理推理混为一谈。本论文的研究动机在于探索：**结构化的 2D 场景表示是否能替代复杂的 3D 监督，在降低计算成本的同时实现高保真的物理接地运动合成？**

#### 2. 方法论
SceMoS 提出了一个两阶段框架，核心思想是将**全局运动规划**与**局部物理执行**解耦：

*   **全局运动规划器（Global Motion Planner）：**
    *   **输入：** 文本指令（T5 编码）和场景的鸟瞰图（BEV）。BEV 图像从场景高处渲染，利用预训练的 **DINOv2** 提取特征，捕捉空间布局和语义可供性（Affordance）。
    *   **架构：** 基于 Transformer 的自回归模型，预测离散的运动标记（Tokens）。
*   **几何接地运动分词器（Geometry-Grounded Motion Tokenizer）：**
    *   **核心：** 采用条件 VQ-VAE（向量量化变分自编码器）。
    *   **关键技术：** 在解码阶段引入**局部 2D 高度图（Heightmap）**。高度图随人体根节点移动而实时更新，将表面物理几何信息直接嵌入到离散的运动词汇表中。
*   **轨迹细化模块（Trajectory Refinement）：**
    *   一个轻量级的回归器，用于修正根节点轨迹，减少足部滑动（Foot Sliding）并提升接触一致性。

#### 3. 实验设计
*   **数据集：** 主要使用 **TRUMANS** 数据集（包含 100 个室内场景、15 小时人体动作及 SMPL-X 表示）。此外在 **HUMANISE** 数据集上测试了泛化性。
*   **基准测试（Benchmark）：** 
    *   **TRUMANS：** 基于扩散模型的自回归基准。
    *   **TeSMo：** 使用 2D 楼层图和 3D 点云的扩散模型。
    *   **Humanise / SceneDiffuser：** 基于 3D 点云的生成模型。
*   **评估指标：** FID（运动质量）、Diversity（多样性）、PFC（足部接触得分）、穿模深度（Penetration）、MPJPE/MPJVE（重建精度）。

#### 4. 资源与算力
*   **硬件：** 使用了 **NVIDIA V100 GPU**。
*   **训练时长：** 
    *   VQ-VAE 阶段：约 98 小时（5000 epoch）。
    *   规划器阶段：约 75 小时。
*   **推理效率：** 生成 80 帧动作（约 4 秒时长）需要约 8 秒。
*   **参数量：** 场景编码器的可训练参数仅约 **4M**，远低于基准方法的 35M-86M。

#### 5. 实验数量与充分性
*   **实验规模：** 论文进行了详尽的定量对比和定性可视化。
*   **消融实验：** 设计了 7 组消融实验（A1-A7），涵盖了场景表示（2D vs 3D）、高度图分辨率、特征融合策略、有无轨迹细化等关键变量。
*   **用户研究：** 邀请了 41 名参与者对 20 组剪辑的“真实感”和“语义对齐度”进行评分。
*   **评价：** 实验设计客观、公平，通过跨数据集验证和多维度消融，充分证明了所提模块的有效性。

#### 6. 主要结论与发现
*   **2D 胜过 3D：** 结构化的 2D 投影（BEV + 高度图）足以提供 3D 交互所需的几何线索，且效率更高。
*   **解耦的优势：** 将规划与执行分开，使模型能更好地处理长程目标导航和复杂的局部接触。
*   **性能领先：** SceMoS 在 TRUMANS 基准上达到了 SOTA 性能，FID 最低（0.31），接触准确率最高（0.98），同时参数量减少了一个数量级。

#### 7. 优点（亮点）
*   **极高的参数效率：** 通过冻结 DINOv2 并使用 2D 线索，极大地降低了场景理解的计算负担。
*   **创新的分词方式：** 将局部几何信息引入 VQ-VAE 词汇表学习，使离散标记本身就具备“物理感知”能力。
*   **长程一致性：** 递归更新 BEV 和高度图的机制支持生成长距离、多阶段的复杂动作。

#### 8. 不足与局限
*   **静态场景限制：** 目前仅假设场景是静态的，无法处理动态障碍物或移动物体。
*   **细粒度缺失：** 高度图设计用于全身交互（坐、走），难以处理精细的手部抓取或物体操控。
*   **环境局限：** 主要针对室内环境优化，在地形极度崎岖或遮挡严重的室外场景中可能需要重新训练。
*   **推理延迟：** 8 秒生成 4 秒动作的速度对于实时交互应用（如游戏 AI）仍有提升空间。

（完）
