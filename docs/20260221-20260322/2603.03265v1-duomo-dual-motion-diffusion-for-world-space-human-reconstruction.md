---
title: "DuoMo: Dual Motion Diffusion for World-Space Human Reconstruction"
title_zh: DuoMo：用于世界坐标系人体重建的双重运动扩散模型
authors: "Yufu Wang, Evonne Ng, Soyong Shin, Rawal Khirodkar, Yuan Dong, Zhaoen Su, Jinhyung Park, Kris Kitani, Alexander Richard, Fabian Prada, Michael Zollhofer"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03265v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 用于世界坐标系人体动作重建的双重动作扩散模型
tldr: DuoMo 是一种从非约束视频中恢复世界坐标系人体运动的生成式方法。针对从噪声视频中提取一致性运动的挑战，该研究提出了双扩散模型架构：首先在相机坐标系估计运动，随后在世界坐标系进行提升与精炼。该方法直接生成网格顶点，无需参数化模型，在 EMDB 和 RICH 数据集上显著降低了重建误差，实现了 SOTA 性能。
selection_source: fresh_fetch
motivation: 旨在解决从包含噪声或不完整观测的非约束视频中，平衡人体运动重建的泛化性与全局一致性之间的矛盾。
method: 采用双扩散模型架构，将运动学习分解为相机空间初步估计和世界空间全局一致性精炼两个阶段，并直接生成网格顶点。
result: "在 EMDB 和 RICH 数据集上分别实现了 16% 和 30% 的世界空间重建误差降低，同时保持了较低的足部滑动。"
conclusion: DuoMo 通过分解扩散过程，有效地实现了从复杂视频中进行高精度、全局一致的世界空间人体运动重建。
---

## 摘要
我们提出了 DuoMo，这是一种生成式方法，能够从具有噪声或不完整观测的非受限视频中恢复世界坐标系下的人体运动。重建此类运动需要解决一个基本的权衡问题：既要从多样且含噪声的视频输入中进行泛化，又要保持全局运动的一致性。我们的方法通过将运动学习分解为两个扩散模型来解决这一问题。相机空间模型首先从视频中估计相机坐标系下的运动。随后，世界空间模型将这一初始估计提升到世界坐标系中，并对其进行细化以实现全局一致性。这两个模型协同工作，能够跨越不同的场景和轨迹重建运动，即使是在观测数据噪声极大或不完整的情况下。此外，我们的公式具有通用性，可以直接生成网格顶点的运动，从而绕过参数化模型。DuoMo 达到了最先进的性能。在 EMDB 数据集上，我们的方法在保持低足部滑动（foot skating）的同时，将世界空间重建误差降低了 16%。在 RICH 数据集上，它将世界空间误差降低了 30%。项目页面：https://yufu-wang.github.io/duomo/

## Abstract
We present DuoMo, a generative method that recovers human motion in world-space coordinates from unconstrained videos with noisy or incomplete observations. Reconstructing such motion requires solving a fundamental trade-off: generalizing from diverse and noisy video inputs while maintaining global motion consistency. Our approach addresses this problem by factorizing motion learning into two diffusion models. The camera-space model first estimates motion from videos in camera coordinates. The world-space model then lifts this initial estimate into world coordinates and refines it to be globally consistent. Together, the two models can reconstruct motion across diverse scenes and trajectories, even from highly noisy or incomplete observations. Moreover, our formulation is general, generating the motion of mesh vertices directly and bypassing parametric models. DuoMo achieves state-of-the-art performance. On EMDB, our method obtains a 16% reduction in world-space reconstruction error while maintaining low foot skating. On RICH, it obtains a 30% reduction in world-space error. Project page: https://yufu-wang.github.io/duomo/

---

## 论文详细总结（自动生成）

以下是对论文《DuoMo: Dual Motion Diffusion for World-Space Human Reconstruction》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决从**非约束单目视频**中重建**世界坐标系（World-space）**下人体运动的挑战。
*   **核心矛盾**：现有的端到端模型在复杂场景中泛化性差（受限于动捕数据多样性），而先在相机空间建模再“提升”到世界空间的方法虽然鲁棒，但缺乏全局一致性，容易产生物理不合理（如足部滑动、轨迹漂移）的问题。
*   **研究动机**：人类视觉能自然地分离相机运动与人体运动。受此启发，作者提出将重建任务分解为“相机空间估计”和“世界空间精炼”两个阶段，利用生成式扩散模型来平衡局部精度与全局一致性。

### 2. 方法论：核心思想与关键技术
DuoMo 采用双重扩散模型架构，直接生成**网格顶点（Mesh Vertices）**而非参数化模型（如 SMPL）参数。

*   **相机空间扩散模型 (Dcam)**：
    *   **输入**：视频帧的图像特征 + 密集 2D 关键点（通过相机内参转换为射线方向编码）。
    *   **输出**：以根节点为中心的网格顶点 $P$ 和根节点位置 $r$。
    *   **创新点**：引入了**高度条件化（Height conditioning）**，通过输入身高信息解决单目重建固有的尺度模糊问题。
*   **世界空间扩散模型 (Dworld)**：
    *   **流程**：将相机空间预测结果利用估计的相机位姿“提升”到世界坐标系，作为含噪声的初始提议（Proposal）。
    *   **输出**：精炼后的网格顶点 $P$ 和根节点速度 $v$（通过速度积分避免绝对位置建模的不稳定性）。
    *   **坐标系设计**：不使用固定的标准坐标系，而是以视频第一帧相机位姿为原点，增强了对复杂地形（如山丘、楼梯）的鲁棒性。
*   **引导采样 (Guided Sampling)**：
    *   在推理阶段使用 **2D 重投影引导**（减少时间漂移）和 **位移引导**（解决长时遮挡后的位置对齐），确保生成的运动既符合物理先验又对齐视频观测。

### 3. 实验设计
*   **数据集**：
    *   **训练**：AMASS, BEDLAM, 3DPW, Goliath。
    *   **评估**：EMDB（野外动捕）、RICH（复杂接触场景）、Egobody（第一视角/遮挡场景）。
*   **Benchmark 与指标**：
    *   **相机空间**：MPJPE, PA-MPJPE, PVE。
    *   **世界空间**：WA-MPJPE（分段对齐误差）、W-MPJPE（漂移敏感误差）、RTE（轨迹误差）、Jitter（平滑度）、Foot-sliding（足部滑动）。
*   **对比方法**：包括 HMR2.0, WHAM, TRAM, GVHMR, GENMO, GLAMR, SLAHMR 等 SOTA 方法。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA H200 GPU** 进行训练。
*   **训练细节**：
    *   两个扩散模型分别独立训练，各运行 **100 万步**。
    *   有效 Batch Size 为 **256**。
    *   训练序列长度 $T=120$ 帧。
*   **推理速度**：在 H200 上，处理一段 20 秒（30FPS）的视频，特征提取约需 35 秒，扩散模型推理仅需 **1.5 秒**。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流基准数据集上进行了详尽测试，涵盖了相机空间精度、世界空间轨迹精度及运动质量。
*   **消融实验**：
    *   验证了“双阶段模型”优于“单阶段世界空间模型”或“纯提升方法”。
    *   验证了“直接生成网格”与“生成 SMPL 参数”的性能差异（网格表示更具竞争力）。
    *   分析了不同相机噪声水平下模型的鲁棒性。
*   **评价**：实验设计全面且客观。通过在 Egobody 数据集上测试遮挡补全，充分展示了其生成式能力的优势。对比实验均使用了相同的相机位姿输入，确保了公平性。

### 6. 主要结论与发现
*   **性能领先**：在 EMDB 上世界空间误差降低 16%，在 RICH 上降低 30%。
*   **尺度关键性**：高度条件化能显著提升单目重建的度量精度。
*   **鲁棒性**：世界空间扩散模型充当了“生成式正则项”，即使相机位姿估计存在漂移，也能保持人体运动的物理合理性和低足部滑动。
*   **通用性**：直接学习网格顶点运动是可行的，这为未来扩展到非人体系（无参数模型类别）提供了可能。

### 7. 优点（亮点）
*   **解耦架构**：巧妙地将复杂的几何关系分解为两个易于学习的扩散过程。
*   **无需参数模型**：摆脱了对 SMPL 等预定义人体模型的依赖，架构更通用。
*   **遮挡处理**：利用扩散模型的生成能力，能自动补全主体出镜或被遮挡时的运动轨迹。
*   **局部坐标系设计**：避免了对地面平面估计的依赖，使其能处理非平坦地面的视频。

### 8. 不足与局限
*   **缺乏场景感知**：模型未显式利用 3D 场景信息（如障碍物、墙壁），可能导致人体与环境发生穿模。
*   **网格变形风险**：在极端或罕见姿态下，直接生成的稀疏网格可能会出现非自然的几何变形。
*   **确定性标签依赖**：关键点可见性目前通过硬阈值判断，未充分利用检测器的不确定性（置信度）信息。

（完）
