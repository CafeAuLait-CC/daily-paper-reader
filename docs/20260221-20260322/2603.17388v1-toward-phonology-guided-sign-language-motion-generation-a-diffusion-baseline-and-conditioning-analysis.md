---
title: "Toward Phonology-Guided Sign Language Motion Generation: A Diffusion Baseline and Conditioning Analysis"
title_zh: 迈向音系学引导的手语动作生成：扩散基准与条件分析
authors: "Rui Hong, Jana Kosecka"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17388v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 使用扩散模型基准进行3D身体动作生成
tldr: 本研究针对从文本生成自然准确的3D手语动作这一挑战，提出了一种基于扩散模型的基准方法。通过引入ASL-LEX 2.0音系属性（如手形、位置和动作）作为条件，并对比了CLIP与T5编码器在不同属性表示下的表现。实验证明，该扩散模型在词汇辨识度上优于现有的CVAE方法，并揭示了输入表示对文本编码器调节效果的关键影响，为手语动作生成提供了新的基准和优化方向。
selection_source: fresh_fetch
motivation: 旨在解决从文本生成自然且符合音系规则的3D手语动作所面临的准确性与平滑度挑战。
method: 建立了一个基于MDM风格的扩散模型基准，并系统分析了不同文本编码器及音系属性表示方式对生成效果的影响。
result: 实验表明该扩散模型在各项指标上均优于SignAvatar，且发现CLIP编码器在处理音系属性时更依赖于自然语言描述而非符号化表示。
conclusion: 研究强调了输入表示在基于文本编码器的属性调节中的关键作用，并提倡采用词汇与音系属性独立编码的结构化调节方法。
---

## 摘要
基于文本输入生成自然、准确且视觉平滑的 3D 虚拟人手语动作仍然具有很大挑战。在这项工作中，我们训练了一个 3D 身体动作生成模型，并利用 ASL-LEX 2.0 注释（如手形、手部位置和动作）探索了音系属性条件在手语动作生成中的作用。我们首先使用带有 SMPL-X 表示的 Human Motion MDM 风格扩散模型建立了一个强大的扩散基准，该模型在词汇（gloss）可辨识度指标上优于最先进的 CVAE 方法 SignAvatar。随后，我们系统地研究了使用不同文本编码器（CLIP 与 T5）、条件模式（仅词汇 vs. 词汇+音系属性）以及属性表示格式（符号化 vs. 自然语言）时文本条件的作用。我们的分析表明，将符号化的 ASL-LEX 注释转换为自然语言是实现有效的基于 CLIP 的属性条件的必要前提，而 T5 很大程度上不受此转换的影响。此外，我们表现最好的变体（带有映射属性的 CLIP）在所有指标上均优于 SignAvatar。这些发现强调了输入表示是基于文本编码器的属性条件的关键因素，并启发了通过独立路径对词汇和音系属性进行编码的结构化条件方法。

## Abstract
Generating natural, correct, and visually smooth 3D avatar sign language motion conditioned on the text inputs continues to be very challenging. In this work, we train a generative model of 3D body motion and explore the role of phonological attribute conditioning for sign language motion generation, using ASL-LEX 2.0 annotations such as hand shape, hand location and movement. We first establish a strong diffusion baseline using an Human Motion MDM-style diffusion model with SMPL-X representation, which outperforms SignAvatar, a state-of-the-art CVAE method, on gloss discriminability metrics. We then systematically study the role of text conditioning using different text encoders (CLIP vs. T5), conditioning modes (gloss-only vs. gloss+phonological attributes), and attribute notation format (symbolic vs. natural language). Our analysis reveals that translating symbolic ASL-LEX notations to natural language is a necessary condition for effective CLIP-based attribute conditioning, while T5 is largely unaffected by this translation. Furthermore, our best-performing variant (CLIP with mapped attributes) outperforms SignAvatar across all metrics. These findings highlight input representation as a critical factor for text-encoder-based attribute conditioning, and motivate structured conditioning approaches where gloss and phonological attributes are encoded through independent pathways.

---

## 论文详细总结（自动生成）

### 迈向音系学引导的手语动作生成：扩散基准与条件分析

#### 1. 核心问题与研究动机
*   **核心问题**：如何生成自然、准确且视觉平滑的3D手语动作。
*   **研究背景**：现有的手语生成方法（如基于词汇标签 Gloss 的生成）往往忽略了手语内在的**音系结构**（Phonological structure），即手形、位置、动作、手掌朝向等关键要素。
*   **动机**：引入音系属性（如 ASL-LEX 2.0 数据库提供的标注）可以增强模型的组合泛化能力、提供可解释的控制，并引入语言学先验知识以提高数据效率。

#### 2. 方法论
*   **核心思想**：构建一个基于扩散模型（Diffusion Model）的生成框架，将词汇标签与音系属性共同作为条件输入，生成 SMPL-X 格式的 3D 动作序列。
*   **关键技术细节**：
    *   **动作表示**：使用 SMPL-X 模型中 43 个上半身关节（脊柱、躯干、手臂、双手），采用 6D 连续旋转表示，每帧特征维度为 258。
    *   **扩散架构**：采用 MDM 风格的 Transformer Encoder 作为去噪器，预测原始动作 $x_0$。使用余弦噪声调度（Cosine schedule）。
    *   **条件机制**：通过预训练的文本编码器（CLIP 或 T5）将“词汇+音系属性”组成的字符串编码为条件向量 $e_c$。
    *   **损失函数**：结合了加权均方误差（MSE）重建损失和速度损失（Velocity loss），对双臂和双手的权重设为 5.0，以强调手语表达的关键部位。
    *   **属性处理**：对比了“符号化表示”（如 ASL-LEX 原始代码）和“自然语言映射”（将代码翻译为描述性文字）两种输入格式。

#### 3. 实验设计
*   **数据集**：**ASL3DWord**（源自 WLASL 视频数据集，包含 103 个词汇类别的 3D 动作序列）和 **ASL-LEX 2.0**（提供 22 个音系维度的标注，本研究选取了手形、位置、路径动作等 8 个属性）。
*   **基准模型（Benchmark）**：**SignAvatar**（目前最先进的基于 CVAE 的手语生成方法）。
*   **对比实验**：
    *   扩散模型 vs. CVAE。
    *   不同文本编码器：CLIP vs. T5。
    *   不同条件模式：仅词汇 vs. 词汇+音系属性。
    *   不同表示格式：符号化 vs. 自然语言映射。
*   **评价指标**：FID（分布相似度）、Accuracy（分类器辨识度）、KNN Accuracy、Diversity（多样性）、Multimodality（多模态性）以及作者提出的 **Variance Ratio**（关节运动幅度比）。

#### 4. 资源与算力
*   **算力设备**：论文提到使用 **单张 GPU** 进行训练。
*   **训练细节**：使用 HuggingFace Accelerate 库，训练 500 个 epoch，Batch size 为 100，采用 AdamW 优化器和混合精度训练（fp16）。
*   **推理**：使用 DDIM 采样，步数为 50 步。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 6 组主要的消融实验（2 种编码器 × 3 种条件组合）。
    *   与基准模型 SignAvatar 进行了全指标对比。
    *   针对身体不同部位（手臂、双手、躯干）进行了运动幅度的定量分析。
*   **充分性评价**：实验设计较为系统，特别是针对文本编码器对输入格式敏感性的分析非常深入。虽然数据集规模（103 个词汇）相对较小，但作为探索音系学引导生成的基准研究，其消融实验足以支撑其核心论点。

#### 6. 主要结论与发现
*   **扩散模型优势**：在词汇辨识度（Accuracy）上显著优于 CVAE 方法，且生成的动作多样性更接近真实数据。
*   **编码器差异**：
    *   **CLIP** 对输入格式极度敏感：直接输入符号化属性会导致性能崩溃，但将其映射为自然语言后，性能大幅提升并达到最优（FID 55.11）。
    *   **T5** 对格式不敏感：由于其子词分词机制，处理符号或自然语言的效果相近，但在处理短词汇时整体表现不如 CLIP。
*   **音系属性作用**：有效的音系属性调节能进一步提升生成质量，使模型在 FID 和辨识度上均达到最佳。
*   **运动特征**：扩散模型倾向于轻微的“过度表达”（Over-articulation），而 CVAE 则倾向于“表达不足”。

#### 7. 优点
*   **语言学结合**：首次系统研究了 ASL-LEX 音系属性在 3D 手语生成中的作用，具有较强的理论指导意义。
*   **分析深入**：不仅对比了模型性能，还深入探讨了文本编码器（CLIP vs T5）处理结构化信息的内在逻辑差异。
*   **新指标**：引入 Variance Ratio 指标，能够直观反映生成动作在解剖学层面的活跃程度。

#### 8. 不足与局限
*   **词汇量限制**：仅在 103 个词汇的小规模词汇表上验证，尚未扩展到大规模词汇或连续句子生成。
*   **标签噪声**：WLASL 视频中的变体动作与 ASL-LEX 的标准音系标注之间存在对齐噪声，这限制了属性调节的上限。
*   **属性路径**：目前采用简单的字符串拼接，未来可能需要更复杂的结构化张量调节（如 FiLM）来独立处理不同音系维度。

（完）
