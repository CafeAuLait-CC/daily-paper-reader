---
title: "Riemannian Motion Generation: A Unified Framework for Human Motion Representation and Generation via Riemannian Flow Matching"
title_zh: 黎曼运动生成：一种通过黎曼流匹配实现人体运动表示与生成的统一框架
authors: "Fangran Miao, Jian Huang, Ting Li"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15016v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 通过黎曼流匹配进行人体动作生成
tldr: 本研究针对人体运动生成的非欧几里得几何特性，提出了黎曼运动生成（RMG）框架。该框架将运动表示为乘积流形上的因子，并利用黎曼流匹配学习动力学。通过测地线插值、切空间监督和流形保持的ODE积分，RMG实现了具有内在归一化特性的无缩放表示。实验表明，RMG在HumanML3D和MotionMillion数据集上均达到SOTA性能，证明了结合几何感知建模在生成高质量人体运动方面的优越性和可扩展性。
selection_source: fresh_fetch
motivation: 传统人体运动生成多在欧几里得空间学习，忽略了运动数据本质的非欧几里得几何结构。
method: 提出RMG框架，通过黎曼流匹配在乘积流形上建模运动，并结合测地线插值与流形保持的ODE进行训练和采样。
result: 在HumanML3D和MotionMillion数据集上均取得SOTA性能，其中HumanML3D的FID达到0.043。
conclusion: 几何感知建模是实现高保真、可扩展人体运动生成的有效且实用的路径。
---

## 摘要
人体运动生成通常在欧几里得空间中学习，尽管有效的运动遵循结构化的非欧几里得几何。我们提出了黎曼运动生成（Riemannian Motion Generation, RMG），这是一个在积流形上表示运动，并通过黎曼流匹配学习动力学的统一框架。RMG 将运动分解为若干流形因子，从而产生具有内在归一化特性的无标度表示，并在训练和采样中使用测地线插值、切空间监督以及保流形 ODE 积分。在 HumanML3D 数据集上，RMG 在 HumanML3D 格式下实现了最先进的 FID（0.043），并在 MotionStreamer 格式下的所有报告指标中排名第一。在 MotionMillion 数据集上，它也超越了强基准模型（FID 5.6, R@1 0.86）。消融实验表明，紧凑的 $\mathscr{T}+\mathscr{R}$（平移 + 旋转）表示是最稳定且有效的，这突显了几何感知建模是实现高保真运动生成的一条实用且可扩展的路径。

## Abstract
Human motion generation is often learned in Euclidean spaces, although valid motions follow structured non-Euclidean geometry. We present Riemannian Motion Generation (RMG), a unified framework that represents motion on a product manifold and learns dynamics via Riemannian flow matching. RMG factorizes motion into several manifold factors, yielding a scale-free representation with intrinsic normalization, and uses geodesic interpolation, tangent-space supervision, and manifold-preserving ODE integration for training and sampling. On HumanML3D, RMG achieves state-of-the-art FID in the HumanML3D format (0.043) and ranks first on all reported metrics under the MotionStreamer format. On MotionMillion, it also surpasses strong baselines (FID 5.6, R@1 0.86). Ablations show that the compact $\mathscr{T}+\mathscr{R}$ (translation + rotations) representation is the most stable and effective, highlighting geometry-aware modeling as a practical and scalable route to high-fidelity motion generation.

---

## 论文详细总结（自动生成）

以下是对论文《Riemannian Motion Generation: A Unified Framework for Human Motion Representation and Generation via Riemannian Flow Matching》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：人体运动本质上受限于骨骼结构，具有非欧几里得几何特性（如旋转、关节约束）。然而，现有的生成模型（如扩散模型、自回归模型）大多在欧几里得空间（$\mathbb{R}^D$）中运行，使用冗余的坐标表示。
*   **研究动机**：
    *   **几何不一致性**：在欧几里得空间学习会导致模型生成的运动可能违反物理约束，通常需要复杂的后处理。
    *   **表示冗余**：现有的运动编码（如 HumanML3D 格式）维度高且各分量相关性强，增加了优化难度。
    *   **理论缺失**：缺乏一个统一的框架来直接在流形（Manifold）上建模运动动力学。
*   **整体含义**：本文提出了 **RMG（黎曼运动生成）** 框架，旨在通过黎曼流匹配（Riemannian Flow Matching）在积流形上直接学习运动生成，实现几何感知（Geometry-aware）的建模。

### 2. 论文提出的方法论
*   **核心思想**：将人体运动分解为多个自然流形因子的乘积，并在这些流形上定义生成流。
*   **关键技术细节**：
    *   **运动表示（积流形）**：将运动分解为全局平移（$\mathbb{R}^3$）、全局定向与关节旋转（单位四元数，属于球面 $S^3$ 的乘积）。最终流形定义为 $\mathcal{M} = \mathbb{R}^3 \times (S^3)^J$。
    *   **黎曼流匹配（RFM）**：
        *   **测地线插值**：在训练时，不使用线性插值，而是使用流形上的测地线（Geodesic）连接噪声分布 $x_0$ 和真实数据 $x_1$。
        *   **切空间监督**：模型学习预测在插值点 $x_t$ 处的切向量（速度场），并将其投影到流形的切空间 $T_{x_t}\mathcal{M}$ 中。
    *   **保流形采样**：在推理阶段，使用黎曼欧拉积分（Riemannian Euler Integration），通过指数映射（Exponential Map）确保每一步更新后的样本都严格落在流形上。
    *   **内在归一化**：由于流形结构（如单位四元数）自带约束，RMG 无需像传统方法那样进行复杂的均值/方差归一化。

### 3. 实验设计
*   **数据集**：
    *   **HumanML3D**：中等规模，包含约 1.46 万个运动和 4.5 万个文本描述。
    *   **MotionMillion**：大规模，包含 100 万个运动片段和 400 万个文本描述，用于验证可扩展性。
*   **Benchmark 与对比方法**：
    *   对比了扩散模型（MLD, MDM, MotionLab）和自回归模型（T2M-GPT, MotionGPT, MoMask）。
    *   评估指标包括 FID（运动质量）、R-Precision（文本匹配度）、Diversity（多样性）和 MultiModality（多模态性）。
*   **输出格式**：同时在标准的 HumanML3D 格式和最新的 MotionStreamer 格式下进行了评估。

### 4. 资源与算力
*   **模型架构**：使用了基于 Transformer 的扩散骨干网络（DiT）。
*   **文本编码器**：使用了 Qwen3-Embedding-0.6B（针对 HumanML3D）和 Qwen3-1.7B（针对 MotionMillion）。
*   **训练细节**：
    *   **HumanML3D**：训练 15 万步，有效 Batch Size 为 256。
    *   **MotionMillion**：训练 60 万步，有效 Batch Size 为 256。
*   **算力说明**：文中列出了超参数（如层数、隐藏层维度、学习率等），但**未明确说明具体的 GPU 型号、数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了全面的定量对比。
    *   **消融实验**：系统研究了不同流形因子（平移、旋转、预形状 P-shape）以及时间差分（Velocity）对生成质量和引导稳定性（Guidance Stability）的影响。
    *   **重复性**：在 HumanML3D 上进行了 20 次独立运行并报告了 95% 置信区间。
*   **充分性评价**：实验设计非常充分。通过对 Guidance Scale 的扫描，证明了 RMG 在高引导强度下比欧几里得模型更稳定。同时，在百万级数据集上的成功应用证明了该方法的工业级潜力。

### 6. 主要结论与发现
*   **性能领先**：RMG 在 HumanML3D 上达到了 SOTA 的 FID（0.043），在 MotionMillion 上也将 FID 从 10.3 显著降低至 5.6。
*   **最优表示**：消融实验证明，**“全局平移 + 关节旋转 ($\mathscr{T}+\mathscr{R}$)”** 是最紧凑且鲁棒的表示方式，加入额外的坐标信息（P-shape）或时间差分反而可能降低稳定性。
*   **几何优势**：黎曼流匹配在统计上比欧几里得流匹配具有更优的收敛速率（理论证明见附录），且能有效消除冗余坐标带来的“平坦方向”优化问题。

### 7. 优点
*   **数学严谨性**：将生成过程与黎曼几何深度结合，提供了坚实的理论支撑。
*   **无需后处理**：由于生成过程严格遵循流形约束，生成的运动天然满足骨骼长度等物理限制。
*   **高稳定性**：在 Classifier-free Guidance 较大时，模型表现出极强的鲁棒性，不易产生畸变。
*   **可扩展性**：首次证明了黎曼流匹配可以扩展到百万级的大规模数据集和现代 Transformer 架构中。

### 8. 不足与局限
*   **自回归探索不足**：目前主要基于流匹配（类扩散）框架，尚未在纯自回归（AR）架构上验证其效果。
*   **模态限制**：实验主要集中在文本到运动生成，未深入探讨音乐、视频等其他条件模态。
*   **细节缺失**：当前框架未包含手指（Hands）和面部（Face）的精细建模。
*   **时长限制**：生成时长目前限制在 10 秒以内，长时运动生成的连贯性有待进一步验证。

（完）
