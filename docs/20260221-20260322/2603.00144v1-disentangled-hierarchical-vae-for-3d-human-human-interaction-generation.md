---
title: Disentangled Hierarchical VAE for 3D Human-Human Interaction Generation
title_zh: 用于 3D 人与人交互生成的解耦分层变分自编码器
authors: "Zichen Geng, Zeeshan Hayder, Bo Miao, Jian Liu, Wei Liu, Ajmal Mian"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2603.00144v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 基于潜空间扩散模型的结构化可控3D人体交互生成
tldr: 针对3D双人交互生成中语义对齐差和物理不合理的问题，本文提出了解耦层次化变分自编码器（DHVAE）。该方法通过CoTransformer模块将全局交互上下文与个体运动模式解耦，并结合对比学习约束增强物理真实性。利用层次化潜空间中的扩散模型，DHVAE实现了高保真、可控且物理一致的双人交互生成，在运动质量和计算效率上均优于现有方法。
selection_source: fresh_fetch
motivation: 现有方法将所有运动信息压缩至单一潜空间，难以捕捉细粒度动作和复杂的交互语义，导致物理穿透或接触缺失等不合理现象。
method: 提出DHVAE架构，利用CoTransformer实现全局与个体特征解耦，并结合对比学习约束和层次化潜空间扩散模型进行生成。
result: 实验结果显示，DHVAE在运动保真度、文本对齐度以及物理合理性方面均优于现有技术，同时具备更高的计算效率。
conclusion: DHVAE通过解耦表示和层次化扩散机制，有效提升了3D双人交互生成的物理一致性与语义准确性。
---

## 摘要
生成逼真的 3D 人与人交互 (HHI) 需要对智能体的物理合理性及其交互语义进行连贯建模。现有方法将所有运动信息压缩到单个潜表示中，限制了它们捕捉细粒度动作和智能体间交互的能力。这通常会导致语义不对齐和物理上不合理的伪影，例如穿模或接触缺失。我们提出了一种基于解耦分层变分自编码器 (DHVAE) 的潜扩散模型，用于结构化且可控的 HHI 生成。DHVAE 通过采用 CoTransformer 模块，显式地将全局交互上下文和个体运动模式解耦为分离的潜结构。为了减轻 HHI 中不合理且物理不一致的接触，我们在 DHVAE 中引入了对比学习约束，以促进更具判别力且物理合理的潜交互空间。为了实现高保真交互合成，DHVAE 在分层潜空间中采用了基于 DDIM 的扩散去噪过程，并由跳跃连接的 AdaLN-Transformer 去噪器进行增强。广泛的评估表明，DHVAE 在运动保真度、文本对齐和物理合理性方面均表现优异，且具有更高的计算效率。

## Abstract
Generating realistic 3D Human-Human Interaction (HHI) requires coherent modeling of the physical plausibility of the agents and their interaction semantics. Existing methods compress all motion information into a single latent representation, limiting their ability to capture fine-grained actions and inter-agent interactions. This often leads to semantic misalignment and physically implausible artifacts, such as penetration or missed contact. We propose Disentangled Hierarchical Variational Autoencoder (DHVAE) based latent diffusion for structured and controllable HHI generation. DHVAE explicitly disentangles the global interaction context and individual motion patterns into a decoupled latent structure by employing a CoTransformer module. To mitigate implausible and physically inconsistent contacts in HHI, we incorporate contrastive learning constraints with our DHVAE to promote a more discriminative and physically plausible latent interaction space. For high-fidelity interaction synthesis, DHVAE employs a DDIM-based diffusion denoising process in the hierarchical latent space, enhanced by a skip-connected AdaLN-Transformer denoiser. Extensive evaluations show that DHVAE achieves superior motion fidelity, text alignment, and physical plausibility with greater computational efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《Disentangled Hierarchical VAE for 3D Human-Human Interaction Generation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 3D 人与人交互（HHI）生成方法通常将两个人的运动信息压缩到单一的、扁平的潜空间（Latent Space）中。这种做法导致个体运动与全局交互语义高度耦合，难以捕捉细粒度的协调动作，常引发语义对齐偏差及物理不合理现象（如肢体穿透、接触缺失）。
*   **研究背景**：在具身智能和虚拟角色动画领域，根据文本提示生成同步、连贯且符合物理规律的双人交互动作是一项重大挑战。

### 2. 方法论：核心思想与技术细节
论文提出了 **DHVAE（解耦分层变分自编码器）** 框架，结合潜扩散模型进行生成：
*   **解耦分层潜空间**：将交互表示显式分解为三个部分：$z_a$（行人 A 的个体运动）、$z_b$（行人 B 的个体运动）和 $z_o$（全局交互上下文）。
*   **CoTransformer 模块**：在编码阶段，利用该模块在保留个体身份的同时，通过交叉注意力机制融合双人的相互感知信息，生成全局交互潜变量 $z_o$。
*   **交互对比学习（Contrastive Learning）**：针对 $z_o$ 引入对比约束。通过对原始动作进行微小平移构造正样本，进行大幅度不合理平移构造负样本，利用三元组损失（Triplet Loss）强制模型学习物理合理的交互空间，提升接触区域的真实性。
*   **分层潜扩散模型**：在 $\{z_o, z_a, z_b\}$ 构成的分层空间中进行 DDIM 扩散去噪。
*   **改进的去噪器**：采用带有跳跃连接（Skip-connection）的 AdaLN-Transformer，并引入**段位置编码（SPE）**和**令牌缩放（Token Scaling）**，以解决不同潜变量组件之间的尺度不平衡和结构异质性问题。

### 3. 实验设计
*   **数据集**：
    *   **InterHuman**：采用 AMASS 骨架格式（22个关节），侧重于全局位置和旋转。
    *   **InterX**：采用 SMPL-X 格式（55个关节，含手部和面部），侧重于精细的全身交互。
*   **对比方法（Baselines）**：包括 InterGen、MoMat-MoGen、in2IN、InterMask、TIMotion 以及基于 MLD 的变体。
*   **评估指标**：FID（运动质量）、R-Precision（文本对齐度）、Multimodal Distance（多模态距离）、Diversity（多样性）、穿透体积（PV）及接触比例（Contact Ratio）。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA H100 GPU** 上完成。
*   **训练时长**：
    *   **DHVAE 阶段**：InterHuman 训练 2000 轮（epochs），InterX 训练 1200 轮。
    *   **去噪器阶段**：InterHuman 训练 1600 轮，InterX 训练 1000 轮。
*   **效率**：论文强调该模型是目前最轻量且推理速度最快的模型之一（参数量 56M，平均推理时间 0.454s）。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准数据集上进行了详尽的定量对比，并提供了定性可视化结果。
*   **消融实验**：非常充分。分别验证了 $z_o$ 变量、CoTransformer、对比学习损失、SPE、令牌缩放以及跳跃连接对最终性能的贡献。
*   **客观性**：引入了物理穿透指标和用户研究（18份有效响应），从客观物理规律和主观人类感知两个维度验证了有效性。

### 6. 主要结论与发现
*   **性能领先**：DHVAE 在两个数据集的所有核心指标（FID, R-Precision 等）上均达到了 SOTA（当前最佳）水平。
*   **物理真实性**：通过对比学习约束，模型显著降低了双人交互中的穿模频率，并提高了有效接触的准确性。
*   **解耦优势**：分层解耦结构不仅提高了生成的控制力，还通过降低潜变量通道间的协方差，简化了扩散模型的去噪难度。

### 7. 优点（亮点）
*   **结构化创新**：打破了传统的扁平潜空间设计，提出的分层解耦表示更符合人类交互的逻辑。
*   **物理约束机制**：巧妙地将对比学习应用于交互潜空间，无需复杂的物理模拟即可提升物理合理性。
*   **高效轻量**：在提升质量的同时，显著降低了参数量和推理延迟，具有更强的实用价值。

### 8. 不足与局限
*   **任务范围限制**：目前仅限于双人（Dyadic）交互，尚未扩展到多人（Crowd）或复杂团队任务。
*   **精细化伪影**：在 SMPL-X 这种高维度表示下，仍存在轻微的足部滑动或精细接触缺失（如手指捏取）。
*   **评估依赖**：现有的定量指标（如 FID）主要源于单人动作模型，可能无法完全捕捉双人同步性的细微差别。

（完）
