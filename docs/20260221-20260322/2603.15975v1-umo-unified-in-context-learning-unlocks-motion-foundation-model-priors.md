---
title: "UMO: Unified In-Context Learning Unlocks Motion Foundation Model Priors"
title_zh: UMO：统一上下文学习解锁运动基座模型先验
authors: "Xiaoyan Cong, Zekun Li, Zhiyang Dou, Hongyu Li, Omid Taheri, Chuan Guo, Abhay Mittal, Sizhe An, Taku Komura, Wojciech Matusik, Michael J. Black, Srinath Sridhar"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15975v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 用于文本到动作生成的基座模型
tldr: 本研究针对现有运动大模型仅限于文本生成运动且难以适配多样化下游任务的问题，提出了UMO统一框架。UMO通过将不同任务转化为原子级的逐帧操作，并引入可学习的元操作嵌入与轻量级时间融合技术，实现了对预训练DiT模型的上下文适配。该方法在不增加显著计算开销的前提下，使单一模型能够支持运动补全、编辑及多角色交互等多种任务，显著提升了运动生成任务的通用性与性能。
selection_source: fresh_fetch
motivation: 旨在解决现有运动大模型在处理多样化下游任务时缺乏通用性且需针对特定任务进行单独适配的问题。
method: 将下游任务统一建模为原子级逐帧操作的组合，并利用可学习的元操作嵌入和时间融合模块来解锁预训练模型的生成先验。
result: 实验表明，UMO在时间补全、文本引导编辑和几何约束等多个基准测试中均优于现有的特定任务模型和免训练基线。
conclusion: UMO通过统一的上下文学习机制，成功将单一用途的运动大模型转化为支持广泛下游任务的通用生成框架。
---

## 摘要
大规模基座模型（LFMs）最近通过从海量 3D 人体运动数据集和配对文本描述中学习强大的生成先验，在文本生成运动（text-to-motion generation）领域取得了显著进展。然而，如何有效且高效地利用此类单一用途的运动 LFM（即文本生成运动合成），在更多样化的跨模态和上下文运动生成下游任务中，仍是一个尚未解决的问题。先前的工作通常以特定任务的方式将预训练的生成先验适配到各个下游任务中。相比之下，我们的目标是在一个统一的框架内解锁这些先验，以支持广泛的下游运动生成任务。为了填补这一空白，我们提出了 UMO，这是一种简单且通用的统一公式，它将多样的下游任务转化为原子级逐帧操作的组合，从而实现上下文适配，以解锁基于 DiT 的预训练运动 LFM 的生成先验。具体而言，UMO 引入了三种可学习的帧级元操作嵌入来指定每帧的意图，并采用轻量级的时间融合将上下文线索注入预训练骨干网络，与基础模型相比，其运行开销几乎可以忽略不计。通过这种设计，UMO 对原本仅限于文本生成运动的预训练模型进行了微调，使其能够支持多种此前不支持的任务，包括时间补全（temporal inpainting）、文本引导的运动编辑、文本序列化的几何约束以及多身份反应生成。实验表明，尽管使用单一的统一模型，UMO 在广泛的基准测试中始终优于特定任务和免训练的基准方法。代码和模型将公开。项目主页：https://oliver-cong02.github.io/UMO.github.io/

## Abstract
Large-scale foundation models (LFMs) have recently made impressive progress in text-to-motion generation by learning strong generative priors from massive 3D human motion datasets and paired text descriptions. However, how to effectively and efficiently leverage such single-purpose motion LFMs, i.e., text-to-motion synthesis, in more diverse cross-modal and in-context motion generation downstream tasks remains largely unclear. Prior work typically adapts pretrained generative priors to individual downstream tasks in a task-specific manner. In contrast, our goal is to unlock such priors to support a broad spectrum of downstream motion generation tasks within a single unified framework. To bridge this gap, we present UMO, a simple yet general unified formulation that casts diverse downstream tasks into compositions of atomic per-frame operations, enabling in-context adaptation to unlock the generative priors of pretrained DiT-based motion LFMs. Specifically, UMO introduces three learnable frame-level meta-operation embeddings to specify per-frame intent and employs lightweight temporal fusion to inject in-context cues into the pretrained backbone, with negligible runtime overhead compared to the base model. With this design, UMO finetunes the pretrained model, originally limited to text-to-motion generation, to support diverse previously unsupported tasks, including temporal inpainting, text-guided motion editing, text-serialized geometric constraints, and multi-identity reaction generation. Experiments demonstrate that UMO consistently outperforms task-specific and training-free baselines across a wide range of benchmarks, despite using a single unified model. Code and model will be publicly available. Project Page: https://oliver-cong02.github.io/UMO.github.io/

---

## 论文详细总结（自动生成）

以下是对论文《UMO: Unified In-Context Learning Unlocks Motion Foundation Model Priors》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 人体运动生成大模型（LFMs）虽然在“文本生成运动”（T2M）上表现优异，但其强大的生成先验仍局限于单一任务。如何高效地将这些先验扩展到多样化的下游任务（如运动补全、编辑、几何约束控制、多角色交互等），且无需为每个任务设计专门的架构，是当前的主要挑战。
*   **研究动机**：
    *   **任务碎片化**：现有方法通常为不同任务开发专用架构，导致知识无法共享。
    *   **先验未解锁**：预训练模型中蕴含的丰富运动规律在非 T2M 任务中未得到充分利用。
    *   **统一性需求**：借鉴视觉和语言领域的成功经验，探索运动生成领域的统一任务表示。

### 2. 方法论
*   **核心思想**：将所有运动生成任务分解为**原子级的逐帧操作**。UMO 认为任何目标帧与源上下文的关系只有三种：**保留（Preserve）**、**生成（Generate）**或**编辑（Edit）**。
*   **关键技术细节**：
    *   **元操作嵌入（Meta-Operation Embeddings）**：引入三个可学习的帧级嵌入 `[preserve]`、`[generate]` 和 `[edit]`，用于显式指定每一帧的生成意图。
    *   **统一语言条件设计**：将所有约束（包括精确的 3D 几何轨迹、障碍物坐标等）全部序列化为文本字符串，利用预训练 LLM（如 Qwen）进行编码，无需额外的空间编码模块。
    *   **时间融合（Temporal Fusion）架构**：在 DiT 骨干网络的输入端，将编码后的上下文运动特征与噪声特征进行元素级相加。这种设计仅增加 0.207M 参数，且运行开销极低。
*   **算法流程**：输入源运动序列和对应的元操作标签 $\rightarrow$ 结合文本指令 $\rightarrow$ 通过时间融合注入预训练 DiT $\rightarrow$ 迭代去噪生成目标运动。

### 3. 实验设计
*   **数据集与场景**：
    *   **HumanML3D**：用于文本生成运动及 4 种时间补全任务（预测、回溯、中间插值、关键帧填充）。
    *   **MotionFix**：用于指令引导的运动编辑。
    *   **Inter-X / InterHuman**：用于双人反应生成。
    *   **自定义几何数据集**：包含 2000 条序列，涵盖线性、圆弧、贝塞尔曲线轨迹及障碍物规避场景。
*   **对比方法**：对比了 MDM、T2M-GPT、MoMask、MotionLab、OmniControl、MaskControl 等超过 20 种特定任务的 SOTA 方法，以及基于反演（Inversion）的免训练基线。

### 4. 资源与算力
*   **算力设备**：训练使用了 **4 台 NVIDIA B200 GPU**；推理评估在 **NVIDIA A6000 GPU** 上完成。
*   **训练细节**：
    *   **统一模型**：在所有任务上联合训练 100k 步。
    *   **专家模型**：针对单一任务微调 6k 步。
    *   **超参数**：Batch size 为 256，学习率为 5e-5，使用 50 步 Euler ODE 求解器。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 大类、10 余种具体的下游任务。
*   **消融实验**：系统对比了四种上下文注入架构（时间融合、序列拼接、AdaLN、ControlNet），验证了时间融合的优越性；同时验证了三个元操作标签的必要性。
*   **充分性评价**：实验设计非常充分，不仅在标准指标（FID、R-Precision、MPJPE）上进行了量化对比，还通过“压力测试”展示了模型在未见过的几何约束和双人交互中的**能力涌现**。对比基线涵盖了从早期扩散模型到最新的大模型适配方案，评估客观公平。

### 6. 主要结论与发现
*   **统一性优势**：单一的 UMO 统一模型在几乎所有任务上都达到或超过了特定任务的专家模型。
*   **先验可迁移性**：单人运动先验可以有效迁移至双人交互任务；LLM 编码的文本几何描述能被 DiT 有效识别并转化为精确的运动轨迹。
*   **高效适配**：轻量级的时间融合模块足以解锁大模型的生成能力，无需笨重的 ControlNet 结构。

### 7. 优点
*   **极简且通用**：通过三个原子操作统一了极其多样的运动任务，架构改动极小。
*   **神经符号接口**：将几何约束转化为文本，利用了 LLM 的结构化理解能力，使模型具备了处理复杂逻辑指令的潜力。
*   **高性能低开销**：在保持 SOTA 性能的同时，推理延迟和参数增量远低于同类方法。

### 8. 不足与局限
*   **粒度限制**：目前的元操作是在整身级别定义的，尚不支持局部肢体（如仅编辑左手）的精细控制。
*   **模态局限**：目前仅支持文本和运动上下文，尚未整合音频（如音乐驱动跳舞）等其他模态。
*   **依赖性**：几何控制的精度高度依赖于 LLM 对坐标字符串的编码质量，对于极高精度的工业级控制可能仍存在偏差。

（完）
