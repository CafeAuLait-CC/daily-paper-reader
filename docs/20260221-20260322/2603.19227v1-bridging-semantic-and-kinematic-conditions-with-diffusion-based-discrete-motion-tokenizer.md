---
title: Bridging Semantic and Kinematic Conditions with Diffusion-based Discrete Motion Tokenizer
title_zh: 利用基于扩散模型的离散运动分词器连接语义与运动学条件
authors: "Chenyang Gu, Mingyuan Zhang, Haozhe Xie, Zhongang Cai, Lei Yang, Ziwei Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19227v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 基于扩散的动作合成与3D动作生成
tldr: 本研究针对人体动作生成中语义控制与运动学约束难以兼顾的问题，提出了一种三阶段框架（感知、规划、控制）。核心创新是MoTok，一种基于扩散模型的离散动作分词器，它通过扩散解码器将语义抽象与精细重建解耦。该方法在规划阶段处理语义和粗略约束，在控制阶段利用扩散优化实现精细运动学控制，在减少Token数量的同时显著提升了生成的保真度与控制精度。
selection_source: fresh_fetch
motivation: 旨在解决现有动作生成方法在结合离散Token的语义处理能力与扩散模型的运动学控制能力方面的局限性。
method: 提出MoTok分词器，通过扩散解码器将运动恢复任务独立出来，实现单层紧凑Token表示，并配合三阶段框架进行分层约束引导。
result: 在HumanML3D数据集上，该方法将轨迹误差从0.72cm降至0.08cm，FID从0.083降至0.029，且仅需六分之一的Token量。
conclusion: 该研究证明了通过解耦语义抽象与精细重建，可以有效桥接语义与运动学条件，实现高保真且高度可控的动作生成。
---

## 摘要
先前的运动生成主要遵循两种范式：擅长运动学控制的连续扩散模型，以及在语义调节方面有效的基于离散 token 的生成器。为了结合两者的优势，我们提出了一个三阶段框架，包括条件特征提取（感知）、离散 token 生成（规划）和基于扩散的运动合成（控制）。该框架的核心是 MoTok，这是一种基于扩散的离散运动分词器，它通过将运动恢复任务交给扩散解码器，实现了语义抽象与细粒度重建的解耦，从而在保持运动保真度的同时实现了紧凑的单层 token。对于运动学条件，粗粒度约束在规划阶段引导 token 生成，而细粒度约束则在控制阶段通过基于扩散的优化来强制执行。这种设计防止了运动学细节干扰语义 token 的规划。在 HumanML3D 数据集上，我们的方法在仅使用六分之一 token 的情况下，相比 MaskControl 显著提高了可控性和保真度，将轨迹误差从 0.72 cm 降低到 0.08 cm，并将 FID 从 0.083 降低到 0.029。与在更强运动学约束下性能会下降的先前方法不同，我们的方法提升了保真度，将 FID 从 0.033 降低到 0.014。

## Abstract
Prior motion generation largely follows two paradigms: continuous diffusion models that excel at kinematic control, and discrete token-based generators that are effective for semantic conditioning. To combine their strengths, we propose a three-stage framework comprising condition feature extraction (Perception), discrete token generation (Planning), and diffusion-based motion synthesis (Control). Central to this framework is MoTok, a diffusion-based discrete motion tokenizer that decouples semantic abstraction from fine-grained reconstruction by delegating motion recovery to a diffusion decoder, enabling compact single-layer tokens while preserving motion fidelity. For kinematic conditions, coarse constraints guide token generation during planning, while fine-grained constraints are enforced during control through diffusion-based optimization. This design prevents kinematic details from disrupting semantic token planning. On HumanML3D, our method significantly improves controllability and fidelity over MaskControl while using only one-sixth of the tokens, reducing trajectory error from 0.72 cm to 0.08 cm and FID from 0.083 to 0.029. Unlike prior methods that degrade under stronger kinematic constraints, ours improves fidelity, reducing FID from 0.033 to 0.014.

---

## 论文详细总结（自动生成）

以下是对论文《Bridging Semantic and Kinematic Conditions with Diffusion-based Discrete Motion Tokenizer》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在人体动作生成中同时实现高层级的**语义控制**（如文本描述）和低层级的**运动学约束**（如精确的关节轨迹）。
*   **研究背景**：
    *   **离散 Token 范式**（如 GPT 类模型）：擅长处理语义，但为了保证重建精度通常需要极高的 Token 采样率，且难以处理精细的运动约束。
    *   **连续扩散模型**：擅长捕捉运动细节和物理一致性，但在处理长序列语义逻辑时效率较低。
*   **动机**：现有的动作分词器（Tokenizer）往往将语义抽象与底层细节耦合在一起。作者希望通过一种“解耦”策略，让离散 Token 负责语义规划，让扩散模型负责细节重建。

### 2. 方法论
论文提出了 **Perception–Planning–Control（感知-规划-控制）** 三阶段框架，核心技术是 **MoTok** 分词器：
*   **核心思想**：将动作恢复任务从离散 Token 预测中剥离，交给一个基于扩散模型的解码器。这样 Token 序列可以非常紧凑（低采样率），而保真度由扩散过程保障。
*   **关键技术细节**：
    1.  **MoTok 分词器**：
        *   **编码器**：通过卷积进行时间维度下采样。
        *   **量化器**：使用单层代码簿（Codebook）生成离散 Token。
        *   **扩散解码器**：不直接回归动作，而是将量化后的特征作为条件输入到一个轻量级扩散模型中，通过迭代去噪生成最终动作。
    2.  **统一规划器（Planning）**：支持自回归（AR）和离散扩散（DDM）两种架构，将文本（全局条件）和轨迹（局部条件）整合进 Token 预测。
    3.  **粗到精的约束引导（Coarse-to-Fine）**：
        *   **规划阶段**：将运动学约束作为“粗略引导”加入 Token 生成。
        *   **控制阶段**：在扩散解码去噪时，通过梯度优化（Refinement）强制执行“精细约束”。

### 3. 实验设计
*   **数据集**：HumanML3D 和 KIT-ML（主流的人体动作-文本配对数据集）。
*   **任务场景**：文本到动作生成（Text-to-Motion）、文本与轨迹联合控制（Text-and-Trajectory Control）。
*   **对比方法**：
    *   **Token 类**：T2M-GPT, MoMask, MaskControl。
    *   **扩散类**：MotionDiffuse, ReMoDiffuse, OmniControl, InterControl。
*   **评价指标**：FID（保真度）、R-Precision（语义一致性）、轨迹误差（Traj. Err.）、足部滑动率（Foot Skating）等。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA H100 GPU** 进行实验（见附录 C）。
*   **训练配置**：
    *   使用 8 张 GPU 进行训练。
    *   MoTok 训练：Batch size 为 512/GPU。
    *   规划器（DDM/AR）训练：Batch size 为 64/GPU。
    *   总训练轮数：24 Epochs。
*   **效率**：生成单条序列耗时 2.63s，远快于 MaskControl 的 32.79s。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比和定性可视化。
*   **消融实验**：非常详尽（表 3、4、5、7），涵盖了：
    *   解码器架构（卷积 vs. 扩散）。
    *   Token 下采样率（从 1 到 16 倍）。
    *   代码簿维度（Latent Dim）。
    *   约束注入的位置（仅规划器、仅解码器、或两者结合）。
    *   CFG（分类器自由引导）尺度的影响。
*   **充分性评价**：实验设计客观且公平，通过“两阶段训练协议”排除了 Token 信息量差异的干扰，证明了扩散解码器本身的优越性。

### 6. 主要结论与发现
*   **高效性**：MoTok 仅需 **1/6** 的 Token 数量（相比 MoMask/MaskControl），就能达到甚至超越前者的保真度。
*   **高精度控制**：在轨迹控制任务中，将平均误差从 0.72cm 降低到 **0.08cm**。
*   **正向增益**：传统方法在增加约束时 FID（质量）往往会变差，而 MoTok 在强约束下 FID 反而从 0.033 提升至 0.014，说明约束信息辅助了生成。
*   **语义增强**：实验证明 MoTok 的 Token 包含更纯粹的语义信息，在动作字幕生成（Motion-to-Text）任务中也表现更好。

### 7. 优点
*   **架构优雅**：通过“感知-规划-控制”解耦，解决了离散模型难以处理连续精确约束的顽疾。
*   **通用性强**：一套分词器可以适配自回归和离散扩散两种主流生成骨干网络。
*   **性能卓越**：在极低 Token 率下实现了极高的物理精度和视觉保真度。

### 8. 不足与局限
*   **推理延迟**：虽然比同类方法快，但由于后端仍涉及扩散模型的迭代去噪，其推理速度仍慢于纯粹的单次前向回归模型。
*   **复杂度**：三阶段框架相比端到端模型在工程实现和超参数调优上更为复杂。
*   **数据依赖**：性能高度依赖于高质量的配对数据，对于极度稀疏或长程复杂的约束，其泛化能力仍有待在更大规模数据集上验证。

（完）
