---
title: "BiMotion: B-spline Motion for Text-guided Dynamic 3D Character Generation"
title_zh: BiMotion：用于文本引导的动态 3D 角色生成的 B 样条运动
authors: "Miaowei Wang, Qingxuan Yan, Zhi Cao, Yayuan Li, Oisin Mac Aodha, Jason J. Corso, Amir Vaxman"
date: 2026-02-21
pdf: "https://arxiv.org/pdf/2602.18873v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 使用 B 样条进行文本引导的动态 3D 角色动作生成
tldr: 针对文本引导的动态3D角色生成中动作质量低、不连贯等问题，本文提出BiMotion框架。该方法利用连续可微的B样条曲线表示动作，通过拉普拉斯正则化的B样条求解器将变长序列压缩为固定控制点，并引入法线融合策略和多种损失函数。结合新构建的BIMO数据集，BiMotion在动作表现力、文本对齐度和生成速度上均优于现有技术。
selection_source: fresh_fetch
motivation: 现有方法受限于固定长度输入和离散帧表示，难以捕捉丰富的动作语义并生成连贯的长序列动作。
method: 采用连续可微的B样条曲线表示动作，并结合拉普拉斯正则化求解器、法线融合策略及局部刚性损失进行优化。
result: 在多项评估中，BiMotion在动作质量、文本对齐度及生成效率方面均显著优于现有最先进的方法。
conclusion: BiMotion通过引入B样条运动表示，为生成具有丰富语义和高连贯性的动态3D角色提供了一种高效且表现力强的解决方案。
---

## 摘要
文本引导的动态 3D 角色生成已取得快速进展，但产生能够忠实反映丰富文本描述的高质量运动仍然具有挑战性。由于固定长度的时间输入和离散的逐帧表示无法捕捉丰富的运动语义，现有方法往往会生成有限的子动作或不连贯的运动。我们通过使用连续可微的 B 样条曲线来表示运动，从而解决了这些局限性，在不修改底层生成模型能力的情况下实现了更有效的运动生成。具体而言，我们的闭式、拉普拉斯正则化 B 样条求解器能够高效地将变长运动序列压缩为具有固定数量控制点的紧凑表示。此外，我们引入了一种法线融合策略以保证对输入形状的遵循，并结合对应感知和局部刚性损失来提高运动恢复质量。为了训练我们的模型，我们整理了 BIMO，这是一个包含多样化变长 3D 运动序列及丰富、高质量文本标注的新数据集。广泛的评估表明，我们的前馈框架 BiMotion 比现有的最先进方法生成的运动更具表现力、质量更高且与提示词对齐得更好，同时还实现了更快的生成速度。我们的项目主页位于：https://wangmiaowei.github.io/BiMotion.github.io/。

## Abstract
Text-guided dynamic 3D character generation has advanced rapidly, yet producing high-quality motion that faithfully reflects rich textual descriptions remains challenging. Existing methods tend to generate limited sub-actions or incoherent motion due to fixed-length temporal inputs and discrete frame-wise representations that fail to capture rich motion semantics. We address these limitations by representing motion with continuous differentiable B-spline curves, enabling more effective motion generation without modifying the capabilities of the underlying generative model. Specifically, our closed-form, Laplacian-regularized B-spline solver efficiently compresses variable-length motion sequences into compact representations with a fixed number of control points. Further, we introduce a normal-fusion strategy for input shape adherence along with correspondence-aware and local-rigidity losses for motion-restoration quality. To train our model, we collate BIMO, a new dataset containing diverse variable-length 3D motion sequences with rich, high-quality text annotations. Extensive evaluations show that our feed-forward framework BiMotion generates more expressive, higher-quality, and better prompt-aligned motions than existing state-of-the-art methods, while also achieving faster generation. Our project page is at: https://wangmiaowei.github.io/BiMotion.github.io/.

---

## 论文详细总结（自动生成）

以下是对论文《BiMotion: B-spline Motion for Text-guided Dynamic 3D Character Generation》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的文本引导动态 3D 角色生成方法主要受限于**离散的逐帧表示**和**固定长度的时间输入**。这导致模型要么只能生成极短的动作片段（裁剪），要么生成不连贯、有抖动的动作（下采样），难以捕捉复杂的、具有完整语义的变长动作（如“士兵举手、后仰、摔倒”这一连串动作）。
*   **核心问题**：如何在容量有限、要求固定大小输入的生成模型中，表示并生成变长的、语义完整的连续 3D 运动？

### 2. 论文提出的方法论
BiMotion 采用**B 样条（B-spline）**作为运动表示的核心，将离散的帧序列转化为连续的数学曲线。
*   **B 样条运动表示**：利用闭式、拉普拉斯正则化的 B 样条求解器，将变长的顶点位移序列压缩为固定数量的控制点（Control Points）。这种表示具有连续性、可微性和局部可控性。
*   **B-spline VAE 架构**：
    *   **法线融合（Normal Fusion）**：在编码器中结合顶点位置和法线信息，增强模型对几何结构的感知，有效区分空间距离近但拓扑距离远的部分。
    *   **多级控制点嵌入**：受小波分解启发，提出层级化嵌入策略，比传统的频率位置编码更能捕捉运动的精细细节。
    *   **损失函数**：引入对应感知损失（Correspondence Loss）和局部刚性损失（Local Rigidity Loss），确保生成的动作既符合轨迹又保持物体形状不畸变。
*   **生成流程**：使用整流流匹配（Rectified Flow-Matching）和 Diffusion Transformer (DiT) 架构。在推理时，根据文本和初始形状生成运动潜码，解码为控制点，最后通过 B 样条重投影生成任意长度的平滑动画。

### 3. 实验设计
*   **数据集**：构建了 **BIMO 数据集**，包含约 3.9 万个变长 3D 运动序列，整合了 DeformingThings4D 和 Objaverse。利用 GPT-5 构建了自动标注流水线，生成高质量、风格统一的文本描述。
*   **对比方法**：
    *   **AnimateAnyMesh**（最相关的 feed-forward 基准）。
    *   **GVFDiffusion**（基于视频的 4D 生成）。
    *   **V2M4**（基于视频的 3D 映射）。
*   **评估指标**：采用 VBench（一致性、稳定性、审美、动态程度等）、用户研究（文本对齐度、合理性、表现力）以及生成效率（时间、显存）。

### 4. 资源与算力
*   **训练算力**：使用了 **8 台 NVIDIA L40 GPU**。
*   **训练时长**：VAE 和速度网络（Velocity Network）共训练了 **7 天**。
*   **推理效率**：在 A100 GPU 上，生成一个序列仅需约 **4.38 秒**，峰值显存仅 **1.25 GB**，显著优于对比方法。

### 5. 实验数量与充分性
*   **实验规模**：在 400 个验证序列上进行了定量评估，并在 20 个不同来源（AI 生成、手工建模等）的静态网格上进行了泛化性测试。
*   **消融实验**：非常充分。分别验证了 B 样条表示、拉普拉斯正则化、法线融合（NF）、多级嵌入（Control-PE）以及局部刚性损失的作用。
*   **客观性**：引入了 20 人参与的双盲用户研究，并使用了标准化的 VBench 视频评估工具，实验设计较为公正。

### 6. 论文的主要结论与发现
*   **连续性优势**：B 样条表示能以极小的代价（固定控制点）捕捉长序列动作，且支持在推理阶段生成任意帧率的平滑动画。
*   **性能领先**：BiMotion 在动作表现力、文本对齐度和几何保真度上全面超越 SOTA 方法。
*   **效率极高**：由于避免了复杂的网格拓扑预处理，其生成速度比 AnimateAnyMesh 快数倍，显存占用更低。

### 7. 优点（亮点）
*   **表示创新**：首次将 B 样条引入 3D 角色动作生成，解决了离散帧表示带来的语义断裂问题。
*   **鲁棒性强**：不依赖特定的网格拓扑，对不同来源的 3D 模型（如 Meshy, Hunyuan3D 生成的模型）具有良好的泛化能力。
*   **数据集贡献**：BIMO 数据集为 4D 角色生成研究提供了高质量的变长动作数据和文本对。

### 8. 不足与局限
*   **高频细节丢失**：对于极其复杂的动作，若控制点数量设置过少，可能会丢失部分高频运动细节。
*   **拓扑固定假设**：模型假设网格拓扑在运动过程中保持不变，无法处理涉及物体破碎、融合等拓扑变化的动作。
*   **计算依赖**：虽然推理快，但高质量 B 样条拟合在预处理阶段对大规模数据集仍有一定计算开销。

（完）
