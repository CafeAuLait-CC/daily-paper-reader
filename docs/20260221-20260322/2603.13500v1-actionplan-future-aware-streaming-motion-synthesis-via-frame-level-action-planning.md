---
title: "ActionPlan: Future-Aware Streaming Motion Synthesis via Frame-Level Action Planning"
title_zh: ActionPlan：通过帧级动作规划实现具有未来感知能力的流式动作合成
authors: "Eric Nazarenus, Chuqiao Li, Yannan He, Xianghui Xie, Jan Eric Lenssen, Gerard Pons-Moll"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13500v1"
tags: ["query:3d-motion-generation"]
score: 10.0
evidence: 用于高质量动作序列生成的动作扩散框架
tldr: "ActionPlan是一个统一的运动扩散框架，通过引入逐帧动作计划（文本潜变量）作为语义锚点，实现了实时流式传输与高质量离线生成的结合。该方法采用特定于潜变量的扩散步骤，支持独立去噪和灵活采样。它不仅在实时生成速度上提升了5.25倍，FID质量也提高了18%，并支持零样本编辑和补全。"
selection_source: fresh_fetch
motivation: 旨在解决现有运动生成模型在实时流式传输与高质量离线生成之间难以兼顾，且缺乏灵活性的问题。
method: 提出逐帧动作计划机制，利用预测的帧级文本潜变量引导去噪，并设计了支持独立采样的潜变量特定扩散步骤。
result: "相比现有最优方法，该模型在实时流式模式下速度提升了5.25倍，运动质量（FID）提升了18%。"
conclusion: ActionPlan通过结构化的动作计划实现了高效、高质量且多功能的运动合成，适用于实时交互和离线创作等多种场景。
---

## 摘要
我们提出了 ActionPlan，这是一个统一的动作扩散框架，在单个模型中实现了实时流式传输与高质量离线生成的结合。其核心思想是引入逐帧动作计划：模型预测帧级文本潜变量，这些潜变量在整个去噪过程中充当密集的语义锚点，并利用它们结合语义和动作线索来对整个动作序列进行去噪。为了支持这种结构化工作流，我们设计了特定于潜变量的扩散步长，允许每个动作潜变量在推理时独立去噪并以灵活的顺序进行采样。因此，ActionPlan 可以在历史条件化、未来感知的模式下进行实时流式传输，同时也支持高质量的离线生成。同样的机制还进一步实现了无需额外模型的零样本动作编辑和动作补全。实验表明，我们的实时流式传输速度提高了 5.25 倍，同时在 FID 指标上比之前的最佳方法提升了 18% 的动作质量。

## Abstract
We present ActionPlan, a unified motion diffusion framework that bridges real-time streaming with high-quality offline generation within a single model. The core idea is to introduce a per-frame action plan: the model predicts frame-level text latents that act as dense semantic anchors throughout denoising, and uses them to denoise the full motion sequence with combined semantic and motion cues. To support this structured workflow, we design latent-specific diffusion steps, allowing each motion latent to be denoised independently and sampled in flexible orders at inference. As a result, ActionPlan can run in a history-conditioned, future-aware mode for real-time streaming, while also supporting high-quality offline generation. The same mechanism further enables zero-shot motion editing and in-betweening without additional models. Experiments demonstrate that our real-time streaming is 5.25x faster while also achieving 18% motion quality improvement over the best previous method in terms of FID.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ActionPlan** 的新型人体动作生成框架，旨在解决实时流式生成与高质量全局一致性之间的矛盾。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心矛盾**：现有的文本驱动动作生成方法分为两类：**离线框架**（质量高但需要全局信息，无法实时）和**流式框架**（延迟低但缺乏“远见”，容易丢失复杂指令中的语义一致性）。
*   **研究动机**：作者认为有效的在线动作合成需要“预见”即将发生的动作，而不是仅仅对过去做出反应。
*   **整体含义**：ActionPlan 通过引入“帧级动作计划”（Frame-Level Action Plan）作为语义锚点，将高层语义规划与底层运动合成解耦，从而在保持低延迟流式传输的同时，实现了超越离线模型的生成质量。

### 2. 论文提出的方法论
*   **核心思想**：分两阶段生成。首先根据全局文本生成一个与时间对齐的逐帧文本潜变量序列（动作计划），然后以此为条件引导动作潜变量的去噪。
*   **关键技术细节**：
    *   **分层表示**：使用 Causal TAE（因果时间自编码器）处理动作，并使用预训练的 MLP 自编码器将 CLIP 文本嵌入压缩为 16 维的帧级文本潜变量。
    *   **特定于潜变量的扩散步长（Latent-specific Timesteps）**：为每个动作帧分配独立的噪声水平（$t_{x_i}$），而文本计划共享全局步长（$t_y$）。这允许在推理时以任意顺序（如随机或光栅顺序）进行采样。
    *   **联合扩散模型**：基于 Transformer 的去噪器 $G_\theta$ 同时预测动作和文本潜变量的去噪速度（Rectified Flow 架构）。
    *   **灵活采样调度**：
        *   **离线模式**：先生成完整动作计划，再以随机金字塔顺序去噪动作。
        *   **流式模式**：在去噪第一个动作帧的同时生成动作计划，随后以光栅顺序（Raster Order）逐帧推进。

### 3. 实验设计
*   **数据集**：主要使用 **HumanML3D-272**（高质量 272 维 SMPL 表示版本）和 **BABEL-272**（提供帧级动作标注）。
*   **Benchmark（基准指标）**：FID（动作质量）、R-Precision（语义准确度）、Multi-modal Matching Score（多模态匹配）和 Diversity（多样性）。
*   **对比方法**：包括扩散模型（MDM, MLD）、VQ 类模型（T2M-GPT, MoMask）、流式模型（MotionStreamer）和掩码自回归模型（MARDM）。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练时长**：训练 10,000 个 epoch，耗时约 **26 小时**。
*   **推理效率**：在单张 A100 上，流式模式下生成单个 token（4 帧）的延迟仅为 **40ms**，比现有最优方法快 5.25 倍。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量对比**：在标准测试集上与 8 种 SOTA 方法进行了全面对比。
    *   **消融实验**：针对帧级文本预测、混合数据集训练、动作计划生成策略、采样调度窗口等关键变量做了 5 组以上的对比实验。
    *   **用户研究**：邀请 30 名参与者对 40 组生成结果（文本生成动作、长序列生成）进行主观评价。
*   **充分性与公平性**：所有基线模型均在相同的 272 维表示下重新训练，确保了对比的客观性。消融实验清晰地展示了“动作计划”对提升 FID 的直接贡献。

### 6. 论文的主要结论与发现
*   **性能飞跃**：ActionPlan 在离线模式下 FID 提升了 22%；在流式模式下，FID 比之前的最佳流式方法提升了 51%，甚至优于大多数离线模型。
*   **速度优势**：通过重叠去噪窗口（Overlapping Denoising），实现了极高的吞吐量，支持真正的实时交互。
*   **多功能性**：单个模型无需微调即可支持**动作编辑（Editing）**、**动作补全（In-betweening）**和**长序列流式生成**。

### 7. 优点（亮点）
*   **解耦设计**：将“做什么”和“怎么做”分开，解决了流式生成中的语义漂移问题。
*   **统一框架**：一个模型涵盖了从实时交互到高质量离线创作的所有场景。
*   **零样本能力**：利用灵活的噪声调度，实现了多种下游任务的零样本迁移。
*   **未来感知**：即使在流式模式下，由于提前生成了动作计划，模型也具备了对未来动作的感知能力。

### 8. 不足与局限
*   **细节缺失**：目前无法建模手指关节运动和面部表情（受限于数据集）。
*   **环境感知**：缺乏对场景或物体的感知，无法处理复杂的人机/人环交互任务（如搬椅子、开门）。
*   **数据依赖**：动作计划的训练依赖于 BABEL 等具有帧级标注的数据，这类高质量标注数据的获取成本较高。

（完）
