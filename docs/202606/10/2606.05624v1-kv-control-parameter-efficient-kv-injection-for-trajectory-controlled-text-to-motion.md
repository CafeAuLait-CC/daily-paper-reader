---
title: "KV-Control: Parameter-Efficient K/V Injection for Trajectory-Controlled Text-to-Motion"
title_zh: KV-Control：面向轨迹控制文本到运动的参数高效K/V注入
authors: "Tengjiao Sun, Pengcheng Fang, Xiaoyu Zhan, Yanwen Guo, Dongjie Fu, Xiaohao Cai, Hansung Kim"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05624v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 文本到3D人体动作生成与轨迹控制
tldr: 文本到运动生成模型难以精确控制轨迹，现有方法需大量参数或测试时优化。KV-Control在冻结的Transformer自注意力层注入控制条件的键值记忆，配合PartVQ和T-Concat实现细粒度控制。在继承细化协议下，根和关节约束追踪精度达子厘米级，且保留运动质量。该方法将轨迹条件转化为轻量级记忆检索，提供了精确、透明的控制接口。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05624v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1809, \"height\": 569}, {\"url\": \"assets/figures/arxiv/2606.05624v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1768, \"height\": 1038}, {\"url\": \"assets/figures/arxiv/2606.05624v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1761, \"height\": 547}, {\"url\": \"assets/figures/arxiv/2606.05624v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1758, \"height\": 778}, {\"url\": \"assets/figures/arxiv/2606.05624v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1585, \"height\": 1681}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05624v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 229}, {\"url\": \"assets/tables/arxiv/2606.05624v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 862, \"height\": 463}, {\"url\": \"assets/tables/arxiv/2606.05624v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 919, \"height\": 163}, {\"url\": \"assets/tables/arxiv/2606.05624v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1465, \"height\": 805}, {\"url\": \"assets/tables/arxiv/2606.05624v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 983, \"height\": 555}, {\"url\": \"assets/tables/arxiv/2606.05624v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 724, \"height\": 393}]"
motivation: 现有轨迹控制方法需复制大量生成器参数或依赖测试时优化，效率低且破坏文本条件运动先验。
method: 在冻结的掩码文本到运动Transformer每层自注意力中注入控制条件的键值记忆，并设计PartVQ和T-Concat实现逐帧部分令牌控制。
result: 在继承的细化协议下，根和关节约束追踪精度达子厘米级，同时保持文本条件运动质量和生成速度。
conclusion: KV-Control将轨迹条件建模为轻量级记忆检索，为文本到运动生成提供参数高效、精确且透明的控制接口。
---

## 摘要
基于文本的三维人体运动模型现在可以从提示中合成合理的运动，但实际的动画和具身智能体工作流程很少止步于文本：角色可能需要遵循手绘的根路径、击中末端效应器目标、或满足多关节轨迹，同时仍然保持语言描述的步态、风格和意图。这暴露了一个控制权衡问题。轨迹控制器应精确而不覆盖预训练的文本条件运动先验，但现有解决方案要么复制生成器的大部分以重新获得逐层控制访问，要么将大量成本转移到测试时优化。我们提出KV-Control，一种用于冻结掩码文本到运动变压器的紧凑注意力侧控制接口。关键思想是使几何约束作为自注意力内部的记忆可用，而不是通过全局姿态token注入或仅在输出侧强制执行。为支持此接口，我们共同设计了部分标记化的运动基础与控制器：\textbf{PartVQ}学习解剖对齐的部分码本，T-Concat将每个帧-部分token暴露为注意力可寻址位置，而KV-Control在每个自注意力层注入控制条件的键/值记忆，同时保留预训练的查询流、文本交叉注意力、FFN和所有骨干权重。生成的适配器仅在共享轨迹编码器之上添加可训练的注入参数，但在继承的细化协议下以亚厘米精度跟踪根和多关节约束，同时保留文本条件运动质量。KV-Control将轨迹条件重述为轻量级记忆检索，为文本到运动生成提供了一个小巧、精确且透明的控制接口。

## Abstract
Text-conditioned 3D human motion models now synthesize plausible motions from prompts, but practical animation and embodied-agent workflows rarely stop at text: a character may need to follow a sketched root path, hit an end-effector target, or satisfy a multi-joint trajectory while still preserving the gait, style, and intent described by language. This exposes a control trade-off. A trajectory controller should be precise without overwriting the pretrained text-conditioned motion prior, yet existing solutions either duplicate large portions of the generator to regain per-layer control access or move much of the cost to test-time optimization. We introduce KV-Control, a compact attention-side control interface for frozen masked text-to-motion transformers. The key idea is to make geometric constraints available as memory inside self-attention rather than injecting them through a global pose token or enforcing them only at the output side. To support this interface, we co-design a part-tokenized motion substrate and controller: \textbf{PartVQ} learns anatomy-aligned part codebooks, T-Concat exposes each frame--part token as an attention-addressable site, and KV-Control injects control-conditioned key/value memories at every self-attention layer while preserving the pretrained query stream, text cross-attention, FFN, and all backbone weights. The resulting adapter adds only trainable injection parameters atop a shared trajectory encoder, yet tracks root and multi-joint constraints with sub-centimeter accuracy under the inherited refinement protocol while retaining text-conditioned motion quality. KV-Control reframes trajectory conditioning as lightweight memory retrieval, providing a small, precise, and transparent control interface for text-to-motion generation.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：KV-Control：面向轨迹控制文本到运动的参数高效K/V注入

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：文本条件的三维人体运动生成模型已能根据提示合成合理运动，但在实际动画和具身智能体工作流中，用户常常需要让角色遵循特定的轨迹（如手绘根路径、末端效应器接触点、多关节轨迹），同时保持文本描述的步态、风格和意图。现有方法面临控制权衡：要么复制生成器的大部分参数以重新获得逐层控制访问（如ControlNet式适配器），导致参数开销大；要么将大量成本转移到测试时优化（如迭代潜码优化），增加推理耗时。缺乏一种既参数高效又保留文本先验的精确控制接口。
- **整体含义**：本文提出一种紧凑、参数高效的注意力侧控制接口KV-Control，将轨迹条件建模为轻量级记忆检索，在冻结的掩码文本到运动Transformer中实现精确的根和多关节轨迹控制，同时维持运动生成质量和效率。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将几何约束作为自注意力内部的附加记忆（key/value）注入，而非通过全局pose token或仅在输出侧强制执行。为此，论文联合设计了三个组件：
  - **PartVQ（解剖感知部分令牌化）**：利用滞后互相关聚类将身体划分为6个数据驱动解剖部分（如骨盆、头、左右脚、左右手腕），每个部分拥有独立的VQ-VAE码本。这使身体结构被显式分解，为细粒度控制提供基础。
  - **T-Concat（令牌级串联骨干）**：将PartVQ的各部分码本沿序列轴展开，使每个(帧, 部分)对成为一个独立的注意力可寻址token（序列长度S = Ttok × Q，Q=6）。相比通道串联（C-Concat），T-Concat使后续K/V注入能够精确作用于特定解剖部分的表示，同时保留每位置文本交叉注意力。
  - **KV-Control（K/V注入适配器）**：在每个自注意力层，将控制轨迹（由共享1D-Conv编码器处理）通过零初始化的低秩投影（rank=64）生成附加的key/value伪token，并拼接到原始key/value序列中。查询流（query）保持不变，文本交叉注意力路径不受干扰。通过控制列注意力偏置（初始化为强负值）和低秩投影的零初始化，适配器开始时不改变原始输出，随着训练自适应学习控制信号的影响。
- **关键细节**：
  - 控制编码器从当前预测的运动（通过冻结的PartVQ解码器获得）与目标轨迹的残差构建输入，结合关节-帧掩码。
  - 训练目标：λ_CE * L_CE（运动令牌掩码交叉熵） + λ_traj * L_traj（受控关节的FK监督损失，通过软码本松弛实现可微）。
  - 推理时可选用多阶段细化（M0/M1/M2/M3）。M2/M3继承MaskControl的细化协议（静态/动态logit优化+后采样令牌优化）。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：HumanML3D（标准文本-运动基准，包含约14616个文本-运动对）。
- **评估协议**：采用MaskControl协议（CFG 3.25，T=10步，种子3407，批次32）。
- **对比方法**：
  - **单关节（骨盆）控制**：GMD、OmniControl、MotionLCM、TLControl、MaskControl（含/不含logit优化）。
  - **多关节控制**：OmniControl、TLControl、MaskControl。
  - **骨干布局消融**：T-Concat vs C-Concat（通道串联）。
  - **机制对比**：KV-Control vs 同骨干的ControlNet式复制分支（39.3M参数）。
  - **细化基线**：不带KV适配器的纯T-Concat骨干进行M2/M3细化。

## 4. 资源与算力

- 文中未明确给出训练总时长、GPU数量等信息，但提及“Wall-clock is approximate per-sample latency on one H100”。在表1脚注中注明：M0约1.5s/样本，M3约15s/样本（H100）。训练阶段使用的GPU型号和数量未说明，但可推测基于H100或类似硬件。
- **不足之处**：缺乏系统性的训练资源报告，妨碍完全复现和算力成本评估。

## 5. 实验数量与充分性

- **实验组数**：论文进行了多组定量和定性实验：
  - 骨干布局消融（表2）：T-Concat vs C-Concat，在M2/M3协议下对比控制精度和生成质量。
  - 轨迹控制对比（表4）：单关节（骨盆）和多关节（所有关节）的全面对比，包含M0/M1/M2/M3四种配置，并与6种先前方法比较。
  - 扩展诊断（表5）：包括匹配得分、多样性、细化基线（对照无KV适配器的情况）。
  - 单关节分解（表6）：多关节适配器对每个解剖部分单独测试控制精度和FID。
  - 定性结果（图3-5）：OOD字母轨迹、多关节应力示例，展示视觉多样性。
- **充分性评价**：实验设计较为充分，覆盖了主要控制场景和消融维度。所有实验在相同协议下进行，对比公平。但缺少对训练稳定性的分析（如不同随机种子下的方差）、仅使用HumanML3D一个数据集（未在AMASS等更大规模数据集验证），可能限制泛化性。

## 6. 论文的主要结论与发现

- **主要结论**：
  1. KV-Control在冻结的T-Concat骨干上仅1.5M机制参数（+9M共享编码器，总计10.5M）即可实现亚厘米精度的轨迹控制（骨盆M3: 0.40cm，多关节M3: 0.71cm）。
  2. 在MaskControl匹配的M2协议下，KV-Control（0.51cm）显著优于同骨干的ControlNet式复制分支（1.24cm，39.3M参数，约26×的更少）。
  3. T-Concat布局相比C-Concat布局在控制精度上提升约2倍（M2: 0.51 vs 1.01cm），证明了解剖部分可寻址表示的重要性。
  4. 细化单独不足以实现控制：没有KV适配器时，细化使位置误差趋近于零但破坏文本匹配（FID炸裂至~104），说明适配器提供了必要的结构条件。
- **发现**：KV-Control通过注意力侧记忆注入实现了参数效率与精度的双赢，并且在不同操作点（M0~M3）下保持文本条件运动质量（FID约0.07，Top-3约0.80）。

## 7. 优点：方法或实验设计上的亮点

- **方法优点**：
  - 极低的参数开销：仅1.5M机制参数，保持骨干完全冻结，易于集成到现有系统。
  - 设计优雅：将轨迹控制转化为轻量级记忆检索，查询流不变，文本交叉注意力无损。
  - 解剖感知分解：PartVQ + T-Concat使控制信号可精确瞄准特定身体部分，减少纠缠。
  - 双近恒等初始化（零低秩投影 + 强负偏置）使适配器易于训练且不破坏初始预训练性能。
- **实验亮点**：
  - 系统消融骨干布局和机制，强有力证明了T-Concat布局的必要性。
  - 设置了四种操作点（M0~M3）覆盖纯前馈到强细化，清晰展示适配器本身的能力和细化补充作用。
  - 包含了“细化基线”作为关键对照，排除了细化本身解释控制能力的可能。
  - 定量和定性结合，定性展示OOD轨道（字母形）和多样化文本提示。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验覆盖不足**：
  - 仅在一个数据集（HumanML3D）上评估，缺少在更大规模或不同骨架结构（如SMPL-X）上的泛化验证。
  - 未报告多个随机种子下的方差，可能影响少数结果（如脚部FID超过0.1）的统计显著性。
  - 仅测试了掩码Transformer骨干，未迁移至扩散（如MDM）或自回归（如T2M-GPT）模型。
- **偏差风险**：
  - 细化的非对称使用：多关节配置下，M1阶段误差仍为3.24cm，提示适配器本身在不细化时效果有限，这可能因关节之间竞争而被掩盖。
  - 脚部FID较高（左0.117，右0.102）表明该方法在密集脚部接触约束时仍存在退步风险，与现实应用中对脚滑的严格控制不符。
- **应用限制**：
  - 推理时若使用强细化（M3），每样本耗时约15s（H100），对实时应用不友好。
  - 控制信号目前仅作为记忆附加，不强制约束，当轨迹需要精确到毫米级且复杂时，可能依赖较多细化迭代。
  - 部分划分（Q=6）是经验设定，未测试其他划分数量或更细粒度的解剖分组。

（完）
