---
title: "Towards 3D-Aware Video Diffusion Models: Render-Free Human Motion Control with Mesh Tokenization"
title_zh: 面向3D感知的视频扩散模型：基于网格标记的无渲染人体运动控制
authors: "Jingyun Liang, Min Wei, Shikai Li, Yizeng Han, Hangjie Yuan, Lei Sun, Weihua Chen, Fan Wang"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02000v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: 使用扩散模型和网格令牌化进行3D人体运动控制
tldr: 视频扩散模型是否真正理解3D结构尚待验证。本文通过人类运动控制任务，提出无需渲染的框架，直接使用压缩的3D人体网格令牌，在DiT架构中联合视频与运动令牌生成。实验表明该方法在运动控制基准上表现优异，减少2D引导导致的伪影。结论证明网格令牌化能增强模型对复杂3D人体结构及其环境交互的建模能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02000v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1768, \"height\": 699, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02000v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 854, \"height\": 708, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02000v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 854, \"height\": 703, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02000v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 831, \"height\": 711, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02000v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 850, \"height\": 821, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02000v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1712, \"height\": 322, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02000v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1652, \"height\": 405, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02000v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1736, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02000v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 760, \"height\": 652, \"label\": \"Table\"}]"
motivation: 探究视频扩散模型能否真正感知3D结构，而非仅复现2D投影，通过人类运动控制这一需要精确3D建模的任务来检验。
method: 提出无需渲染的框架，将视频生成直接基于压缩的3D人体网格令牌，与视频令牌在DiT架构中联合处理，保留完整3D几何信息。
result: 在人类运动控制基准上取得强性能，减少了视图依赖2D引导和编辑中轨迹-姿态不匹配导致的伪影。
conclusion: 视频扩散模型配备网格令牌化后，能更好地捕捉复杂3D人体结构及其与环境的交互。
---

## 摘要
扩散模型在视频生成方面取得了显著成功。然而，这些模型是否真正意识到视觉观察背后的3D结构，而不仅仅是生成合理的2D投影，仍然是一个未解之谜。在本文中，我们通过人体运动控制这一任务来探讨这个问题，该任务要求精确建模3D人体几何、运动、相机视角和场景上下文。与以往依赖渲染的2D运动引导视频的方法不同，我们提出了一种无渲染框架，直接以压缩的3D人体网格标记作为视频生成的条件。这种表示保留了完整的3D几何信息，同时支持基于标记的统一生成流程，在基于DiT的架构中将视频标记与运动标记联合处理。这种设计要求模型在视频生成过程中联合推理外观、3D结构和相机视角。实验结果表明，该方法在人体运动控制基准上表现强劲，同时减少了由视角依赖的2D引导以及编辑过程中轨迹-姿态不匹配所导致的伪影。这些发现表明，配备网格标记的视频扩散模型能够更好地捕捉复杂的人体3D结构及其与周围环境的交互。

## Abstract
Diffusion models have shown remarkable success in video generation. However, whether such models are truly aware of the 3D structure underlying visual observations, rather than simply reproducing plausible 2D projections, remains an open question. In this work, we investigate this question through human motion control, a task that requires precise modelling of 3D human geometry, motion, camera viewpoint, and scene context. Unlike prior methods that rely on rendered 2D motion guidance videos, we propose a render-free framework that conditions video generation directly on compressed 3D human mesh tokens. This representation preserves full 3D geometric information while enabling a unified token-based generation pipeline that processes video tokens jointly with motion tokens in a DiT-based architecture. This design requires the model to reason jointly about appearance, 3D structure, and camera viewpoint during video generation. Experimental results demonstrate strong performance on human motion control benchmarks, while reducing artifacts induced by view-dependent 2D guidance and trajectory-pose mismatches during editing. These findings suggest that video diffusion models, when equipped with mesh tokenization, can better capture complex 3D human structures and their interactions with the surrounding environment.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：视频扩散模型（VDM）是否真正理解视觉观测背后的3D结构，还是仅仅复现合理的2D投影？
- **研究动机**：现有可控人体视频生成方法依赖渲染的2D引导信号（如骨架、深度图、法线图等），这些表示本质上是视图依赖的，丢失了3D几何信息（如遮挡面、背面），模型可能退化为视频到视频的翻译，而非对3D运动进行推理。因此，需要一个能够检验VDM 3D感知能力的任务框架。
- **整体含义**：本文通过人体运动控制任务，提出无渲染的3D网格标记化框架，直接以压缩的3D人体网格标记作为条件，迫使模型在生成过程中联合推理外观、3D结构和相机视角，从而评估VDM的3D意识。实验表明，VDM在获得适当3D表示后能够更好捕捉人体3D结构。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将人体运动表示为SMPL网格序列，通过VQ-VAE压缩为离散潜标记，结合轨迹信息构成运动标记；再将运动标记通过交叉注意力注入DiT视频生成骨干（基于Wan2.1 I2V），实现无渲染的端到端条件生成。
- **关键技术细节**：
  - **运动表示**：使用SMPL参数模型，将人体网格表示为全局平移、全局旋转、姿态参数和形状参数，仅保留与运动相关的部分。
  - **网格标记化**：
    - 将网格平移到原点并移除全局旋转，得到规范网格；丢弃形状参数，聚焦姿态。
    - 使用全卷积网格自编码器将6890个顶点映射到N=54个低维潜向量（每向量9维）。
    - 通过VQ-VAE量化，从大小为S×L的码本中查找最近项，得到离散标记组 \( Z_p \in \mathbb{R}^{54 \times 9} \)。
    - 对序列施加自注意力层增强跨身体部分和时间的连贯性，再线性投影到DiT通道维度（5120）。
  - **轨迹嵌入**：将全局平移（\( \gamma \)）、世界空间旋转矩阵（\( R_w \)）和相机空间旋转矩阵（\( R_c \)）通过MLP映射得到轨迹嵌入 \( Z_t \in \mathbb{R}^{5120} \)，与姿态标记相加得到最终运动标记 \( Z_m \)。
  - **统一视频生成**：基于Wan2.1 I2V模型冻结骨干，在每一DiT块中的文本交叉注意层后插入每帧交叉注意模块，使每个潜视频帧只关注对应时间步的运动标记。新模块的最后一层权重初始化为零。训练采用rectified flow matching目标。
- **公式/伪代码**（文字描述）：  
  \( Z_p = Q(\phi(V)) \) （VQ编码）  
  \( \hat{Z}_p = Proj(Z_p + SA(Z_p)) \)  
  \( Z_t = MLP([\gamma_w, vec(R_w), \gamma_c, vec(R_c)]) \)  
  \( Z_m = Z_t + \hat{Z}_p \)  
  \( \bar{Z}_t = Z_t + CA(Z_t, Z_m) \) （每帧交叉注意）

### 3. 实验设计

- **数据集**：使用内部人体运动数据集（约300K视频片段，多分辨率、多帧率），通过GVHMR估计人体网格，LLaVA生成文本描述。
- **基准（Benchmark）**：
  - **Trajectory100**：包含全局平移和旋转评估指标。
  - **RealisDance-Val**：使用VBench-I2V指标（I2V Subject/BG、Subject Consistency、Background Consistency、Temporal Flicker、Motion Smooth、Dynamic Degree、Aesthetic Quality）。
- **对比方法**：
  - 直接基线：Wan2.1-I2V（骨干模型）、Tora、RealisDance-DiT、RealisMotion（使用背景输入）。
  - 更多方法：Animate-X、ControlNeXt、MimicMotion、MooreAA、MusePose等（在RealisDance-Val上对比）。
- **评估指标**：PSNR、SSIM、LPIPS、FID、FVD、Translation Error (m)、Rotation Error (deg)。

### 4. 资源与算力

- **训练资源**：未明确说明GPU型号，但提及使用8块H100 GPU进行推理（训练也推测使用类似配置）。训练采用级联策略：先在240p分辨率训练160k步（batch size 64），再480p 80k步（batch 32），最后720p 40k步（batch 8），总训练约20天。优化器为AdamW，学习率1e-5，权重衰减1e-4。
- **推理资源**：生成97帧720p视频需约4分钟（8 H100 GPU）。
- **模型规模**：总参数20.33B（仅训练新增运动模块，骨干冻结）。

### 5. 实验数量与充分性

- **实验组数**：
  - 在两个基准数据集上进行定量比较（表1、表2），覆盖多个基线。
  - 编辑运动实验（表3）：选取50个视频，修改动作为“Walking”和“Jogging”两种模式，对比Wan2.1-I2V和RealisDance-DiT。
  - 消融实验（表4）：包括轨迹条件（完整/无/仅相机/仅世界）、姿态输入（无/无精炼）、运动表示（MLP编码/SMPL参数）、每帧交叉注意 vs 全局交叉注意、骨干冻结 vs 微调。共10+组实验。
  - 可视化对比（图2-5）提供定性支持。
- **充分性与公平性**：
  - 消融实验覆盖关键设计选择，分析充分。
  - 对比方法包括相同骨干的RealisDance-DiT，保持公平。
  - 编辑运动实验验证了方法对不完美运动输入的鲁棒性。
  - 客观指标全面（PSNR/SSIM/LPIPS/FID/FVD+运动误差+VBench），主观也有图示。
- **结论**：实验设计合理，结果可信，消融充分。

### 6. 论文的主要结论与发现

- 视频扩散模型在配备3D网格标记化后，能够更好地捕捉复杂3D人体结构及其与环境交互，表现出较强的3D感知能力。
- 无渲染框架避免了视图依赖2D引导的信息损失，在视角变化和运动编辑场景下鲁棒性更优。
- 分解的轨迹+姿态表示允许更灵活的编辑控制，减少伪影（如脚漂浮、穿透、运动不连贯）。
- 在Trajectory100和RealisDance-Val上，所提方法在多项指标上达到或超越现有方法，尤其在高动态和遮挡场景下优势明显。

### 7. 优点

- **方法创新**：首次提出无渲染的3D网格标记化条件框架，直接压缩3D运动为令牌，保留完整几何信息，避免渲染瓶颈。
- **架构统一**：所有模态（图像、视频、文本、运动）均表示为令牌，在DiT中通过注意力交互，设计简洁。
- **控制解耦**：轨迹和姿态分解，支持独立编辑和组合控制，提升编辑鲁棒性。
- **实验扎实**：多基准、多基线、多消融，定性定量结合，论证充分。
- **实际效果**：在视角变化、人体-物体交互、编辑场景下视觉质量明显优于对比方法。

### 8. 不足与局限

- **数据域限制**：仅针对人体运动控制，未推广到通用3D物体或动态场景；训练数据为内部人体数据集，分布可能受限。
- **SMPL估计依赖性**：运动表示依赖单目3D人体估计方法GVHMR，其误差会传播到下游（虽然论文报告了低重建误差，但未见鲁棒性测试）。
- **计算资源需求大**：模型20.33B参数，训练20天，推理4分钟/97帧（8 H100），部署成本高。
- **实验覆盖有限**：未评估复杂多人交互或大幅度快速运动场景；未与更多近期方法（如基于3D高斯泼溅的方法）对比。
- **量化指标改进幅度小**：在部分指标上虽最优但领先不明显（如表1中Translation Error与RealisDance-DiT接近）；消融实验中某些设计（如冻结骨干 vs 微调）差异很小，可能未充分挖掘潜力。
- **安全性/偏置**：未讨论生成内容的公平性或潜在滥用风险。

（完）
