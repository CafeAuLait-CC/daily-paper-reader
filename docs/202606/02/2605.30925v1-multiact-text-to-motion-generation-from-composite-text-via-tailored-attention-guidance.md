---
title: "MultiAct: Text-to-Motion Generation from Composite Text via Tailored Attention Guidance"
title_zh: "MultiAct: 通过定制注意力引导从复合文本生成运动"
authors: "Nathan Sala, Ofir Abramovich, Ariel Shamir, Daniel Cohen-Or, Andreas Aristidou, Sigal Raab"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.30925v1"
tags: ["query:3d-motion-generation"]
score: 9.0
evidence: "文本到动作生成,针对复合动作通过注意力引导改进预训练生成器"
tldr: 文本到动作生成模型处理复合描述时，常偏向单一动作而忽略其他，导致语义损失。本文提出MultiAct，一种无需重新训练的推理时框架，通过自适应增强与未充分表示组件相关的交叉注意力分数，并引入轻量级决策方案自动选择最优参数。实验表明，MultiAct在复合提示上显著优于基线，提升了语义覆盖同时保持了运动真实感。该工作为复合文本驱动的动作生成提供了即插即用的注意力增强解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30925v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 880, \"height\": 135}, {\"url\": \"assets/figures/arxiv/2605.30925v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 487}, {\"url\": \"assets/figures/arxiv/2605.30925v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 878, \"height\": 565}, {\"url\": \"assets/figures/arxiv/2605.30925v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 886, \"height\": 463}, {\"url\": \"assets/figures/arxiv/2605.30925v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 508, \"height\": 472}, {\"url\": \"assets/figures/arxiv/2605.30925v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1860, \"height\": 601}, {\"url\": \"assets/figures/arxiv/2605.30925v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1851, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2605.30925v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 765, \"height\": 480}, {\"url\": \"assets/figures/arxiv/2605.30925v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1482, \"height\": 357}, {\"url\": \"assets/figures/arxiv/2605.30925v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 890, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.30925v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1299, \"height\": 1880}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.30925v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 812, \"height\": 455}, {\"url\": \"assets/tables/arxiv/2605.30925v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1325, \"height\": 401}, {\"url\": \"assets/tables/arxiv/2605.30925v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 883, \"height\": 206}, {\"url\": \"assets/tables/arxiv/2605.30925v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 833, \"height\": 405}]"
motivation: 现有模型处理复合文本提示时，常忽略部分动作导致语义崩溃，需无需重训的改进方法。
method: MultiAct在推理时自适应放大交叉注意力分数，并利用轻量级决策方案确定最佳强化参数。
result: 定量和定性评估表明，MultiAct在复合提示上优于基线，提升语义覆盖且运动真实感不变。
conclusion: MultiAct无需重训即可改善复合文本到动作生成的完整性和真实感，具有即插即用优势。
---

## 摘要
文本到运动生成近年来发展迅速，为动画和人机交互提供了富有表现力的接口。然而，当前模型在处理描述同时发生的多个动作的提示时仍然脆弱。模型往往优先考虑单个主导动作而忽略其余部分，而不是实现复合描述的所有组成部分，导致运动不完整或模糊。我们提出MultiAct，一种无配对、推理时的组合文本到运动合成框架，直接对预训练的运动生成器进行操作，无需重新训练或架构修改。我们的方法通过自适应放大与代表性不足的提示组件相关的交叉注意力分数来对抗语义崩溃。我们注意到有效调制依赖于提示特定的选择，例如目标标记和层，并引入了一个轻量级辅助决策方案，确定最有效的注意力增强参数化。大量的定量和定性评估表明，MultiAct在复合提示上始终优于现有基线，实现了改进的语义覆盖，同时保持了运动真实性。项目页面：https://natsala13.github.io/multiact.github.io。

## Abstract
Text-to-motion generation has progressed rapidly in recent years, offering an expressive interface for animation and human-computer interaction. However, current models remain brittle when handling prompts that describe multiple actions occurring at the same time. Rather than realizing all components of a composite description, models frequently prioritize a single dominant action and neglect the rest, leading to incomplete or ambiguous motion. We present MultiAct, an unpaired, inference-time framework for compositional text-to-motion synthesis that operates directly on pretrained motion generators without retraining or architectural modification. Our method counteracts semantic collapse by adaptively amplifying cross-attention scores associated with underrepresented prompt components. We note that effective modulation depends on prompt-specific choices, such as which tokens and layers to target, and introduce a lightweight auxiliary decision scheme that determines the most effective attention-strengthening parametrization. Extensive quantitative and qualitative evaluations demonstrate that MultiAct consistently outperforms existing baselines on composite prompts, achieving improved semantic coverage while preserving motion realism. Project page: https://natsala13.github.io/multiact.github.io.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有文本到运动生成模型在处理**复合文本提示**（描述同时发生的多个动作，如“跑步同时挥动手臂”）时，会面临**语义崩溃（vanishing semantics）**——模型倾向于仅增强主导动词对应的动作，而压制甚至完全忽略其他动作语义（如“挥动手臂”或“向后移动”），导致生成的运动不完整、语义覆盖差。
- **研究背景**：虽然文本到运动生成领域已取得显著进展（如MDM、MoMask等），但这些模型在处理**同步复合动作**时表现脆弱。现有方法要么需要结构化输入（如STMC要求动作-身体部位映射），要么依赖额外的图推理网络（如GraphMotion），限制了推理时的通用性和即插即用能力。
- **整体含义**：作者提出**MultiAct**，一种**无配对、推理时**的框架，无需重新训练或修改原有生成器架构，直接通过**自适应增强交叉注意力**来缓解语义崩溃，为复合文本驱动的运动生成提供了一种轻量、即插即用的解决方案。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在预训练的扩散运动生成器（MDM*）的逆向扩散过程中，对**未充分代表的文本token**的交叉注意力分数进行梯度上升，使其对最终生成结果产生更强的语义约束，从而恢复缺失的动作细节。
- **关键技术细节**：
  1. **注意力对齐损失**：  
     定义损失  
     \[
     \mathcal{L}_{\text{atn}} = \frac{1}{N}\sum_{i=1}^N (1 - A_{i,m})^2
     \]  
     其中 \(A_{i,m}\) 是第 \(i\) 帧对第 \(m\) 个token的交叉注意力分数，\(N\) 为帧数。该损失鼓励注意力分数趋近于1，增强对目标token的关注。
  2. **参数化注意力优化**：  
     在选定的扩散步 \(t\)、变换器层 \(\ell\) 和token索引 \(m\) 下，对运动潜在表示 \(X\) 进行梯度更新：
     \[
     X' = X - \eta \nabla_X \mathcal{L}_{\text{atn}}
     \]
  3. **ParamGate决策方案**：  
     自动预测对每个提示最优的参数组合 \(\theta = \{m, \ell, \hat{t}\}\)（token索引、层索引、应用优化的扩散步范围）。包括：
     - **层选择**：通过最近邻搜索，从预计算数据集中找出语义最相似的提示，并继承其最优层。
     - **步数选择**：基于骨干网络生成运动的偏差误差，利用预定义阈值进行分段选择。
     - **token选择**：两种策略：① 快速法：从聚类剪枝后的候选集（动作细节token）中随机选；② 测试时缩放：用LLM提取候选token（动词、动作细节），逐一生成运动并评估偏差，选最优。
  4. **算法流程**（文字说明）：  
     ① 给定提示 \(y\)，ParamGate预测参数 \(\theta\)。  
     ② 在逆向扩散的第 \(T\) 到 \(\hat{t}\) 步之间，每步先执行**注意力优化**（算法1）：计算当前 \(X_t\) 的交叉注意力，得到 \(\mathcal{L}_{\text{atn}}\)，梯度下降更新 \(X_t\)。  
     ③ 执行标准扩散去噪步骤，预测 \(\hat{X}_0\)，再重噪声到 \(X_{t-1}\)。  
     ④ 重复至 \(t=0\)，输出干净运动。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：  
  - 主体实验使用**自建的复合提示集** \(\mathcal{Y}\)（140个提示），要求动作同时发生、可人为执行、对基线具有挑战性。筛选自HumanML3D“<prefix> while <suffix>”格式及人工设计。  
  - 附加实验在**HumanML3D测试集的“while”子集**（539提示）上进行。
- **基准**：没有直接使用标准“3D运动生成”基准，而是对比了四种基线：  
  - **MDM***：重训练的MDM扩散模型（骨干网络）。  
  - **MoMask**：基于VQ-VAE的层级掩码生成模型。  
  - **STMC**：专为同步动作设计，需输入身体部位-动作映射。  
  - **Attend-and-Excite***：将图像域的注意力增强方法适配到运动域。
- **评价指标**：  
  - **R Precision**（Top1/2/3）：文本-运动对齐的匹配精度。  
  - **Dual MultiModal Distance**：前缀和后缀偏差的L2范数，越小越好。  
  - **用户研究**：28组对比（每个基线7组），每组16-18名用户对**质量、文本对齐、整体偏好**投票。

### 4. 资源与算力

- **训练与适配开销**：  
  - 将MultiAct适配到新骨干需**一次性构建运动集合** \(I = \{ motion(y,\theta) \}\)，约**4000次生成**，使用**4块GPU**、batch size=8，耗时**约2小时**。  
  - 未明确GPU型号。
- **推理开销**：  
  - 相对骨干网络推理时间，MultiAct增加约**2倍**（仅优化循环）。  
  - 若使用测试时缩放选token，额外增加约**3倍**（评估2–3个候选token），总开销约**6倍**骨干网络时间。  
  - 对比STMC约为5倍开销。

### 5. 实验数量与充分性

- **实验组数**：  
  - 定量对比表1：与4个基线在R Precision、Dual MM Dist、用户研究上的比较。  
  - 表2：在HumanML3D子集上与骨干对比。  
  - 表3：消融实验（固定参数→逐步加入层、步数、token选择），共5种配置。  
  - 用户研究：28个对比，每对比16-18人，总计约450+投票。  
  - 定性结果：多组可视化（图7–11）。
- **充分性评价**：  
  - **优点**：包含与多种基线（通用型+复合专用型）的对比，消融实验完整，用户研究规模合理。  
  - **客观性**：自建提示集特意筛选了“硬且清晰”的案例，这有助于凸显方法优势，但也可能导致**选择偏差**（对基线不公平）。HumanML3D子集上的表现提升较小（表2），说明在真实分布上仍有局限。  
  - **公平性**：STMC要求特定输入格式，作者进行了适配；Attend-and-Excite*也被公平改编，但未与其他复合方法（如GraphMotion、CoMo）对比，基线覆盖不够全面。

### 6. 论文的主要结论与发现

- MultiAct显著提升了对复合文本提示的语义覆盖，同时保持运动真实性，在R Precision、多模态距离和用户偏好上均优于所有基线。
- 注意力优化效果高度依赖于提示特定参数（token、层、步数），固定参数配置会失败；ParamGate自动选择是提升性能的关键。
- 动作细节token（如方向、副词）比动词本身更适合作为增强目标，因为自注意力中它们编码了更完整的语义信息。
- 方法可即插即用，无需重新训练，适用于不同骨干（但需一次性适配）。

### 7. 优点：方法或实验设计上的亮点

- **无需训练/架构修改**：直接作用于预训练模型的推理过程，大幅降低应用成本。
- **模型无关性**：可适配多种扩散/自回归骨干（已验证MDM*和MoMask相关基准）。
- **自动参数选择**：ParamGate通过轻量级聚类+最近邻+阈值法解决提示依赖问题，避免手动调参。
- **实验设计细致**：包括用户研究、消融分析、注意力可视化（图3、5），从多个角度验证方法有效性。
- **偏差度量创新**：提出Dual MultiModal Distance，分别评估复合提示的前后部分，更好地反映完整语义对齐。

### 8. 不足与局限

- **实验覆盖不足**：  
  - 仅使用自建提示集（140个）和HumanML3D单子集，未在更多样化的基准（如BABEL、RICH）或真实长文本上评估。  
  - 未与最新复合方法（如GraphMotion、CASIM、FineMoGen）直接对比，可能导致结论略显片面。  
- **偏差风险**：  
  - 提示集由ChatGPT生成后经人工过滤，可能存在领域偏差，且“硬且清晰”的标准容易放大方法优势、低估基线。  
  - 用户研究中的视频展示可能引入视觉偏好偏差（如运动平滑度比语义准确性更易被偏好）。  
- **应用限制**：  
  - 推理时间开销**6倍**骨干网络，对实时应用不友好。  
  - 依赖骨干网络质量，继承其伪影（如抖动、地面穿透）。  
  - 对**三个及以上同步动作**、或具有交互对象的场景未验证。  
  - token选择方法中，测试时缩放虽精确但耗时，快速法则可能错估最优token。  
- **泛化性**：只验证了基于MDM*的版本，是否稳定适配其他架构（如MoMask）仅靠间接比较，缺乏直接实验。

（完）
