---
title: How to Guide Your Language Flow
title_zh: 如何引导你的语言流
authors: "Rohit Dilip, Tianrong Chen, Yuyang Wang, David Van Valen, Joshua Susskind, Miguel Angel Bautista"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.19356v1"
tags: ["query:dmg"]
score: 6.0
evidence: 面向流匹配模型的引导方法
tldr: 扩散与流匹配语言模型需要引导来提升生成质量，但现有 autoguidance 方法依赖额外前向计算，弱强模型动态也难以对齐。本文提出 probe guidance，复用已训练扩散模型冻结的内部状态构造引导信号，推理时无需增加前向传播。它在连续扩散语言模型的无条件生成上刷新最优结果，并提升 1.7B 模型的多选问答表现。作者还通过探针发现，传统自引导的弱模型须来自低熵训练区域，从而解释其机制并给出实用改进。
source: arxiv
selection_source: fresh_fetch
motivation: 扩散与流匹配语言模型依赖引导提升生成，但现有 autoguidance 需额外前向计算，弱强模型动态难对齐，机制也尚不清晰。
method: 提出 probe guidance：复用已训练扩散模型冻结的内部状态构造引导信号，无需推理时额外前向传播，并确保弱强模型动态相似。
result: 在连续扩散语言模型无条件生成上达到 SOTA；1.7B 模型多项选择题基准稳定提升，并发现传统 autoguidance 的弱模型须来自低熵训练区域。
conclusion: probe guidance 为扩散语言模型提供实用改进，也揭示 autoguidance 的有效条件，深化对其机制的理解。
---

## 摘要
我们提出了一种引导流匹配模型的新方法。我们的方法称为探针引导（probe guidance），它利用现有扩散模型的冻结内部状态来构建引导信号。其工作原理与自动引导（autoguidance）类似，但无需在推理时进行额外的前向传播，并提供了一条可靠的路径来确保弱模型和强模型具有相似的动力学。我们将该方法应用于连续扩散语言模型并进行了基准测试，其中探针引导在无条件生成上创下了新的最先进性能。当应用于一个 1.7B 扩散语言模型时，探针引导在多项选择问答基准上持续取得提升。借助我们的探针，我们研究了传统的自动引导设置，其中强模型是一个弱检查点，并发现弱模型必须来自训练中的低熵区域。这些发现既提供了一种改进扩散语言模型的实用方法，也揭示了目前仍理解不足的自动引导背后的实际机制。

## Abstract
We introduce a new method to guide flow matching models. Our approach, which we call probe guidance, uses the frozen internal states of an existing diffusion model to construct a guidance signal. This works using a similar principle as autoguidance, but eliminates the need for an additional forward pass at inference time and provides a reliable path to ensure that the weak and strong model share similar dynamics. We apply and benchmark this method on continuous diffusion language models, where probe guidance sets a new state-of-the-art performance on unconditional generation. When applied to a 1.7B diffusion language model, probe guidance consistently improves on multiple choice question answering benchmarks. Using our probes, we study the traditional autoguidance setting where the strong model is a weak checkpoint, and find that the weak model must come from a low-entropy region of training. These findings both provide a practical way to improve diffusion language models and shed light on the actual mechanism behind autoguidance, which is currently poorly understood.